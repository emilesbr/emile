# STATUS — État actuel du projet (point d'entrée unique)

> Ce document est le point d'entrée pour quiconque reprend ce projet. Il ne remplace pas `PLAN.md` (qui reste le plan directeur détaillé, phase par phase) mais donne, en un seul endroit, la liste à jour de « quel document lire pour quel sujet » et « quels documents ignorer ». En cas de doute entre ce fichier et `PLAN.md`, `PLAN.md` fait foi sur le fond (roadmap, phases) — ce fichier fait foi sur le classement documentaire.

## Résumé exécutif

Le projet formalise, à partir du manuel officiel PRO Indicators (Philippe Roux) et de 17 sources vidéo « Trading Lessons », une stratégie de trading crypto multi-timeframe testée par backtest (BTC/ETH/BNB/SOL, données Binance Futures réelles 2020-2026), avant tout engagement de capital réel. La Phase 0 (extraction des règles) et la Phase 1 (backtest multi-timeframe sur un proxy de signal générique) sont substantiellement achevées : **H4 et D1 sont viables (GO), H1 et M15 ne le sont pas (NO-GO, détruits par les frais de transaction)**. La Phase 2 a traversé plusieurs itérations (v4 : money management complet ; v5 : signal reconstruit du corpus (TSI+cycle+structure) ; v6 : classification de régime + interdiction de trader en Excès ; v7 : validation croisée multi-timeframe réelle H4/D1) — voir `MTF_CROSS_VALIDATION_H4_D1.md` pour le résultat le plus solide à ce jour (direction confirmée sur les 4 actifs, ampleur modeste une fois le cycle rendu causal — ne plus citer les anciens chiffres +5 à +13 points de win rate).

**Limite fondamentale, vraie depuis le début** : tout le travail repose sur un **signal proxy**, jamais validé contre le vrai indicateur PRO Framework/Momentum (dont la formule n'est pas divulguée par l'éditeur). Aucun résultat chiffré ci-dessus ne doit être lu comme une validation de la méthode réelle de Philippe Roux — seulement de ce proxy.

**Réserve P0 traitée ce cycle de travail — MITIGÉE, pas résolue au sens plein** : la composante « cycle » utilise désormais `code/proxy_v2.py::compute_cycle_phase_causal` (fenêtre glissante), plus le calcul batch non-causal de l'ancienne `compute_cycle_phase` (conservée pour archive/comparaison uniquement). v5/v6/v7 et la validation OOS XRP ont été rejoués. Résultat honnête : la performance **globale** rapportée sur H4 reste positive en direction (100 % des configurations testées) mais son **ampleur** était surestimée d'environ 4× et son win rate d'environ 10 points — nouvelle référence chiffrée : `phase2_v5_causal_results.csv`, `phase2_v6_regime_causal_results.csv`, `phase2_v7_mtf_causal_results.csv`. Constat plus sérieux, à ne pas minimiser : le composant cycle **isolé** ne montre plus de corrélation significative avec le rendement futur une fois causal, sur aucun des 5 actifs testés (BTC/ETH/BNB/SOL + XRP OOS) — l'hypothèse la plus probable est que l'edge global tient surtout grâce au momentum/structure, non confirmée formellement (test d'ablation recommandé, pas fait). Détail complet : `COUVERTURE_ENSEIGNEMENTS.md` section ⚠️→◐, et `PLAN.md` (backlog, item P0 désormais marqué "Traité").

**Pour la liste exhaustive et à jour des lacunes ouvertes (corpus↔code) et leur priorisation : ne pas se fier au paragraphe ci-dessus au-delà du P0, consulter `COUVERTURE_ENSEIGNEMENTS.md` et le backlog priorisé de `PLAN.md`** — ce résumé exécutif n'est pas remis à jour à chaque changement, ces deux documents le sont.

**Configuration recommandée pour la Phase 3 (paper trading), ajoutée ce cycle, PARTIELLEMENT CORRIGÉE** : `CONFIGURATION_RECOMMANDEE.md` répond pour la première fois à "quelle config utiliser ?" — synthèse des 12 mesures existantes en 10 décisions ON/OFF. **Correction (rappel direct de l'utilisateur, "la performance du Proxy ne décide jamais d'utiliser ou non l'IP de Philippe, on l'utilise dans tous les cas")** : 4 de ces 10 décisions (stop cross-timeframe UT+1, abstention Wall Street, diversification, +Reverse) désactivaient par erreur des RÈGLES LITTÉRALES du corpus sur la seule base d'une contre-performance mesurée — corrigé par `code/backtest_phase2_faithful.py` (nouveau moteur, les active sans condition), qui doit désormais être considéré comme le protocole RANGE à utiliser opérationnellement (pas `recommended.py` seul). Résultat honnête vs `recommended.py` (16 combinaisons), RE-régénéré après 3 corrections de fidélité IP trouvées par audit proactif ultérieur (EXCES-H4, pyramidalisation-régime, Conflit MTF — cf. paragraphe suivant) : retour moyen **-37,2 pts** (était -77,3 pts avant ces corrections) mais drawdown moyen **+11,5 pts** (était +7,5 pts, 16/16 combinaisons améliorées) — cohérent avec un stop UT+1 mécaniquement plus large. Le gate Andrews Pitchfork reste une vraie hypothèse d'implémentation, légitimement réglable par performance ; le gate Fibonacci RANGE, lui, s'est révélé une classification INCOMPLÈTE (pas fausse) — le manuel donne une lecture littérale distincte (seuils par régime) jamais comparée, laissée backlog P0/P1 pour une raison précise (2 des 3 sous-conditions n'ont aucune définition codable dans le corpus), pas simplement "réglable". Moteurs : `code/backtest_phase2_recommended.py` (alias `recommended`, 13e) et `code/backtest_phase2_faithful.py` (alias `faithful`, 15e) de `code/run_all.py`. Détail complet : `CONFIGURATION_RECOMMANDEE.md` (tête de document + section 5ter), `PLAN.md`.

**Protocole unifié — routeur range ↔ tendance, traité ce cycle puis CORRIGÉ cinq fois** : `code/unified_protocol.py` répond "oui" à la question "les moteurs sont-ils unifiés dans un même protocole ?" — routeur bar-par-bar RANGE/TENDANCE, **gérés de façon INDÉPENDANTE** (exclusivité mutuelle retirée, non justifiée par le corpus, cf. "CORRECTION" dans `unified_protocol.py`), puis **CONSOLIDÉ** (même cycle) : le côté RANGE utilise désormais les mêmes règles littérales que `backtest_phase2_faithful.py` (stop D1 UT+1, abstention Wall Street, +Reverse TRES_AGRESSIF), pas celles de `recommended.py`, puis **CORRIGÉ une 3e fois (correction EXCES-H4, mobilisation multi-agents, audit proactif)** : le gate ne vérifiait plus le régime EXCES du H4 natif depuis le refactor v7, seulement le D1 — régression silencieuse, corrigée. Puis **CORRIGÉ une 4e fois (correction pyramidalisation-régime, 2e round de mobilisation multi-agents, audit proactif, cycle suivant)** : le renfort (pas l'entrée fraîche) n'était plus restreint au régime TENDANCE/RANGE_TENDANCIEL depuis le même refactor v7 (`RULES_EXTRACTION.md` §3, "Renfort" absent de la table RANGE) — même root cause qu'EXCES-H4, 25,7% des renforts réellement ouverts l'étaient en régime RANGE_NEUTRE avant correction. Puis **CORRIGÉ une 5e fois (correction Conflit Multi-Timeframe, 3e round de mobilisation multi-agents — design puis implémentation, cycle suivant)** : `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` désigne "ne jamais trader une borne de range si un range TF supérieur est actif" comme "l'erreur numéro un" du corpus — jamais implémentée avant cette correction. Niveau retenu après mesure empirique décisive (D1, pas Hebdomadaire — Hebdo aurait bloqué ~89% des tranches RANGE actuelles contre ~12-19% pour D1), appliqué uniformément entrée+renfort faute de distinction textuelle. Résultat mesuré honnête (BTC/ETH/BNB/SOL × 4 profils, comparé à `faithful.py`, régénéré après les 3 corrections) : 147 campagnes tendance déclenchées, toutes closes en étape Accumulation — 10/16 combinaisons dégradées en retour (moyenne -9,3 pts), drawdown moyen -1,9 pt, toujours pas une amélioration en moyenne. **BNB/TRES_AGRESSIF, identifié avant la correction EXCES-H4 comme catastrophique (-72,5% de drawdown, -42,6% de retour, année 2021 seule à -63,5%/-45,9%), est très largement résolu après ces 3 corrections** (-25,2% de drawdown, +22,0% de retour ; 2021 à -9,5%/+5,2%) — n'est plus à exclure d'un usage réel, seulement à surveiller comme le profil le plus faible des 16 combinaisons testées. `decide_now()` fournit une décision live structurée, RANGE et TENDANCE rapportés séparément. 14e moteur de `code/run_all.py` (alias `unified`), `code/test_unified_protocol.py` (13/13 tests). Détail : `CONFIGURATION_RECOMMANDEE.md` sections 5bis/5quater/5quinquies, `PLAN.md` section "Protocole unifié"/"Correction EXCES-H4"/"Correction Conflit Multi-Timeframe".

**"Conflit Multi-Timeframe" — désormais implémenté (mobilisation multi-agents, design puis décision puis implémentation, cycle suivant)** : `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` désigne cette règle comme "l'erreur numéro un" du corpus — jamais implémentée avant ce cycle. Niveau retenu (D1, pas Hebdomadaire) et application uniforme entrée+renfort décidés par citation + mesure empirique (Hebdo aurait bloqué ~89% des trades actuels, D1 seulement ~12-19%), pas par supposition. `faithful.py`/`unified_protocol.py` corrigés, `recommended.py` volontairement non étendu. **Gate Fibonacci RANGE — toujours backlog P0/P1**, raison désormais précise : condition composite à 3 volets du manuel (`RULES_EXTRACTION.md` §1), dont 2 des 3 n'ont aucune définition codable ailleurs dans le corpus (vérifié par re-lecture exhaustive, 3 agents convergents) — implémenter exigerait d'inventer ces définitions, refusé. Cf. `PLAN.md` backlog item 10 et section "Correction Conflit Multi-Timeframe", `CONFIGURATION_RECOMMANDEE.md` section 5quinquies.

**4e round de mobilisation multi-agents (cycle suivant), vérification + bilan directeur "regard neuf"** : (1) vérification adversariale de Conflit MTF — tous les chiffres confirmés sauf un CSV non régénéré (`oos_xrp_faithful_results.csv`, 3→0 trades, corrigé — pas un bug, une règle littérale désormais aussi appliquée sur l'échantillon XRP) ; effet de bord réel documenté (le gate peut aussi fermer une tranche pas encore Validée, pas seulement bloquer une ouverture). (2) diff systématique v6↔v7 : fermé, rien de nouveau au-delà d'EXCES-H4/pyramidalisation-régime. (3) Bilan directeur : la section "Critères de sortie Phase 2→Phase 3" elle-même était restée figée depuis plusieurs cycles — réécrite ; `risk_aggregation_triple_system.py` (LE chiffre le plus cité du projet) n'avait aucun test malgré 3 resynchronisations manuelles déjà nécessaires — corrigé (`test_risk_aggregation_triple_system.py`, 4/4) ; `decide_now()` ne signalait aucune information sur le risque agrégé — corrigé (champ `aggregate_risk_warning`) ; garde-fou mécanique anti-automatisation construit (`test_no_execution_automation.py`, 2/2) plutôt que reporté encore une fois ; 2 incohérences documentaires (68/112 stale, numérotation "15e"/"14e" moteur) corrigées. 21/21 fichiers de test verts. Détail complet : `PLAN.md` section "4e application".

**5e round de mobilisation multi-agents (cycle suivant), audit exhaustif du corpus complet ("reste-t-il de l'IP de Philippe non implémentée ?")** : 3 agents ont relu DE ZÉRO le manuel + les 17 sources vidéo, sans se fier aux classifications déjà écrites — ~18 éléments supplémentaires trouvés, classés catégorie A (3, probablement pas reproductible — dépend de l'algorithme propriétaire non divulgué, jamais tenté), catégorie B (3, littéral/faible coût) et catégorie C (10+, littéral mais exige une décision de conception avant tout code, backlog). Détail complet : `COUVERTURE_ENSEIGNEMENTS.md` section "Audit exhaustif du corpus complet", `PLAN.md` backlog item 11. **Catégorie B, item (i) traité ce cycle** : "Règle des 50%" du Pull-Back (#13) — 2e condition cumulative manquante (pénétration dans les 50% inférieurs du canal de contexte) ajoutée à `code/fibonacci.py` (`compute_context_position`/`classify_regle_50`, colonnes `fib_context_position`/`fib_regle_50`), câblée dans `code/backtest_phase2_fib.py` (`use_fib_regle_50`).

**6e round de mobilisation multi-agents (cycle suivant), catégorie B restante + vérification adversariale à froid du round précédent** : (1) vérification adversariale de la "Règle des 50%" — **un vrai bug trouvé et corrigé** (`classify_regle_50` ne plafonnait pas le retracement, supprimant le Red Flag #10 documenté par le fichier lui-même ; corrigé, `fib_regle_50` est désormais un sous-ensemble strict vérifié de `fib_favorable`, 920 vs 2388 bougies BTC H4) plus 2 erreurs de chiffres et 2 imprécisions de citation, toutes corrigées dans les 4 documents de suivi ; résultat honnête final : 2-13 trades selon l'actif (vs 80-137 pour `fib_favorable`), rendement absolu négatif sur 15/16 couples, dégradé vs baseline sur 16/16 — implémenté quand même. `code/test_fibonacci.py` désormais 8/8. (2) Sensibilité `MIN_BORDERS` (catégorie B, item iii) : grille 2/3/4/5 rejouée sur les VRAIS moteurs (`run_v7`/`trend_table`) — 0,0 pt d'écart sur les 32 couples testés, mais **le gate s'avère INERTE** (n_borders médian=9, minimum réel chez les candidates=5, seuil 3 ou 4 ne mord jamais) — pas une preuve de robustesse, une trouvaille qui déplace la vraie question vers une décision de conception (catégorie C) sur la définition même de `n_borders`. `code/min_borders_sensitivity.py`, `test_min_borders_sensitivity.py` (9/9). Trouvaille annexe : `phase2_v7_mtf_results.csv`/`MTF_CROSS_VALIDATION_H4_D1.md` périmés depuis EXCES-H4/Conflit MTF (BTC 504→492 trades) — régénérés et section refresh. (3) "Stop Loss = taille du canal" (catégorie B, item ii) : interrompu par un rate-limit en cours de route, agent relancé avec son contexte préservé après diagnostic d'un `KeyError` bloquant — **terminé et intégré, vérifié indépendamment** (recalcul manuel du mécanisme des deux "/2" de la citation, non-régression bit-à-bit des 9 moteurs existants quand la règle est désactivée, delta moyen recalculé indépendamment depuis les CSV). Citation `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` §5, 4 hypothèses documentées (H-Canal-Large-1..4), activé sans condition dans `faithful.py`/`unified_protocol.py` (côté RANGE, canal D1). Résultat honnête : `faithful.py` -2,6 pts de retour en moyenne (BTC/BNB dégradés jusqu'à -13,5 pts, SOL amélioré jusqu'à +8,1 pts, ETH inchangé) ; effet favorable sur le risque agrégé (66/112→64/112 dépassements du plafond) ; BNB/TRES_AGRESSIF (déjà le couple le plus fragile suivi) impacté négativement (+22,0%/-25,2%→+12,2%/-30,5%, toujours loin du -72,5% d'origine). Implémenté malgré ce résultat mixte à dégradé, conformément au principe inviolable du projet. `test_position_engine.py` 8→13, `test_regime_classifier.py` 10→15. Suite complète : 157/157 tests verts. Détail complet : `PLAN.md` section "6e application".

**7e round de mobilisation multi-agents (cycle suivant), catégorie C prioritaire — investiguée, PAS implémentée, décision documentée** : 2 agents de conception indépendants (même méthode que "recherche + second avis indépendant" pour Conflit MTF) sur l'item le plus prioritaire de la catégorie C ("amplitude Validation/Confirmation jamais réellement cross-timeframe"). **Verdict, vérifié personnellement par citation exacte avant tranchage** : la citation de départ était partiellement fausse (UT+1 = le stop, déjà implémenté — pas la Validation, dont l'amplitude reste correctement locale). La moitié restante (Confirmation = amplitude UT+2 réelle) n'a PAS été implémentée : le corpus lui-même ne budgète que 2 UT pour la table RANGE (`TRADING_LESSONS_MTF_SUIVI_TENDANCE.md`, vérifié), le manuel officiel décrit "Confirmation" comme un niveau structurel absolu (pas une amplitude), et toute lecture littérale mettrait en péril deux éléments du corpus déjà mieux établis (le breakeven différé, la validation OOS XRP). Requalifié en un nouvel item catégorie C plus précis : "Confirmation comme niveau structurel absolu". Aucun code modifié — investigation et correction documentaire uniquement (citation UT+1/Validation corrigée dans `COUVERTURE_ENSEIGNEMENTS.md`/`PLAN.md`). Détail complet : `PLAN.md` section "7e application", `COUVERTURE_ENSEIGNEMENTS.md`.

**8e round (cycle suivant), catégorie C — "définition stricte de la 3ème borne par clôtures (pas mèches)" : investiguée, PAS implémentée, item RETIRÉ et remplacé par deux items nouveaux mieux étayés** : **l'énoncé de l'item était une erreur de catégorie**, vérifiée par relecture intégrale de `TRADING_LESSONS_TROISIEME_BORNE.md` — les deux clauses "clôtures" des "3 critères séquentiels" (*"cassure du contexte ... validée par des clôtures de bougies au-delà du contexte"*, *"le prix maintient ses clôtures sous/sur le contexte"*) sont des **prédicats de validation d'un niveau déjà tracé, jamais une règle de localisation d'un point de swing** ; la source ne dit rien de la façon dont le prix d'une borne est déterminé. Les 3 critères sont déjà couverts, chacun à sa place (`trend_table.py::breakout_raw`/`excess_raw` cassent un niveau-mèches sur une CLÔTURE ; H5 combine explicitement mèche + clôture ; `fibonacci.py` porte le 61,8% de cette même source ; `PHASE2_CORRECTION_CLOSES.md` a déjà basculé les déclenchements sur clôture). **3 sources contredisent frontalement la lecture "détection par clôtures"** : `CLUSTERS_PRIX.md:30` (*"bas de clôture **ou** mèche"* — le corpus refuse de trancher), `ZONE_ACCUMULATION.md:38` (*"sous le **point bas** de la 4ème borne"*), `STRUCTURES_ALTERATIONS.md:28` (*"les mèches peuvent pénétrer ... les clôtures doivent rester à l'extérieur pour valider"* — le niveau vient des extrêmes, les clôtures sont le test). **Mesure empirique (BTC/ETH/BNB/SOL, H4+D1)** : fréquence de détection quasi identique (-1,0% à +2,1%) mais ~40% seulement des barres communes — **blast radius inversé par rapport à l'item** : aucun effet utile sur `n_borders` (gate `MIN_BORDERS=3` reste INERTE dans les 8 configurations, premier seuil mordant 6→5/6/7), mais 5,6-8,1% de bascules du signal d'entrée `score >= 2` via `compute_ascending_lows` — le consommateur que l'item ne désignait pas. Aucun code modifié, aucun CSV régénéré, **157/157 tests inchangés**. **Deux items nouveaux ajoutés à la catégorie C** : (1) *"règle d'or : calculs sur clôtures, jamais sur les mèches"* (#15, `PYRAMIDALISATION.md:20`, énoncé le plus littéral du corpus) vs `local_range`/`context_range` calculés en `max(high) - min(low)` et projetés en `val_px`/`conf_px`/`lim_px` — mesuré comme un vrai changement (-20% sur `local_range` H4, -45% en D1), **prochain item C recommandé** ; (2) `fibonacci.py::compute_retracement` hybride (numérateur clôture, dénominateur mèches). **Trouvaille annexe, même classe d'erreur qu'au 7e round** : `PHASE2_CORRECTION_CLOSES.md` attribuait la règle des clôtures à *"#9, #11, #14"* avec la glose *"(considérées comme du bruit)"* — #9 ne dit rien sur le sujet (0 occurrence) et la glose n'existe nulle part dans le corpus ; attribution corrigée (#11/#14/#10/#13/#15). Détail complet : `PLAN.md` section "8e application", `COUVERTURE_ENSEIGNEMENTS.md`.

**9e round (cycle suivant), catégorie C — "Red Flags d'invalidation précoce" investigués, PAS implémentés, décision documentée** : relecture intégrale de `TRADING_LESSONS_ZONE_ACCUMULATION.md` (3 Red Flags exactement, lignes 44-47, citations vérifiées mot pour mot) + balayage des 17 sources et de `RULES_EXTRACTION.md`, puis mesure sur les VRAIS moteurs avant de trancher. **La prémisse de l'item était fausse** : *"pas de mécanisme distinct du stop de protection standard"* — `position_engine.py::process_tranche` a bien un second mécanisme, la sortie "flip de signal" tant que ni Validation ni Confirmation ne sont atteintes, qui est littéralement une invalidation PRÉCOCE et qui porte déjà les règles d'abstention du corpus (EXCES-H4, UT+2, Conflit MTF) comme conditions de SORTIE ; mesuré sur 4 388 trades de `faithful.py` (BTC/ETH/BNB/SOL × 4 profils) : **FLIP 93,8% / LIMITE 3,8% / STOP 2,4%** des sorties. Verdict par Red Flag : **RF2** (>61% maintenu) **déjà implémenté deux fois** comme plafond d'ENTRÉE (`trend_table.py::ACCUM_RETRACEMENT_HIGH=0.61`, `fibonacci.py::FAVORABLE_MAX=0.618` qui le cite nommément) — en faire aussi une sortie serait un 3e usage du même chiffre, alors que le corpus l'emploie comme **minimum** d'entrée dans 3 autres passages (inégalité inverse), et *"maintenue"* n'est chiffré nulle part ; **RF3** (contexte opposé) **doublon bit-à-bit du stop et prouvé NON précoce** — le stop est déjà exactement à `ctx_support_d1` (branche `entry*0.999` jamais mordante, 0/4 388), le niveau est atteint par 24 trades et dans **24/24 cas la bougie de déclenchement EST la bougie de sortie**, donc 0/4 388 trade sortirait plus tôt (la seule variante réellement précoce serait un stop suiveur sur la bande de contexte, ce qui contredit la règle la mieux étayée du corpus — interdiction de resserrer avant la Confirmation, sources #12/#13/#15/#16) ; **RF1** (clôtures du contexte sous la MA20) **seul non redondant, et vrai apport du round** — 2e source plus précise trouvée et jamais reliée jusqu'ici (`TRADING_LESSONS_CLUSTERS_PRIX.md:50`, section « Règle de Trois »), mais bloqué par 2 paramètres à inventer ("multiples", "marginales"), un conflit d'UT entre ses 2 sources (#10 dit "du contexte", le tableau de #16 est calé sur l'UT de trading H1) et le piège terminologique des **trois** "moyennes 20 périodes" déjà distinctes du projet (prix/UT d'exécution/entrée dans `cluster_technique.py`, **volume** dans `trend_table.py` H9, prix/contexte/invalidation ici) ; impact mesuré s'il était codé quand même : avant la sortie réelle sur 8,3%/6,9% des trades (avance médiane 6 bougies), mais avant le stop dans 65%/54% des 104 sorties au stop (avance médiane 103-214 bougies) — pas un no-op. **Nouveau candidat catégorie C trouvé en chemin** : puce 3 de la « Règle de Trois » de #16 ("toute désynchronisation entre patterns ou cycles doit entraîner un arrêt des nouveaux engagements") — seule la puce 1 de cette section est implémentée. Aucun changement de comportement, aucun CSV régénéré, suite complète **157/157** avant et après ; seule modification de code : un bloc de commentaires en tête de `code/position_engine.py` (zéro comportement). Détail complet : `PLAN.md` section "9e application", `COUVERTURE_ENSEIGNEMENTS.md`.
**10e round de mobilisation (cycle suivant), catégorie C — contrainte "espace libre" MTF avant breakout : IMPLÉMENTÉE, mesurée, résultat honnête mitigé à dégradé** : item cité sur LA MÊME SOURCE que celui refusé au 7e round (`TRADING_LESSONS_BREAKOUT_RATIO11.md`), donc traité avec la même méfiance envers la citation de départ — mais cette fois **la citation s'est révélée EXACTE** (l.17, vérifiée mot pour mot : *"il est impératif de vérifier l'absence d'obstacles sur les unités de temps supérieures (UT+1 et UT+2)... un espace libre de toute structure majeure"*), avec une **2e source indépendante** du même mécanisme (`TRADING_LESSONS_PULLBACK_MATURITE.md` l.14, "Le Potentiel" — item de catégorie C listé séparément à tort, en réalité le même enseignement, désormais couvert). **Point décisif : la conclusion "2 UT pour RANGE / 3 UT pour TENDANCE" du 7e round s'applique ici EN SENS INVERSE** — `TRADING_LESSONS_MTF_SUIVI_TENDANCE.md` l.13/l.15 (revérifiées) donnent *"3 UT impératives"* et *"validation sur 2 UT supérieures"* à la table TENDANCE, celle de `trend_table.py` : le motif qui bloquait le round précédent autorise ce chantier. Aucune définition inventée (obstacle = `ctx_resistance` déjà présent dans le fichier, calculé sur de vraies bougies D1/Hebdo et joint par la jointure causale existante de `backtest_phase2_ut2.py` ; rendement escompté = `local_range` déjà calculé, Ratio 1:1 cité par la source elle-même) ; hypothèses H13-H17 documentées. Faisabilité vérifiée AVANT de coder : **pas un no-op** (ratio de marge D1/H4 2,1-2,4×, Hebdo/H4 2,8-5,5×, alors que `ctx_high` EST un no-op et a été écarté — l'invariance mesurée au 7e round est confirmée indépendamment) et **0,00% de NaN** sur D1/Hebdo. Résultat honnête (`phase2_trend_table_results.csv`, colonne `space_gate`) : **1 couple amélioré (BTC +1,1 pt), 3 dégradés (ETH -1,9 / SOL -1,1 / BNB -0,8), 12 inchangés** — implémenté quand même, gardé optionnel (`use_breakout_space_gate=False`) car ce moteur est le banc de mesure isolé de la table de tendance (H11) ; non-régression bit-à-bit vérifiée. **Le gate n'est PAS inerte** (contrairement à `MIN_BORDERS`) : diagnostic dédié (`phase2_trend_table_space_gate_diagnostic.csv`) — **34-43% des 264-306 bougies candidates au breakout sont refusées au Ratio 1:1**. **Correction documentaire trouvée en passant** : l'affirmation *"100% des campagnes de tendance se referment en Accumulation, aucune n'atteint jamais Breakout"*, répétée dans 4 endroits, n'est vraie que pour 3 profils sur 4 — le profil FAIBLE ouvre 30 campagnes dont **8 atteignent le Breakout, 5 la Divergence et 1 l'Excès final**. `test_trend_table.py` 6→12, suite complète **163/163**. Détail complet : `PLAN.md` section "10e application", `COUVERTURE_ENSEIGNEMENTS.md`.

**11e round (cycle suivant), catégorie C — "Mécanisme d'emboîtement T/T-1" investigué, PAS implémenté, item FERMÉ** : relecture intégrale de `TRADING_LESSONS_ZONE_ACCUMULATION.md`, grep `emboît`/`structure interne`/`T-1`/`millefeuille` sur les 17 sources + `RULES_EXTRACTION.md`, lecture complète de `trend_table.py`/`backtest_phase2_ut2.py`, puis mesure sur le VRAI moteur avant de trancher. **Deux prémisses de l'item étaient fausses.** (1) La paraphrase de l'audit amputait le mot porteur : la source dit *"le **canal de tendance** observé sur votre unité de temps de contexte (T) n'est rien d'autre que la structure interne de l'unité de temps inférieure (T-1)"* — or « canal de tendance » n'est pas un synonyme de « contexte » mais un objet situé **un cran sous** lui (#11:14-18 oppose *"UT « Contexte »"* et *"UT « Canal de Tendance »"*, #12:7 place le *"canal de tendance de l'UT+1"* sous le *"range de contexte principal"* de l'UT+2), ce qui fait de la citation une **identité de lecture graphique** — sans seuil, sans gate, sans si/alors — et non une procédure de calcul. (2) **H11 ne parle pas du même sujet** : elle décline la validation par l'UT **SUPÉRIEURE** (*"H4 validé par D1"*), la direction que le corpus prescrit, alors que l'item réclamait la structure de l'UT **INFÉRIEURE**. **Partiellement DÉJÀ COUVERT** (même classe de trouvaille que le Red Flag 3 / la sortie flip au 9e round) : la seule traduction opérationnelle de l'emboîtement dans tout le corpus est la projection vers le BAS — *"Extreme Channel (contexte de l'UT supérieure **affiché sur l'UT de trading**)"* (#16 `CLUSTERS_PRIX.md:12`), stop sous ce canal (#16:28, #12:7, #10:38) — soit mot pour mot le **stop UT+1 déjà implémenté sans condition** (`ctx_support_d1`, `faithful.py`) et déjà paramétrable (`use_mtf_stop` de `run_ut2`). La table TENDANCE a bien droit à 3 UT (`MTF_SUIVI_TENDANCE.md:13` vérifié : *"2 UT (Range) | **3 UT impératives** (Tendance)"*) mais la ligne suivante tranche la direction (*"Nécessite validation sur **2 UT supérieures**"*), et **5 sources contre 1** placent l'exécution en bas de l'échelle (#15:10, #16:10, #12:19, #14:15, #11:9 — *"toute borne sur l'UT « Tendance » devient inexistante et non-avenue"*). **Mesures (BTC/ETH/BNB/SOL, historique complet ; harnais validé — il reproduit bit-à-bit les 7/9/16/8 déclenchements Accumulation de `code/phase2_trend_table_results.csv`)** : joindre un niveau EN DESSOUS est mécaniquement gratuit et causal (`attach_context_level` marche à l'identique avec `closure_delay=1h`) **mais n'apporte AUCUNE information** — `ctx_high`/`ctx_low` calculés en H1 puis joints au H4 sont identiques aux valeurs H4 natives sur **100,00%** des barres après neutralisation de l'artefact du `shift(1)` (un max/min sur fenêtre **calendaire** est exactement invariant par agrégation de bougies ; généralisation exacte du "ratio médian 1,00" du 7e round), et la clôture H1 jointe est celle de la bougie H4 précédente sur 100% des barres ; le gate **additif** « structure T-1 mature » est **inerte bit-à-bit, écart 0 sur 4/4 actifs** (`n_borders` H1 = médiane 37 contre 9 en H4, **32-34 au minimum** sur les barres candidates, contre `MIN_BORDERS=3` — 3e confirmation du "gate inerte" des 6e/8e rounds) ; la seule composante dépendante du niveau est la bande EMA±ATR (2,1-3,8% vers le bas, 7,4-12,7% vers le haut ; rejets de canal BTC 902 en H4 vs 825 en H1, **115 communs**), artefact de notre proxy que le corpus place **au-dessus** ; et dans cette direction prescrite le chantier est **incompatible avec H3 dans ce moteur** — stop D1 à 14,75-19,88% au lieu de 1,20-2,26% (×8,6-14,6) fait tomber le plafond de campagne de 2,2-4,2 fois U à **0,25-0,34**, les 4 profils s'écrasent sur le même plafond et la table §4 (Renfort +25/+50/+100/+150/+200%) devient inexprimable ; utiliser le canal D1 comme référence du **rejet** fait tomber les déclenchements de 7/9/16/8 à **0/0/0/3**. **Contrainte supposée corrigée** : le NO-GO H1/M15 ne bloque PAS ce chantier (il porte sur H1/M15 comme UT d'**exécution**, détruites par les frais ; un gate de structure ne paie aucun frais et le H1 est la donnée brute du projet) — ce sont la lecture du corpus et les mesures qui bloquent. **Deux items nouveaux ajoutés à la catégorie C** : (1) le `ctx_support` de `trend_table.py` **n'est pas l'« Extreme Channel » du corpus** (bande H4 native au lieu du contexte UT+1, écart médian 7,4-12,7%) — plus une hypothèse de scope mais un niveau de stop non conforme, avec un blocage concret (H3) ; (2) **tension #10:18 vs #11:9/18** sur le statut de la structure inférieure (intrant de validation contre *"« bruit »"*), jamais consignée. Aucun changement de comportement, aucun CSV régénéré, suite complète **163/163** avant et après ; seule modification de code : une note de docstring insérée entre H11 et H12 dans `code/trend_table.py` (insertion pure, zéro ligne exécutable). Détail complet : `PLAN.md` section "11e application", `COUVERTURE_ENSEIGNEMENTS.md`.

**12e round (cycle suivant), catégorie C — "règle d'or : calculs sur clôtures, jamais sur les mèches" (#15) vs `local_range`/`context_range` : investiguée, PAS implémentée, item RETIRÉ ; item compagnon `fibonacci.py` fermé comme CONFORME** : item explicitement recommandé comme *"prochain item C, priorité"* par le 8e round. Relecture intégrale de `TRADING_LESSONS_PYRAMIDALISATION.md` (43 lignes), grep exhaustif des 17 sources + `RULES_EXTRACTION.md` sur `clôtur`/`mèche`/`wick`/`amplitude`/`projeter`/`report`, relecture intégrale de #5/#10/#12/#13/#16, mesures avant de trancher. **La citation existe bien** (#15:20 titre de section *"règle d'or : calculs sur clôtures, jamais sur les mèches"*, précisée l.22 *"validation au ratio 1:1 (report de l'amplitude du range en clôture)"* et l.39), **mais son OBJET est ambigu et le 8e round n'a retenu qu'une des deux lectures** : (a) *l'amplitude* se mesure sur les clôtures, (b) *la validation* du 1:1 se constate sur une clôture — et **la lecture (b) est déjà implémentée exactement** (`process_tranche` déclenche sur `c[i]` depuis `PHASE2_CORRECTION_CLOSES.md`, où le 8e round a lui-même rétabli #15 parmi les sources) : l'item faisait porter la même phrase **deux fois, sur deux mécanismes**. **La lecture (a) contredit 5 passages du corpus** (#14:28 *"les mèches peuvent pénétrer ... les clôtures doivent rester à l'extérieur pour valider la structure"* ; #16:30 *"bas de clôture **ou** mèche"* ; #10:38 *"sous le **point bas** de la 4ème borne"* ; **#5:56** *"Objectifs : ... débordement du **point extrême** précédent"* — la seule phrase du corpus sur la mesure d'un objectif de trade de range, donc de `lim_px` ; `RULES_EXTRACTION.md:41` où *"clôturée"* qualifie le CONSTAT du franchissement d'un niveau structurel). **Argument décisif nouveau, mesuré** : #12:8, source du mécanisme codé, assigne à la Validation la finalité *"payer son stop loss"*, donc ~1 R — mesuré sur BTC/ETH/BNB/SOL H4, `(val_px-entry)/(entry-stop_D1)` médian **0,88/0,91/0,91/1,06 en mèches** (déjà calé sur le 1:1) contre **0,69/0,71/0,72/0,85 en clôtures**, part de R<1 48-54% → 55-62% : la lecture (a) **dégrade** la fidélité. **Requalification vers `trend_table.py` (hypothèse du brief) écartée** : il exclut la pyramidalisation par son H12 et **n'a aucune projection d'amplitude 1:1** (*"des ÉVÉNEMENTS DE STRUCTURE DE MARCHÉ détectés, pas des niveaux de prix fixes issus de l'entrée"*). **Mesures faites quand même** : ratio clôtures/mèches **revérifié personnellement, conforme au 8e round au millième** (0,786-0,808 / 0,862-0,876 en H4 ; 0,552-0,578 / 0,720-0,752 en D1) ; impact si (a) appliquée — `v7` +6,2 pts de moyenne mais **10/16 couples dégradés** (moyenne tirée par BTC/TRES_AGRESSIF +72), drawdown **-2,21 pt plus profond**, `faithful` **-0,9 pt**, 9/16 dégradés : signe mixte, la performance ne tranche pas ; **invariante `val_px<=conf_px<=lim_px` préservée par construction dans les deux définitions** (5D ⊂ 15D), 0 violation sur 55 171 bougies ; **aucun chiffre de fenêtre nulle part dans le corpus**. Portée recomptée : 25 fichiers `code/*.py` (4 de tests), 5 producteurs, **19** appelants de `make_open_tranche_fn` (et non 13), 7 copies inline, **35 CSV sur 63**. **Item compagnon tranché et PAS dans le sens annoncé** : `fibonacci.py::compute_retracement` n'est pas un hybride accidentel — dénominateur = structure (extrêmes, #10:38/#5:56), numérateur = clôture comme test (#11:23, #10:46 *"clôture maintenue au-delà de 61% de la structure"*, mot pour mot la formule codée) — **fermé comme CONFORME**. **Nouveau candidat catégorie C trouvé en chemin** : le coefficient `1.5` de `lim_px = entry + 1.5 * context_range` n'a **aucune source** dans le corpus et n'est documenté dans aucun des 40 `.md`, alors qu'il est recopié en littéral dans 7 fichiers (le corpus donne pourtant 2 définitions littérales de l'objectif d'un trade de range, #5:56, non implémentables telles quelles). Aucun changement de comportement, aucun CSV régénéré, suite complète **163/163** avant et après ; seules modifications de code : deux blocs de commentaires (`position_engine.py`, `fibonacci.py`) et le remplacement du commentaire inline "TENSION OUVERTE, NON RÉSOLUE" laissé au 8e round. Détail complet : `PLAN.md` section "12e application", `COUVERTURE_ENSEIGNEMENTS.md`, `CONFIGURATION_RECOMMANDEE.md` §5nonies.

**14e round (cycle suivant), catégorie C — "Altérations de structure Vague 1 / Vague 5" (#14) : investiguées, PAS implémentées, item FERMÉ** : relecture intégrale de `TRADING_LESSONS_STRUCTURES_ALTERATIONS.md` (54 lignes), grep exhaustif des 17 sources + `RULES_EXTRACTION.md` sur `Overlap`/`Rivière`/`River`/`vague`/`biseau`/`V-Top`/`V-Bottom`/`double excès`/`chiffre rond`/`extension`/`161`/`127`/`138`/`261`, lecture complète de `trend_table.py`/`position_engine.py`/`fibonacci.py`/`manual_trend_channel.py`, mesures sur données réelles avant de trancher. **La 2e moitié de l'énoncé de l'item était FAUSSE, et c'est la trouvaille principale** : *"cadre de type Elliott non repris, ne correspond pas au modèle 5 étapes déjà en place"* — or la source **décrit** ce modèle et le désigne comme la norme (l.5 *"« La Maison de la Tendance » — modèle canonique (**Vague 3 étendue = la norme**)"*, l.7-11 **Accumulation / Breakout / Divergence / Pullback / Excès Final**, nom pour nom et dans le même ordre que `RULES_EXTRACTION.md` §4 et §1:23, donc exactement ce que `trend_table.py` implémente) ; "Vague 1"/"Vague 5" sont deux ALTÉRATIONS du même modèle, pas un formalisme rival, et **"Elliott" a 0 occurrence dans tout le corpus** (glose du projet, pas le vocabulaire de Philippe — donc pas un motif d'écartement recevable). Le reste de #14 est déjà couvert (l.13 *"min. 3 bornes testées"* = `MIN_BORDERS=3` ; l.17 "Loi de l'UT Supérieure" = famille MTF ; l.21-23 = note de calibration). **Sur les 3 éléments restants** : **Overlap (l.28) DÉJÀ IMPLÉMENTÉ** dans `breakout_raw`/`excess_raw` (niveau = extrême, clôture = test, mèche tolérée) et **mesurément actif** — valider sur la mèche ferait passer les cassures de **307/287/296/253 à 453/438/465/374**, soit **32,2-36,3%** de candidates Breakout et **43,5-46,7%** d'Excès final rejetées par la clause "clôtures" ; sa seule lecture non couverte (validité continue) exigerait d'inventer l'action ET donnerait un stop ~8× plus serré que le stop littéral du corpus (#12:7/#16:28/#10:38) — mesuré, niveau cassé à **−0,11 à −0,13 R**, **0,0%** au-dessus du break-even (mon hypothèse de départ était fausse), touché avant le stop dans **85,8-90,9%** des cas en 4-7,5 bougies. **"Rivière" (l.30)** : objet réellement nouveau et constructible sans rien inventer (couverture **99,8-99,9%**, écart médian **2,9-9,3%** vs les 3 références hautes existantes, distinct aussi d'`andrews_pitchfork.py`) mais le corpus ne donne que l'endroit où le retournement *"se cherche"*, jamais le déclencheur — et le contact seul touche **30,1-32,1%** des bougies. **Prises de profit partielles Vague 5 (l.37)** : mécanisme déjà couvert par les tables §3/§4 (autorité plus haute), et les 3 objectifs exigent **4 paramètres inventés** (**0 ratio d'extension Fibonacci dans tout le corpus** — tous ses nombres Fibonacci sont des retracements ; "résistance équivalente" jamais définie ; granularité des chiffres ronds absente ; fraction par objectif absente) ; **mesure qui va contre la thèse commode, rapportée quand même** : la Vague 5 n'est PAS déjà exclue par EXCES (centile supérieur de rendement 30 bougies : **40,6-55,0%** en EXCES contre **35,7-51,0%** en TENDANCE donc tradables, base 17,1-19,6%) — c'est l'absence de définitions qui tranche, pas la redondance ; blocage amont indépendant : l.54 exige d'identifier d'abord le type d'extension et l.39 de distinguer Vague 5 et Bulle, distinction que `add_regime` ne porte pas. **Direction de l'effet volontairement NON mesurée** : aucune variante n'étant implémentable sans invention, il n'existe pas de "variante fidèle" à chiffrer — et le seul chiffre calculable (Overlap converti en sortie) n'a pas été produit, pour ne pas laisser une performance peser sur une décision qui ne lui appartient pas. **2 items nouveaux** : la ligne "River" (seul objet de structure du corpus jamais construit, constructible avec les primitives existantes) et `fibonacci.py` qui ne couvre que les retracements alors que #14:37 réclame des extensions pour les sorties. Aucun changement de comportement, **aucun CSV régénéré** ; suite complète **164/164 avant → 166/166 après**, mais **le +2 n'est pas entièrement de ce round** : +1 est le test ajouté ici (`test_trend_table.py::test_overlap_convention_already_in_breakout_raw`, vérité terrain synthétique avec contrôles positif ET négatif, non-vacuité vérifiée en le rejouant contre une variante "mèche" du moteur), +1 provient d'une session concurrente ayant écrit dans le même checkout (`test_range_limit_is_not_independent_of_the_broken_level`, non revendiqué ici) — contribution nette **164 → 165**. Seules modifications de code : un bloc documentaire en tête de `code/trend_table.py` (zéro ligne exécutable) et ce test. **Écart documentaire relevé en passant, non corrigé** : `STATUS.md` n'a aucune entrée pour la 13e application, alors que les rounds 4 à 12 y figurent tous. Détail complet : `PLAN.md` section "14e application", `COUVERTURE_ENSEIGNEMENTS.md`.

**17e round (cycle suivant), catégorie C — "Sizing 1-1,5% / R:R 1:5 spécifique aux trades d'accumulation" (#10) : investigué, PAS implémenté, item FERMÉ** : grep exhaustif des 17 sources + `RULES_EXTRACTION.md` sur `1:5`/`1,5 %`/`R:R`/`ratio`/`récompense`/`sizing`/`taille de position`, relecture intégrale de `TRADING_LESSONS_ZONE_ACCUMULATION.md` (51 lignes), `..._MAITRISE_GRADIENT_RISQUE.md`, `..._BREAKOUT_RATIO11.md`, `..._PYRAMIDALISATION.md`, `..._CLUSTERS_PRIX.md` et du manuel, mesures sur données réelles avant de trancher. **Trois prémisses de l'énoncé étaient fausses.** (1) **La source n'est pas celle supposée** : `MAITRISE_GRADIENT_RISQUE.md` (#5) **ne contient ni "1:5" ni "1-1,5%"** (son sizing — *"Stop Loss = taille du canal"*, *"/2 si canal très large"* — est déjà implémenté) ; la seule source est #10, lignes 36-37 et 50 — et **`:36`/`:37` sont des puces de synthèse, `:50` est la seule citation verbatim**, celle qui dit *"des ratios **supérieurs à** 1:5"*. (2) **Le "spécifique à l'accumulation" ne tient pas** : aucun chiffre de sizing du corpus n'est scopé par type de trade, ils le sont par expérience (`RULES_EXTRACTION.md:64-65`, #15:40-41) ou par unité d'agrégation (#15:28 *"par position"*, #16:40-41 *"par zone de prix"*), et #10:37 indexe lui-même sa taille sur le R:R — *"car l'espérance de gain est déjà élevée grâce au R:R"* — pas sur le type de trade. (3) **Le conflit redouté avec le "Ratio 1:1" n'existe pas**, le corpus le tranchant lui-même : le 1:1 de #12:8-9 est un report d'amplitude, et #15:24 précise *"le ratio 1:1 n'est pas un objectif de profit mais un point de validation mathématique du trade"* — **ce n'est donc pas le motif du refus**. **Motifs réels** — côté sizing : **(d)** le profil FAIBLE (`risk_pct = 0.01`) EST déjà la borne basse de [1% ; 1,5%] et le ≤1% du manuel, **prouvé et non affirmé** (forcer 1,0% sur les 4 profils de v7 est un **no-op bit-à-bit sur exactement les 4 couples FAIBLE**, écart 0,0 pt / 0 trade), et **(c)** un taux fixe écraserait la ligne Accumulation du **manuel** (`RULES_EXTRACTION.md:54-59`, *"Attente / +25% / +50% / +100%"*, déjà implémentée en `trend_table.py::PROFILES_TREND.accum_frac` = 0.00/0.25/0.50/1.00, correspondance exacte des 4 lignes — les 4 profils deviendraient identiques à cette étape) ainsi que le gradient expérience × psychologie × palier de `:65-66` (choix utilisateur + `capital_tiers.py`) ; côté R:R : **(b)** le "5" **n'est pas transposable**, se mesurant sur le stop de #10:38 (*"largeur moyenne du canal de tendance récent, **ou** sous le point bas de la 4ème borne"* — deux définitions, non tranchées) et non sur l'"Extreme Channel" UT+1 du moteur (#16:28 + #12:7, mieux établi) — **mesuré sur les bougies d'entrée réelles** (BTC/ETH/BNB/SOL H4, gate MTF v7) : Validation **1,21-1,26 R** (stop H4) vs **0,53-0,58 R** (stop D1), Confirmation 1,93-2,10 vs 0,90-0,93, **Limite 2,89-3,15 R vs 1,35-1,40 R**, soit un **facteur 2,3** pour le même niveau — et **(c)** `:50`, seule citation verbatim, exige des ratios ***supérieurs* à 1:5**, qu'un take-profit dur à 5 R rendrait impossibles par construction, la lecture fidèle (retirer le plafond) supprimant l'étape *"**Limite** (target atteinte) → TP100%"* de `RULES_EXTRACTION.md:41` et de sa table `:43-48` ; #15:30-31 donne au surplus un **autre** nombre pour le même type d'énoncé (*"ratio moyen visé 1:4"*), rendant arbitraire le choix de 5 plutôt que 4. **Le processus de #10 est par ailleurs déjà implémenté** : `:39-42` ("Connective Tissue" — Validation → stop réduit, Confirmation → Break-Even, *"option gratuite"*) est exactement l'échelle `val_px`/`conf_px`/`conf_to_be` de `process_tranche` ; l'item n'ajoutait que les deux annotations chiffrées. **Mesures faites quand même** (monkeypatch jeté, aucun moteur modifié ; v7, 4 actifs × 4 profils, un paramètre à la fois) : "R:R 1:5" → **+3,71 pt de moyenne, 12/16 couples AMÉLIORÉS**, 4/16 dégradés, drawdown +0,74 pt (moins profond), −7,2 trades, dispersion ETH/TRES_AGRESSIF −70,9 pt à BTC/TRES_AGRESSIF +50,9 pt ; `risk_pct` à 1,0% → −51,94 pt (12/16 dégradés, 4/16 identiques) ; à 1,5% → −35,61 pt. ⚠️ **La variante R:R 1:5 est donc mesurée LÉGÈREMENT FAVORABLE et reste néanmoins non retenue** : elle n'est pas écartée pour dégradation (motif interdit par la règle du projet) **ni retenue parce qu'elle améliore** — la règle interdit **symétriquement** au Proxy de décider de la fidélité au corpus ; seuls (b) et (c) fondent la décision. **Apport au backlog, sans le refermer** : #10:36 a été testé comme source possible du coefficient `1.5` de `lim_px` (signalé SANS SOURCE au 12e round, littéral dans 7 fichiers) — **réponse : non**, unités différentes (`1.5` multiplie un `context_range`, "1:5" un RISQUE) et conversion instable (**2,38-2,60** sur stop H4 contre **5,38-5,57** sur stop D1) ; **cet item de backlog reste OUVERT**. Aucun changement de comportement, **aucun CSV régénéré** — vérifié, `phase2_v7_mtf_results.csv` recalculé est **bit-à-bit identique** au CSV commité (64 × 9). **Aucun test ajouté** (bloc purement documentaire). **Comptage rendu ambigu par des sessions concurrentes écrivant dans le même checkout**, donc mesuré proprement : sur un checkout **HEAD propre** (`git archive HEAD`) la suite est à **164/164**, et en y appliquant **uniquement les deux éditions de ce round** elle reste à **164/164** — **contribution nette : 0 test ajouté, 0 test cassé**. Le checkout partagé a traversé un état transitoire à **7 échecs** (`test_backtest_phase2_faithful.py` ×6, `test_unified_protocol.py` ×1), tous sur `backtest_phase2_faithful.py` — fichier **jamais touché par ce round**, alors en cours d'édition par une autre session ; non imputables ici, volontairement non "corrigés" pour ne pas écraser du travail en vol, et **résolus depuis par cette session** : le checkout partagé termine à **172/172 verts** (ce round + trois sessions concurrentes). Seules modifications de code : un bloc de commentaire PUR en tête de `code/position_engine.py` et une note documentaire adossée à `PROFILES_TREND` dans `code/trend_table.py` (zéro ligne exécutable). Détail complet : `PLAN.md` section "17e application", `COUVERTURE_ENSEIGNEMENTS.md`.

**15e round (cycle suivant, en parallèle du précédent), catégorie C — confusion terminologique "Cluster technique" (#12) : investiguée, PAS implémentée, item FERMÉ** : relecture intégrale de `TRADING_LESSONS_BREAKOUT_RATIO11.md` (38 lignes) et de `TRADING_LESSONS_CLUSTERS_PRIX.md` (52 lignes), grep exhaustif des 17 sources + `RULES_EXTRACTION.md` sur `convergen`/`confluen`/`cluster`/`trendline`/`tolérance`/`proximité`/`distance`, lecture complète de `code/cluster_technique.py` et de `trend_table.py::breakout_raw`, mesures sur les vraies bougies candidates AVANT de trancher. **L'énoncé de l'item est EXACT sur ses deux affirmations vérifiables** (contrairement aux 7e/8e/9e/11e rounds) : les deux règles homonymes sont bien distinctes — #16:19-25 définit son "Cluster Technique" par **MA20 + Zone de Demande**, *"impérativement [dans] un flux tendanciel établi (**post-breakout**)"*, quand #12:29 vise **trendlines + bornes de canaux + limites de range** AVANT la cassure (citation vérifiée mot pour mot, sous le titre *"Critères de maturité avant breakout"* — donc un critère d'éligibilité, pas une "confirmation", nuance corrigée) — et `breakout_raw` teste bien **un seul niveau de prix** (`c[i-1] > local_high_v[i-1]`, plus volume et `score >= 2` qui ne sont pas des structures ; le gate H13-H17 du 10e round vient de la même source mais mesure l'espace libre AU-DESSUS, pas une convergence AU niveau cassé). **Vrai apport du round : la confusion est TRIPLE, pas double** — #10:25 (*"Retracement dans un cluster 38-50%"*) et #13:48 (*"Cluster Fibonacci | Entre 23% et 38%"*) emploient "cluster" pour une **BANDE de retracement, pas une convergence**, et celle-là est **déjà implémentée** (`fibonacci.py`, `ACCUM_RETRACEMENT_LOW/HIGH`) ; même famille de piège que les trois "MA20" du 9e round. **4 blocages, chacun vérifié ou mesuré** : (1) **deux paramètres à inventer** — *"plusieurs"* ne dit pas combien, *"en une même zone"* pas à quelle distance, et **`RULES_EXTRACTION.md`, l'autorité la plus haute, ne contient AUCUNE occurrence** de convergence/confluence/cluster/trendline (0 résultat), le seul énoncé du corpus sur la largeur d'une zone refusant explicitement de la chiffrer (#14:28, *"zone de tolérance, pas une ligne mathématique"*) ; (2) **c'est la DÉFINITION, pas la règle, qui déciderait du résultat** (même schéma que Conflit MTF 89% vs 12-19%) — sur les **264-312 bougies candidates au Breakout** (BTC/ETH/BNB/SOL H4) avec les 3 familles citées et déjà calculées par le projet (`ctx_high`, `ctx_resistance`, `channel_resistance`), la part acceptée va de **0,0-0,7 %** (N ≥ 3, tolérance 0,25 %) à **98,9-100 %** (N ≥ 1, 5 %), et de **1,0-4,2 %** à **99,6-100 %** en tolérance k × ATR ; (3) **une des trois familles n'est pas indépendante du niveau cassé** — `local_high` et `ctx_high` sont deux maxima de la MÊME série de hauts sur fenêtres **emboîtées** (5D ⊂ 15D) : `ctx_high >= local_high` sur **100,0 %** des candidates et **exactement égal sur 51,9-56,9 %** (distance médiane **0,00 %**, contre 1,52-2,85 % et 1,84-3,29 % pour les deux autres), donc la compter reviendrait à compter le niveau de référence avec lui-même — verrouillé par un test à vérité terrain avec contrôle positif, non-vacuité vérifiée par contrôle négatif ; (4) **implémenter sacrifierait un élément MIEUX établi** (même arbitrage qu'au 7e round) — mesuré via le hook existant `use_breakout_space_gate` (scratchpad, aucun fichier de production modifié), le temps en POST_BREAKOUT de **BTC/FAIBLE, seule instanciation empirique substantielle des étapes 2-5 de `RULES_EXTRACTION.md` §4**, tombe de **48,9 % à 0,0 %** pour tous les réglages testés sauf les deux plus permissifs, échantillon très mince (8 Breakouts) dit comme tel. **Nuance relevée et écartée comme non décisive** : la l.29 est une puce de synthèse du preneur de notes, pas une phrase entre guillemets attribuée à Philippe (contrairement à la l.17 implémentée au 10e round) — ce n'est PAS un motif de refus et ça n'a joué aucun rôle. **Condition explicite de réouverture** : une citation donnant soit un nombre de structures, soit une tolérance. **Nouveau candidat catégorie C** : la checklist pré-trade de #5 (l.63-70) est classée en bloc "garde-fous Phase 4, hors périmètre code" alors qu'au moins 3 de ses 7 postes sont depuis devenus du code — à réauditer poste par poste. Aucun changement de comportement, **aucun CSV régénéré** (`phase2_trend_table_results.csv` rejoué en entier, **identique bit-à-bit**) ; suite complète **164/164 avant → 166/166 après**, dont **+1 seulement de ce round** (le +1 restant vient de la session concurrente ci-dessus). Seules modifications de code : deux blocs documentaires (`code/trend_table.py` à côté des H13-H17 de la même source, `code/cluster_technique.py` là où l'homonymie se lit), **zéro ligne exécutable**, plus le test. Détail complet : `PLAN.md` section "15e application", `COUVERTURE_ENSEIGNEMENTS.md`.

**16e round (cycle suivant, en parallèle des précédents), catégorie C — "Confirmation comme niveau structurel absolu" (`RULES_EXTRACTION.md:41`) : **IMPLÉMENTÉE**, testée, mesurée, mais **défaut OFF** pour un motif de fidélité MESURÉ** : item requalifié par le 7e round et décrit par lui comme "hors périmètre" (le plus lourd de la catégorie C restante). Relecture intégrale du manuel et de `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md`, grep de `confirmation` sur les 17 sources, lecture complète de `position_engine.py`/`v7`/`faithful`/`trend_table`/`fibonacci`, et **mesures faites AVANT tout code**. **La citation est vraie et mieux étayée que ne le disait le 7e round** : le manuel §3:41 formule ses **4** étapes structurellement, et `MAITRISE_GRADIENT_RISQUE.md:55` (#5) corrobore en **chiffrant** — *"Confirmation (**structurelle**) : clôture d'une bougie sous/au-dessus **la médiane (50%)** du contexte"* — donc **aucun paramètre à inventer** (contrairement au gate Fibonacci RANGE ou aux Red Flags). **Deux prémisses du 7e round corrigées** : (a) sa liste "4 sources sur 6" citait #16, qui ne définit structurellement que la *Validation* — la 4e source réelle est #10:41 (le total 4/2 tient) ; (b) *"`position_engine.py` ne recalcule aucun seuil après l'ouverture"* est **faux** — le hook `update_levels_fn` existe depuis le refactor d'origine et est exercé en production par `backtest_phase2.py`, donc **aucune refonte de `process_tranche`** n'était nécessaire (1 fonction pure + 1 colonne additive). **Ce qui bloque vraiment, et c'est mesuré** (4 076 tranches, BTC/ETH/BNB/SOL × 4 profils) : pour les **4** lectures possibles de "canal de contexte" présentes dans le projet, le niveau est sous `val_px` dans **100,0%** des cas, si bien que la Confirmation se déclencherait dès la bougie suivant la Validation dans **98,1-100%** des cas (délai médian **0** contre **43** bougies) — ce qui, `conf_to_be=True`, supprimerait en substance la règle du break-even différé (*"la correction la mieux étayée de tout le corpus"*, 4 sources). Le conflit vient de **nos briques** (le signal entre dans la moitié HAUTE du canal 9 fois sur 10 ; `val_px` projette +17-28%), pas du corpus. **Mécanisme donc codé, testé (`test_position_engine.py` 13→19, vérité terrain à la main, contrôle positif ET négatif) et mesuré** (`code/structural_confirmation_measure.py`, 2 CSV nouveaux) : OFF→ON donne **-4,14 pt** de retour et **+1,95 pt** de drawdown (moins profond) sur `faithful.py`, **-17,16 pt** / **+1,96 pt** sur v7 — **chiffres qui n'ont joué aucun rôle dans la décision**. **Portée recomptée** (l'estimation "~27 fichiers / ~30-35 CSV" du 7e round était basse) : 19 appelants de `make_open_tranche_fn`, 46 CSV sur 64 dans la clôture transitive — mais **0 CSV existant modifié** (non-régression bit-à-bit vérifiée par rejeu réel des deux moteurs). Détail complet : `PLAN.md` section "16e application", bloc dédié en tête de `code/position_engine.py` (H-Conf-Struct-1..5).

## Classement des documents

### Référence actuelle (à lire — reflète l'état le plus à jour du travail)

| Document | Sujet |
|---|---|
| `PLAN.md` | Plan directeur, feuille de route par phase, statut le plus à jour |
| `STATUS.md` (ce document) | Point d'entrée unique, classement documentaire |
| `CONFIGURATION_RECOMMANDEE.md` | LA configuration recommandée pour la Phase 3 (synthèse des 10 décisions ON/OFF, chiffre de référence unique, walk-forward, OOS XRP) — réponse à "quelle config utiliser ?" |
| `RULES_EXTRACTION.md` | Règles extraites du manuel PDF officiel (Phase 0) |
| `TRADING_LESSONS_INDEX.md` | Index des 17 sources Trading Lessons, statut de la question multi-timeframe (résolue) |
| `TRADING_LESSONS_*.md` (17 fichiers listés dans l'index) | Sources primaires (transcriptions/résumés) référencées par l'index — consulter via `TRADING_LESSONS_INDEX.md` pour le contexte |
| `BACKTEST_RESULTS_MTF.md` | Résultats bruts du backtest multi-timeframe Phase 1 (proxy générique), base factuelle toujours valide pour son périmètre (moteur de risque simple, pré-Phase 2) |
| `WALKFORWARD_ANALYSIS.md` | Analyse walk-forward année par année du backtest Phase 1, toujours valide |
| `PHASE1_CLOSEOUT.md` | État réel de la Phase 1 (cascade multi-timeframe réelle, conclusion GO/NO-GO par timeframe) |
| `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` | Moteur de risque Phase 2 — implémentation actuelle et complète (remplace toutes les itérations précédentes) |
| `PROXY_V2_TSI_CYCLE_STRUCTURE.md` | Signal Phase 2 reconstruit à partir du corpus (TSI + cycle + structure), résultat le plus solide à ce jour |
| `AUDIT_QUALITE_ET_CORRECTION_CYCLE.md` | Audit qualité, contrôle aléatoire, bug de signe trouvé et corrigé, liste des lacunes encore ouvertes |
| `FUNDING_RATE_ANALYSIS.md` | Analyse du coût de funding Binance Futures, jamais modélisé jusqu'ici (nouveau) |
| `CASCADE3_H1_EXECUTION_TEST.md` | Test cascade Daily→H4→H1, refait le 2026-09-07 avec le moteur de risque actuel (`code/position_engine.py`) ; conclusion NO-GO reconduite |
| `REGIME_CLASSIFIER_RANGE_VS_TENDANCE.md` | Classification de régime (Range/Tendance/Excès), interdiction de trader en Excès (v6) |
| `MTF_CROSS_VALIDATION_H4_D1.md` | Validation croisée multi-timeframe réelle H4/D1 (v7) — résultat le plus solide et uniforme à ce jour |
| `OOS_VALIDATION_CYCLE_SIGN.md` | Validation hors-échantillon indépendante (XRP) du signe de la composante cycle (batch, section 6) + addendum causal (section 7, ajouté ce cycle) : corrélation causale non significative sur XRP, cohérent avec BTC/ETH/BNB/SOL |
| `COUVERTURE_ENSEIGNEMENTS.md` | Table de croisement exhaustive corpus↔code (✅ implémenté / ❌ manquant), y compris le statut à jour de la réserve P0 (traitée, mitigée) — référence pour "tout est-il pris en compte ?" |
| `code/position_engine.py`, `code/test_position_engine.py` | Moteur de gestion de position factorisé + `make_open_tranche_fn` (factory partagée, fin de la duplication d'`open_tranche_fn` dans les 9 moteurs `backtest_phase2_*.py` — v4/v5/v6/v7/ut2/patterns/capital_tiers/fib/recommended, chacun vérifié bit-identique par rejeu réel avant/après migration) + tests unitaires sur cas synthétiques |
| `code/proxy_v2.py`, `code/test_proxy_v2.py` | Signal proxy (TSI+cycle+structure), calcul du cycle désormais causal (`compute_cycle_phase_causal`, réserve P0 traitée) + 4 tests de régression (dont le test de causalité elle-même) |
| `code/cycle_causal_window_selection.py` | Sélection empirique de la fenêtre causale (150) par stabilité inter-tiers temporels sur BTC/ETH/BNB/SOL réels |
| `code/regime_classifier.py`, `code/backtest_phase2_v6.py`, `code/backtest_phase2_v7.py` | Moteurs v6 (régime) et v7 (validation croisée MTF) |
| `code/run_all.py`, `code/test_run_all.py` | Point d'entrée unique pour rejouer les 14 moteurs (`backtest_phase2_*.py` + `unified_protocol.py`) d'un coup (`--only alias1,alias2` ou `--confirm-full`), critère de sortie Phase 2→3 item 4 (`PLAN.md`). N'écrit jamais dans le dépôt (sortie dans `code/run_all_output/`) — ne remplace pas les CSV commités, qui restent la référence tant qu'il n'a pas été relancé et ses sorties comparées à la main. Rejeu complet des 12 moteurs pré-existants mesuré : 158,8s (~2,6 min), +7,0s pour `recommended`, +12,9s pour `unified` |
| `code/backtest_phase2_recommended.py`, `code/test_backtest_phase2_recommended.py` | LA configuration recommandée (synthèse) — cf. `CONFIGURATION_RECOMMANDEE.md`. 7/7 tests |
| `code/walkforward_recommended.py` | Walk-forward annuel de la config recommandée (BTC/ETH/BNB/SOL, profil MODERE) |
| `code/oos_xrp_recommended.py` | Validation hors-échantillon XRP de la config recommandée (D1, gate MTF désactivé faute de données H4/H1 — cf. `CONFIGURATION_RECOMMANDEE.md` section 4) |
| `code/unified_protocol.py`, `code/test_unified_protocol.py` | Protocole unifié CONSOLIDÉ (RANGE fidèle + TENDANCE, `run_unified`/`decide_now`) — LE protocole complet à utiliser opérationnellement — cf. `CONFIGURATION_RECOMMANDEE.md` section 5bis, `PLAN.md` section "Protocole unifié". **`decide_now()` inclut désormais un champ `aggregate_risk_warning`** (risque RANGE réellement engagé, warning informatif, pas un plafond appliqué — trouvé manquant par bilan directeur "regard neuf", cf. `PLAN.md`). 14e moteur de `code/run_all.py` (alias `unified`), 13/13 tests |
| `code/backtest_phase2_faithful.py`, `code/test_backtest_phase2_faithful.py` | Config FIDÈLE (moteur RANGE seul) — active sans condition 3 règles littérales du corpus désactivées à tort dans `recommended.py` (stop UT+1, abstention Wall Street, +Reverse scopé TRES_AGRESSIF), **+ correction EXCES-H4** (gate vérifie désormais aussi le régime EXCES du H4 natif) **+ correction pyramidalisation-régime** (le renfort exige désormais le régime TENDANCE/RANGE_TENDANCIEL, `RULES_EXTRACTION.md` §3) **+ correction Conflit MTF** (le gate bloque désormais aussi quand le régime D1 est lui-même en range, source #5 "l'erreur numéro un" — cf. mobilisation multi-agents/audit proactif, 3 rounds) — cf. `CONFIGURATION_RECOMMANDEE.md` section 5ter, `PLAN.md`. 15e moteur de `code/run_all.py` (alias `faithful`), 9/9 tests |
| `code/walkforward_faithful.py` | Walk-forward annuel de `faithful.py` (BTC/ETH/BNB/SOL, profil MODERE) — aucune année catastrophique, strictement meilleur que l'ancien walk-forward de `recommended.py` sur chaque métrique — cf. `CONFIGURATION_RECOMMANDEE.md` section 5ter |
| `code/walkforward_unified.py` | Walk-forward annuel de `unified_protocol.py` (BTC/ETH/BNB/SOL MODERE + BNB TRES_AGRESSIF), régénéré après les corrections EXCES-H4/pyramidalisation-régime/Conflit MTF/Stop Loss = taille du canal — l'année 2021 de BNB/TRES_AGRESSIF, seule responsable de l'ancien -72,5%/-45,9% agrégé, reste à -9,5%/+5,2% (inchangée par le dernier correctif) ; sa pire année est désormais 2024, à -27,2%/-11,7% — cf. `CONFIGURATION_RECOMMANDEE.md` section 5bis/5septies, `PLAN.md` |
| `code/oos_xrp_faithful.py` | OOS XRP de `faithful.py` (mobilisation multi-agents) — résultat honnête non concluant, inchangé par la correction EXCES-H4 (3 trades, échantillon trop petit pour que le régime EXCES y intervienne) mais RE-régénéré par la correction Conflit MTF (0 trade sur les 4 profils, le rôle "D1"/stop de ce moteur étant tenu par l'Hebdomadaire XRP dans cette procédure) — pas un bug, conclusion inchangée (échantillon déjà trop mince pour conclure) — cf. `CONFIGURATION_RECOMMANDEE.md` section 5ter |
| `code/cross_stress_test_faithful_gates.py`, `code/cross_stress_test_unified_capital_tiers.py` | Recherche systématique d'autres combinaisons dangereuses (mobilisation multi-agents, AVANT la correction EXCES-H4) — aucune nouvelle trouvée ; capital par palier ne résolvait pas BNB/TRES_AGRESSIF, gate Fibonacci le neutralisait incidemment — mesures conservées pour traçabilité, le cas qu'elles décrivaient n'est plus catastrophique depuis — cf. `CONFIGURATION_RECOMMANDEE.md` section 5bis |
| `code/backtest_phase2_faithful_manual_channel.py` | Canal manuel (Supports→Apex→Tangente) reconstruit sur D1 (mobilisation multi-agents, Agent A, hérite automatiquement des corrections EXCES-H4/pyramidalisation-régime/Conflit MTF via `faithful.py`) — comparé au stop UT+1 (EMA±ATR D1) actif dans `faithful.py`/`unified_protocol.py` : retour moyen supérieur (8,78% vs 3,42%) mais drawdown moyen plus profond (-6,05% vs -3,24%) — ni l'un ni l'autre remplacé, les deux lectures restent valides — cf. `CONFIGURATION_RECOMMANDEE.md` section 5quater |
| `code/risk_aggregation_triple_system.py`, `code/test_risk_aggregation_triple_system.py` | Risque nominal agrégé RANGE+TENDANCE+diversification tournant simultanément (mobilisation multi-agents, Agent B, backlog item 8 de `PLAN.md`, patché quatre fois pour rester cohérent avec `unified_protocol.py`) — maximum observé 17,00% (BTC/TRES_AGRESSIF, inchangé à travers les 4 corrections) contre un plafond global 5% documenté, **64/112** combinaisons walk-forward le dépassent (68→67→66→64 au fil des 4 corrections, la dernière baisse venant de "Stop Loss = taille du canal"), root cause = pyramidage RANGE seul déjà au-dessus du plafond ; volontairement pas comblé par un plafond inventé — cf. `CONFIGURATION_RECOMMANDEE.md` section 5quater. **Fichier de test ajouté (4/4)** après un bilan directeur "regard neuf" ayant trouvé qu'aucun test n'existait malgré 3 resynchronisations manuelles déjà nécessaires — garde-fou anti-dérive direct (compare le gate dupliqué au VRAI moteur sur données réelles) |
| `code/test_no_execution_automation.py` | Garde-fou mécanique anti-automatisation-prématurée (2/2 tests, mobilisation multi-agents, bilan directeur) — échoue si un motif de passage d'ordre/credentials d'exchange apparaît dans `code/` ; confirmé propre aujourd'hui, cf. `PLAN.md` section "Garde-fou mécanique" |

### Historique / supersédé (pour archive — ne pas utiliser comme source de vérité)

| Document | Pourquoi supersédé | Remplacé par |
|---|---|---|
| `PHASE2_MONEYMANAGEMENT.md` | Première itération du money management Phase 2 (breakeven remonté trop tôt, à la Validation au lieu de la Confirmation — bug corrigé ensuite) ; ne couvrait que H4/D1 | `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` |
| `PHASE2_FULL_MATRIX.md` | Complète `PHASE2_MONEYMANAGEMENT.md` (mêmes limites : bug de breakeven prématuré, engendrant le résultat "H1 s'effondre" qui reflète surtout ce bug) | `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` |
| `PHASE2_CORRECTION_BREAKEVEN.md` | Documente une correction intermédiaire (breakeven différé à la Confirmation) désormais pleinement intégrée et documentée dans la version finale du moteur | `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` |
| `PHASE2_CORRECTION_CLOSES.md` | Documente une correction intermédiaire (validation sur clôtures, pas sur mèches) désormais pleinement intégrée dans la version finale du moteur | `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` |
| `PHASE2_V3_ATTEMPT_REGRESSION.md` | Tentative combinée (Règle de Trois + amplitude réelle + Extreme Channel) rejetée en l'état — régression sur H4 due à un bug de calibration (fenêtres en nombre de bougies, pas en durée réelle), corrigé depuis | `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` (le document lui-même l'indique : « remplace le cadre accepté/rejeté utilisé précédemment ») |
| ~~`CASCADE3_H1_EXECUTION_TEST.md` (version pré-correction)~~ | Résultat négatif/inconclusif calculé avec un moteur de risque antérieur à la correction « clôtures vs mèches » | `CASCADE3_H1_EXECUTION_TEST.md` a été refait le 2026-09-07 avec le moteur actuel (position_engine.py) — conclusion reconduite (voir référence actuelle ci-dessus) |

Les fichiers `.csv` du dépôt (résultats bruts de backtest) ne sont pas reclassés ici : ils restent les données sources citées par les documents ci-dessus, à jour ou historiques selon le document qui les référence.
