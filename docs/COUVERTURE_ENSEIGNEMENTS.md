# Couverture des enseignements — audit complet, implémenté vs manquant

Réponse à "est-ce que tous les enseignements sont pris en compte dans le plan à venir ?" : **non**. Voici l'audit précis, élément par élément, croisant `RULES_EXTRACTION.md` (manuel PDF) et `TRADING_LESSONS_INDEX.md` (17 sources vidéo) contre le code actuel (`code/`).

## ✅ Implémenté

| Élément | Où |
|---|---|
| Money management "trade spéculatif" (range), 4 profils | `code/backtest_phase2.py` et suivants |
| Breakeven différé à la Confirmation (7 sources) | Tous moteurs depuis la correction |
| Validation sur clôtures, pas mèches (3 sources) | Tous moteurs depuis la correction |
| Règle de Trois (risque /2 après 3 zones gagnantes) | v4, v5, v6, v7 |
| Amplitude réelle calibrée en durée (pas en bougies) | v4, v5, v6, v7 |
| Maturité par détection de swing points (≥3 bornes), **calcul rendu causal** (confirmation à t+SWING_ORDER, pas à l'instant du swing) **— cf. ⚠️→◐ P0-bis ci-dessous** | v4, v5, v6, v7, `code/backtest_phase2_capital_tiers.py` |
| Pyramidalisation multi-tranches | v4, v5, v6, v7 |
| TSI(14,7,9), momentum | `code/proxy_v2.py` |
| Cycle (approximation Hilbert du Sine Wave), signe corrigé et validé hors-échantillon, **calcul rendu causal** (fenêtre glissante) **— edge global mitigé, edge isolé du cycle non confirmé sur données réelles, cf. ⚠️→◐ ci-dessous** | `code/proxy_v2.py::compute_cycle_phase_causal` |
| Creux ascendants (structure), **calcul rendu causal** (confirmation à t+SWING_ORDER) **— cf. ⚠️→◐ P0-bis ci-dessous** | `code/proxy_v2.py::compute_ascending_lows`/`compute_swing_low_confirmed` |
| Classification de régime (Range/Tendance/Excès), interdiction de trader en Excès — **gate H4 natif restauré dans les moteurs opérationnels après un gap trouvé ce cycle, voir note dédiée ci-dessous** | `code/regime_classifier.py`, v6, v7, `code/backtest_phase2_faithful.py`, `code/unified_protocol.py`, `code/backtest_phase2_recommended.py` |
| **"Conflit Multi-Timeframe"** (source #5, "l'erreur numéro un" désignée par le corpus lui-même) — ne jamais trader une borne de range si un range d'unité de temps supérieure est déjà actif, désormais vérifié sur D1, voir note dédiée ci-dessous | `code/backtest_phase2_faithful.py`, `code/unified_protocol.py` |
| Pyramidalisation réservée au régime Tendance — **affirmation fausse pour v7 et ses descendants avant correction ce cycle, cf. note dédiée ci-dessous** | v6 (correct), `code/backtest_phase2_faithful.py`, `code/unified_protocol.py`, `code/backtest_phase2_recommended.py` (corrigés) |
| Validation croisée multi-timeframe (H4 exécution / D1 référence) | v7 |
| Extreme Channel pour le STOP = canal réel de l'UT supérieure (volet cross-timeframe seulement — voir note ci-dessous) | `code/backtest_phase2_v7.py` (`attach_higher_context` + `use_mtf_stop`), `MTF_CROSS_VALIDATION_H4_D1.md` |
| Garde-fous Phase 4 (checklist pré-trade, latence 10s, session max 60-90min) | Mentionnés dans `PLAN.md`, sources #5/#6 |
| **Table "trade de tendance" (5 étapes : Accumulation/Breakout/Divergence/Pull-Back/Excès final)** — implémentée, gate régime TENDANCE réutilisé tel quel, résultat mesuré MITIGÉ (cf. note dédiée ci-dessous) | `code/trend_table.py` (moteur), `code/backtest_phase2_trend.py` (backtest), `code/test_trend_table.py` (6/6 tests unitaires) |
| **Funding rate exact par timestamp** (coût réel événement-par-événement, appliqué à la taille réelle de chaque trade, tenant compte des sorties partielles Validation/Confirmation) | `code/position_engine.py` (trace optionnelle `record_trace=True`, additive, 5/5 tests toujours au vert), `code/backtest_phase2_v7.py` (`run_v7(..., record_trace=True)`), `code/funding_rate_exact.py`, `code/test_funding_rate_exact.py` (4/4), résultats chiffrés et conclusion : `FUNDING_RATE_ANALYSIS.md` section "Calcul EXACT par timestamp" — l'estimation grossière annualisée était une approximation correcte pour FAIBLE/MODERE (écart ≤ ±1,6 pt) mais sous-estimait le retour net réel sur les profils agressifs (jusqu'à −19,9 pts SOL TRES_AGRESSIF) ; sur H4/FAIBLE (le profil jugé le plus à risque par l'estimation grossière), le coût exact NE renverse ni n'efface l'edge sur aucun des 4 actifs testés |
| **Capital par palier (<10k€/10-100k€/>100k€), modulation du sizing** — voir note détaillée juste en dessous | `code/capital_tiers.py`, `code/backtest_phase2_capital_tiers.py`, `code/test_capital_tiers.py` |
| **Fibonacci retracement comme critère d'entrée additionnel** (règle exacte resynthétisée depuis les 5 sources, PAS la "zone dorée 38-61,8%" supposée au départ ; inclut désormais la "Règle des 50%" complète à 2 conditions cumulatives de #13 — voir note détaillée juste en dessous) | `code/fibonacci.py`, `code/backtest_phase2_fib.py`, `code/test_fibonacci.py` (8/8) |
| **Règle "Wall Street" (abstention), canal manuel (Supports→Apex→Tangente), Fourchette d'Andrews** — 3 patterns géométriques, chacun mesuré comme gate optionnel indépendant, résultats mitigés (voir note dédiée ci-dessous) | `code/wall_street_pattern.py`, `code/manual_trend_channel.py`, `code/andrews_pitchfork.py`, `code/backtest_phase2_patterns.py`, tests dédiés (4/4, 5/5, 5/5) |
| **Règle "UT+2" exacte (Hebdo, en sautant le D1)** — conclusion retenue après relecture précise des 6 sources : système à 3 niveaux à rôles distincts, pas une règle cumulative — voir note dédiée ci-dessous | `code/backtest_phase2_ut2.py`, `code/test_ut2.py` (4/4) |
| **Mécanisme "+Reverse" du profil Très Agressif à la Limite — TABLE RANGE spécifiquement** (RULES_EXTRACTION.md §3, "Très agressif : RIEN / — / RIEN / TP100%+Reverse") — à ne pas confondre avec le "+Reverse" de la table TENDANCE (§4, profil Très Agressif, étape Excès final, `trend_table.py::step_reverse`, hypothèse H10 — structurellement différent : inverse après une campagne à jambes multiples, pas après une simple tranche long). Manuel muet sur taille/stop/cible de la jambe reverse — **hypothèse d'implémentation explicite** posée (jambe short "miroir" : même taille, même distance % de stop/cible que la jambe long, ouverte au prix de clôture de la Limite) — voir note dédiée ci-dessous | `code/position_engine.py` (`process_reverse`, `reverse_at_limit=False` par défaut, 8/8 tests dont 3 nouveaux dédiés au reverse), `code/backtest_phase2_v7.py` (`run_v7(..., reverse_at_limit=True)`), `code/backtest_phase2_v7_reverse.py` (mesure BTC/ETH/BNB/SOL, profil TRES_AGRESSIF) |
| **Diversification statistique du risque "1%+1%" (2 patterns indépendants) + "Cluster Technique" (MA20+support) comme second pattern** — Pattern A = `proxy_v2.py` réutilisé tel quel, Pattern B = nouveau pattern MA20+support construit fidèlement depuis la source #16, chacun risquant 1% indépendamment (1 seule tranche par pattern, pas de pyramidalisation), plafond 2%/zone de prix de la source respecté par construction. Résultat mesuré **MITIGÉ** (voir note détaillée ci-dessous : l'effet isolé sur le drawdown est proche de zéro et mixte selon l'actif, PAS la réduction franche suggérée par une comparaison naïve) | `code/cluster_technique.py` (Pattern B, 6/6 tests), `code/diversification.py` (moteur, 5/5 tests), `code/backtest_phase2_diversification.py` (mesure BTC/ETH/BNB/SOL), `phase2_diversification_results.csv` |

**Note sur la classification de régime EXCES — gap H4 trouvé et corrigé (mobilisation multi-agents, audit proactif, ce cycle)** : `RULES_EXTRACTION.md` §1 ("Bulle/Excès -> NE PAS TRADER") est une règle littérale et inconditionnelle sur le régime du marché **réellement tradé** (H4 natif) — pas seulement sur son contexte supérieur. Depuis l'introduction de la validation croisée D1 dans `backtest_phase2_v7.py`, le gate ne vérifiait plus QUE le régime D1/Hebdomadaire ; le régime H4 natif restait calculé (`h4["regime"]`, via `regime_classifier.py::add_regime`, réutilisé tel quel) mais n'était plus jamais relu par le gate. Régression silencieuse introduite par le refactor v6→v7, jamais documentée comme un changement de comportement — preuve directe : le commentaire de tête de `backtest_phase2_fib.py` affirme encore *"ni le H4 ni le D1 ne doivent être en régime EXCES"* alors que son code ne vérifie que le D1, incohérence qui ne s'explique que par un oubli, pas un choix. Propagé silencieusement à `ut2.py`, `capital_tiers.py`, `fib.py`, `recommended.py`, `faithful.py`, `unified_protocol.py` (côté RANGE) et `diversification.py` (Pattern A) — tous héritent de la même lignée v7. Le côté TENDANCE de `unified_protocol.py` (via `trend_table.py`) n'était PAS affecté : il vérifie son propre régime H4 (`ev["regime_excess"]`), preuve que le bon comportement était connu et appliqué ailleurs dans le projet, seulement oublié lors du refactor v7.

**Corrigé, avec impact chiffré, dans les 3 moteurs opérationnellement recommandés** : `backtest_phase2_faithful.py` (nouvelle clé `regime_h4` dans `_prepare_features`, relue par le gate), `unified_protocol.py` côté RANGE (réutilise `feat["regime"]`, déjà calculé pour le côté TENDANCE — aucun nouveau calcul), `backtest_phase2_recommended.py` (même correction, par cohérence, moteur toujours activement cité/comparé). **Non corrigé, volontairement, documenté comme tel plutôt que silencieusement laissé de côté** : `v7.py`, `ut2.py`, `capital_tiers.py`, `fib.py`, `diversification.py` (Pattern A) — moteurs superseded ou de comparaison, pas réouverts pour ce fix (cohérent avec la discipline déjà appliquée aux autres chantiers clos de ce projet). **Impact le plus significatif** : BNB/TRES_AGRESSIF, identifié comme point le plus fragile de `unified_protocol.py` avant cette correction (retour -42,6%, drawdown -72,5% agrégés, année 2021 seule à -45,9%/-63,5%), redevient un profil simplement "à surveiller" après correction (retour +24,5%, drawdown -27,1% agrégés ; année 2021 à +6,2%/-8,4%) — cf. `PLAN.md` section "Correction EXCES-H4" et `CONFIGURATION_RECOMMANDEE.md` sections 5bis/5ter pour le détail chiffré complet. Découverte : audit systématique proactif (mobilisation multi-agents), pas une contestation utilisateur — cf. `PLAN.md` tableau "2e pattern récurrent", occurrence #3.

**Note sur la pyramidalisation réservée au régime Tendance — gap trouvé et corrigé (mobilisation multi-agents, 2e round, audit proactif, cycle suivant)** : `RULES_EXTRACTION.md` §3 (table Money Management RANGE) ne contient JAMAIS de cellule "Renfort", à aucune ligne de profil — contrairement à §4 (table TENDANCE) qui en a systématiquement. `backtest_phase2_v6.py` traduisait déjà correctement cette règle en code (`pyramiding_allowed = regime in ("TENDANCE", "RANGE_TENDANCIEL")`, appliqué SEULEMENT au renfort — sa propre docstring le dit explicitement : *"RANGE_NEUTRE : entrée standard, PAS de pyramidalisation (renfort réservé au suivi de tendance dans le manuel)"*) — règle perdue silencieusement au MÊME refactor v6→v7 qui a causé le gap EXCES-H4 ci-dessus (même root cause exacte : une donnée déjà calculée, `regime_h4`/`feat["regime"]`, mais jamais relue pour CETTE règle précise). Preuve littérale de la régression : le commentaire de `backtest_phase2_v7.py::gate_extra` dit lui-même *"Même gate pour l'entrée fraîche et le renfort (pas de distinction ici, contrairement à v6/fib)"* — un abandon documenté AU MOMENT du refactor, jamais remonté jusqu'à la ligne ✅ ci-dessus (qui affirmait à tort "v6, v7" comme si les deux respectaient la règle). Vérifié empiriquement AVANT correction (script de reproduction utilisant directement `position_engine.make_open_tranche_fn`, la vraie factory, pas une réimplémentation) : sur BTC/ETH/BNB/SOL réels, **25,7% des renforts réellement ouverts** (4544 renforts observés au total sur `faithful.py`/`recommended.py` × 4 actifs × 4 profils) l'étaient en régime RANGE_NEUTRE — exactement ce que v6 bloquait. **Corrigé pour le protocole opérationnel** (`backtest_phase2_faithful.py`, `unified_protocol.py`, `backtest_phase2_recommended.py`, + `risk_aggregation_triple_system.py` par cohérence) — `gate_extra` distingue désormais entrée fraîche (inchangée) et renfort (`pyramiding_allowed` réutilisant la donnée déjà calculée). **Non corrigé, documenté comme tel** dans les mêmes moteurs superseded que EXCES-H4 (`v7`, `ut2`, `capital_tiers`, `fib`). Tests dédiés ajoutés (scénario synthétique à vérité terrain connue, contrôle positif ET négatif) dans `test_backtest_phase2_faithful.py`/`test_backtest_phase2_recommended.py`/`test_unified_protocol.py`. Impact chiffré : mouvement modeste dans le même sens que EXCES-H4 sur toutes les métriques déjà régénérées (cf. `PLAN.md`/`CONFIGURATION_RECOMMANDEE.md` pour le détail avant/après complet). Découverte : audit systématique proactif (2e round de mobilisation multi-agents), même méthode que l'occurrence #3 — cf. `PLAN.md` tableau "2e pattern récurrent", occurrence #4.

**Note sur "Conflit Multi-Timeframe" — trouvée puis implémentée (mobilisation multi-agents, 2 rounds : re-audit puis design/décision/implémentation)** : `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` (source #5) désigne cette règle comme *"l'erreur numéro un"* : *"Ne jamais trader une borne de range si un range d'unité de temps supérieure est déjà actif. La structure supérieure prime systématiquement."* — confirmée par sa propre checklist pré-trade. `TRADING_LESSONS_INDEX.md` (ligne 11) avait déjà noté, au moment du traitement initial des sources, que cette règle est DISTINCTE de "UT+2" (déjà implémentée) — jamais reprise depuis dans ce document. Vérifié dans le code AVANT correction : aucun gate ne vérifiait si le régime du contexte supérieur était LUI-MÊME en range avant d'ouvrir une tranche RANGE sur H4 — le gate Hebdomadaire existant ("UT+2 strict", score≥2) est une condition de momentum/alignement, logiquement distincte d'une exclusion "le TF supérieur est en range".

**Décision de conception (2 points que le corpus ne tranche pas explicitement, résolus par citation + mesure empirique plutôt que laissés en attente indéfiniment)** :
- **Niveau : D1, pas Hebdomadaire.** 3 agents (recherche dédiée + second avis totalement indépendant) convergent : caler le gate sur le niveau Hebdomadaire bloquerait ~89% des tranches RANGE actuellement ouvertes (mesuré empiriquement, `record_trace=True` sur `faithful.py` réel) — quasi-suppression du système RANGE, incohérent avec le fait que le corpus décrit lui-même RANGE comme le régime dominant (~75% cumulé, §1). D1 ne bloque que ~12-19% (BTC 13,0%/ETH 7,5%/BNB 19,1%/SOL 3,7%). Étayé textuellement par analogie avec `TRADING_LESSONS_TROISIEME_BORNE.md` (source #11 : "Contexte" = UT immédiatement supérieure à l'exécution, vocabulaire cohérent avec "UT+1" déjà utilisé dans ce projet pour le stop D1).
- **Entrée fraîche ET renfort, uniformément.** Ni source #5 ni source #11 ne distinguent les deux cas (contrairement à pyramidalisation-régime, où l'absence littérale de "Renfort" en §3 tranchait précisément) ; la table RANGE n'a de toute façon pas de mécanique "renfort" distincte d'une "entrée" dans l'architecture de ce projet. Appliqué via `gate()`, partagé par les deux cas.

**Implémenté** dans `code/backtest_phase2_faithful.py`/`code/unified_protocol.py` (RANGE) — donnée réutilisée, pas recalculée : `ctx["D1"]["regime"]`, déjà calculée pour le stop `ctx_support_d1`, jamais lue pour cette règle précise (même schéma "donnée déjà calculée, jamais relue" que EXCES-H4/pyramidalisation-régime). **Non étendu à `code/backtest_phase2_recommended.py`** (exige une dépendance D1 hors de son périmètre documenté). `code/risk_aggregation_triple_system.py` patché par cohérence (3e patch). Tests dédiés (4 nouveaux, scénario synthétique à vérité terrain connue). Impact chiffré : mouvement modeste dans le même sens que EXCES-H4/pyramidalisation-régime — cf. `PLAN.md`/`CONFIGURATION_RECOMMANDEE.md` pour le détail avant/après complet. Découverte initiale : audit systématique proactif ; implémentation : décision de conception explicite, pas une invention silencieuse.

**Note sur "Capital par palier" (item 6 du backlog `PLAN.md`, traité ce cycle)** : `RULES_EXTRACTION.md` §5 ne donne AUCUN chiffre de `risk_pct` par palier — seulement une recommandation qualitative ("agressivité quasi obligatoire" <10k€, "arbitrage risque/sécurité" 10-100k€, "agressivité maximale déconseillée par précaution" >100k€). Traduit en une règle testable via une **hypothèse d'implémentation explicite**, documentée en tête de `code/capital_tiers.py` : multiplicateur ×1.25 du `risk_pct` du profil choisi sous 10k€, aucune modulation entre 10k et 100k€, plafond dur à 2% au-dessus de 100k€ (neutralise concrètement AGRESSIF/TRES_AGRESSIF pour les gros capitaux) — toujours sous le plafond absolu de 5% du §5. Le nombre de tranches simultanées (`MAX_TRANCHES`) n'est **pas** modulé par palier : la source ne relie jamais explicitement le nombre de positions au capital pour cette règle précise (à ne pas confondre avec la "diversification 1%+1%" du corpus #16, sujet distinct, désormais traité — cf. note dédiée ci-dessous — via `code/diversification.py`).

Résultat chiffré, honnête, BTC/ETH/BNB/SOL, H4 gated par D1 (`phase2_capital_tiers_results.csv`) — 5 000€/50 000€/500 000€ testés comme représentants des 3 paliers :
- Comme attendu d'un sizing qui ne fait que changer la fraction du capital risquée par trade, **le win rate et le profit factor ne bougent quasiment pas** entre paliers (le sizing ne change pas quels trades gagnent ou perdent) — seuls le retour total et le drawdown en absolu sont affectés, proportionnellement au risk_pct effectif.
- Palier 1 (×1.25) vs palier 2 (référence) : retour total moyen (4 actifs) **+7,3 pts** (FAIBLE) à **+27,0 pts** (AGRESSIF) — TRES_AGRESSIF déjà plafonné à 5% par la règle dure, donc **aucun effet** (le multiplicateur ×1.25 pousserait à 6,25%, plafonné à 5%).
- Palier 3 (plafond 2%) vs palier 2 : **aucun effet** sur FAIBLE/MODERE (déjà ≤2%) ; AGRESSIF **-43,6 pts** de retour total moyen, TRES_AGRESSIF **-104,4 pts** — la précaution "agressivité maximale déconseillée" a un effet mesuré substantiel sur les profils agressifs à gros capital.
- Conforme au principe de tête de ce document : ces chiffres ne remettent pas en cause l'implémentation (la règle du manuel dit explicitement de moins risquer avec un gros capital, l'effet mesuré va dans ce sens et le confirme, il n'a jamais servi à décider si l'implémenter).

**Mise à jour ce cycle (dette de requantification P0-bis résorbée)** : les chiffres ci-dessus dataient de la structure batch (pré-P0-bis). Rejoués avec la structure causale (`compute_swing_low_confirmed`) — même conclusion qualitative, ampleur légèrement réduite (retour total moyen ÷1,08 environ, win rate/profit factor quasi stables, 0/48 bascule de signe) : voir le tableau chiffré complet dans la section ⚠️→◐ P0-bis ci-dessous.

**Note sur "Diversification 1%+1% + Cluster Technique" (item 7 du backlog `PLAN.md`, moitié traitée ce cycle — source #16, `TRADING_LESSONS_CLUSTERS_PRIX.md`)** : règle extraite littéralement — "1% sur la pattern breakout/pullback + 1% sur la pattern de moyenne mobile (cluster)", plafond "jamais >2% de risque maximal par zone de prix". Hypothèses d'implémentation explicites (détail complet dans les docstrings de tête de `code/cluster_technique.py` et `code/diversification.py`, toutes numérotées H1-H6) : Pattern A = `proxy_v2.py` réutilisé tel quel (le seul signal de tendance déjà en production dans ce projet, candidat le plus fidèle pour "breakout/pullback") ; Pattern B ("Cluster Technique") = MA20 (rebond mean-reversion, mèche touche/traverse la MA mais clôture au-dessus) + confluence support (proximité PERCENTILE ADAPTATIVE — pas une fraction fixe, cf. correction empirique ci-dessous — avec l'Extreme Channel de l'UT supérieure D1) + régime TENDANCE lu sur le D1 (flux de référence) ; risque FIXE 1%/1% (pas modulé par profil, contrairement au reste du risk management de ce projet) ; 1 seule tranche par pattern (pas de pyramidalisation pour cette règle précise, jamais mentionnée par la source ici) ; plafond capital_tiers.HARD_MAX_RISK_PCT (§5, 5%) réutilisé tel quel comme garde-fou au-dessus du plafond 2% propre à cette règle.

**Correction faite après vérification empirique AVANT d'aller plus loin** (discipline "mode ingénieur senior", `PLAN.md`) : une première définition de la confluence support (distance fixe <= 0,5 x largeur de canal) ne se déclenchait JAMAIS sur BTC/ETH/BNB/SOL H4 réels (0/14 239 bougies x 4 actifs) — en régime TENDANCE, la distance médiane au support mesurée est d'environ 6 largeurs de canal (minimum observé ~0,42), la borne fixe était donc quasiment inatteignable par construction du canal EMA±ATR déjà documenté ailleurs comme imparfait. Corrigé avec la MÊME solution déjà utilisée par `regime_classifier.py` pour le même problème d'échelle : un seuil percentile adaptatif calculé sur fenêtre glissante causale. Revérifié : produit un signal rare mais réel.

**Résultat chiffré, honnête, BTC/ETH/BNB/SOL H4** (`code/backtest_phase2_diversification.py`, `phase2_diversification_results.csv`) — **MITIGÉ, pas résolu au sens plein, pour une raison de méthode découverte en cours de route, pas cachée après coup** : une comparaison NAÏVE (diversifié 1%+1% vs `run_v7` MODERE à 2%, référence pyramidalisée) montrait une réduction de drawdown massive (+6,5 à +13,2 points selon l'actif) — mais `code/diversification.py` (H4 de sa docstring) ne pyramidalise JAMAIS par construction (1 tranche/pattern), contrairement à `run_v7` (jusqu'à 3 tranches). Cette comparaison mélangeait donc DEUX effets : l'ajout réel du Cluster Technique, ET la suppression de la pyramidalisation (déjà documentée ailleurs dans ce projet comme "le principal moteur de rendement" — ici on découvre qu'elle est AUSSI le principal moteur du drawdown). Une comparaison ISOLÉE (même moteur sans pyramide des deux côtés, Pattern B désactivé vs activé) donne l'effet MARGINAL réel : **+0,2 pt (BTC), +0,3 pt (ETH), -0,2 pt (BNB), -0,1 pt (SOL)** — proche de zéro et MIXTE (2/4 actifs légèrement réduits, 2/4 légèrement aggravés), PAS une réduction de drawdown consistante comme le suggère la source. L'implémentation est fidèle au corpus et fonctionnelle (les deux patterns tournent bien simultanément et indépendamment, vérifié par test), mais l'avantage en drawdown promis par la source #16 pour cette diversification précise **n'est pas confirmé empiriquement** une fois l'effet confondu retiré — conforme au principe de tête de ce document : ce résultat décevant ne remet pas en cause l'implémentation elle-même.

**Risque agrégé RANGE+TENDANCE+diversification, chiffré (mobilisation multi-agents, Agent B, cycle suivant)** : le plafond §5 (5% global) réutilisé ci-dessus comme garde-fou concerne cette règle de diversification prise isolément — mais rien ne chiffrait jusqu'ici le cumul quand `unified_protocol.py` (RANGE+TENDANCE) ET `diversification.py` (Pattern A + Pattern B) tournent TOUS EN MÊME TEMPS sur le même actif, chacun avec son propre risque nominal, sans plafond commun. `code/risk_aggregation_triple_system.py` (nouveau) calcule ce cumul bougie par bougie. Résultat honnête (`risk_aggregation_full_history.csv`/`risk_aggregation_walkforward.csv`, BTC/ETH/BNB/SOL × 4 profils, RE-régénéré après les corrections pyramidalisation-régime, Conflit MTF, PUIS "Stop Loss = taille du canal" — cf. `PLAN.md`) : risque agrégé maximal **17,00%** (BTC/TRES_AGRESSIF, inchangé), très au-dessus du plafond global 5% ; **64/112** combinaisons année×actif×profil le dépassent en walk-forward (était 68/112 avant toute correction, 66/112 après Conflit MTF — la baisse à 64/112 vient de "Stop Loss = taille du canal", qui réduit le capital risqué sur les bougies à canal très large). Root cause : le pyramidage RANGE seul (3×5%=15% en TRES_AGRESSIF) dépasse déjà ce plafond avant toute combinaison — pas un défaut de la diversification elle-même. Volontairement pas comblé par un plafond agrégé inventé (le corpus ne spécifie rien pour cette combinaison précise de systèmes) — documenté comme limite ouverte, cf. `PLAN.md`/`CONFIGURATION_RECOMMANDEE.md` section 5quater.

**Vague 5 — robustesse du seuil de confluence Cluster Technique (`SUPPORT_PROXIMITY_PCTL`, `PLAN.md` "Plan d'autonomie 8h"), traitée** : le seuil percentile adaptatif retenu ci-dessus (0,20) avait été choisi pour une raison EMPIRIQUE (c'est la valeur qui a permis au signal de se déclencher du tout, pas une valeur choisie pour maximiser une performance) — jamais stress-testé depuis. Test de SENSIBILITÉ (pas une recherche du meilleur seuil, cf. mise en garde explicite reçue pour ce chantier) : `code/cluster_technique_threshold_robustness.py` (`code/test_cluster_technique_threshold_robustness.py`, 5/5 tests, dont 2 tests de non-régression vérifiant que la duplication de `prepare_diversified`/`run_diversified` avec `proximity_pctl` paramétrable reproduit EXACTEMENT `diversification.py` au seuil de référence — aucun fichier de production modifié, même discipline que `backtest_phase2_fib.py`). Grille SYMÉTRIQUE fixée a priori autour de 0,20 : **0,10 / 0,15 / 0,20 (référence, inchangée) / 0,25 / 0,30**, comparaison ISOLÉE reprise à l'identique (pyramidalisation neutralisée des deux côtés, celle qui donne l'effet marginal réel, pas la comparaison naïve).

Résultat chiffré, honnête, BTC/ETH/BNB/SOL H4, 4 actifs × 5 seuils = 20 configurations (`code/cluster_technique_threshold_robustness_results.csv`) — effet marginal isolé sur le drawdown (delta_dd, positif = réduit le DD) :

| Actif | 0,10 | 0,15 | 0,20 (référence) | 0,25 | 0,30 | Amplitude | Signe stable ? |
|---|---|---|---|---|---|---|---|
| BTC | +0,2 | +0,3 | +0,2 | +0,2 | +0,3 | 0,1 pt | Oui (toujours positif) |
| ETH | 0,0 | +0,2 | +0,3 | +0,1 | +0,2 | 0,3 pt | Oui (toujours ≥0) |
| BNB | +0,1 | 0,0 | **-0,2** | -0,1 | +0,1 | 0,3 pt | Non (change de signe) |
| SOL | 0,0 | 0,0 | -0,1 | -0,4 | -0,4 | 0,4 pt | Oui (toujours ≤0) |

**Confirmé au seuil de référence (0,20)** : les 4 valeurs (+0,2/+0,3/-0,2/-0,1) reproduisent EXACTEMENT celles déjà citées ci-dessus (`phase2_diversification_results.csv`) — la duplication du moteur pour ce test de robustesse est fidèle, pas une réimplémentation qui aurait dérivé.

**Lecture de robustesse, pas une découverte à célébrer** : sur les 20 configurations testées, l'effet marginal du Cluster Technique sur le drawdown reste **partout du même ordre de grandeur — jamais au-delà de ±0,4 point** — quel que soit le seuil choisi dans cette grille raisonnable. L'amplitude totale observée (écart entre le minimum et le maximum sur toute la grille) est de 0,7 point. Un seul actif (BNB) change de signe selon le seuil (-0,2 à 0,20, mais +0,1 à 0,10/0,30) — à signaler explicitement comme un signal de fragilité potentielle plutôt qu'à passer sous silence, mais l'amplitude de ce changement de signe (0,3 point) reste elle-même proche de zéro, cohérente avec un effet qui est déjà, au départ, à peine distinguable du bruit plutôt qu'avec une conclusion qui basculerait réellement d'un seuil à l'autre (aucun seuil ne fait apparaître un effet net et cohérent dans un sens ou dans l'autre). **Conclusion honnête** : la conclusion "effet quasi nul et mixte selon l'actif" mesurée au seuil de production (0,20) est **robuste** au choix précis du percentile de confluence dans cette plage — ce n'est PAS un artefact du seuil retenu, ni un signe de sur-ajustement caché sur ce paramètre précis. Ceci ne dit rien sur la robustesse des AUTRES hypothèses d'implémentation du module (H1 rebond MA20 sans tolérance, H3 lecture du régime, etc.), non testées ici.

**Note sur "Extreme Channel pour le STOP" (P1 du backlog, traité ce cycle)** : le stop bénéficie désormais de la même jointure cross-timeframe sans lookahead que le signal (`ctx_support` D1 transmis au H4). Statut honnête, en deux volets à ne pas confondre :
- Volet cross-timeframe ("contexte de l'UT supérieure affiché sur l'UT de trading", #12/#16) : ✅ résolu et mesuré (`MTF_CROSS_VALIDATION_H4_D1.md`, chiffres recalculés avec le moteur déjà causal — cf. ⚠️→◐ juste au-dessus) — la mesure montre que ce stop D1 réel **dégrade** le ratio retour/drawdown dans 13/16 combinaisons actif×profil testées, tout en réduisant le drawdown absolu dans 16/16 (le sizing à risque fixe réduit mécaniquement la taille de position quand le stop réel est plus loin, ~78% du temps).
- Volet "channel" (une vraie construction géométrique de canal, pas une bande EMA±ATR) : toujours ❌, cf. l'item "Canal de tendance manuel (Supports→Apex→Tangente)" dans le tableau ci-dessous — hors périmètre de cette correction.

**CORRECTION (8 sept. 2026)** : la phrase qui suivait ici ("seul le stop H4 reste donc le réglage par défaut... la nouvelle option restant disponible pour comparaison") appliquait à tort le principe "la performance ne remet pas en cause l'implémentation" en s'arrêtant à mi-chemin — le stop UT+1 est une règle LITTÉRALE du corpus (`TRADING_LESSONS_BREAKOUT_RATIO11.md` #12, "Stop-loss = clôture la plus basse du canal de tendance de l'UT+1"), pas une simple variante de réglage : la contre-performance mesurée ne justifie pas de le laisser optionnel-off-par-défaut dans le protocole réellement utilisé, exactement comme pour n'importe quel autre élément de l'IP de Philippe. `code/backtest_phase2_faithful.py` l'active désormais SANS CONDITION (cf. `CONFIGURATION_RECOMMANDEE.md` section 5ter, `PLAN.md`) — `use_mtf_stop=False` reste le comportement de `recommended.py`/`v7.py` (moteurs de comparaison, inchangés), mais n'est plus "le" réglage recommandé pour un usage réel.

**Note sur "Fibonacci retracement" (item 4 du backlog `PLAN.md`, traité ce cycle)** : la tâche demandait de vérifier la "zone dorée 38-61,8%" supposée au départ auprès des 5 sources citées (#9, #10, #11, #13, #14, identifiées via `TRADING_LESSONS_INDEX.md`). Une fois relues précisément, **ces 5 sources ne disent PAS ça** — constat honnête documenté en tête de `code/fibonacci.py` plutôt que la supposition initiale reproduite sans vérification :
- #13 (`TRADING_LESSONS_PULLBACK_MATURITE.md`, la plus précise) : 23% = seuil MINIMAL de validation, 38% = "standard et optimal", 50% = "exceptionnel" ; table GO/WAIT : GO entre 23% et 38%.
- #10 (`TRADING_LESSONS_ZONE_ACCUMULATION.md`) : cluster favorable 38-50%, 61% = seuil de tolérance MAXIMUM (Red Flag au-delà).
- #11 (`TRADING_LESSONS_TROISIEME_BORNE.md`) : niveau PONCTUEL 61,8% pour le pullback post-3ème-borne (pas un intervalle général).
- #9 (`TRADING_LESSONS_MTF_SUIVI_TENDANCE.md`) et #14 (`TRADING_LESSONS_STRUCTURES_ALTERATIONS.md`) : **aucun niveau de retracement chiffré** (respectivement règle UT+2 et extensions Fibonacci pour la prise de profit, pas l'entrée) — 2 des 5 sources attribuées à cette ligne ne documentent en réalité aucune règle d'entrée par retracement.
- Le niveau "76,4%" cité dans l'ancienne ligne ❌ ci-dessous n'apparaît dans AUCUNE des 5 sources une fois relues — signalé plutôt que reproduit tel quel.

Synthèse retenue : zone FAVORABLE = [23%, 61,8%] (union du minimum #13 et du plafond de tolérance #10/#11), sous-zone OPTIMALE = [23%, 50%] (cluster #10/#13). Pivot de mesure : dernier swing low suivi du dernier swing high (détection `scipy.signal.argrelextrema`, MÊME fonction/ordre que `proxy_v2.py::compute_ascending_lows`, réutilisée telle quelle).

**CORRECTION PARTIELLE (mobilisation multi-agents, Agent C, re-audit — cycle suivant)** : le constat "76,4% n'apparaît dans AUCUNE des 5 sources" (ligne ci-dessus) ne vérifiait ces 5 sources QUE contre les 17 sources vidéo Trading Lessons — jamais contre `RULES_EXTRACTION.md` (le manuel PDF officiel) lui-même, qui donne pourtant LITTÉRALEMENT ce chiffre : §1 ("Classification du contexte") — *"Range neutre : Retracement fibo ≥76,4%... Range tendanciel : Retracement ≥61,8%... Tendance : Retracement ≥23%"*, un seuil DIFFÉRENT par régime, jamais implémenté ni comparé à la synthèse [23%,61,8%] retenue ci-dessus. `TRADING_LESSONS_INDEX.md` (ligne 11) note d'ailleurs elle-même une "tension à réconcilier" avec `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` (signal couleur→61% Fibo possible, signal gris→76% Fibo exigé) — jamais réconciliée depuis. Le gate Fibonacci RANGE reste classé "hypothèse d'implémentation, réglable par performance" (cf. `CONFIGURATION_RECOMMANDEE.md` section 5ter) pour la lecture vidéo déjà testée — cette classification est correcte sur SON périmètre, mais incomplète : elle n'a jamais couvert la lecture manuel-littérale par régime.

**Approfondi ce cycle (round suivant, 3 agents convergents — recherche dédiée + second avis indépendant)** : ce n'est en réalité PAS un simple seuil à faire varier par régime — §1 pose une condition COMPOSITE à 3 volets (retracement + "débordement du contexte" + "signal & triangle de confirmation"), dont 2 des 3 n'ont AUCUNE définition utilisable ailleurs dans le corpus, vérifié par relecture exhaustive des 17 sources. "Triangle" n'a qu'UNE seule mention dans tout le corpus (`TRADING_LESSONS_ZONE_ACCUMULATION.md`, un pattern algorithmique de retournement de prix, jamais relié à cette règle), et aucun code du projet ne le détecte. "Débordement" n'est défini que pour un usage DIFFÉRENT (`TRADING_LESSONS_PULLBACK_MATURITE.md` : *"Débordement franc du sommet précédent, sortie des prix hors des enveloppes de contexte"* — un critère d'Excès Final/fin de cycle TENDANCE, pas une condition d'entrée RANGE_NEUTRE). **Risque de confusion supplémentaire identifié, à ne jamais reproduire silencieusement** : le même chiffre "76%" désigne ICI un seuil d'ENTRÉE (§1) et, ailleurs (`TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` §5 : *"Objectifs : Range Neutre = 76% Fibonacci de la vague précédente"*), une cible de SORTIE — deux règles distinctes qui partagent un chiffre par coïncidence apparente, pas la même règle. Implémenter la lecture §1 exigerait d'inventer 2 définitions non citées ("débordement", "triangle") — refusé explicitement, cohérent avec le principe "aucune invention non citée" de ce projet. **Non implémenté, décision prise et documentée** (pas seulement "pas encore tranché") : backlog P0/P1 — cf. `PLAN.md` backlog item 10, `CONFIGURATION_RECOMMANDEE.md` section 5quinquies.

Implémentation : filtre optionnel (`use_fib_gate`, défaut `False`, comme `use_mtf_stop`) ajouté à `open_tranche_fn` du nouveau moteur `code/backtest_phase2_fib.py` (dupliqué de `backtest_phase2_v7.py::run_v7`, ne touche à aucun fichier de production, réutilise `add_proxy_v2_score` tel quel). Appliqué à l'ENTRÉE FRAÎCHE seulement par défaut, PAS aux renforts/pyramidalisation (`fib_gate_pyramid=False`) — raison structurelle, pas un réglage choisi pour arranger le chiffre : un renfort dans ce moteur n'est tenté que lorsque le prix fait un NOUVEAU PLUS HAUT (`high[i-1] > last_pyramid_high`), donc à un retracement proche de 0% par construction, une contradiction quasi structurelle avec l'exigence d'un retracement 23-61,8%. Cohérent avec le corpus lui-même (source #9 distingue explicitement "trade d'accumulation" [pullback, la phase que Fibonacci décrit] et "renfort de position" sur la "verticalité" [nouveau plus haut, pas un retracement]).

Résultat chiffré, honnête, BTC/ETH/BNB/SOL × 4 profils, H4 gated par D1 (`phase2_fib_results.csv`) — **32/32 configurations dégradées** par le filtre, quelle que soit la variante testée :

| Variante | Retour total moyen | Win rate moyen | Profit factor moyen | n_trades moyen |
|---|---|---|---|---|
| Baseline v7 (sans Fibonacci) | +107,8 % | 42,5 % | 1,92 | 456,5 |
| + Fibonacci favorable [23%,61,8%], entrée fraîche seule | **-13,8 %** | 31,4 % | 0,66 | 144,5 |
| + Fibonacci optimal [23%,50%], entrée fraîche seule | **-12,7 %** | 31,1 % | 0,68 | 136,0 |
| + Fibonacci favorable, y compris renforts | **-17,5 %** | 28,9 % | 0,47 | 121,8 |

Vérifié qu'il ne s'agit pas d'un bug (pas d'inversion de sens ni de fuite temporelle) : les entrées retenues ont bien un retracement mesuré entre 23% et ~61% au moment de l'entrée (tracé bougie par bougie sur les 4 actifs), et la réduction du nombre de trades est répartie sur toutes les années testées (2020-2026), pas concentrée sur une année atypique. Sur ce proxy précis (TSI+cycle+structure), le filtre Fibonacci retracement **dégrade uniformément** la performance : il exclut disproportionnellement les entrées à faible retracement (près des plus hauts, où ce proxy momentum/breakout tend à mieux performer) et retient les pullbacks profonds, qui s'avèrent être des signaux de moins bonne qualité pour CE proxy précis. Conforme au principe de tête de ce document : **ce résultat décevant ne remet pas en cause l'implémentation** — l'élément est fidèlement implémenté et documenté ; il est gardé optionnel (`use_fib_gate=False` par défaut) plutôt qu'adopté par défaut. **Nuance ajoutée après correction (cf. note "Extreme Channel pour le STOP" ci-dessus)** : `use_fib_gate` reste correctement optionnel-par-défaut, mais pour une raison DIFFÉRENTE de celle donnée jusqu'ici pour `use_mtf_stop` — la règle Fibonacci littérale du corpus existe (23-38%/38-61%) mais documente un pullback de TENDANCE, déjà implémentée sans condition dans `trend_table.py` ; l'appliquer EN PLUS comme filtre d'entrée sur la table RANGE (ce que fait ce gate) est une extrapolation à nous, pas ce que le corpus dit pour ce protocole précis — une VRAIE hypothèse d'implémentation, contrairement au stop UT+1 qui, lui, est désormais activé sans condition (`backtest_phase2_faithful.py`).

**MISE À JOUR (5e round de mobilisation multi-agents, catégorie B, traitée ce cycle) : "Règle des 50%" complétée, PUIS CORRIGÉE (vérification adversariale dédiée, 6e round)**. La classification favorable/optimale ci-dessus (23-61,8%/23-50%) ne teste QUE le retracement — l'audit exhaustif (section dédiée plus bas) avait trouvé que la 2e condition cumulative de #13 ("pénétration dans les 50% inférieurs du CONTEXTE", pas du retracement lui-même) était citée dans le commentaire de `code/fibonacci.py` mais jamais codée. Comblé : `compute_context_position`/`classify_regle_50` (canal de contexte `CONTEXT_DURATION="15D"`, MÊME définition que `trend_table.py::add_trend_context`/`accum_retracement_frac` — PAS `backtest_phase2_v7.py::prepare`, référence corrigée après vérification : ce fichier ne calcule ni `ctx_high` ni `ctx_low` — recalculée localement dans `fibonacci.py` pour ne pas créer de dépendance, H6 de `trend_table.py` préservé), colonnes `fib_context_position`/`fib_regle_50` ajoutées à `add_fibonacci_columns`, câblées comme nouvelle variante `use_fib_regle_50` dans `backtest_phase2_fib.py::run_v7_fib` (même pattern que `use_fib_optimal`). **Bug réel trouvé par la vérification adversariale, corrigé** : la version initiale ne plafonnait le retracement que par le bas (sans `r <= FAVORABLE_MAX`), supprimant de fait le Red Flag #10 que ce fichier documente lui-même comme invalidant au-delà de 61,8% — mesuré : la version buguée était en réalité PLUS LARGE que `fib_favorable` en nombre de bougies (2642 vs 2388 sur BTC H4), contredisant la prétention "beaucoup plus restrictive" de la version d'origine. Plafond ajouté ; `fib_regle_50` est désormais un sous-ensemble STRICT vérifié de `fib_favorable` (920/14239 bougies BTC H4). **Nuance sur la distinction avec H7** (formulation corrigée — l'affirmation initiale d'une séparation nette était une justification a posteriori fragile) : la tension entre cette règle et l'hypothèse H7 de `trend_table.py` ("retour min 50% contexte", RULES_EXTRACTION.md §1, interprétée là-bas comme "recovery_frac >= 0.50") N'EST PAS réconciliée — les deux phrases décrivent très probablement la même étape Pull-Back de la même table compressée, #13 désambiguïsant plausiblement dans le sens "position dans le canal" plutôt que "recovery_frac". H7 n'est pas modifiée (changement de comportement hors périmètre), mais la tension est désormais tracée dans `trend_table.py` lui-même, à côté de H7, pour qu'un futur lecteur la voie. Résultat mesuré (`phase2_fib_results.csv`, colonne `variant` = `fib_regle_50 [...]`, régénéré après correction), honnête : 2 à 13 trades selon l'actif (vs **80-137**, chiffre corrigé — "80-167" cité initialement n'existait dans aucune variante mesurée, vs `fib_favorable`) ; rendement absolu négatif sur 15/16 couples actif×profil (seul SOL/FAIBLE positif, +0,5 pt) mais **dégradé vs `baseline_v7` sur 16/16** (delta -21 à -201 pts — l'ancienne formulation "15/16 dégradés" confondait le signe absolu avec la comparaison à la baseline, corrigée). Implémenté intégralement malgré ce résultat, conformément au principe inviolable du projet : la performance du proxy ne décide jamais si un élément littéralement documenté de l'IP de Philippe doit être implémenté. 3 nouveaux tests à vérité terrain (`test_fibonacci.py`, désormais 8/8, dont un dédié au cas `retracement>61,8%` qui aurait révélé le bug initial).

**Note sur "Table trade de tendance" (item 1 du backlog `PLAN.md`, traité ce cycle)** : implémentée fidèlement (RULES_EXTRACTION.md §4), moteur dédié (`code/trend_table.py` + `code/backtest_phase2_trend.py`), n'ouvre une position QUE quand `regime_classifier.add_regime` classe la bougie en TENDANCE (réutilisé tel quel, pas modifié). `position_engine.py` n'a PAS été réutilisé (ni modifié) : sa machine à états à 4 seuils fixes (Validation/Confirmation/Invalidation/Limite) est structurellement incompatible avec une table dont les étapes sont des événements de structure de marché détectés, dont le renforcement démarre AVANT l'entrée et dont un profil exige une position short après clôture (+Reverse) — justification complète en tête de `code/trend_table.py`. 6/6 tests unitaires passent (`code/test_trend_table.py`, cas calculés à la main, séquence complète Accumulation→Breakout→Divergence→Pull-Back→Excès final avec P&L exact, plus le mécanisme +Reverse et le plafond de risque testés isolément).

12 hypothèses d'implémentation documentées explicitement en tête de `code/trend_table.py` (le manuel est sous-spécifié sur l'unité de "Renfort +X%", le calcul de retracement, la divergence "standard", le proxy de volume, le mécanisme +Reverse, etc. — H1 à H12, pas d'invention silencieuse). Point notable : H3 introduit un plafond de risque de campagne à 5% (RULES_EXTRACTION §5, "perte spéculative jamais >5% du capital") ABSENT du §4 lui-même mais nécessaire pour rendre la table applicable sans le violer (les "Renfort" cumulés du profil Très Agressif atteignent 4x la taille de risque nominale du profil).

**Résultat chiffré, honnête, BTC/ETH/BNB/SOL H4** (`phase2_trend_table_results.csv`), pas de validation croisée MTF ici (scope volontaire, cf. hypothèse H11 — mesurer cette table isolément) :

| Symbole | FAIBLE (n / retour%) | MODERE (n / retour%) | AGRESSIF (n / retour%) | TRES_AGRESSIF (n / retour%) |
|---|---|---|---|---|
| BTC | 0 / 0,0% | 7 / +0,1% | 7 / +0,2% | 7 / 0,0% |
| ETH | 1 / -0,1% | 8 / -2,8% | 8 / -5,5% | 8 / -10,8% |
| BNB | 3 / +1,1% | 16 / -8,1% | 16 / -15,6% | 16 / -27,4% |
| SOL | 3 / +1,1% | 8 / -5,1% | 8 / -9,9% | 8 / -16,9% |

**Constat le plus important, pas maquillé** : en instrumentant le moteur pour tracer l'étape à laquelle chaque campagne se clôture, **100% des campagnes fermées, sur les 4 actifs et les 3 profils MODERE/AGRESSIF/TRES_AGRESSIF (qui partagent les mêmes déclencheurs d'événements, seule la taille diffère), se ferment en étape ACCUMULATION** — soit par le stop de protection, soit par abandon régime-EXCES — **aucune n'atteint jamais Breakout**. Vérifié directement : `accumulation_active` (le déclencheur d'ouverture) ne s'active que 7 à 16 fois sur tout l'historique H4 de chaque actif (2020-2025), et le nombre de trades enregistrés correspond exactement à ce compte — cohérent avec le constat qu'aucune campagne ne progresse plus loin. La conjonction "régime TENDANCE (~25% du temps mesuré) + range mature + rejet du canal (~6%) + retracement contexte 38-61% (~23%)" est si restrictive qu'elle laisse un nombre de configurations d'entrée trop faible pour qu'une seule, empiriquement, ait le temps de rencontrer aussi "cassure locale + expansion de volume 1,5x + score≥2" avant que le stop ou le régime EXCES ne referme la position. **Conséquence directe à ne pas minimiser** : les chiffres ci-dessus mesurent en réalité seulement le comportement d'entrée/stop de l'étape Accumulation — les étapes Breakout/Divergence/Pull-Back/Excès final, bien qu'implémentées et vérifiées unitairement (6/6, P&L exact calculé à la main), restent **non exercées empiriquement** sur ce jeu de données avec ces approximations causales.

  **PRÉCISION AJOUTÉE (8e round de mobilisation), en faveur de ce paragraphe** : ce constat était déjà correctement SCOPÉ aux 3 profils MODERE/AGRESSIF/TRES_AGRESSIF (c'est écrit ci-dessus) — ce sont les résumés ultérieurs (`PLAN.md`, `CONFIGURATION_RECOMMANDEE.md`, `backtest_phase2_recommended.py`, `unified_protocol.py::main`) qui ont laissé tomber le périmètre et écrit "100% des campagnes, aucune n'atteint jamais Breakout", tous corrigés au 10e round. Le compte exact, mesuré par instrumentation du VRAI moteur : MODERE/AGRESSIF/TRES_AGRESSIF = 39 campagnes chacun, **0 Breakout** (constat confirmé) ; **profil FAIBLE = 30 campagnes, dont 8 atteignent le Breakout, 5 la Divergence, 1 le Pull-Back/Excès final** — les étapes 2 à 5 SONT donc exercées, mais uniquement par ce profil, et la 4e/5e étape par une seule campagne (SOL). Cause mécanique identifiée : FAIBLE a `accum_frac = 0.00` (rien n'est engagé en Accumulation), donc son stop de protection — qui exige `remaining > 0` — ne peut pas refermer la campagne avant le Breakout, contrairement aux 3 autres profils. Constat rapporté tel quel, pas "corrigé" dans le moteur : le modifier serait un changement de mécanisme, hors périmètre.

Conforme au principe de tête de ce document : ce résultat mitigé (voire décevant sur la fréquence de déclenchement réel) NE remet PAS en cause l'implémentation — la table est un élément documenté de l'IP de Philippe Roux, implémentée fidèlement au corpus (RULES_EXTRACTION §4) quel que soit le résultat du proxy. Aucun seuil de détection n'a été assoupli après coup pour améliorer ce chiffre (ça aurait été de l'ajustement a posteriori, contraire au principe). Piste explicite pour un futur cycle : soit relâcher/revoir une ou plusieurs des hypothèses H5/H6/H9 (canal, retracement, volume) pour permettre à davantage de campagnes de progresser, soit accepter que cette conjonction précise du manuel soit, empiriquement, très rare sur crypto H4 — distinction non tranchée ici, à traiter explicitement plutôt qu'à décider en silence.

**Note sur "+Reverse — table RANGE" (item 7 du backlog `PLAN.md`, moitié traitée ce cycle — l'autre moitié, diversification 1%+1%/Cluster Technique, également traitée ce cycle par un agent en parallèle, cf. note dédiée ci-dessus)** : RULES_EXTRACTION.md §3 (table "trade spéculatif", range, PAS la table tendance déjà traitée ci-dessus) donne, pour le profil Très agressif, une seule ligne : `RIEN | — | RIEN | TP100%+Reverse`. Validation/Confirmation/Invalidation ne changent rien au comportement déjà en place (`val_close`/`conf_close` = 0 dans `PROFILES_V4`, Invalidation déjà une clôture totale au stop, codée en dur pour tous les profils). Le seul comportement manquant était "+Reverse" à la Limite : `code/position_engine.py` reste structurellement long-only depuis le début du projet ; il expose désormais `process_reverse` et un paramètre optionnel `reverse_at_limit=False` (par défaut, AUCUN changement de comportement pour tous les appelants existants — vérifié en relançant `code/test_position_engine.py` immédiatement après la modification : 5/5 historiques toujours au vert, avant même d'ajouter les nouveaux tests).

Le manuel ne donne NI taille, NI stop, NI cible pour la jambe reverse — **hypothèse d'implémentation explicite** posée (H-Reverse-Range, documentée en tête de `code/position_engine.py`, sur le modèle de `code/capital_tiers.py` pour une règle source sous-spécifiée) : jambe short "miroir" de la jambe long qui vient de se clôturer — même taille (le `remaining` clôturé), stop à la même distance % au-dessus du nouveau prix d'entrée que le long en avait en dessous, cible à la même distance % en dessous que le long avait au-dessus pour sa Limite, ouverte immédiatement au prix de clôture qui a déclenché la Limite. Jambe bornée simple (un seul stop, une seule cible, pas de sous-étapes Validation/Confirmation côté short) — même esprit de simplification assumée que le "+Reverse" de la table TENDANCE (`trend_table.py`, hypothèse H10), mais **à ne pas confondre** : celui-là inverse après une campagne à jambes multiples avec renfort progressif (mécanisme déjà construit précédemment), celui-ci inverse après la clôture d'une simple tranche long à son niveau Limite — deux mécanismes distincts pour deux tables distinctes du corpus. Stop de la jambe reverse déclenché sur la MÈCHE (même convention que l'Invalidation long), cible déclenchée sur la CLÔTURE (même convention que la Limite long) — réutilise les deux conventions déjà établies plutôt que d'en inventer une troisième.

8/8 tests unitaires passent (`code/test_position_engine.py` : 5 historiques + 3 nouveaux — séquence complète Validation→Confirmation→Limite→Reverse avec P&L exact des deux jambes calculé à la main, comportement par défaut inchangé vérifié explicitement, `process_reverse` testé isolément pour les deux conventions de déclenchement mèche/clôture).

Appliqué à `code/backtest_phase2_v7.py` (nouveau paramètre `reverse_at_limit`, même principe que `use_mtf_stop` déjà en place) via un script de mesure dédié `code/backtest_phase2_v7_reverse.py` (pas dans `main()` de v7 : cette boucle compare gate/stop MTF sur LES 4 PROFILS, une dimension transverse, alors que "+Reverse" est spécifique au seul Très Agressif d'après la source — les mélanger aurait produit un tableau confus, choix documenté en tête du nouveau fichier). Mesuré BTC/ETH/BNB/SOL, profil TRES_AGRESSIF, gate D1 actif (`phase2_v7_reverse_results.csv`) :

| Symbole | Retour total sans Reverse | Retour total avec Reverse | Δ retour | Win rate sans/avec | Profit factor sans/avec |
|---|---|---|---|---|---|
| BTC | +147,8% | +150,0% | +2,2 pts | 42,5% / 41,4% | 2,05 / 2,07 |
| ETH | +200,5% | +219,2% | +18,7 pts | 44,3% / 42,8% | 2,05 / 2,10 |
| BNB | +65,6% | +59,5% | -6,1 pts | 41,0% / 40,0% | 1,46 / 1,43 |
| SOL | +196,5% | +203,0% | +6,5 pts | 38,9% / 37,9% | 2,07 / 2,09 |

**Constat honnête, pas maquillé** : 3/4 actifs (BTC, ETH, SOL) voient leur retour total s'améliorer avec le reverse activé, 1/4 (BNB) se dégrade — pas un edge univoque. Le win rate global baisse systématiquement de -1,0 à -1,5 pt sur les 4 actifs (les jambes reverse ajoutées, 9 à 15 par actif selon le nombre de fois où la Limite a été touchée, gagnent moins souvent que la moyenne des trades long), tandis que le profit factor s'améliore légèrement sur 3/4 actifs — cohérent avec un mécanisme qui ajoute des trades globalement moins fréquemment gagnants mais dont les gains, quand ils surviennent, compensent. Conforme au principe de tête de ce document : ce résultat mitigé (mixte, pas un edge clair) NE remet PAS en cause l'implémentation — le mécanisme est un élément documenté de l'IP de Philippe Roux (RULES_EXTRACTION §3), implémenté fidèlement quel que soit le résultat du proxy. **Reste ouvert** : l'hypothèse H-Reverse-Range (miroir stop/cible) n'est qu'UNE lecture possible d'un manuel muet sur ces paramètres — une autre hypothèse (taille différente, stop/cible asymétriques) donnerait un résultat chiffré différent, non testée ici.

## ⚠️→◐ Réserve méthodologique P0 — TRAITÉE ce cycle de travail (mitigée, PAS résolue au sens plein — lire la nuance avant de conclure)

**Historique** : découverte pendant l'écriture des tests de régression de `proxy_v2.py` (cycle de travail précédent), déjà partiellement documentée dans `OOS_VALIDATION_CYCLE_SIGN.md` (section 5) mais jamais remontée jusqu'ici avant d'être formalisée en P0 (`PLAN.md`). **Ce cycle de travail traite ce P0** : implémentation d'un calcul causal, rejeu complet de v5/v6/v7 + validation XRP, conclusion chiffrée ci-dessous.

### Ce qui a été fait

1. **`compute_cycle_phase_causal(close, window=150)`** (`code/proxy_v2.py`) : fenêtre glissante de 150 barres (pas la série entière, pas une fenêtre expansive — trop lente sur plusieurs années), `hilbert()` recalculé sur chaque fenêtre, seule la phase du **dernier point** (= l'instant *t*, aucune barre future) est conservée. Vectorisé (`sliding_window_view` + `hilbert(axis=1)`). L'ancienne fonction `compute_cycle_phase` (batch) est **conservée telle quelle**, avec un avertissement explicite ⚠️ non-causal dans sa docstring — gardée pour comparaison/archive, plus jamais appelée en production.
2. **Choix de la fenêtre (150)** : comparé empiriquement 40/60/100/150 sur BTC/ETH/BNB/SOL réels (H4 + D1, tout l'historique), critère = **stabilité** (écart-type le plus faible) de la corrélation cycle/rendement-futur entre 3 tiers temporels de la série — pas la seule vitesse (voir `code/cycle_causal_window_selection.py` → `cycle_causal_window_selection.csv`). Résultat important à connaître avant d'interpréter ce choix : **aucune des 4 tailles testées ne montre d'edge causal significatif sur données réelles** (détail au point 3) — 150 est la plus stable parmi des candidats qui, de toute façon, ne montrent pas d'edge.
3. **Test de régression de la causalité elle-même** (`code/test_proxy_v2.py::test_cycle_phase_causal_matches_truncated_series`) : vérifie que la valeur en *t* est identique, à tolérance flottante près, qu'on calcule sur la série complète ou tronquée à *t* — et vérifie, en contrôle négatif, que ce même test détecte bien que l'ancienne fonction batch échoue à cette propriété. 4/4 tests passent.
4. **`add_proxy_v2_score` bascule sur la version causale** (seul point d'entrée utilisé par v5/v6/v7).
5. **Rejeu complet** : v5, v6, v7 (BTC/ETH/BNB/SOL, H4+D1, 4 profils) + validation OOS XRP (`OOS_VALIDATION_CYCLE_SIGN.md`), avec la version causale, comparé ligne à ligne aux résultats batch existants (déjà trackés : `phase2_v5_FIXED_results.csv`, `phase2_v6_regime_results.csv`, `phase2_v7_mtf_results.csv`). Nouveaux fichiers : `phase2_v5_causal_results.csv`, `phase2_v6_regime_causal_results.csv`, `phase2_v7_mtf_causal_results.csv`, `cycle_causal_vs_batch_comparison.csv` (jointure batch/causal, 144 lignes), `oos_xrp_cycle_validation_CAUSAL.csv`.

### Résultat chiffré — deux niveaux à ne pas confondre (c'est là que se joue la nuance "mitigé" vs "résolu")

**Niveau 1 — le composant cycle ISOLÉ (corrélation sinewave/rendement futur), sur données réelles (pas synthétiques) :**

| Actif/TF | Corrélation BATCH (ancienne, en place jusqu'ici) | Corrélation CAUSALE (nouvelle) |
|---|---|---|
| BTC H4/D1, ETH H4/D1, BNB H4/D1, SOL H4/D1 (8 séries) | +0.29 à +0.30, **toutes hautement significatives** | **-0.01 à -0.03, p entre 0.12 et 0.71 (AUCUNE significative)** |
| XRP D1 365j (OOS, actif indépendant) | +0.32, p < 10⁻⁹ (`OOS_VALIDATION_CYCLE_SIGN.md`) | **-0.055, p = 0.42, IC bootstrap [-0.18, +0.07] contient 0** |

Sur les **5 actifs testés** (BTC/ETH/BNB/SOL + XRP hors-échantillon), en H4 comme en D1, **le composant cycle isolé ne montre aucune corrélation significative avec le rendement futur une fois le calcul rendu réellement causal** — alors que le calcul batch (celui utilisé jusqu'ici pour TOUTE la performance rapportée) montrait une corrélation forte et hautement significative partout. Ce n'est pas une nuance de magnitude : sur ce plan précis, **l'edge mesuré en batch ne survit pas au calcul causal sur données réelles**, un résultat plus sévère que l'estimation faite sur sinusoïde synthétique pure (0,93→0,38, qui suggérait un edge affaibli mais réel). Sur sinusoïde synthétique, le résultat causal windowed s'est en outre révélé **très sensible au ratio fenêtre/période** (de -0.27 à +0.92 selon la fenêtre testée pour une série de période 40) — confirmant que la méthode windowed est fragile hors d'un cas de laboratoire à fréquence unique connue, cohérent avec l'absence d'edge trouvée sur des marchés réels qui n'ont pas une période dominante unique et stable.

**Niveau 2 — la performance GLOBALE des moteurs (v5/v6/v7), qui combinent momentum + cycle + structure (score ≥ 2) :**

| Moteur | Retour moyen BATCH | Retour moyen CAUSAL | Win rate moyen BATCH | Win rate moyen CAUSAL | Bascule de signe |
|---|---|---|---|---|---|
| v5 (32 configs, H4+D1) | +1324,7 % | +288,9 % (÷4,6) | 44,2 % | 35,6 % | 4 configs D1 (BNB +→−) / 4 configs D1 (SOL −→+) |
| v6 avec régime (64 configs, H4+D1) | +879,3 % | +222,0 % (÷4,0) | 45,6 % | 36,4 % | 8 configs D1 (BNB +→−) / 8 configs D1 (SOL −→+) |
| v7 MTF H4/D1 (48 configs, **H4 uniquement**) | +955,5 % | +243,1 % (÷3,9) | 51,8 % | 41,0 % | **0 bascule — 48/48 gardent le même signe** |

Lecture honnête : **sur H4 — le timeframe le plus testé et celui du résultat phare v7 — le SENS de l'edge global tient à 100 % (48/48 configurations restent positives)**, mais son **ampleur était surestimée d'un facteur ~4** par le calcul batch, et le win rate est surestimé de ~9 à 11 points. Sur D1 (déjà un échantillon très mince — 13 à 32 trades sur 6 ans, déjà signalé comme peu concluant dans les documents antérieurs), quelques configurations changent de signe dans un sens ou dans l'autre (BNB positif→négatif, SOL négatif→positif) — plus probablement du bruit de petit échantillon qu'un effet causal systématique, mais ce n'est pas exclu et personne ne doit lire les chiffres D1 comme fiables, avant comme après cette correction.

### Conclusion — "mitigé" ≠ "résolu", explicitement (mode ingénieur senior, `PLAN.md`)

- **Mitigé, PAS résolu** : la performance **globale** rapportée sur H4 (v5/v6/v7, y compris le résultat phare `MTF_CROSS_VALIDATION_H4_D1.md`) reste positive en direction après correction causale, mais son ampleur était surestimée d'environ 4× et son win rate de ~10 points — **c'est la nouvelle référence à citer désormais**, pas les anciens chiffres batch. Ceci s'applique au **panier momentum+cycle+structure pris ensemble**, pas au cycle isolément.
- **Toujours ouvert, plus sérieux qu'une simple question de magnitude** : rien dans les données réelles testées (5 actifs, H4+D1) ne confirme que le **composant cycle apporte, à lui seul, une information causale valide**. L'hypothèse la plus probable, cohérente avec le maintien du signe au niveau global, est que **la performance globale survit surtout grâce au momentum (TSI) et à la structure (creux ascendants)** — le cycle, en devenant causal, se comporte plutôt comme un filtre quasi aléatoire qui dilue la précision (d'où la baisse de win rate) sans annuler l'edge des deux autres composantes. Ce n'est **pas prouvé** (nécessiterait un test d'ablation dédié, non fait ici, cf. section suivante) mais c'est l'explication la plus cohérente avec l'ensemble des chiffres ci-dessus — présentée comme hypothèse, pas comme fait établi.
- **Recommandation directe pour la suite** : avant Phase 3, un test d'ablation (score sans le cycle, momentum+structure seuls, score ≥ 1 sur 2) permettrait de trancher si le cycle cause une vraie dilution ou reste neutre — non fait ici (hors scope initial de ce chantier), à ajouter au backlog (`PLAN.md`).

**Point connexe — tranché ce cycle** (sans lien direct avec P0) : la condition composite `cycle_ascending` reste bien anti-corrélée au rendement futur sur sinusoïde synthétique pure (r≈-0,41, avec ou sans calcul causal), mais mesurée directement sur BTC/ETH/BNB/SOL réels (H4+D1) elle ne montre aucune corrélation significative et cohérente avec le rendement futur (\|r\|≤0,045, signe incohérent d'un actif à l'autre) — l'hypothèse posée à l'origine est confirmée : la sinusoïde pure est un artefact de terrain de test, pas une propriété du signal réel. Refermée comme "expliquée, pas un bug" ; détail complet dans `code/test_proxy_v2.py` (docstring) et `PLAN.md` occurrence #3.

### Test d'ablation du cycle — fait (recommandation ci-dessus traitée), résultat NON concluant, pas un "cycle neutre confirmé"

Recommandé juste au-dessus comme prochaine étape logique, fait sans attendre qu'on le redemande (`code/ablation_test_cycle.py`, ne touche à aucun fichier de production — score ablaté = momentum+structure seuls, seuil ">= 2" devenant "les deux d'accord"). Comparaison score complet (momentum+cycle+structure) vs score ablaté (sans le cycle), gate MTF activé, 4 actifs × 4 profils :

| Actif | Δ win rate (ablaté − complet), moy. 4 profils | Δ profit factor (ablaté − complet), moy. 4 profils | Lecture |
|---|---|---|---|
| ETH | **+0,7 pt** | **+0,76** | Retirer le cycle améliore nettement — cohérent avec l'hypothèse "dilue sans apporter" |
| BTC | -5,5 pts | +0,37 | Mixte : moins précis en fréquence, plus précis en ampleur des gains |
| BNB | -4,1 pts | -0,01 | Retirer le cycle dégrade légèrement le win rate, PF inchangé |
| SOL | -2,6 pts | **-0,64** | Retirer le cycle dégrade nettement — **contredit** l'hypothèse "dilue sans apporter" |

Détail complet (32 lignes) : `ablation_test_cycle_results.csv`.

**Conclusion honnête, pas forcée dans un sens qui arrangerait une histoire propre** : les 4 actifs **ne s'accordent pas sur la direction**. Sur ETH, retirer le cycle améliore tout ; sur SOL, retirer le cycle dégrade tout ; BTC et BNB sont mixtes. Ça **infirme** l'hypothèse simple posée après le traitement du P0 ("le cycle dilue partout sans nuire") — la réalité est plus compliquée : le cycle isolé ne montre pas de corrélation causale significative avec le rendement futur (mesuré ci-dessus), mais son interaction avec momentum+structure dans le score composite n'est pas neutre non plus, et varie par actif. Aucune conclusion univoque ne peut en être tirée sans sur-interpréter un échantillon de 4 actifs. **Recommandation révisée** : ne pas retirer le cycle du score sur la seule base de ce test (échantillon trop petit, résultat non consensuel) ; le signal composite (score >= 2 sur 3) reste le réglage par défaut. Si une décision de retrait devait un jour être prise, elle demanderait un échantillon d'actifs plus large et une explication du mécanisme d'interaction, pas juste ce tableau.

## ⚠️→◐ P0-bis — TRAITÉE ce cycle de travail (mitigée, ampleur small confirmée sur v5/v6/v7 ET désormais sur v4/capital_tiers — requantification complète, plus de propagation restante)

**Découverte** (cycle de travail précédent) : en vérifiant indépendamment le résultat de l'agent Fibonacci (qui indiquait réutiliser "la même détection de swing que `compute_ascending_lows`"), vérification de cette détection elle-même. `code/proxy_v2.py::compute_ascending_lows` appelait `scipy.signal.argrelextrema(low_v, ..., order=SWING_ORDER)` **en un seul appel batch sur toute la série historique** — par construction, la classification "swing low" à l'instant *t* dépend des `SWING_ORDER=3` barres qui SUIVENT *t*. Vérifié empiriquement sur BTC H4 réel (200 barres échantillonnées) : 23/200 (11,5%) changeaient de classification entre le calcul batch et un calcul recalculé sans aucune barre future.

### Ce qui a été fait

1. **Méthode retenue — vérifiée empiriquement avant d'être appliquée** (mode ingénieur senior, point 1) : `argrelextrema(..., order=N)` ne compare un point k qu'aux `N` barres AVANT/APRÈS lui, jamais à la série entière — donc, pour un point intérieur, la classification batch sur toute la série est **bit-à-bit identique** à un recalcul sur une série tronquée juste après k+N (vérifié sur 494 points synthétiques ET 14 233 points BTC H4 réels, **0 différence** dans les deux cas — `code/structure_causal_vs_batch_comparison.py`). Conséquence directe : **l'option "a" de la tâche a été retenue** (décaler le *moment* où la classification batch a le droit d'être consommée, à t+SWING_ORDER, une fois ces barres réellement observées) plutôt que l'option "b" (recalcul incrémental barre par barre, plus lent) — les deux sont mathématiquement équivalentes ici, "a" est donc strictement suffisante et moins coûteuse.
2. **`compute_swing_low_confirmed`/`compute_swing_high_confirmed`** (`code/proxy_v2.py`) : primitives causales partagées, réutilisées par `compute_ascending_lows` (structure du score proxy_v2) ET importées dans `backtest_phase2_v4/v5/v6/v7.py` et `backtest_phase2_capital_tiers.py` pour `n_borders` (maturité).
3. **Test de régression de la causalité elle-même** (`code/test_proxy_v2.py::test_ascending_lows_causal_matches_truncated_series`), sur le même modèle que `test_cycle_phase_causal_matches_truncated_series` (P0) : la valeur "creux ascendants" en t ne change pas selon que la série contient des barres après t ou non, avec contrôle négatif (la classification batch brute, sans décalage, échoue bien à cette propriété).
4. **`code/fibonacci.py` mis à jour** : `compute_swing_highs_lows` réutilise désormais les mêmes primitives causales (au lieu de sa propre classification batch, déjà honnêtement documentée comme lookahead dans sa docstring d'origine). Tests (`code/test_fibonacci.py`) ajustés en conséquence — les indices attendus sont décalés de `SWING_ORDER` (confirmation, pas détection instantanée) : 5/5 tests passent toujours.
5. **`code/trend_table.py`** n'a nécessité AUCUNE modification directe : son propre "swing_high" est un simple maximum courant (pas `argrelextrema`), et il n'hérite de la détection de swing que via `prepare()` de `backtest_phase2_v7.py`, désormais causale — propagation automatique, vérifiée (6/6 tests `test_trend_table.py` toujours au vert).
6. **Rejeu complet v5/v6/v7** (BTC/ETH/BNB/SOL, H4+D1, 4 profils), comparé à l'état juste avant ce fix (`phase2_v{5,6,7}_*causal_results.csv`, qui a déjà le cycle causal mais la structure encore batch) : nouveaux fichiers `phase2_v5_structure_causal_results.csv`, `phase2_v6_regime_structure_causal_results.csv`, `phase2_v7_mtf_structure_causal_results.csv`, et les comparaisons `v{5,6,7}_structure_causal_vs_batch_comparison.csv` (`code/replay_structure_causal_v5_v6_v7.py`).
7. **Dette de requantification résorbée ce cycle : `code/backtest_phase2_v4.py` et `code/backtest_phase2_capital_tiers.py` rejoués** (item 8 du backlog `PLAN.md`, ouvert depuis le traitement initial de P0-bis). Vérifié empiriquement AVANT de rejouer, pas supposé : les deux fichiers importent bien `compute_swing_low_confirmed` directement depuis `code/proxy_v2.py` (`from proxy_v2 import compute_swing_low_confirmed` / `from proxy_v2 import add_proxy_v2_score, compute_swing_low_confirmed`) — aucune copie locale non corrigée, aucun import périmé trouvé. Rejoués sur BTC/ETH/BNB/SOL, tous profils (`phase2_v4_results.csv` pour v4, H4+D1 ; `phase2_capital_tiers_results.csv` pour capital_tiers, 3 paliers de capital, H4 gated par D1). Rappel de statut, pas une nouvelle référence : `v4.py` reste un moteur **superseded** (`STATUS.md` : v4→v5→v6→v7, chaîne déjà remplacée avant même ce fix) — ce rejeu comble une dette documentaire de complétude ("dette de requantification connue et documentée" citée par `PLAN.md`), il ne fait pas de v4 une référence active pour de futures décisions.

### Résultat chiffré

**Niveau classification** (impact du fix sur `compute_ascending_lows` lui-même, indépendamment du reste du score, `code/structure_causal_vs_batch_comparison.py`) :

| Actif | "creux ascendants" change | `structure_favorable` change | `long_signal` (score≥2) batch | `long_signal` causal |
|---|---|---|---|---|
| BTC H4 | 16,7 % des barres | 6,5 % des barres | 25,9 % | 24,9 % |
| ETH H4 | 16,4 % | 6,2 % | 25,1 % | 24,1 % |
| BNB H4 | 16,3 % | 6,5 % | 26,1 % | 25,4 % |
| SOL H4 | 16,0 % | 6,1 % | 24,3 % | 23,5 % |

Le taux de reclassification de `compute_ascending_lows` seul (~16 %, plus élevé que les 11,5 % mesurés sur 200 échantillons — attendu, la mesure ci-dessus porte sur la série H4 complète, ~14 200 barres, pas un échantillon) se propage à un effet plus modeste une fois combiné avec le filtre EMA (`structure_favorable`, ~6 %) et encore plus modeste sur la fréquence du signal final (`long_signal`, environ -1 point de pourcentage).

**Niveau performance des moteurs** (H4, le timeframe solide — D1 reste un échantillon trop mince pour être lu comme fiable, avant comme après ce fix, comme déjà noté pour le P0) :

| Moteur | Retour moyen (structure batch, référence P0) | Retour moyen (structure causale, ce fix) | Win rate moyen batch | Win rate moyen causal | Bascule de signe |
|---|---|---|---|---|---|
| v5 (16 configs H4) | +572,9 % | +501,4 % (≈ ÷1,14, soit -12 %) | 38,3 % | 38,9 % | 0/16 |
| v6 avec régime (32 configs H4) | +440,0 % | +389,0 % (≈ ÷1,13, soit -12 %) | 38,9 % | 39,2 % | 0/32 |
| v7 MTF (48 configs, H4 uniquement) | +243,1 % | +208,2 % (≈ ÷1,17, soit -14 %) | 41,1 % | 41,3 % | **0/48** |

Sur D1 (échantillon déjà mince, 13-32 trades sur 6 ans, déjà signalé comme peu concluant) : bascules de signe fréquentes (12/16 v5, 21/32 v6) mais ampleur absolue minuscule (quelques points de pourcentage de retour) — cohérent avec du bruit de petit échantillon plutôt qu'un effet causal systématique, exactement le même constat que pour le P0 du cycle.

**Rejeu `v4.py`/`capital_tiers.py` (dette de requantification résorbée ce cycle, point 7 ci-dessus)** — comparé à l'état batch cité dans `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` et dans la note "Capital par palier" ci-dessus :

| Moteur | H4 (le résultat qui compte) | D1 (échantillon mince, déjà peu concluant) |
|---|---|---|
| `backtest_phase2_v4.py` (16 configs H4, 4 actifs × 4 profils) | **Identique bit-à-bit à l'ancien chiffre batch, 0 changement sur les 16/16 lignes** (`n_trades`, retour, win rate, profit factor tous rigoureusement égaux) | 7/16 bascules de signe (BTC : les 4 profils passent de négatif à positif ; SOL : 3/4 profils passent de positif/quasi-nul à négatif) — même constat de bruit de petit échantillon que pour v5/v6/v7 |
| `backtest_phase2_capital_tiers.py` (48 lignes, 4 actifs × 4 profils × 3 paliers, H4 gated par D1) | **0/48 bascule de signe** ; retour total moyen ÷1,08 environ (-8 % en moyenne, mín -43,6 %/max +26,7 % selon la ligne) ; win rate moyen quasi inchangé (42,51 %→42,42 %, -0,1 pt) ; profit factor moyen 1,92→1,81 (-5,7 %) | n/a (capital_tiers ne teste que H4 gated par D1, pas de campagne D1 seule) |

**Pourquoi `v4.py` H4 ne bouge pas du tout, contrairement à v5/v6/v7 (-12 à -14 %)** — vérifié, pas supposé : `v4.py` n'utilise PAS `compute_ascending_lows` (la comparaison directe des 2 derniers swings confirmés, sensible à un décalage de quelques barres) comme v5/v6/v7 via `add_proxy_v2_score`. Il utilise `compute_swing_low_confirmed` uniquement pour une **maturité par comptage** (`n_borders`, somme glissante sur une fenêtre de 15 jours ≈ 90 bougies H4, seuil `>= 3`). Décaler la confirmation de `SWING_ORDER=3` bougies (12h) à l'intérieur d'une fenêtre glissante de 90 bougies ne fait jamais passer ce compte sous ou au-dessus du seuil 3 aux instants de décision effectivement échantillonnés sur les 4 actifs testés — d'où l'identité bit-à-bit. Sur D1, la même fenêtre de 15 jours ne représente que 15 bougies (le ratio décalage/fenêtre passe de ~3% à 20%), ce qui explique la sensibilité beaucoup plus grande observée en D1, cohérente avec le pattern déjà documenté (D1 = échantillon mince, bascules de signe fréquentes, sans valeur probante).

Détail complet (fichiers de résultats, comparaison ligne par ligne) : `phase2_v4_results.csv` (racine du repo) et `code/phase2_capital_tiers_results.csv`.

### Conclusion — honnête sur l'ampleur, pas gonflée en "enjeu majeur"

- **Sur H4 (le résultat qui compte, y compris le résultat phare `MTF_CROSS_VALIDATION_H4_D1.md`) : 100 % des configurations testées (48/48 v7, 32/32 v6, 16/16 v5) gardent le même signe.** Le fix ne change pas la conclusion directionnelle du projet.
- **Ampleur réelle, bien plus petite que le P0 du cycle** (qui divisait le retour par ~4 et le win rate de ~10 points) : ici, le retour diminue d'environ 12-14 % (pas ÷4), et le win rate **s'améliore très légèrement** (+0,3 à +0,6 point) plutôt que de se dégrader — sens inverse du P0. C'est exactement ce que `PLAN.md` anticipait ("ampleur bien plus petite que #2 (fenêtre fixe de 3 barres, pas la série entière)") — confirmé, pas juste supposé.
- **"Mitigé" plutôt que "résolu" au sens plein**, pour une raison différente du P0 : la correction technique elle-même est complète et vérifiée (test de causalité, propagation à v4/v5/v6/v7/capital_tiers/fibonacci). **Mise à jour de ce cycle : `backtest_phase2_v4.py` et `backtest_phase2_capital_tiers.py` ont désormais été rejoués/requantifiés** (voir le tableau dédié ci-dessus) — la dette de requantification documentée jusqu'ici est résorbée, plus de propagation restante silencieuse sur aucun des moteurs qui utilisent la structure causale. Résultat : capital_tiers confirme le même schéma que v5/v6/v7 (0/48 bascule de signe, ampleur réduite d'environ 8 %, win rate/profit factor quasi stables) ; v4 est **encore plus stable sur H4** (0 changement, bit-à-bit identique sur les 16 configurations) pour une raison structurelle identifiée (sa maturité repose sur un comptage glissant sur 90 bougies, peu sensible à un décalage de 3 bougies, contrairement à `compute_ascending_lows` utilisé par v5/v6/v7). Rappel : v4 reste un moteur superseded (`STATUS.md`) — ce rejeu sert la complétude documentaire, pas une réhabilitation de v4 comme référence active.
- **Fibonacci et trend_table sont, eux, pleinement propagés et vérifiés** (tests ajustés, 5/5 et 6/6) — pas une propagation restante, contrairement à ce que redoutait la découverte initiale.

## ❌ Jamais implémenté (liste exhaustive, pas de trou silencieux)

| Élément | Source | Pourquoi ça manque |
|---|---|---|
| ~~Table "trade de tendance" (5 étapes : Accumulation/Breakout/Divergence/Pull-Back/Excès final)~~ | ~~RULES_EXTRACTION §4~~ | **Fait** — moteur dédié implémenté fidèlement + testé unitairement, mesuré sur données réelles, résultat MITIGÉ (les stages 2-5 ne se déclenchent jamais dans ce backtest) — voir note détaillée dans la section ✅ ci-dessus |
| ~~Règle "Wall Street" (structure en élargissement = abstention totale)~~ | ~~#3, #4~~ | **Fait** — voir note dédiée ci-dessous |
| ~~Canal de tendance manuel (Supports→Apex→Tangente)~~ | ~~#3~~ | **Fait** — voir note dédiée ci-dessous |
| ~~Fourchette d'Andrews ("Inside Pitchfork")~~ | ~~#3, #4~~ | **Fait** — voir note dédiée ci-dessous |
| ~~Diversification 1%+1% (2 patterns indépendants) + "Cluster Technique" (MA20+support) comme second pattern~~ | ~~#16~~ | **Fait** — voir note dédiée ci-dessus (section ✅), résultat MITIGÉ (effet isolé sur le drawdown proche de zéro et mixte selon l'actif) |
| ~~Mécanisme "+Reverse" du profil Très Agressif à la Limite — TABLE RANGE spécifiquement~~ | ~~Extraction manuel, §3~~ | **Fait** — voir note dédiée ci-dessus (section ✅) |
| ~~Règle "UT+2" exacte (2 niveaux au-dessus, pas le niveau immédiat)~~ | ~~#9, #10, #12, #14, #15, #16 (6 sources)~~ | **Fait** — voir note dédiée ci-dessous |
| ~~"Conflit Multi-Timeframe" (ne jamais trader une borne de range si un range TF supérieur est actif)~~ | ~~#5, désignée "erreur numéro un" par le corpus lui-même~~ | **Fait** (cycle suivant, mobilisation multi-agents : design puis décision puis implémentation) — voir note dédiée ci-dessus (section ✅), `code/backtest_phase2_faithful.py`/`code/unified_protocol.py` |
| **Gate Fibonacci RANGE, seuils littéraux du manuel PAR RÉGIME** (76,4%/61,8%/23% selon Range neutre/Range tendanciel/Tendance, condition composite à 3 volets) | `RULES_EXTRACTION.md` §1 | Trouvé par re-audit (mobilisation multi-agents) — DISTINCT de la synthèse vidéo [23%,61,8%] déjà implémentée (celle-là reste une hypothèse légitimement réglable, cf. section 5ter de `CONFIGURATION_RECOMMANDEE.md`). Non implémenté, décision explicite : 2 des 3 volets de la condition ("débordement du contexte", "signal & triangle de confirmation") n'ont AUCUNE définition codable ailleurs dans le corpus — les implémenter exigerait d'inventer, refusé. Voir note dédiée ci-dessus (section ✅), backlog P0/P1 item 10 de `PLAN.md` |

**Note sur "Wall Street", "Canal manuel" et "Andrews Pitchfork" (item 7 partiel, backlog `PLAN.md`)** : les 3 modules détecteurs (`code/wall_street_pattern.py`, `code/manual_trend_channel.py`, `code/andrews_pitchfork.py`) ont été construits par un agent délégué qui a extrait fidèlement les définitions exactes du corpus (#3/#4) — voir les docstrings de chaque fichier pour le détail complet des sources citées et des hypothèses d'implémentation posées explicitement là où le corpus est sous-spécifié (ex. Wall Street : aucune définition numérique donnée, traduite en "2 derniers swings hauts/bas confirmés en désaccord de direction simultanément" ; Andrews : géométrie standard publique de l'outil, le corpus ne redéfinissant que son rôle et la variante "Inside"). **L'agent a été interrompu par un rate-limit d'infrastructure juste après avoir fini les 3 modules, avant d'écrire leurs tests ni le moteur de mesure prévu par la tâche.** Complété directement (pas par un agent) : `code/test_wall_street_pattern.py` (4/4), `code/test_manual_trend_channel.py` (5/5, géométrie vérifiée par calcul à la main), `code/test_andrews_pitchfork.py` (5/5, y compris la fonction géométrique pure testée isolément), tous causaux par construction (réutilisent `compute_swing_low_confirmed`/`compute_swing_high_confirmed`, corrigés au P0-bis). `code/backtest_phase2_patterns.py` (nouveau, construit directement) mesure chacun des 3 comme gate optionnel sur v7, indépendamment, sur BTC/ETH/BNB/SOL × 4 profils (`phase2_patterns_results.csv`) :

- **Wall Street (abstention)** : effet quasi neutre — win rate moyen 38,2% vs 38,9% (référence), profit factor 1,57 vs 1,53, ~12% de trades en moins. Ni confirmation nette ni dégradation. **CORRECTION (8 sept. 2026)** : cette mesure a d'abord servi (à tort) à justifier de NE PAS activer l'abstention par défaut ("bénéfice net nul") — or c'est une règle LITTÉRALE du corpus (#3/#4, "aucun outil ne fonctionne, arrêter tout", abstention TOTALE prescrite, pas une prudence optionnelle), pas une hypothèse à nous ; seule sa définition NUMÉRIQUE (H1, combien de bornes) en est une. `code/backtest_phase2_faithful.py` l'active désormais SANS CONDITION (cf. `CONFIGURATION_RECOMMANDEE.md` section 5ter).
- **Canal manuel (stop)** : substitue `ctx_support` par `channel_support` quand disponible. Effet net et cohérent : win rate moyen **-9,5 points** (29,4% vs 38,9%) mais retour total médian plus élevé (305% vs 212%) et plus de trades (le canal donne parfois un stop plus proche, ce qui autorise plus d'entrées à passer le seuil de sizing) — profil risque/récompense clairement différent (moins souvent gagnant, gains individuels plus gros), pas strictement meilleur ni pire, à traiter comme une alternative de style, pas un remplacement. **Reconstruction sur D1 (mobilisation multi-agents, Agent A, cycle suivant, RE-régénéré après la correction pyramidalisation-régime)** : cette mesure était limitée jusqu'ici au timeframe H4 natif, jamais combinée au stop cross-timeframe UT+1 (D1) de `backtest_phase2_faithful.py` — `code/backtest_phase2_faithful_manual_channel.py` (nouveau, réutilise `_prepare_features`/`_run_core` de `faithful.py` sans modification — hérite donc automatiquement du fix pyramidalisation-régime, cf. `PLAN.md` tableau "2e pattern récurrent" occurrence #4) reconstruit le canal manuel directement sur D1, couverture confirmée 97,77-99,53% des barres selon actif/année (walk-forward 2020-2026, `code/backtest_phase2_faithful_manual_channel_walkforward_results.csv`). Résultat : retour total moyen **9,91%** (canal manuel D1, était 10,46% avant le fix) contre **4,02%** (EMA±ATR D1, stop actuellement actif, était 5,31%), mais drawdown moyen plus profond aussi (**-6,21%** contre **-3,58%**, étaient -6,58%/-4,14%) — encore une fois pas strictement meilleur ni pire, conclusion inchangée par le fix. Le corpus ne tranche pas lequel des deux stops littéraux prime en cas de désaccord ; décision suivie : ne PAS remplacer le stop actif dans `faithful.py`/`unified_protocol.py`, documenter les deux comme des lectures valides.
- **Andrews Pitchfork (gate d'entrée)** : **hypothèse d'intégration posée ici, pas une règle du corpus** — le corpus documente le nom/rôle/chiffre (90% des cas correctifs) de l'outil mais pas comment l'utiliser comme filtre d'entrée ; hypothèse retenue (n'entrer que si `close > pitchfork_p1`) mesurée honnêtement : dégrade nettement le retour total (médian 18% vs 212% référence, moyenne négative sur ETH -9,5% à -21,9% selon profil) malgré un win rate stable — coupe disproportionnellement les gros trades gagnants. Résultat négatif à imputer à l'hypothèse de gating choisie ici, pas nécessairement au pattern lui-même (piste ouverte : une autre lecture de comment l'utiliser en entrée pourrait donner un résultat différent, non testée).

**Vague 5 — lecture ALTERNATIVE du gating Andrews Pitchfork (`PLAN.md` "Plan d'autonomie 8h"), traitée** : la piste ouverte ci-dessus ("une autre lecture... non testée") a été traitée par `code/andrews_gate_alternative.py` (`code/test_andrews_gate_alternative.py`, 8/8 tests, dont un test de non-régression vérifiant que son mode `andrews_permanent` reproduit EXACTEMENT `backtest_phase2_patterns.py` — comparaison apples-to-apples garantie, pas deux moteurs qui auraient dérivé). Une seule lecture alternative retenue, justifiée par le TEXTE du corpus (pas par un balayage de paramètres) : le corpus dit que la fourchette "**prend le relais QUAND** la tendance est **brisée**" — un relais est par nature CONDITIONNEL à un état de marché, pas un filtre permanent. Traduit en **H-Andrews-Contextuel** : le gate (`close > pitchfork_p1`) n'est appliqué QUE lorsque le régime de la bougie précédente (`regime_classifier.py`, réutilisé tel quel) est `RANGE_TENDANCIEL` — la seule catégorie déjà disponible dans ce projet décrivant une tendance qui a cessé de progresser mais garde un biais directionnel hérité (ni `TENDANCE` intacte, où le manuel place le Canal de Tendance comme outil de référence et pas encore la fourchette, ni `RANGE_NEUTRE`/`EXCES`, jamais mentionnés par le corpus comme terrain de cet outil). Dans tout autre régime, le signal passe tel quel (comme le mode "none").

Résultat chiffré, honnête, BTC/ETH/BNB/SOL × 4 profils, H4 (`code/andrews_gate_alternative_results.csv`), moyennes sur les 16 combinaisons :

| Mode | Retour total moyen | Win rate moyen | Profit factor moyen | n_trades moyen |
|---|---|---|---|---|
| Aucun gate ("none") | +501,4% | 38,9% | 1,53 | 1361,8 |
| Gate permanent (hypothèse originale) | **+29,0%** | 40,7% | 1,25 | 946,5 |
| Gate contextuel RANGE_TENDANCIEL (H-Andrews-Contextuel) | **+315,6%** | 39,5% | 1,47 | 1390,0 |

Vérifié pourquoi (pas laissé comme une boîte noire) : `RANGE_TENDANCIEL` est un régime RARE (3,4% à 4,1% du temps selon l'actif, mesuré directement) — le gate contextuel ne s'applique donc qu'à une fraction étroite des bougies candidates à signal (107 à 161 bougies concernées selon l'actif sur tout l'historique H4, dont 43% à 61% effectivement bloquées par la condition de prix), contre 100% des bougies pour le gate permanent. Constat honnête, pas maquillé : **la lecture contextuelle réduit très nettement la dégradation par rapport à l'hypothèse originale** (le retour moyen passe de +29,0% à +315,6%, le win rate et le profit factor se rapprochent nettement du "none"), mais **ne l'élimine pas** — le retour moyen reste inférieur de ~37% à l'absence de gate (+315,6% vs +501,4%), et sur certains couples actif/profil le nombre de trades EXCÈDE même celui du mode "none" (effet de dépendance au chemin du moteur de position à tranches multiples : bloquer une entrée à un instant donné modifie l'état `last_pyramid_high`/tranches ouvertes pour tout ce qui suit, ce n'est pas un simple filtre indépendant sur un ensemble fixe de trades — déjà observé ailleurs dans ce projet, ex. le canal manuel). Une poignée d'entrées bloquées dans une fenêtre `RANGE_TENDANCIEL` peut donc se répercuter de façon disproportionnée sur le retour cumulé final, cohérent avec un proxy dont la performance dépend fortement de quelques grosses séquences gagnantes (déjà documenté pour SOL/AGRESSIF ailleurs dans ce fichier). Conforme au principe de tête de ce document : ce résultat (mitigé, pas un succès net) n'est pas maquillé pour paraître meilleur qu'il ne l'est — la lecture contextuelle est une meilleure lecture TEXTUELLE du corpus que le gate permanent, et son effet mesuré est nettement moins destructeur, mais elle ne rend pas le gate Andrews positif pour ce proxy précis. **Reste ouvert** : ni le gate permanent ni le gate contextuel ne sont adoptés par défaut dans `CONFIGURATION_RECOMMANDEE.md` (déjà `use_andrews_gate` absent des 10 décisions ON/OFF) — ce chantier documente une lecture alternative honnêtement mesurée, ne tranche pas en faveur d'un défaut.

**Note sur "Règle UT+2 exacte" (item 3 du backlog, traité ce cycle)** : relecture précise des 6 sources (pas une supposition) — conclusion retenue : le corpus décrit un système à 3 niveaux à RÔLES DISTINCTS (le niveau immédiatement supérieur joue un rôle de canal/stop, déjà implémenté en v7 ; la validation de TENDANCE porte sur le niveau 2 crans au-dessus, EN SAUTANT le niveau intermédiaire), **pas** une règle cumulative "les 2 niveaux immédiatement supérieurs doivent être alignés". `code/backtest_phase2_ut2.py` généralise `attach_higher_context` (v7, lu mais jamais modifié) à N niveaux de contexte, teste 4 configurations (`none`/`d1_only`/`ut2_strict`/`d1_and_weekly`) sur BTC/ETH/BNB/SOL × 4 profils (`phase2_ut2_results.csv`). Point technique vérifié avant usage (piège potentiel évité) : une bougie Hebdomadaire (`resample('W')`) est entièrement close 1 JOUR après son `date` (label = fin de semaine), pas 7 jours — confirmé indépendamment sur données réelles BTC (`code/test_ut2.py::test_weekly_closure_delay_is_one_day_not_one_week`, 3/3... 4/4 tests, écrits directement après l'interruption de l'agent).

Résultat chiffré, moyennes sur les 16 combinaisons actif×profil :

| Mode | Win rate | Profit factor | Max DD | Retour moyen | Trades |
|---|---|---|---|---|---|
| Aucune validation (H4 seul) | 38,9% | 1,53 | -30,7% | +501,4% | 1362 |
| D1 seul (déjà connu, v7) | 42,4% | 1,81 | -19,5% | +85,2% | 446 |
| **UT+2 strict (règle littérale retenue)** | **42,1%** | **2,14** | -21,5% | +133,2% | 428 |
| D1 ET Hebdo (cumulatif, PAS la règle littérale, gardé pour comparaison) | 44,4% | 2,43 | -15,2% | +64,9% | 193 |

La règle UT+2 littérale (Hebdo seul, D1 sauté) confirme la direction déjà connue (moins de trades, meilleur win rate/profit factor, drawdown réduit vs H4 seul) et fait même légèrement mieux que le simple D1 sur le profit factor (2,14 vs 1,81) — sans trancher clairement si elle est supérieure ou juste différente du D1 seul déjà en place. La variante cumulative (D1 ET Hebdo, pas la règle du corpus) est la plus conservatrice des 4 sur tous les indicateurs sauf le volume de trades.
| ~~Capital par palier (<10k€/10-100k€/>100k€)~~ | ~~RULES_EXTRACTION §5~~ | **Fait** — voir section ✅ ci-dessus pour la règle, les hypothèses d'implémentation et les chiffres |
| ~~Funding rate exact par timestamp (pas juste l'ordre de grandeur annuel)~~ | ~~Découverte de l'agent délégué~~ | **Fait** — voir section ✅ ci-dessus pour le calcul, les hypothèses de modélisation et les chiffres |
| ~~Test d'ablation du composant cycle~~ | ~~Découverte de ce cycle de travail~~ | **Fait** — résultat non concluant (4 actifs en désaccord de direction), cf. section ⚠️→◐ ci-dessus, "Test d'ablation du cycle" |
| ~~Fibonacci retracement comme critère d'entrée~~ | ~~#9, #10, #11, #13, #14~~ | **Fait** — règle resynthétisée fidèlement (PAS la "zone dorée 38-61,8%" supposée au départ, 2 des 5 sources ne documentent en réalité aucun niveau d'entrée), filtre optionnel implémenté et mesuré : dégrade uniformément la performance de ce proxy (32/32 configs), gardé optionnel — voir section ✅ ci-dessus pour le détail complet. **"Règle des 50%" à 2 conditions cumulatives (#13) complétée dans un cycle ultérieur** (5e round de mobilisation, catégorie B) — cf. note "MISE À JOUR" dédiée ci-dessus |

## Audit exhaustif du corpus complet (mobilisation multi-agents, 5e round) — réponse à "reste-t-il de l'IP de Philippe non implémentée ?"

3 agents ont relu, DE ZÉRO et indépendamment, le manuel PDF entier + les 17 sources Trading Lessons entières (pas un résumé des tables ci-dessus), en vérifiant chaque règle/seuil/procédure directement dans `code/` par grep/lecture, sans se fier aux classifications déjà écrites dans ce document — exactement la méthode qui avait révélé Conflit MTF au round précédent. **Réponse honnête : oui, un nombre significatif d'éléments supplémentaires, classés ci-dessous par catégorie plutôt que traités uniformément.**

### Catégorie A — probablement PAS reproductible (dépend de l'algorithme propriétaire non divulgué, même limite déjà acceptée pour tout le projet)

`RULES_EXTRACTION.md` le dit dès sa première ligne : *"Non divulgué par l'éditeur (donc non reproductible) : la formule exacte du momentum PRO... et l'algorithme de détection (>150 patterns momentum, >100 patterns cycles, ~10 conditions/pattern)."* Les éléments suivants, trouvés ce round, en dépendent directement — ce ne sont pas des oublis, c'est la même limite structurelle déjà acceptée pour tout le proxy :
- **"Gradient de confiance" 0-100 à bandes** (`TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` : "&lt;50 abstention totale... 80-90+ zone de confort maximale", "carton plein") — suppose une sortie continue 0-100 du PRO Framework, jamais divulguée ; notre proxy ne produit qu'un score discret 0-3.
- **Compensation Fibonacci par qualité de signal** (couleur→61% Fibo vs gris→76% Fibo, même source) — suppose la classification en 9 catégories de signal (`RULES_EXTRACTION.md` §2 : TP/Overload/DIV standard/DIV cachée/EXIT/SurAchat/SurVente/BULL/BEAR/SQUEEZE/CONTEXT, Gris/Rouge/Bleu/Noir/Jaune/Orange) — sortie directe de l'algorithme propriétaire, pas de notre proxy.
- **"Cas d'urgence : renversement de contexte"** (bascule de couleur bleu→rouge, `TRADING_LESSONS_PULLBACK_MATURITE.md`) — même dépendance à la classification couleur.
- **NOUVEAU (36e round) — le régime "Chaos" du guide officiel PRO Indicators** (`Chaos/Chaos.png`,
  1ère des 4 branches du menu d'accueil "QUEL EST MON CONTEXTE ?") **n'avait JAMAIS été croisé
  contre le code, dans aucun round précédent** (l'audit du 29e/30e round n'avait couvert que
  Excès/Range/Tendance — un oubli, pas un choix) — trouvé en revérifiant la structure de décision
  du guide à la demande directe de l'utilisateur : à CHAQUE niveau (l'actif/UT lui-même, puis
  chaque sous-dossier), le guide pose la MÊME question à 4 issues, jamais 3. `regime_classifier.py::
  add_regime` ne connaît QUE 4 régimes (`RANGE_NEUTRE`/`RANGE_TENDANCIEL`/`TENDANCE`/`EXCES`) —
  **aucune notion de "Chaos"** (grep confirmé, 0 occurrence dans tout `emile/`). Absent aussi du
  manuel PDF (`RULES_EXTRACTION.md`, l'autorité la plus haute) ; `TRADING_LESSONS_CHAOS_DOPAMINE.md`
  (source #7) évoque un "chaos" théorique (Feigenbaum) sans rapport avec les 3 critères
  opérationnels de cet écran (**Contextes irréguliers**, **Moyenne plate**, **Momentum bruyant** →
  action **PARTEZ**). Comme Neuneu, cette notion est propre à ce seul guide. **Catégorie A** : les 3
  critères ne sont assortis d'AUCUN seuil chiffré nulle part (contrairement au 76%/61% ailleurs) —
  rien à implémenter sans inventer. **Conséquence pratique à connaître, réelle malgré tout** :
  `add_regime` classe aujourd'hui tout bar ambigu en `RANGE_NEUTRE` ("en cas de doute, range",
  commentaire du fichier lui-même) — un choix différent de celui du guide, qui traiterait la même
  ambiguïté comme potentiellement Chaos ("PARTEZ") plutôt que comme un Range tradable. Détail
  complet : `docs/GUIDE_STRATEGIE_PRO_INDICATORS.md` section 5, item 8 ; `PLAN.md` "36e application".

### Catégorie B — littéral, computable, PAS reproductibilité-limité : candidats d'implémentation directe

- ~~**"Règle des 50%" du Pull-Back** (`TRADING_LESSONS_PULLBACK_MATURITE.md`) : condition COMPOSITE à 2 volets — retracement ≥23% ET pénétration dans les 50% inférieurs du contexte. `code/fibonacci.py` cite cette règle verbatim dans son propre commentaire (lignes 25-27) mais `classify_retracement` (lignes 172-178) n'implémente QUE le premier volet (fraction de retracement) — le second (position dans le contexte) n'est calculé nulle part. Trouvé absent de la table ❌ ci-dessus malgré être cité en toutes lettres dans le code de production.~~ — **Fait ce cycle**, cf. note "MISE À JOUR ... Règle des 50% complétée" dans la note Fibonacci ci-dessus pour le détail complet (implémentation, distinction avec H7, résultat chiffré honnête).
- ~~**"Stop Loss = taille du canal"** (`TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md`) : si le canal de tendance est très large, réduire de moitié SIMULTANÉMENT la taille du stop ET la taille de position. `code/position_engine.py` calcule `size_frac = risk_pct / stop_pct` (normalisation par le risque), pas ce mécanisme de double réduction symétrique — un mécanisme distinct, jamais implémenté ni documenté comme manque.~~ — **Fait (6e round)**, cf. bloc "STOP LOSS = TAILLE DU CANAL" en tête de `code/position_engine.py` (`WIDE_CHANNEL_STOP_FRAC`/`WIDE_CHANNEL_SIZE_FRAC`, hypothèses H-Canal-Large-1..4). **Trouvaille (42e round)** : cette puce était restée non barrée malgré l'implémentation — corrigé ici, pas un nouvel écart.
- ~~**Tension "3 vs 4 bornes"** (maturité de range) : `TRADING_LESSONS_ZONE_ACCUMULATION.md` dit "minimum 4 bornes", d'autres sources disent 3 — `MIN_BORDERS=3` utilisé partout, la tension étant déjà notée en commentaire (`backtest_phase2_v4.py:32`, "règle des '3-4 bornes'") mais jamais remontée en tension formelle à trancher ni testée en sensibilité (`MIN_BORDERS=4` jamais essayé).~~ — **Mesurée ce cycle (mobilisation multi-agents, 6e round)**, trouvaille plus significative que la question posée : `code/min_borders_sensitivity.py` (`test_min_borders_sensitivity.py`, 9/9) rejoue les VRAIS moteurs de production (`run_v7`/`trend_table`, override temporaire, aucun fichier modifié) sur une grille 2/3/4/5 × BTC/ETH/BNB/SOL × 4 profils — **0,0 pt d'écart sur les 32 couples testés, quel que soit le seuil**. Diagnostic dédié : `n_borders` (fenêtre glissante fixe de 15 jours) vaut au minimum 5 chez les bougies candidates (médiane 9) — le premier seuil qui "mordrait" est 6, pas 3 ou 4. **Le gate est INERTE, ce n'est PAS une preuve que 3 est le bon choix** — la grandeur calculée (comptage sur fenêtre fixe) et celle décrite par le corpus (points de contact d'une structure de range identifiée) ne sont pas au même ordre de grandeur. Relecture complète confirmée : le corpus est réellement partagé entre "min 4" et "min 3" bornes selon la source (`TRADING_LESSONS_ZONE_ACCUMULATION.md`/`PULLBACK_MATURITE.md` vs `BREAKOUT_RATIO11.md`/`STRUCTURES_ALTERATIONS.md`), avec une lecture qui réconcilie les deux (`TRADING_LESSONS_MTF_SUIVI_TENDANCE.md` : 3e et 4e borne = deux ÉTAPES successives, pas deux valeurs concurrentes du même seuil). Décision de conception NON tranchée (catégorie C) : recalibrer `n_borders` sur une structure de range identifiée (seul point qui rendrait "3 vs 4" à nouveau mesurable), ou lire le corpus comme 2 gates distincts. Détail complet, citations exactes par source, chiffres complets par actif/profil/seuil : `PLAN.md` section "6e application".

### Catégorie C — réels, littéraux, mais exigent une décision de conception avant tout code (même discipline que Fibonacci RANGE/Conflit MTF — pas une invention silencieuse)

- ~~**Mécanisme d'emboîtement T/T-1** (`TRADING_LESSONS_ZONE_ACCUMULATION.md`, "le canal observé sur l'UT contexte T n'est que la structure interne de T-1") — `trend_table.py` reconnaît DÉJÀ lui-même (H11) être mono-timeframe, jamais cette validation croisée précise.~~ — **INVESTIGUÉ (11e round de mobilisation), décision prise : PAS implémenté et item FERMÉ. Deux prémisses de l'énoncé ci-dessus étaient fausses, et le mécanisme est partiellement déjà couvert.**

  **Citation exacte, relue mot pour mot (`TRADING_LESSONS_ZONE_ACCUMULATION.md:13`)** : *"Pour un professionnel, l'analyse d'un graphique unique est insuffisante. Le succès repose sur la compréhension de la mécanique d'emboîtement des unités de temps (UT). La règle d'or : le **canal de tendance** observé sur votre unité de temps de contexte (T) n'est rien d'autre que la structure interne de l'unité de temps inférieure (T-1)."*

  **Prémisse fausse n°1 — la paraphrase de l'audit amputait le mot porteur.** L'item écrivait *"le canal observé"* ; la source dit *"le **canal de tendance** observé"*. « Canal de tendance » n'est pas un synonyme de « contexte » dans ce corpus, c'est un objet nommé situé **un cran sous** le contexte : `TRADING_LESSONS_TROISIEME_BORNE.md:14-18` (#11) oppose deux colonnes, *"UT « Contexte »"* et *"UT « Canal de Tendance »"* (cette dernière = *"mouvement interne"*, qui *"devient « bruit » dès qu'une borne supérieure est détectée"*) ; `TRADING_LESSONS_BREAKOUT_RATIO11.md:7` (#12) situe le *"canal de tendance de l'**UT+1**"* sous le *"range de contexte principal"* de l'UT+2 (l.9). Restituée, la phrase énonce donc une **identité de lecture graphique** (« l'objet que tu affiches comme canal de tendance vit un cran sous ton contexte »), sans seuil, sans gate, sans si/alors — pas une prescription de recalculer une structure. Seule occurrence "emboîtement" du corpus (grep `emboît`/`structure interne`/`T-1`/`millefeuille` sur les 17 sources + `RULES_EXTRACTION.md`), et les deux occurrences de « mille-feuille » disent l'inverse : #9:7 *"synchroniser trois unités de temps"*, #11:43 *"maîtrise du « millefeuille » avec **respect de la priorité du contexte sur la tendance**"*.

  **Prémisse fausse n°2 — H11 ne parle pas du même sujet.** H11 dit *"ce moteur ne fait PAS de validation croisée multi-timeframe (**H4 validé par D1**)"* : la direction **ascendante** (UT+1/UT+2), celle que le corpus prescrit. L'item réclamait la structure de l'UT **inférieure**. « Mono-timeframe » couvre trivialement les deux, mais l'exemple et la justification de H11 ne concernent que l'ascendante — l'item s'appuyait sur un aveu qui ne portait pas sur lui (même classe d'erreur qu'au 7e round, où "UT+1 = Validation" était en fait "UT+1 = le stop").

  **La table TENDANCE a bien droit à 3 UT, mais les deux autres sont AU-DESSUS** (`TRADING_LESSONS_MTF_SUIVI_TENDANCE.md:13`, vérifié : *"| Unités de temps | 2 UT (ex: Daily/H4) | **3 UT impératives** (ex: Mensuel/Hebdo/Daily) |"*) — la ligne suivante de la même table tranche la direction (l.15 : *"| Hiérarchie | … | Nécessite validation sur **2 UT supérieures** |"*), et l'exemple place le Daily tradé en bas du trio. **5 sources contre 1** placent l'exécution en bas de l'échelle : #15:10 (*"on descend de deux niveaux sous la référence"*), #16:10 (*"Unité de Temps de Trading | Horaire (H1) — Soit 2 niveaux en dessous"*), #12:19, #14:15 (*"la Loi de l'Unité de Temps Supérieure"*), #11:9 (*"toute borne sur l'unité de temps « Tendance » devient inexistante et non-avenue … La priorité est toujours donnée à la structure macro"*). Le seul passage plaçant le trade au-dessus de sa structure est la glose du projet en #10:16-18, pas la citation.

  **Partiellement DÉJÀ COUVERT** (même classe de trouvaille que le Red Flag 3 / la sortie flip au 9e round) : la seule traduction opérationnelle de l'emboîtement dans tout le corpus est la projection vers le **bas** — #16 `CLUSTERS_PRIX.md:12` *"| Contextes | **Extreme Channel** (contexte de l'UT supérieure **affiché sur l'UT de trading**) |"*, l.28 *"Stop Loss : placé sous l'Extreme Channel"* ; #12:7 *"le canal de l'UT supérieure définit la zone de protection technique réelle"* ; #10:38 lui-même. C'est mot pour mot le **stop UT+1 déjà implémenté sans condition** (`ctx_support_d1`, `backtest_phase2_faithful.py` : structure détectée sur le H4 tradé, canal lu sur le D1) et déjà paramétrable (`use_mtf_stop` de `backtest_phase2_ut2.py::run_ut2`).

  **Mesures (BTC/ETH/BNB/SOL, historique complet ; harnais validé — il reproduit les 7/9/16/8 déclenchements Accumulation qui donnent les `n_trades` de `code/phase2_trend_table_results.csv`)** : (1) joindre un niveau EN DESSOUS est mécaniquement gratuit et causal (`attach_context_level` fonctionne à l'identique avec `closure_delay=1h` — la dernière bougie H1 close à l'instant d'une bougie H4 est la 4e sous-bougie de la H4 précédente) ; (2) **mais n'apporte aucune information** : `ctx_high`/`ctx_low` calculés en H1 puis joints au H4 sont identiques aux valeurs H4 natives sur 94,6-95,5% des barres et sur **100,00%** après neutralisation de l'artefact de granularité du `shift(1)` — un max/min sur fenêtre **calendaire** est exactement invariant par agrégation de bougies (généralisation exacte du "ratio médian 1,00" du 7e round), et la clôture H1 jointe est celle de la bougie H4 précédente sur 100% des barres ; donc `accum_retracement_frac`, seule grandeur numérique du gate Accumulation, est insensible au choix T/T-1 ; (3) le gate **additif** « exiger une structure T-1 mature » est **inerte bit-à-bit — 7/7, 9/9, 16/16, 8/8, écart 0 sur 4/4 actifs** (`n_borders` = comptage sur fenêtre calendaire de 15 j, donc médiane 37 en H1 contre 9 en H4 et **32-34 au minimum** sur les barres candidates, contre `MIN_BORDERS=3` : 3e confirmation indépendante du "gate inerte" des 6e/8e rounds) ; (4) la seule composante réellement dépendante du niveau est la bande EMA±ATR (écart médian 2,1-3,8% vers le bas, 7,4-12,7% vers le haut ; rejets de canal BTC 902 en H4 vs 825 en H1, **115 communs**) — artefact de notre proxy, non spécifié par le corpus, et placé **au-dessus** par celui-ci ; (5) dans la direction prescrite, le chantier est **incompatible avec H3 dans ce moteur** : stop D1 = 14,75-19,88% de distance au lieu de 1,20-2,26% (×8,6-14,6), donc plafond de campagne H3 (5%) tombant de 2,2-4,2 fois U à **0,25-0,34** — les 4 profils s'écrasent sur le même plafond et la table §4 (Renfort +25/+50/+100/+150/+200%) devient inexprimable (`position_engine.py` échappe à ce problème : tranches dimensionnées indépendamment, sans plafond cumulé) ; et utiliser le canal D1 comme référence du **rejet** fait tomber les déclenchements de 7/9/16/8 à **0/0/0/3**.

  **Correction honnête d'une contrainte supposée** : le NO-GO H1/M15 de `STATUS.md` ne bloque PAS ce chantier — il porte sur H1/M15 comme **UT d'exécution** (*"détruits par les frais de transaction"*), or un gate de structure ne paie aucun frais et le H1 est la donnée BRUTE du projet (`load_h1`, 4 actifs). Ce sont la lecture du corpus et les mesures qui bloquent, pas la faisabilité. (Note obsolète corrigée au 43e round : au moment de ce 11e round, M15 n'existait que pour BTC — la donnée M15 native couvre désormais BTC/ETH/BNB/SOL, cf. `CLAUDE.md`, section données ; n'affecte pas la conclusion de ce chantier, qui n'utilisait pas M15.)

  **Deux items nouveaux, mieux étayés, ajoutés à la catégorie C à sa place** : (1) **le `ctx_support` de `trend_table.py` n'est PAS l'« Extreme Channel » du corpus** — #16:12 le définit comme le contexte de l'UT SUPÉRIEURE affiché sur l'UT de trading, `faithful.py` l'honore (`ctx_support_d1`), mais `trend_table.py` (comme v6 et `run_ut2` sans `use_mtf_stop`) donne ce nom à la bande `ema_slow - 2*ATR` de l'UT tradée elle-même (écart médian mesuré 7,4-12,7%) : ce n'est plus une hypothèse de scope mais un niveau de stop non conforme, avec un blocage concret identifié (H3) — soit porter le stop UT+1 ET redéfinir H1/H3 pour que la table §4 reste exprimable, soit renommer honnêtement la bande H4 ; (2) **tension #10:18 vs #11:9/18 sur le statut de la structure inférieure** (intrant de validation contre *"inexistante et non-avenue"* / *"« bruit »"*), jamais consignée — à trancher avant tout futur chantier descendant, les deux lectures produisant des gates de signe opposé. Aucun changement de comportement ce cycle, aucun CSV régénéré, **157/157** tests avant et après ; seule modification de code : une note de docstring insérée entre H11 et H12 dans `code/trend_table.py` (insertion pure, zéro ligne exécutable). Détail complet : `PLAN.md` section "11e application".
- ~~**Contrainte "espace libre" MTF avant breakout** (`TRADING_LESSONS_BREAKOUT_RATIO11.md`, vérifier l'absence d'obstacles UT+1/UT+2 avant un breakout) — `trend_table.py::breakout_raw` ne teste qu'un niveau de prix + volume, aucune distance/espace disponible.~~ — **IMPLÉMENTÉ (10e round de mobilisation), mesuré, résultat honnête mitigé à dégradé, gardé optionnel comme banc de mesure.** Détail complet : `PLAN.md` section "10e application". Résumé :

  **Citation de départ vérifiée mot pour mot, et cette fois EXACTE** (contrairement au round précédent sur la même source) — `TRADING_LESSONS_BREAKOUT_RATIO11.md` **ligne 17** : *"Contrainte Multi-Timeframe (MTF) : il est impératif de vérifier l'absence d'obstacles sur les unités de temps supérieures (UT+1 et UT+2). Le breakout doit disposer d'un 'rendement escompté' suffisant, c'est-à-dire d'un espace libre de toute structure majeure pour permettre l'épanouissement de la tendance."* Le libellé porte bien sur les DEUX niveaux (UT+1 ET UT+2), et sur une place disponible — pas sur un rôle Stop/Validation/Confirmation. **2e source indépendante du même mécanisme**, `TRADING_LESSONS_PULLBACK_MATURITE.md` **ligne 14** : *"UT Supérieure ('Le Potentiel') : vérifier qu'il reste assez de 'jus' (marge de progression) pour un nouvel objectif"* (item listé séparément plus bas dans cette même section, "Hiérarchie UT Supérieure = Le Potentiel" — c'est en réalité LE MÊME enseignement, désormais couvert par la même implémentation).

  **Règle vérifiée DISTINCTE des deux règles MTF déjà codées** (ce n'est pas un doublon) : "Conflit MTF" (`TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` l.7, *"Ne jamais trader une borne de range si un range d'unité de temps supérieure est déjà actif"*) porte sur le RÉGIME d'un trade de RANGE ; "Rigueur Multi-Timeframe" (même source #12, l.21, *"Si l'UT+1 est en fin de cycle, le breakout local est un piège"*) porte sur la PHASE DE CYCLE de l'UT+1. Aucune des deux ne parle de distance disponible.

  **POINT CRUCIAL — la conclusion "2 UT / 3 UT" du round précédent s'applique ICI EN SENS INVERSE, et rend ce chantier légitime.** La raison de refus n°1 du 7e round était que le corpus ne budgète que 2 UT à la table RANGE. La MÊME ligne, revérifiée mot pour mot (`TRADING_LESSONS_MTF_SUIVI_TENDANCE.md` **ligne 13**), dit l'inverse pour la table TENDANCE — celle de `trend_table.py` : *"| Unités de temps | 2 UT (ex: Daily/H4) [Range] | **3 UT impératives** (ex: Mensuel/Hebdo/Daily) [Tendance] |"*, et **ligne 15** : *"| Hiérarchie | Dépend de l'UT supérieure | Nécessite validation sur **2 UT supérieures** |"*. La table de tendance a donc explicitement droit à un UT+1 ET un UT+2 réels — le motif qui bloquait le round précédent ne s'applique pas ici, il autorise au contraire ce chantier précis.

  **Aucun détecteur de structure inventé** (c'était le motif de refus du gate Fibonacci RANGE) : "structure majeure" = la borne HAUTE du canal Extreme Channel de l'UT concernée (`ctx_resistance` = ema_slow + 2×ATR, définition déjà présente dans `trend_table.py::add_trend_context`, miroir exact du `ctx_support` que le corpus fait déjà servir de structure de stop UT+1), calculée sur de VRAIES bougies D1/Hebdo et jointe par la jointure causale déjà existante (`CLOSURE_DELAY` de `backtest_phase2_ut2.py`, réutilisée telle quelle, ce fichier partagé n'a pas été modifié) ; "rendement escompté" = l'amplitude du range d'accumulation local, c'est-à-dire l'objectif Ratio 1:1 que la même source définit 9 lignes plus haut (l.8), déjà calculée par `prepare` (`local_range`) et jusqu'ici jamais lue par ce moteur. Hypothèses numérotées H13-H17 dans `trend_table.py`.

  **Mesures de faisabilité faites AVANT de coder** (les deux vérifiées personnellement, pas déduites) : (a) **ce n'est pas un no-op entre UT**, contrairement à l'amplitude refusée au round précédent — marge médiane jusqu'à l'obstacle 2,52% en H4 / 5,99% en D1 / 8,50% en Hebdo (BTC), ratio médian D1/H4 = 2,1-2,4× et Hebdo/H4 = 2,8-5,5× selon l'actif, parce que `EMA±ATR` est path-dépendant ; `ctx_high` (max glissant), lui, EST un quasi no-op (6,22% / 6,36% / 7,47% sur BTC) et a donc été explicitement ÉCARTÉ — l'invariance mesurée au round précédent est ainsi confirmée indépendamment, et évitée. (b) **aucun problème de données** : 0,00% de NaN sur D1 comme sur Hebdo après warmup, 2117-2374 bougies D1 et 303-340 bougies Hebdo selon l'actif (rien à voir avec les 13 bougies Mensuelles qui condamnaient l'item précédent côté `oos_xrp_faithful.py`, moteur qui n'utilise de toute façon pas cette table).

  **Résultat mesuré, honnête** (`phase2_trend_table_results.csv`, colonne `space_gate`, régénéré) : au Ratio 1:1, **12/16 couples actif×profil strictement inchangés, 1 amélioré (BTC +1,1 pt), 3 dégradés (ETH -1,9 / SOL -1,1 / BNB -0,8 pt)** — mitigé à dégradé. Implémenté quand même (principe inviolable du projet), gardé OPTIONNEL (`use_breakout_space_gate=False` par défaut, comportement historique préservé bit-à-bit, vérifié : les 16 lignes historiques du CSV sont identiques au chiffre près) parce que ce moteur est le banc de mesure isolé de la table de tendance (H11).

  **Le gate n'est PAS inerte** (contrairement à `MIN_BORDERS`), diagnostic dédié `phase2_trend_table_space_gate_diagnostic.csv` : sur 264-306 bougies candidates au breakout par actif, il en refuse **34-43% au Ratio 1:1** (21-26% à 0,5× ; 44-51% à 1,5×), et l'obstacle vient plus souvent de l'UT+1 que de l'UT+2 (76-108 vs 36-68 blocages selon l'actif/le multiplicateur). Les 12/16 couples inchangés le sont pour une raison STRUCTURELLE, pas parce que la règle ne mord pas — cf. correction ci-dessous.

  **Correction documentaire trouvée en passant (généralisation abusive répétée dans 4 endroits)** : "100% des campagnes de tendance se referment en étape Accumulation, aucune n'atteint jamais Breakout" (`PLAN.md` ×2, `CONFIGURATION_RECOMMANDEE.md`, `backtest_phase2_recommended.py`, `unified_protocol.py::main`) n'est vrai que pour 3 profils sur 4. À noter en faveur de la mesure d'origine : la section "table de tendance" de CE document, elle, scopait correctement le constat aux 3 profils MODERE/AGRESSIF/TRES_AGRESSIF — ce sont les résumés qui ont perdu le périmètre en route. Compté par instrumentation du VRAI moteur : profils MODERE/AGRESSIF/TRES_AGRESSIF = 39 campagnes chacun, **0 atteint le Breakout** (l'affirmation tient) ; profil **FAIBLE = 30 campagnes, dont 8 atteignent le Breakout, 5 la Divergence et 1 le Pull-Back/Excès final** (l'affirmation est fausse) — les étapes 2 à 5 de la table SONT donc exercées empiriquement, sur ce seul profil. C'est exactement pourquoi le gate ne peut faire bouger que les 4 couples FAIBLE.
- ~~**Amplitude Validation/Confirmation jamais réellement cross-timeframe** malgré des rôles UT+1/UT+2 explicitement prescrits par le corpus (`TRADING_LESSONS_BREAKOUT_RATIO11.md`) — `local_range`/`context_range` (`position_engine.py`) sont deux fenêtres glissantes de durées différentes calculées sur le MÊME H4, jamais de vraies bougies UT+1/UT+2 — même catégorie de risque de méthode que Fibonacci/Conflit MTF, touchant les 3 moteurs opérationnels. Signalé comme la trouvaille C la plus significative de ce round.~~ — **INVESTIGUÉ (7e round de mobilisation multi-agents, 2 agents de conception indépendants), décision prise : PAS implémenté, citation corrigée.**

  **Citation de départ partiellement fausse (trouvée indépendamment par les 2 agents, vérifiée personnellement mot pour mot)** : `TRADING_LESSONS_BREAKOUT_RATIO11.md` lignes 7-9 — *"Stop-loss = ... canal de tendance de **l'UT+1**"* (l.7) ; *"Phase de Validation ... amplitude du range d'accumulation local **(UT)**"* (l.8 — UT LOCALE, pas UT+1) ; *"Phase de Confirmation ... utiliser **l'UT+2**"* (l.9). La source se résume elle-même, l.19 : *"UT+1 sert au positionnement du stop, UT+2 sert à la confirmation/déblocage du breakeven."* **UT+1 est le rôle du STOP** (déjà implémenté sans condition, `ctx_support_d1`), **pas de la Validation**. `local_range` est donc déjà fidèle (UT locale = H4) ; seule `context_range` (Confirmation) a un rôle UT+2 littéral dans cette lecture. Le périmètre réel de l'item était deux fois plus étroit que documenté.

  **Décision de ne PAS implémenter la moitié restante (Confirmation = amplitude UT+2 réelle), 3 raisons vérifiées personnellement, indépendamment des 2 rapports d'agents** :
  1. Le corpus ne budgète que **2 UT pour la table RANGE** (celle qu'implémente `position_engine.py`) : `TRADING_LESSONS_MTF_SUIVI_TENDANCE.md:13` — *"Unités de temps | 2 UT (ex: Daily/H4) [Range] | 3 UT impératives [Tendance]"*. Une vraie dépendance UT+2 dans la machine à états RANGE contredit cette ligne ; la 3e UT est réservée à `trend_table.py` (table TENDANCE), qui de toute façon n'utilise pas `local_range`/`context_range` (vérifié par grep — elle a ses propres `ctx_high`/`ctx_low`/`local_high`).
  2. **Le corpus lui-même est partagé sur ce qu'est "Confirmation"** : `RULES_EXTRACTION.md` §3 (LE manuel officiel, l'autorité la plus haute du projet) dit *"Confirmation (médiane canal contexte, **clôturée**)"* (vérifié, ligne 41) — un NIVEAU STRUCTUREL ABSOLU confirmé par clôture, pas une distance projetée depuis l'entrée. 4 sources sur 6 (le manuel + `MAITRISE_GRADIENT_RISQUE.md`/`PULLBACK_MATURITE.md`/`CLUSTERS_PRIX.md`) partagent cette lecture structurelle ; seules 2 (`BREAKOUT_RATIO11.md`/`PYRAMIDALISATION.md`) décrivent l'amplitude/Ratio 1:1 actuellement codée. `PULLBACK_MATURITE.md:11` réconcilie même les deux vocabulaires lui-même (vérifié) : *"Même intitulé exact... reformulé en termes de prix plutôt qu'en unités de temps"* — indice fort que "UT+2" désigne QUEL canal on projette, pas une prescription de rééchantillonner l'OHLC.
  3. **Toute lecture littérale avec de vraies bougies Hebdomadaires met en péril deux éléments du corpus déjà mieux établis** : (a) le passage au breakeven à la Confirmation est *"la correction la mieux étayée de tout le corpus"* (`TRADING_LESSONS_INDEX.md`, confiance très élevée, 2 sources concordantes) — une cible repoussée à +46-88% du prix (mesuré, fenêtre Hebdo × 15 bougies) la rend quasi inatteignable ; (b) `oos_xrp_faithful.py` (la seule validation hors-échantillon du moteur phare) n'a que 13 barres Mensuelles disponibles — une fenêtre Hebdo comparable à l'actuelle produirait ~100% de NaN sur cette validation, la détruisant.

  Argument complémentaire (non décisif seul) : au niveau D1 (UT+1), "rendre cross-timeframe" est mesuré comme un quasi no-op — `max(high)-min(low)` sur une fenêtre calendaire est invariant par agrégation de bougies (ratio médian 1,00 sur les 4 actifs, mesuré indépendamment par les 2 agents), contrairement au stop `EMA±ATR` (path-dépendant, chantier D1 substantiel).

  **Requalifié** en un nouvel item catégorie C, mieux étayé par le manuel officiel : *"Confirmation comme niveau structurel absolu (médiane du canal de contexte, clôturée) plutôt qu'une distance `entry + context_range` figée à l'entrée"* — hors périmètre de ce cycle (mécanisme plus profond : `position_engine.py` ne recalcule aujourd'hui aucun seuil après l'ouverture d'une tranche ; un niveau absolu lu en direct exigerait une mécanique proche de `ctx_resistance`/`ctx_high` de `trend_table.py`, pas un simple changement d'appelant). Portée si un jour traité : 13 appelants directs de `make_open_tranche_fn` + 5 copies inline non refactorées + ~9 wrappers (~27 fichiers), ~30-35 CSV à régénérer. Aucun code modifié ce cycle — investigation et correction documentaire uniquement.

- ~~**NOUVEAU (7e round) — "Confirmation comme niveau structurel absolu (médiane du canal de contexte, clôturée) plutôt qu'une distance `entry + context_range` figée à l'entrée"**~~ — **TRAITÉ à la 16e application (cf. `PLAN.md`) : mécanisme IMPLÉMENTÉ, testé et mesuré, mais DÉSACTIVÉ PAR DÉFAUT pour un motif de fidélité au corpus MESURÉ — jamais de performance. Item retiré de la catégorie C. Deux prémisses du 7e round ci-dessus sont FAUSSES et corrigées ici.**

  **Citation confirmée, et RENFORCÉE par une seconde source dont le 7e round citait le nom sans donner le texte.** `RULES_EXTRACTION.md:41` est exact mot pour mot ; relire la ligne ENTIÈRE ajoute un argument que le 7e round n'avait pas fait : **les 4 étapes y sont formulées structurellement** (*Validation* = "borne opposée canal tendance", *Invalidation* = "cassure forte canal tendance"), pas seulement la Confirmation — la lecture "niveau structurel" est le registre de toute la ligne. Surtout, `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md:55` (#5, §5, *"Phases de la position"*) dit, vérifié mot pour mot : *"Confirmation (**structurelle**) : clôture d'une bougie sous/au-dessus **la médiane (50%)** du contexte"* — elle **qualifie l'étape de structurelle**, **chiffre la médiane**, et reprend la même échelle à 3 crans que le manuel. **Le motif de refus "il faudrait inventer un paramètre" ne s'applique donc PAS ici**, contrairement au gate Fibonacci RANGE, aux Red Flags 1/2 ou au Cluster technique.

  **Décompte des sources REFAIT personnellement — total juste, liste fausse d'un membre.** 4 structurelles (manuel §3:41 ; #5:55 ; **#10** `ZONE_ACCUMULATION.md:41`, *"Confirmation : clôture franche à l'intérieur du contexte OU sortie confirmée du range de départ → position mise à Break-Even"* ; **#13** `PULLBACK_MATURITE.md:9`) contre 2 en amplitude projetée (#12:9, #15:22/28). Le 7e round citait **#16 `CLUSTERS_PRIX.md`** parmi les structurelles : vérifié, #16 ne définit structurellement que la **Validation** (l.29, *"atteinte du contexte vendeur opposé (résistance)"*) et ne donne **aucun niveau** pour la Confirmation. La 4e source réelle est #10. **Argument de scope nouveau et décisif** : les 2 sources "amplitude" sont BREAKOUT/TENDANCE (#12 = *"Masterclass Breakout"*, #15 = pyramidalisation post-breakout), les 2 sources "médiane" sont RANGE (titre de §3 ; *"Anatomie du Range"* pour #5) — et `position_engine.py` implémente la table RANGE.

  **PRÉMISSE ARCHITECTURALE DU 7e ROUND : FAUSSE.** *"`position_engine.py` ne recalcule aujourd'hui aucun seuil après l'ouverture"* est démenti par le code : `run_position_engine` expose **depuis le refactor d'origine** le hook `update_levels_fn(tr, i)` (appelé avant `process_tranche` pour chaque tranche ouverte), **exercé en production** par `backtest_phase2.py`. Les latches one-shot `val_done`/`conf_done` ne sont pas non plus un obstacle : `process_tranche` lit la valeur COURANTE de `conf_px`, donc "le prix touche une bande qui se déplace" était déjà exprimable. **Coût réel : 1 fonction pure + 1 colonne additive, zéro ligne modifiée dans la boucle** — pas la refonte annoncée.

  **CE QUI BLOQUE VRAIMENT, ET C'EST MESURÉ (4 076 tranches, BTC/ETH/BNB/SOL × 4 profils, 212 atteignant la Validation).** Le corpus ne NOMME pas quel canal ; les **4 lectures de "canal de contexte" présentes dans ce projet** ont donc toutes été mesurées (`code/structural_confirmation_diagnostic.csv`) : le niveau structurel est sous `val_px` **dans 100,0% des cas pour les 4**, et sous le prix d'entrée dans 91,4% / 96,2% / 98,8% / 92,0%. Conséquence : la Confirmation se déclencherait **dès la bougie suivant la Validation dans 98,1 à 100% des cas** (délai médian **0 bougie**, contre **43** aujourd'hui ; 212/212 atteintes contre 176/212). Comme `conf_to_be=True`, l'activer ferait passer le stop au break-even quasi immédiatement après la Validation, c'est-à-dire **supprimer en substance la règle du break-even différé** — 4 sources concordantes (#12:11 *"L'erreur fatale…"*, #13:9 *"impératif de DISSOCIER deux étapes cruciales"*, #15:28, #16:32) et *"la correction la mieux étayée de tout le corpus"* selon `TRADING_LESSONS_INDEX.md:35`. **Même arbitrage qu'au 7e round, mais pour la première fois MESURÉ et non estimé.**

  **Origine du conflit, dite honnêtement : nos briques, pas le corpus.** La géométrie du manuel est emboîtée (achat au BAS du canal de tendance → Validation à sa borne opposée → médiane du canal de contexte, plus large, encore DEVANT). Notre proxy ne la reproduit pas : (i) `score >= 2` entre dans la moitié **HAUTE** du canal de contexte **9 fois sur 10** — ce qui contrevient à `RULES_EXTRACTION.md:17` (*"Ne jamais … acheter la partie haute"*, **règle de base #2, jamais implémentée comme gate d'entrée** : nouvel item catégorie C, cf. ci-dessous) ; (ii) `val_px = entry + local_range` projette **+17,2% à +28,2%** au-dessus de l'entrée.

  **Implémenté** : `position_engine.py` (`CONTEXT_MEDIAN_FRAC = 0.5`, `context_channel_median`, `make_structural_conf_update_fn`, hypothèses **H-Conf-Struct-1..5** documentées en tête avec les citations) ; `backtest_phase2_v7.py` (colonne additive `ctx_median` + `use_structural_confirmation=False`) ; `backtest_phase2_faithful.py` (idem, **exception au contrat "toutes les règles littérales sans condition" signalée explicitement en tête de ce fichier**) ; `test_position_engine.py` **13/13 → 19/19** (vérité terrain à la main, contrôle positif ET négatif) ; `code/structural_confirmation_measure.py` + 2 CSV nouveaux. **Effet mesuré OFF→ON, rapporté tel quel** : `faithful` **-4,14 pt** de retour / **+1,95 pt** de drawdown (moins profond, 10/16), 5 améliorés / 11 dégradés, 4 113 → 4 533 trades ; v7 **-17,16 pt** / **+1,96 pt**, 2 / 14. Ces chiffres **n'ont joué aucun rôle dans la décision**.

  **Portée RECOMPTÉE** (le 7e round disait "~27 fichiers, ~30-35 CSV") : **19 fichiers** appellent `make_open_tranche_fn`, **20** `run_position_engine` (17 hors tests), **6** portent une copie inline de `conf_px` hors `position_engine.py`, clôture transitive **45 fichiers hors tests (+19 de tests)** et **46 CSV sur 64** — l'estimation était basse sur les deux axes. Portée réellement matérialisée, le défaut restant OFF : **3 fichiers de production, 0 CSV existant modifié** (`backtest_phase2_faithful_results.csv` et `phase2_v7_mtf_results.csv` régénérés par rejeu réel, **identiques bit-à-bit**).

  **Condition explicite de réouverture** (pas "jamais") : que le projet se dote d'un canal de tendance géométrique emboîté dans le canal de contexte ET d'un gate d'entrée "partie basse du canal" — à ce moment-là le niveau redevient un jalon réel et le défaut doit repasser à ON sans nouvelle discussion de performance.

- **NOUVEAU (16e round, catégorie C) — `RULES_EXTRACTION.md:17`, règle de base #2 : *"Ne jamais vendre la partie basse du canal de tendance / acheter la partie haute"*, jamais implémentée comme gate d'entrée.** Trouvée en mesurant l'item ci-dessus, et directement chiffrée par lui : **89,6% à 94,3% des tranches réellement ouvertes par `faithful.py` le sont dans la moitié HAUTE du canal de contexte**. Le prédicat existe déjà dans le projet (`fibonacci.py::compute_context_position >= 0,50`, *"moitié basse du canal"*) mais sert une autre règle (Pull-Back de §1) derrière un gate OFF par défaut. **Catégorie C et non B** : deux décisions de conception à trancher avant tout code — quel canal (le manuel dit "canal de tendance", le prédicat disponible mesure le canal de contexte) et quelle conséquence (abstention totale ou réduction de taille, le manuel ne le dit pas).
- ~~**Définition stricte de la "3ème borne"** (3 critères séquentiels, validée par CLÔTURES pas par mèches, `TRADING_LESSONS_TROISIEME_BORNE.md`) — toute la détection de bornes du projet (`proxy_v2.py::compute_swing_*_confirmed`, `argrelextrema`) travaille sur les mèches (high/low), jamais cette validation par clôtures. Blast radius large (utilisé par `n_borders` partout).~~ — **INVESTIGUÉ (8e round de mobilisation), décision prise : PAS implémenté, l'énoncé de l'item était une ERREUR DE CATÉGORIE. Deux items nouveaux, mieux étayés, trouvés à la place ; une citation partiellement fausse trouvée et corrigée dans `PHASE2_CORRECTION_CLOSES.md`.**

  **Citation exacte, relue en entier (`TRADING_LESSONS_TROISIEME_BORNE.md` lignes 20-23, "Définition stricte d'une 3ème borne valide (3 critères séquentiels, aucune approximation acceptée)")** :
  1. *"**Mouvement tendanciel** : flux directionnel puissant et sans ambiguïté"*
  2. *"**Cassure du contexte** : le prix doit rompre la structure de prix, validée par des **clôtures de bougies au-delà du contexte** (pas un simple dépassement intra-bougie)"*
  3. *"**Le Pullback** : retour tester la zone rompue, zone d'intervention à **Fibonacci 61,8%**, uniquement si le prix maintient ses clôtures sous/sur le contexte — un simple effleurement sans tenue des clôtures = invalidation technique"*

  **Erreur de catégorie (le point décisif)** : les deux clauses "clôtures" de la source sont des **prédicats de validation appliqués à un niveau déjà tracé** (le prix casse-t-il le contexte ? le pullback tient-il ses clôtures ?), **jamais une règle de localisation d'un point de swing**. La source ne dit pas un mot de la façon dont le prix d'une borne est déterminé. L'item, tel qu'il était formulé, transposait une règle de validation en une règle de détection — et rien dans le corpus n'autorise cette transposition. Aucun autre seuil/chiffre caché : le seul chiffre de ces 3 critères est le 61,8%, déjà implémenté (`fibonacci.py::FAVORABLE_MAX = 0.618`, `test_fibonacci.py` verrouille le cas 61,8% en citant explicitement la source #11).

  **Les 3 critères sont en réalité DÉJÀ couverts, chacun à sa place** (vérifié par lecture directe, pas par confiance dans les tables ci-dessus) : critère 2 = `trend_table.py::breakout_raw` (`c[i-1] > local_high_v[i-1]` — niveau tracé sur le max glissant des HAUTS, cassure testée sur la CLÔTURE) et `excess_raw` (`c[i] > ctx_high_v[i-1]`, même schéma) ; critère 3 = niveau 61,8% de `fibonacci.py` + déclenchements Validation/Confirmation/Limite sur clôture (`PHASE2_CORRECTION_CLOSES.md`) ; l'hypothèse H5 de `trend_table.py` code même explicitement la combinaison des deux conventions : *"la mèche basse touche/dépasse `ctx_support` ET la clôture referme au-dessus"*. Il ne reste donc rien, dans ces 3 critères, qui corresponde à `compute_swing_*_confirmed`.

  **3 sources contredisent frontalement une détection de bornes par clôtures** (relecture des 17 sources + du manuel, grep `clôtur`/`mèche`/`wick`/`borne`, pas la citation de départ) :
  - `TRADING_LESSONS_CLUSTERS_PRIX.md:30` (#16) — *"Gestion du stop après validation : remonté sous le dernier creux structurel (**bas de clôture ou mèche**)"* : le corpus **refuse explicitement de trancher** pour le creux structurel. Une lecture "stricte par clôtures" est démentie mot pour mot.
  - `TRADING_LESSONS_ZONE_ACCUMULATION.md:38` (#10) — *"Stop-loss = largeur moyenne du canal de tendance récent, ou sous le **point bas de la 4ème borne**"* : une borne a un *point bas* (un extrême, donc une mèche), sous lequel on place le stop.
  - `TRADING_LESSONS_STRUCTURES_ALTERATIONS.md:28` (#14) — *"Règle de l'Overlap : l'ancienne résistance devient support. C'est une **zone de tolérance, pas une ligne mathématique** — les mèches peuvent pénétrer l'ancien territoire, mais les clôtures de bougies doivent rester à l'extérieur pour valider la structure."* : énoncé le plus net de la convention réelle du corpus, et c'est l'INVERSE de l'item — le niveau est tracé par les extrêmes, les clôtures sont le TEST. (`TRADING_LESSONS_BREAKOUT_RATIO11.md:27` va dans le même sens : *"minimum 3 bornes **testées**"*.)
  - Nuance honnête, dans l'autre sens, pour ne pas surinterpréter : `TRADING_LESSONS_BREAKOUT_RATIO11.md:7` définit bien UN niveau par des clôtures — *"Stop-loss = **clôture la plus basse** (pour un achat) du canal de tendance de l'UT+1"*. C'est le SEUL endroit du corpus où un niveau structurel est localisé par une clôture, et il concerne le stop UT+1, pas une borne.

  **Contradiction mécanique induite** : une borne n'est pas qu'un booléen, c'est un PRIX consommé ailleurs (`compute_ascending_lows` compare `low_v[i - SWING_ORDER]` ; `fibonacci.py::compute_retracement` utilise les prix de swing au dénominateur ; le corpus place le stop *"sous le point bas de la 4ème borne"*). Détecter par clôtures impose de prendre la clôture comme prix de la borne — donc un stop placé À L'INTÉRIEUR de la mèche, contre la convention déjà écrite du projet (`position_engine.py` lignes 87-90 : *"stop touché sur la MÈCHE (ordre réel intrabar) ; cible atteinte sur la CLÔTURE"*) et contre #10:38.

  **Mesure empirique (exigée avant de conclure, pas supposée — BTC/ETH/BNB/SOL, H4 et D1, tout l'historique)** : la fréquence de détection ne change quasi pas (swing lows H4 : 1476→1461 BTC, 1444→1448 ETH, 1413→1409 BNB, 1259→1286 SOL, soit -1,0% à +2,1%) **mais seulement ~40% des barres détectées sont les mêmes** (582/~1470 sur BTC H4) — le changement DÉPLACE les bornes sans en changer le nombre. Conséquences chiffrées : (a) sur le consommateur que l'item désignait (`n_borders` comme gate de maturité), **strictement aucun effet utile — le gate reste INERTE dans les deux définitions** : `n_borders` médian 9,0 → 9,0, minimum chez les bougies candidates 5→5 (BTC), 5→4 (ETH), 5→6 (BNB), 5→5 (SOL), premier seuil mordant 6→6/5/7/6, et `MIN_BORDERS=3` ne mord jamais dans aucune des 8 configurations (cohérent avec la trouvaille "gate inerte" du 6e round) ; (b) sur le consommateur que l'item NE désignait PAS (`compute_ascending_lows` → `structure_favorable` → `score` de `proxy_v2`), effet silencieux réel : `structure_favorable` change sur **10,7% à 14,9%** des barres et le signal d'entrée `score >= 2` **basculerait sur 5,6% à 8,1% des barres** sur les 8 couples actif × TF. **Le blast radius annoncé par l'item est inversé** : nul là où il était signalé, large là où il ne l'était pas.

  **Portée réelle si un jour traité** (grep exhaustif, pas une estimation) : 10 fichiers avec un vrai site d'appel de `compute_swing_low_confirmed`/`compute_swing_high_confirmed` (`proxy_v2.py`, `fibonacci.py`, `andrews_pitchfork.py`, `manual_trend_channel.py`, `wall_street_pattern.py`, `backtest_phase2_v4/v5/v6/v7.py`, `backtest_phase2_capital_tiers.py`) ; 5 fichiers producteurs de `n_borders` ; 30 fichiers `code/*.py` qui le mentionnent (dont 6 fichiers de tests) ; et, via le `score` de `proxy_v2`, la quasi-totalité des CSV de résultats (~30-35 sur 63) — même ordre de grandeur que le chantier écarté au 7e round, sans la moindre citation à l'appui.

  **Décision** : NE PAS implémenter (aucun code modifié, aucun CSV régénéré, 157/157 tests inchangés). Sacrifier 3 sources concordantes et une convention déjà codée pour satisfaire une lecture qu'aucune source n'énonce serait exactement le "risque de méthode" que ce projet refuse — même arbitrage qu'au 7e round. **Item retiré de la catégorie C** (ce n'est pas un manque : c'est déjà couvert, ailleurs et correctement), remplacé par les deux items ci-dessous, qui sont ce que la relecture a réellement trouvé.

- ~~**NOUVEAU (8e round) — "règle d'or : calculs sur clôtures, jamais sur les mèches" (#15) vs `local_range`/`context_range` calculés sur les mèches.**~~ — **INVESTIGUÉE (12e application, cf. `PLAN.md`), PAS IMPLÉMENTÉE, item RETIRÉ de la catégorie C. La formulation ci-dessous est conservée telle quelle pour traçabilité, mais elle est corrigée sur deux points par la relecture du 12e round (détail complet et chiffres après le bloc barré).** `TRADING_LESSONS_PYRAMIDALISATION.md:20` est le titre de section lui-même : *"## Patterns de pyramidalisation (**règle d'or : calculs sur clôtures, jamais sur les mèches**)"*, précisé ligne 22 — *"validation au ratio 1:1 (**report de l'amplitude du range en clôture**)"* — et ligne 39 — *"ratios de validation **en clôture**"*. C'est l'énoncé LE PLUS LITTÉRAL et le plus général du corpus sur clôtures vs mèches, et il porte sur les **amplitudes projetées**. Or `backtest_phase2_v7.py::prepare` calcule `local_range = max(high) - min(low)` et `context_range = max(high) - min(low)` (fenêtres 5D/15D), c'est-à-dire des amplitudes **purement mèches**, projetées telles quelles en `val_px = entry + local_range`, `conf_px = entry + context_range`, `lim_px = entry + 1.5 * context_range` (`position_engine.py` lignes 409-411). `PHASE2_CORRECTION_CLOSES.md` a corrigé les **déclenchements** (mèche → clôture) mais a laissé les **amplitudes** sur les mèches : une incohérence interne jamais relevée jusqu'ici, alors que c'est précisément ce que la règle d'or vise. **Mesuré** (BTC/ETH/BNB/SOL) : une amplitude en clôtures vaut 0,786-0,808 de l'amplitude en mèches sur `local_range` H4 (médiane, soit -20%), 0,862-0,876 sur `context_range` H4 (-13%), et 0,552-0,578 / 0,720-0,752 en D1 (-45% / -27%) — donc des cibles Validation/Confirmation/Limite nettement plus proches, pas un no-op. Décision de conception requise avant tout code (le corpus ne dit pas si "amplitude du range" désigne le range de la fenêtre glissante ou celui d'une structure identifiée) ; portée = les 13 appelants de `make_open_tranche_fn` + copies inline, ~30-35 CSV, la même surface que l'item écarté au 7e round dont celui-ci est le voisin direct. **À traiter en priorité de la catégorie C** : c'est le seul item "clôtures vs mèches" du corpus qui soit à la fois littéral, non ambigu sur le principe, et non couvert par le code.
  - **Correction 1 — l'objet de la règle est AMBIGU, et le 8e round n'a retenu qu'une des deux lectures.** Le complément *"en clôture"* de la ligne 22 admet (a) *"l'amplitude se mesure sur les clôtures"* (lecture de l'item) et (b) *"la validation du ratio 1:1 se constate sur une clôture"*. La lecture (b) est appuyée par la ligne 39 de la même source (*"ratios **de validation** en clôture"*) et par la tournure du titre (*"jamais sur les mèches"*), qui est la formule stock du corpus pour "ne pas valider sur une mèche" (#11:22 *"pas un simple dépassement intra-bougie"*, #14:28 *"les clôtures ... pour **valider** la structure"*). **La lecture (b) est déjà implémentée, exactement** (`process_tranche` déclenche Validation/Confirmation/Limite sur `c[i] >= ...` depuis `PHASE2_CORRECTION_CLOSES.md`) — et c'est déjà la lecture que le projet applique à cette phrase, puisque le 8e round a lui-même rétabli #15 parmi les sources de la correction des **déclenchements**. L'item demandait donc de faire porter la même phrase **deux fois, sur deux mécanismes distincts**.
  - **Correction 2 — la lecture (a) contredit la convention que le corpus énonce partout ailleurs** (*le NIVEAU/la STRUCTURE vient des extrêmes, la CLÔTURE est le TEST*), vérifiée dans **5** passages relus : #14 `STRUCTURES_ALTERATIONS.md:28` ; #16 `CLUSTERS_PRIX.md:30` (*"bas de clôture **ou** mèche"*, refus explicite de trancher) ; #10 `ZONE_ACCUMULATION.md:38` (*"sous le **point bas** de la 4ème borne"*) ; **#5 `MAITRISE_GRADIENT_RISQUE.md:56`** (*"Objectifs : Range Neutre = 76% Fibonacci de la vague précédente ; Range Vendeur/Acheteur = **débordement du point extrême précédent**"* — **la seule phrase de tout le corpus qui dise comment se mesure un OBJECTIF de trade de range, donc l'étape Limite, donc `lim_px` : elle dit "point extrême", pas "clôture"**) ; `RULES_EXTRACTION.md:41` (*"Confirmation (médiane canal contexte, **clôturée**)"* — "clôturée" qualifie le CONSTAT du franchissement d'un niveau structurel, pas la mesure de ce niveau). C'est mot pour mot le raisonnement par lequel le 8e round a lui-même refusé de détecter les bornes par clôtures.
  - **Argument décisif nouveau, mesuré au 12e round : la lecture (a) casse la finalité que la source du mécanisme lui assigne.** Le mécanisme codé vient de #12 `BREAKOUT_RATIO11.md:8` — *"Objectif = **"payer son stop loss"** — prise de profit partielle à ce niveau finance statistiquement le risque initial. Le trade devient "gratuit""* — donc la cible de Validation doit valoir ~1 R. Mesuré sur BTC/ETH/BNB/SOL H4, `(val_px - entry) / (entry - stop_D1)`, médiane : **0,88 / 0,91 / 0,91 / 1,06 en mèches** (la définition en place est déjà calée sur le 1:1 exigé par #12) contre **0,69 / 0,71 / 0,72 / 0,85 en clôtures** ; part des bougies où R<1 : 48-54% → 55-62%. #12 lui-même est **muet** sur clôtures vs mèches (sa seule occurrence de "clôture", ligne 7, concerne le stop UT+1).
  - **Requalification vers `trend_table.py` examinée et écartée** : #15 décrit bien la pyramidalisation post-breakout en contexte de TENDANCE, mais la pyramidalisation de ce projet est dans `position_engine.py` (`MAX_TRANCHES=3`), et `trend_table.py` l'exclut explicitement (H12) **et n'a aucune projection d'amplitude 1:1** (*"des ÉVÉNEMENTS DE STRUCTURE DE MARCHÉ détectés ..., pas des niveaux de prix fixes issus de l'entrée"* — vérifié par grep : ni `val_px` ni `local_range` consommés). La règle d'or n'a aucune cible dans ce fichier.
  - **Mesures faites quand même** : ratio amplitude clôtures/mèches **revérifié personnellement et conforme au 8e round au millième près** ; impact backtest complet si (a) était appliquée (BTC/ETH/BNB/SOL × 4 profils, un seul paramètre changé) : `v7` +6,2 pts en moyenne mais **10/16 couples dégradés** (moyenne tirée par le seul BTC/TRES_AGRESSIF, +72 pts), drawdown **-2,21 pt (plus profond)**, -10 trades ; `faithful` **-0,9 pt**, 9/16 dégradés, drawdown -1,38 pt, -13,6 trades — signe mixte, drawdown systématiquement plus profond, **la performance ne tranche pas**. **Invariante `val_px <= conf_px <= lim_px` préservée par construction dans les deux définitions** (5D ⊂ 15D), 0 violation sur 55 171 bougies — ce risque signalé au 7e round n'était pas un obstacle. **Aucun chiffre de fenêtre nulle part dans le corpus** (grep exhaustif de `amplitude`/`projeter`/`report d`/`hauteur` : uniquement #12:8-9 et #15:16/22/24) : `LOCAL_DURATION`/`CONTEXT_DURATION` restent l'hypothèse en place. Portée réelle recomptée : **25 fichiers `code/*.py`** (dont 4 de tests), **5 producteurs**, **19 appelants** de `make_open_tranche_fn` (et non 13), **7 copies inline** de `val_px`/`conf_px`/`lim_px`, **35 CSV sur 63**.
  - **Décision** : NE PAS implémenter, item **retiré de la catégorie C**. Aucun code de comportement modifié, aucun CSV régénéré, **157/157 tests avant et après** ; seules modifications : un bloc documentaire en tête de `code/position_engine.py`, un en tête de `code/fibonacci.py`, et le remplacement du commentaire inline "TENSION OUVERTE, NON RÉSOLUE" laissé au 8e round dans `make_open_tranche_fn`.
- ~~**NOUVEAU (8e round) — `fibonacci.py::compute_retracement` est hybride mèches/clôtures, non documenté comme tel.**~~ — **FERMÉ au 12e round comme CONFORME, pas comme défaut, et retiré de la catégorie C.** `(swing_high - close) / (swing_high - swing_low)` n'est pas un hybride accidentel : c'est la transcription **exacte** de la convention ci-dessus — le dénominateur mesure une STRUCTURE, donc ses extrêmes (#10:38 *"point bas"*, #5:56 *"76% Fibonacci de la **vague** précédente"*, #16:30 qui refuse même de trancher), et le numérateur situe la CLÔTURE dans cette structure (#11:23 *"uniquement si le prix maintient ses **clôtures** sous/sur le contexte"* ; #10:46 *"**clôture** maintenue au-delà de 61% de **la structure**"* — mot pour mot la formule codée, "la structure" étant le dénominateur et "clôture" le numérateur). Passer le dénominateur en clôtures serait exactement le changement que le 8e round a refusé pour la détection des bornes, et **aucune citation du corpus ne mesure une profondeur de retracement Fibonacci sur des clôtures** (grep exhaustif des 17 sources + `RULES_EXTRACTION.md`). Note documentaire ajoutée en tête de `code/fibonacci.py`.
- **NOUVEAU (12e round) — le coefficient `1.5` de `lim_px = entry + 1.5 * context_range` n'a AUCUNE source dans le corpus et n'est documenté nulle part.** Vérifié par grep exhaustif : aucun "1,5"/"1.5" du corpus ne s'applique à un objectif de trade (les seuls 1,5 du projet sont le `volume > 1.5x sa moyenne` de H9 de `trend_table.py` et `TREND_SLOPE_THRESHOLD=1,5%` de `regime_classifier.py`, sans rapport), et aucun des 40 `.md` ne le justifie — alors qu'il est recopié **en littéral dans 7 fichiers**. À comparer avec `backtest_phase2.py`, dont les multiples d'ATR (`LIMITE_MULT = 3.5`) sont explicitement documentés comme approximation assumée. Le corpus donne pourtant deux définitions littérales, typées par régime, de l'objectif d'un trade de range (#5:56) — non implémentables telles quelles (elles exigent *"76% Fibonacci de la vague précédente"* et un *"débordement du point extrême précédent"*, dont le piège du 76% est déjà documenté dans `CONFIGURATION_RECOMMANDEE.md` §5quinquies). C'est le paramètre inventé le moins tracé du moteur RANGE.
- ~~**UT+2 réservé aux trades de "renversement", pas de "continuation"** (`TRADING_LESSONS_ZONE_ACCUMULATION.md`) — le gate Hebdomadaire actuel s'applique uniformément ; le projet n'a aucune notion de "continuation vs renversement" à coder cette distinction.~~ — **INVESTIGUÉ (13e application, cf. `PLAN.md`), PAS implémenté, décision prise et documentée. L'énoncé de l'item est ici EXACT (contrairement aux 7e/8e/9e rounds) ; ce qui bloque est mesurable et se trouve ailleurs.**
  - **Citation exacte, vérifiée mot pour mot (`TRADING_LESSONS_ZONE_ACCUMULATION.md` ligne 7)** : *"Typologies d'accumulation : **Continuation** (2/3 des cas, sort dans le sens de la tendance précédente) vs **Renversement/Revers** (1/3 des cas). Pour le trade en 'Revers', une règle de prudence s'impose : il est **obligatoire de vérifier la tendance sur une unité de temps située deux degrés au-dessus** (ex: consulter le Mensuel pour un trade en Daily). Si le flux de cette UT supérieure valide le mouvement, le trade est acceptable. En cas de doute, privilégiez toujours le flux le plus probable ou **attendez le Breakout pour confirmer la direction**."* — plus la glose du projet, ligne 9 : *"un trade de continuation est déjà aligné avec le TF supérieur (pas de vérification supplémentaire nécessaire)"*.
  - **Les deux axes de confusion possibles ont été vérifiés, et il n'y en a pas** : (a) c'est bien le gate de `backtest_phase2_ut2.py` qui est visé — le fichier le savait déjà, sa docstring ligne 28 écrit *"Spécifique aux trades de renversement"* en passant sans en tirer de conséquence ; (b) le scope est bien la table **RANGE** (§3) et non la table TENDANCE — #10 gère son trade avec le vocabulaire exact de §3 (*"Minimum 4 bornes"*, *"Validation : prix atteint la borne opposée"*, *"Confirmation : … mise à Break-Even"*), pas les 5 étapes de §4. #9 restreint de la même façon sa clause de transgression aux trades *"contre le flux"*.
  - **Blocage 1 — la distinction est INEXPRIMABLE sans inventer un 5e régime.** `regime_classifier.add_regime` est structurellement asymétrique : `TENDANCE` et `RANGE_TENDANCIEL` exigent tous deux `slope_pct > 0`, donc une tendance BAISSIÈRE retombe en `RANGE_NEUTRE`, indiscernable d'un range plat. Mesuré : 32,4-49,8% des barres H4 ont `slope_pct < -1,5%` et **80,7-84,3% d'entre elles sont RANGE_NEUTRE** (100,0% en Hebdomadaire sur BTC/ETH/BNB). Verrouillé par `test_ut2.py::test_no_bearish_regime_exists_so_reversal_vs_continuation_is_undetectable` (vérité terrain synthétique, contrôle positif : 403 barres TENDANCE en dérive +0,4%/barre, **0** sur le miroir exact −0,4% ; non-vacuité du test vérifiée par contrôle négatif). Blast radius d'un régime baissier : **134 sites de comparaison de chaînes `regime` hors tests, dans 22 fichiers de production** (30 fichiers `code/*.py` en comptant les 8 fichiers de tests concernés), dont `d1_not_range` de `faithful.py` (`not in ("RANGE_NEUTRE","RANGE_TENDANCIEL")`) qui laisserait PASSER un label inconnu et ouvrirait silencieusement des trades aujourd'hui bloqués.
  - **Blocage 2 — c'est la DÉFINITION, pas la règle, qui décide du résultat** (même schéma que le niveau de contexte de Conflit MTF, 89% vs 12-19%). 4 définitions candidates construites sur des briques déjà en place, mesurées sur `faithful.py`/MODERE : `regime_W != TENDANCE` = **no-op exact** (SOL 192 trades / 18,30%, bit-à-bit la référence) ; `regime_d1 != TENDANCE` = BTC 404 / 18,10% ; `close_H4 <= EMA55` (le propre filtre de tendance de fond de `proxy_v2.structure_favorable`) = **quasi-suppression du gate** (encore actif sur 17 à 45 barres seulement sur 1051-1408 candidates ; SOL 357 / 50,40%, soit le résultat du gate retiré) ; détecteur baissier D1 inventé = BTC 470 / 18,70%. Référence : BTC 256 / 15,60%, SOL 192 / 18,30% ; gate retiré : BTC 517 / 27,80%, SOL 370 / 50,40%.
  - **Blocage 3 — la population "renversement" est déjà VIDE par construction, dans les deux moteurs.** RANGE : le gate Conflit MTF (source #5, *"l'erreur numéro un"*, mieux établi) exige `regime_d1` hors range, et **100%** des barres qui combinent contexte D1 baissier (détecteur miroir) + signal long H4 + Conflit MTF passé sont en `regime_d1 == EXCES` — l'état que le manuel interdit de trader (§1) — pour seulement 4,4-14,9% des candidates, contre 65,3-75,6% en `regime_d1 == TENDANCE` (continuations par toute lecture). TENDANCE : `trend_table.py` n'a qu'un seul chemin d'ouverture, `accumulation_active = regime_v[i-1] == "TENDANCE" and ...` — toute campagne d'accumulation y est une continuation par construction, le cas "Revers" (1/3 des cas selon #10) n'y est jamais atteignable.
  - **Blocage 4 — la justification que la source donne à l'exemption est FALSIFIÉE par nos briques.** Ligne 9 affirme qu'une continuation est *"déjà alignée avec le TF supérieur"* : mesuré, **32,2-42,1%** des candidates de continuation (`regime_d1 == TENDANCE`) ne sont PAS alignées Hebdomadaire (355 BTC / 387 ETH / 336 BNB / 222 SOL barres). Et le gate n'est pas "rarement actif" : il retire **36,1-48,8%** des candidates restantes et divise par ~2 le nombre de trades.
  - **Signal d'alarme, dit honnêtement** : **toutes** les variantes "renversement seulement" AUGMENTENT le rendement backtesté (BTC 15,60% → 18,10-28,70% ; SOL 18,30% → 18,30-50,40%). Un item dont le seul effet mesurable est de relâcher une règle prudente et d'améliorer le backtest, avec une amplitude fixée par une définition non tranchée, est exactement la tentation que ce projet refuse. Point de droit : le corpus n'INTERDIT pas de vérifier l'UT+2 sur une continuation, il dit que ce n'est *"pas nécessaire"* — l'application uniforme est donc de la prudence EN TROP, pas une infidélité, et elle suit le défaut déjà arbitré pour Conflit MTF (*"appliqué uniformément aux deux — lecture la plus conservatrice"*).
  - **Nouveau candidat catégorie C trouvé en chemin, dans le sens INVERSE de l'item** : le gate UT+2 teste `score >= 2` (proxy momentum/cycle/structure), pas `regime == "TENDANCE"`, alors que #9 dit *"une tendance **confirmée** sur la 3ème unité de temps"* et #10 *"vérifier la **tendance** sur une UT située deux degrés au-dessus"*. Mesuré : parmi les barres qui passent aujourd'hui le gate, l'Hebdomadaire est en `RANGE_NEUTRE` dans 77,5% (BTC 618/797), 85,5% (ETH), 65,0% (BNB), 85,4% (SOL) des cas — une lecture littérale "régime Hebdo = TENDANCE" serait donc BEAUCOUP plus stricte que le gate actuel (jusqu'à ~18× moins de barres sur ETH).
  - Aucun changement de comportement, aucun CSV régénéré (`phase2_ut2_results.csv` rejoué et vérifié **bit-à-bit identique**) ; suite complète **163/163 avant → 164/164 après** (le +1 est le test de prémisse). Modifications de code : bloc documentaire en tête de `code/backtest_phase2_ut2.py` (zéro comportement) + le test dans `code/test_ut2.py` — même discipline que la note Red Flags de `position_engine.py` (9e round) et la note H7 de `trend_table.py` (6e round).
- ~~**"Variante 2 — 3ème borne squeezée"** (entrée alternative à 50% de l'amplitude sans retracement préalable, `TRADING_LESSONS_PYRAMIDALISATION.md`) — mécanisme d'entrée alternatif absent de `trend_table.py`.~~ — **IMPLÉMENTÉE à la 18e application, mesurée, effet QUASI NUL rapporté tel quel ; une prémisse de l'énoncé était fausse (le foyer n'est pas `trend_table.py`).** Citation vérifiée mot pour mot (`TRADING_LESSONS_PYRAMIDALISATION.md:24`) et cette fois **EXACTE** — contrairement aux 7e/8e/11e rounds, rien à corriger sur la citation. **Prémisse fausse corrigée** : la Variante 1, dont la Variante 2 est l'alternative, vit dans `position_engine.py` et nulle part ailleurs — ses deux constituants nommés y sont (*"maturation de la **3ème borne**"* = `n_borders >= MIN_BORDERS(3)` ; *"validation au **ratio 1:1** (report de l'amplitude du range)"* = `val_px = entry + local_range`), et **aucun des deux n'existe dans `trend_table.py`**, qui n'a AUCUNE projection d'amplitude 1:1 (`local_range` n'y sert qu'au gate d'espace libre H15) — même constat qu'au 12e round pour la règle d'or de la même source, revérifié indépendamment. Le foyer est donc la table RANGE, comme "+Reverse" (§3) avant elle. **Les 4 motifs de refus examinés avant de coder, aucun ne tient** : (a) citation exacte ; (d) le plus proche candidat, l'étape Pull-Back de la table TENDANCE (renfort 23-38% + recovery 50%), est une autre table, un autre niveau, conditionné à une Divergence, et **mesuré comme jamais exercé** (les profils à `pullback_frac > 0` n'atteignent jamais le Breakout ; le seul qui y arrive, FAIBLE, a `pullback_frac = 0.00`) — côté RANGE, aucun chemin d'entrée par ordre en attente n'existait, les 19 appelants entrent tous à `o[i]` ; (c) le mécanisme est strictement ADDITIF (rien de retiré, `mature` reste exigé pour l'entrée standard) ; (b) **une seule** grandeur est libre — la durée de vie de l'ordre — tout le reste étant cité (50%, ratio 1:1, seuil 23% de `RULES_EXTRACTION.md` §1/#13:27 déjà codé en `fibonacci.FAVORABLE_MIN`) ou défini par le corpus (*"dernier support significatif"* ← #16:30 *"sous le dernier creux structurel"*, #10:38) ; c'est exactement la situation de H-Reverse-Range, où le projet a implémenté quand même avec une hypothèse explicite. **Implémenté** : `compute_squeezed_third_border` (pur, causal, ne recalcule aucun indicateur) + branche "Limite Achat" de `make_open_tranche_fn`, toutes deux inertes par défaut, 8 hypothèses numérotées H-Squeeze-1..8 en tête de `code/position_engine.py`, paramètre `use_squeezed_third_border` dans `code/backtest_phase2_v7.py`, banc de mesure dédié `code/backtest_phase2_v7_squeeze.py` (modèle exact de `backtest_phase2_v7_reverse.py`), **9 nouveaux tests à vérité terrain calculée à la main** (`code/test_position_engine.py`, 13 → 22). **Deux conceptions écartées par la MESURE avant d'être codées** : l'origine du mouvement prise sur la fenêtre glissante 5 jours (tautologique — `local_range` EST le max-min de cette fenêtre) et le retracement normalisé par l'avance courante (0 déclenchement sur 4/4 actifs, artefact : la barre d'origine donne trivialement 1,0) ; **un défaut réel de la première implémentation trouvé en écrivant le test à vérité terrain** (la barre du creux comptait sa propre amplitude comme un retracement fantôme), corrigé avant toute mesure publiée (effet : BTC/ETH/BNB inchangés, SOL 11 → 14 barres armées). **Résultat chiffré, honnête** (`code/phase2_v7_squeeze_results.csv`, 4 actifs × 4 profils × durées de vie 10/30/90) : **4/16 configurations modifiées — les 4 profils de SOL uniquement** (retour 23,0→23,1 / 55,0→55,2 / 97,8→98,4 / 196,5→197,4), BTC/ETH/BNB inchangés au chiffre près ; Δ retour moyen **+0,113 pt**, Δ drawdown moyen +0,037 pt, et **−4 trades** au total (la tranche posée par ordre limite occupe un emplacement du pyramidage et décale la séquence — ce n'est pas un pur ajout de trades). **La sensibilité à la seule invention (H-Squeeze-6) est exactement ZÉRO** : 10, 30 et 90 bougies donnent des résultats identiques au chiffre près, le seul ordre qui se remplit se remplissant en moins de 10 bougies. **Diagnostic du pouvoir discriminant** (indispensable, même exigence qu'au 10e round) : sur 4 actifs et ~6 ans, le mécanisme pose **UN** ordre et le remplit **UNE** fois (BTC 2 barres armées/0 ordre, ETH 13/0, BNB 6/0, SOL 14/1) — deux causes mesurées : la configuration est rarissime (0,01 à 0,11 % des barres, cohérent avec une source qui la présente comme exceptionnelle), et surtout **quand elle survient le moteur est déjà investi à fond** (6/6 des barres armées de BNB, 12/13 d'ETH, 1/2 de BTC tombent alors que les 3 tranches sont ouvertes, donc la factory n'est même pas appelée) : le manque à gagner que la Variante 2 répare chez un opérateur humain — avoir raté l'entrée — est un état qu'un moteur qui entre au marché à chaque signal éligible n'atteint presque jamais. Conforme au principe de tête de ce document : ce résultat quasi nul **ne remet pas en cause l'implémentation**. Défaut `False` par motif de MÉTHODE et non de performance (l'effet mesuré est légèrement POSITIF, donc la désactivation ne peut pas être un arbitrage de performance déguisé) : H-Squeeze-6 reste un paramètre libre (statut de H-Reverse-Range, lui aussi laissé off) et la série historique de v7 doit rester comparable. **Non-régression** : les 16 lignes `off` sont rigoureusement identiques (écart **0** sur 5 métriques × 16 configs) à `phase2_v7_mtf_results.csv` ; suite **164/164 → 173/173**. **Reste ouvert, dit plutôt que tu** : l'activation dans `backtest_phase2_faithful.py` (moteur opérationnel) n'est pas tranchée — elle exige sa propre passe de non-régression. Détail complet : `PLAN.md` section "18e application".
- ~~**Altérations de structure Vague 1/Vague 5** (Overlap, stratégie "Rivière", prises de profit partielles Vague 5, `TRADING_LESSONS_STRUCTURES_ALTERATIONS.md`) — cadre de type Elliott non repris, ne correspond pas au modèle 5 étapes déjà en place.~~ — **INVESTIGUÉES (14e application, cf. `PLAN.md`), PAS implémentées, item FERMÉ. La 2e moitié de la formulation ci-dessus était FAUSSE, corrigée ci-dessous.**
  - **Correction de la prémisse — la source DÉCRIT le modèle 5 étapes, elle ne lui fait pas concurrence.** Lignes 5-11 vérifiées mot pour mot : *"« La Maison de la Tendance » — modèle canonique (**Vague 3 étendue = la norme**)"*, *"5 briques constitutives"*, puis **Accumulation / Breakout / Divergence / Pullback / Excès Final** — nom pour nom et dans le même ordre que `RULES_EXTRACTION.md` §4 (*"5 étapes : Accumulation → Breakout → Divergence → Pull-Back → Excès final"*) et §1:23, donc exactement ce que `trend_table.py` implémente. La "Vague 3 étendue" est le nom que #14 donne au cas canonique DÉJÀ codé ; Vague 1 / Vague 5 sont deux ALTÉRATIONS du même modèle. **Le mot "Elliott" a 0 occurrence dans tout le corpus** (17 sources + manuel) — sa seule occurrence du dépôt était cette ligne : une glose du projet, pas le vocabulaire de Philippe, donc pas un motif d'écartement recevable. Autres contenus de #14 déjà couverts : l.13 (*"min. 3 bornes testées"*) = `MIN_BORDERS=3` (source déjà citée par `min_borders_sensitivity.py`) ; l.17 (*"Loi de l'Unité de Temps Supérieure"*) = famille MTF déjà implémentée ; l.21-23 = note de calibration, pas une règle.
  - **Overlap (l.28) — DÉJÀ IMPLÉMENTÉ sous un autre nom, et mesurément ACTIF.** *"les mèches peuvent pénétrer l'ancien territoire, mais les clôtures de bougies doivent rester à l'extérieur pour valider la structure"* = exactement la convention de `breakout_raw` (`c[i-1] > local_high_v[i-1]`) et `excess_raw` (`c[i] > ctx_high_v[i-1]`) : niveau = extrême (max glissant des HAUTS), clôture = test, mèche inopérante. **Mesuré** (BTC/ETH/BNB/SOL H4) : valider sur la mèche ferait passer les cassures de **307/287/296/253 à 453/438/465/374** — la clause "clôtures" rejette **32,2-36,3%** des candidates Breakout et **43,5-46,7%** de celles de l'Excès final. Verrouillé par `test_trend_table.py::test_overlap_convention_already_in_breakout_raw` (contrôles positif ET négatif, non-vacuité vérifiée en rejouant contre une variante "mèche" du moteur). Seule lecture non couverte, la validité CONTINUE : écartée car (a) la source ne dit pas quoi faire à la violation (*"pour valider la structure"* qualifie une lecture graphique, pas un ordre) et (b) **mesuré** — le niveau cassé est à **−0,11 à −0,13 R** de l'entrée, **0,0%** au-dessus du break-even (l'hypothèse de départ "resserrement au-dessus du BE" était fausse), touché avant le stop réel dans **85,8-90,9%** des cassures en **4 à 7,5 bougies** : un stop ~8× plus serré que le stop littéral du corpus (#12:7, #16:28, #10:38), donc sacrifier un élément mieux établi.
  - **"Rivière" (l.30) — objet réellement nouveau, mais déclencheur absent du corpus.** *"le point de retournement (« River ») se cherche au contact de la **ligne de tendance reliant les deux sommets précédents**"* — 1 seule occurrence dans tout le corpus. Constructible sans rien inventer (`compute_swing_high_confirmed`/`SWING_ORDER`, comme `manual_trend_channel.py` H1 pour les creux) : **couverture 99,8-99,9%**, et bien distinct des références hautes existantes (écart médian **2,9-5,4%** vs `ctx_resistance`, **3,7-7,2%** vs la résistance du canal manuel, **4,5-9,3%** vs `ctx_high` ; distinct aussi d'`andrews_pitchfork.py`, qui part de pivots ALTERNÉS). Bloqué par l'action : le corpus dit où le retournement *"se cherche"*, jamais ce qui le confirme — et le contact seul touche **30,1-32,1%** des bougies, près d'une sur trois. Restrictions propres à la source : *"Pour les plus expérimentés"*, portée limitée à la Vague 1 étendue en "double excès". Le reste de la l.30 est déjà couvert (*"attendre le pullback technique"* = `PULLBACK_WATCH`).
  - **Prises de profit partielles Vague 5 (l.37) — mécanisme déjà couvert, objectifs non définissables.** Le mécanisme (sortie par fractions) vient d'une autorité plus haute : tables §4 (`div_close_frac`) et §3 (`val_close_frac`/`conf_close_frac`). Ce qui diffère est le déclencheur — #14 propose des OBJECTIFS DE PRIX là où le manuel prescrit des ÉTAPES DE STRUCTURE — et les 3 objectifs exigent **4 paramètres inventés** : *"extensions de Fibonacci"* (**0 ratio d'extension dans tout le corpus** — grep 127/138/161/261 sur 17 sources + manuel ; tous les nombres Fibonacci du corpus sont des RETRACEMENTS), *"niveaux de résistance équivalents"* (expression unique, jamais définie), *"chiffres ronds"* (granularité absente), et la fraction par objectif (absente). **Mesure qui va CONTRE la thèse commode, rapportée quand même** : la Vague 5 n'est PAS déjà exclue par le régime EXCES — sur le centile supérieur de rendement à 30 bougies H4, seulement **40,6-55,0%** sont EXCES contre **35,7-51,0%** en TENDANCE donc tradables (base : 17,1-19,6% d'EXCES toutes bougies) ; c'est l'absence de définitions qui tranche, pas la redondance. Blocage amont indépendant : l.54 exige d'*"identifier d'abord la structure, déterminer le type d'extension"* et l.39 de distinguer Vague 5 et Bulle — `regime_classifier.add_regime` ne porte pas cette distinction (EXCES = canal trop large OU trop étroit) et le corpus ne la rend nulle part calculable.
  - **Deux items nouveaux ajoutés à la catégorie C** : (1) la **ligne "River"** (2 derniers sommets confirmés) est le seul objet de structure du corpus jamais construit par le projet, et il est constructible avec les primitives existantes — à rouvrir si une source fournit le critère de confirmation manquant ; (2) **`fibonacci.py` ne couvre que les RETRACEMENTS** alors que #14:37 réclame des EXTENSIONS pour les sorties — lacune réelle, aujourd'hui non comblable (0 ratio dans le corpus), à consigner comme telle plutôt que sous un "Fibonacci = fait".
  - Aucun changement de comportement, **aucun CSV régénéré** (rien ne change numériquement). Suite complète **164/164 avant → 166/166 après**, mais le +2 n'est pas entièrement de ce round : +1 est le test ci-dessus, +1 provient d'une session concurrente ayant écrit dans le même checkout (`test_range_limit_is_not_independent_of_the_broken_level`, non revendiqué ici) — contribution nette **164 → 165**. Modifications de code : un bloc documentaire en tête de `code/trend_table.py` (zéro ligne exécutable) + le test dans `code/test_trend_table.py`.
- ~~**"Cluster technique" de confirmation du breakout** (convergence de plusieurs structures — trendlines, bornes, limites de range — `TRADING_LESSONS_BREAKOUT_RATIO11.md`) — **à ne pas confondre** avec le "Cluster Technique" (MA20+support) de la source #16 déjà implémenté dans `code/cluster_technique.py` pour la diversification : même terme, deux règles distinctes, seule la seconde est codée. `trend_table.py::breakout_raw` ne teste qu'un niveau isolé.~~ — **INVESTIGUÉE (15e application, cf. `PLAN.md`), PAS implémentée, item FERMÉ et RETIRÉ de la catégorie C. L'énoncé ci-dessus est EXACT sur ses deux affirmations vérifiables** (les deux règles sont bien distinctes ; `breakout_raw` teste bien un seul niveau) — ce qui bloque est ailleurs, et la relecture a élargi la confusion de deux à **trois** acceptions.
  - **Citation exacte, vérifiée mot pour mot (`TRADING_LESSONS_BREAKOUT_RATIO11.md` ligne 29, sous le titre *"Critères de maturité avant breakout"* — donc un critère d'éligibilité AVANT la cassure, pas une "confirmation" après, nuance de l'énoncé corrigée ici)** : *"- **Cluster technique** : le breakout ne se traite pas sur un niveau isolé, mais sur la convergence de plusieurs informations (trendlines majeures, bornes de canaux de contexte, limites de range) en une même zone"*.
  - **Distinction avec #16, confirmée par relecture intégrale des deux sources** : #16 (`CLUSTERS_PRIX.md:19-25`) définit son "Cluster Technique" par **MA20 + Zone de Demande + momentum optionnel**, *"impérativement [dans] un flux tendanciel établi (**post-breakout**)"* — entrée en mean-reversion APRÈS la cassure ; #12:29 vise **trendlines + bornes de canaux + limites de range**, AVANT la cassure. Un seul composant se recouvre partiellement. #12 ne cite jamais la MA20, #16 ni trendlines ni limites de range.
  - **TROISIÈME acception, trouvée en relisant (vrai apport de ce round)** : dans **#10:25** (*"Retracement dans un cluster **38-50%**"*) et **#13:48** (*"Cluster Fibonacci | Entre 23% et 38%"*), "cluster" désigne une **BANDE de retracement, pas une convergence du tout** — et celle-là est **déjà implémentée** (`fibonacci.py`, `trend_table.py::ACCUM_RETRACEMENT_LOW/HIGH`). Même famille de piège que les trois "MA20" documentées au 9e round. Voisin utile dans le sens de #12 : **#5:66** (*"Zone spéculative : prix dans le cluster (Fibonacci + zone graphique) ?"*) est déjà, de fait, ce qu'exige `try_open_campaign` à l'Accumulation (retracement 38-61% **ET** rejet du canal, même bougie) — le PRINCIPE de confluence n'est donc pas absent du projet, `cluster_technique.py` le nomme lui-même dans son H2 (*"confluence de deux techniques au même endroit"*).
  - **Blocage 1 — deux paramètres à inventer, zéro chiffre dans le corpus.** *"plusieurs"* ne dit pas combien, *"en une même zone"* ne dit pas à quelle distance. Grep exhaustif des 17 sources + du manuel sur `convergen`/`confluen`/`cluster`/`trendline`/`tolérance`/`proximité`/`distance` : aucun chiffre. **`RULES_EXTRACTION.md` — l'autorité la plus haute — ne contient AUCUNE occurrence** de convergence/confluence/cluster/trendline (0 résultat), et le seul énoncé du corpus sur la largeur d'une zone refuse de la chiffrer (#14:28, *"zone de tolérance, pas une ligne mathématique"*). Motif de refus déjà retenu pour Fibonacci RANGE §1 et le Red Flag 2.
  - **Blocage 2 — c'est la DÉFINITION, pas la règle, qui décide du résultat** (même schéma que Conflit MTF 89% vs 12-19%). Sur les **264-312 bougies candidates au Breakout** (BTC/ETH/BNB/SOL H4), avec les 3 familles citées et déjà calculées par le projet (`ctx_high`, `ctx_resistance` H14, `channel_resistance` de `manual_trend_channel.py`), la part acceptée va de **0,0-0,7 %** (N ≥ 3, tolérance 0,25 %) à **98,9-100 %** (N ≥ 1, 5 %) ; en tolérance k × ATR, de **1,0-4,2 %** (N ≥ 3, 0,5×ATR) à **99,6-100 %** (N ≥ 1, 2×ATR). Tout l'espace des résultats, sans argument textuel pour choisir.
  - **Blocage 3 — une des trois familles n'est pas indépendante du niveau cassé.** `local_high` et `ctx_high` sont deux maxima de la MÊME série de hauts sur fenêtres **emboîtées** (5D ⊂ 15D) : mesuré, `ctx_high >= local_high` sur **100,0 %** des candidates et **exactement égal sur 51,9-56,9 %** (distance médiane **0,00 %**, contre 1,52-2,85 % pour `ctx_resistance` et 1,84-3,29 % pour `channel_resistance`). La compter comme structure convergente reviendrait à compter le niveau de référence avec lui-même. Verrouillé par `test_trend_table.py::test_range_limit_is_not_independent_of_the_broken_level` (vérité terrain synthétique, contrôle positif ; non-vacuité vérifiée par contrôle négatif). Le 10e round avait déjà écarté `ctx_high` pour H14 pour une raison voisine.
  - **Blocage 4 — implémenter sacrifierait un élément MIEUX établi** (même arbitrage qu'au 7e round). Mesuré via le hook existant `use_breakout_space_gate` (scratchpad, aucun fichier de production modifié) : le temps en POST_BREAKOUT de **BTC/FAIBLE — la seule instanciation empirique substantielle des étapes 2-5 de `RULES_EXTRACTION.md` §4** — tombe de **48,9 % à 0,0 %** pour tous les réglages testés sauf les deux plus permissifs (N ≥ 1 et N ≥ 2 à 2×ATR) ; N ≥ 1 à 1×ATR le détruit déjà. La tolérance inventée décide donc seule si la table §4 du manuel existe encore empiriquement. **Honnêteté** : échantillon très mince (8 Breakouts, un seul profil) — cette raison renforce les trois précédentes, elle ne les remplace pas.
  - **Nuance relevée et écartée comme non décisive** : la ligne 29 est une **puce de synthèse du preneur de notes**, pas une phrase entre guillemets attribuée à Philippe (contrairement aux l.11/17/21/24/38 de la même source, dont la l.17 implémentée au 10e round). Ce n'est PAS un motif de refus et ça n'a joué aucun rôle dans la décision — consigné parce que la moindre précision de la formulation explique l'absence totale de paramètre.
  - **Condition explicite de réouverture** : une citation donnant SOIT un nombre de structures, SOIT une tolérance de distance — ou une reformulation en un prédicat qui n'exige ni l'un ni l'autre. **Nouveau candidat catégorie C trouvé en chemin** : la **checklist pré-trade de #5 (lignes 63-70)** est classée en bloc depuis le début comme "garde-fous Phase 4, hors périmètre code", alors qu'au moins 3 de ses 7 postes sont depuis devenus du code (Conflit MTF, stop dimensionné sur le canal, cluster Fibonacci) — à réauditer poste par poste plutôt qu'en bloc.
  - Aucun changement de comportement, **aucun CSV régénéré** (`phase2_trend_table_results.csv` rejoué en entier et vérifié **identique bit-à-bit**) ; suite complète **164/164 avant → 166/166 après**, dont **+1 seulement de ce round** (le +1 restant vient d'une session concurrente sur un autre item, cf. `PLAN.md`). Modifications de code : deux blocs documentaires (`code/trend_table.py` à côté des H13-H17 de la même source, `code/cluster_technique.py` là où l'homonymie se lit), **zéro ligne exécutable**, plus le test ci-dessus.
- ~~**Red Flags d'invalidation précoce** (clôture sous MA20, retracement maintenu &gt;61%, retour au contexte opposé, `TRADING_LESSONS_ZONE_ACCUMULATION.md`) — pas de mécanisme distinct du stop de protection standard.~~ — **INVESTIGUÉS (9e application, cf. `PLAN.md`), PAS implémentés, décision prise et documentée. La formulation ci-dessus était doublement inexacte, corrigée ci-dessous.**
  - **Citation exacte, vérifiée (`TRADING_LESSONS_ZONE_ACCUMULATION.md` lignes 44-47, exactement 3 puces)** : *"## Signaux d'invalidation précoce (« Red Flags »)"* / *"- Clôtures **du contexte** sous la MA20"* (le "du contexte" avait été perdu dans la citation ci-dessus — il est pourtant load-bearing, c'est lui qui désigne l'unité de temps) / *"- Retracement profond : clôture maintenue au-delà de 61% de la structure"* / *"- Retour au contexte opposé : atteinte de la borne extérieure opposée du range initial"*.
  - **Prémisse fausse, corrigée** : *"pas de mécanisme distinct du stop de protection standard"* est INEXACT. `position_engine.py::process_tranche` a DEUX voies de sortie distinctes : (2) l'Invalidation/stop sur la mèche, et (3) la **sortie "flip de signal" tant que ni Validation ni Confirmation ne sont atteintes** — littéralement un mécanisme d'invalidation PRÉCOCE, déjà le canal par lequel les règles d'abstention littérales du corpus (EXCES-H4, gate UT+2 Hebdomadaire, Conflit MTF D1) ferment une tranche déjà ouverte (comportement déjà documenté dans `backtest_phase2_faithful.py`). Mesuré sur les 4 388 trades de `faithful.py` (BTC/ETH/BNB/SOL × 4 profils, historique complet) : **FLIP 93,8% / LIMITE 3,8% / STOP 2,4%** des sorties — l'invalidation précoce est déjà la voie DOMINANTE, pas une lacune.
  - **RF3 (retour au contexte opposé) = doublon bit-à-bit du stop, prouvé NON précoce.** C'est la même règle que la ligne 28 (*"l'accumulation est invalidée"*) et que le critère de maturité ligne 26 du MÊME fichier — une règle énoncée trois fois, pas trois règles. Le stop RANGE est déjà à ce niveau exact (`min(ctx_support_d1[j], entry*0.999)`, mèche) ; mesuré : la branche `entry*0.999` ne mord jamais (0/4 388), le niveau est atteint par 24 trades (0,5%) et dans **24/24 cas la bougie de déclenchement EST la bougie de sortie** — **0/4 388 trade sortirait plus tôt**. La seule variante réellement précoce (`ctx_support` relu en direct, 1,0% des trades, avance médiane 96 bougies) est un stop suiveur sur la bande de contexte, ce qui contredit la règle la mieux étayée du corpus (interdiction de resserrer avant la Confirmation, sources #12/#13/#15/#16 nommées, #16 s'annonçant lui-même comme la « 5ᵉ/6ᵉ confirmation »).
  - **RF2 (>61% maintenu) = DÉJÀ implémenté deux fois, comme plafond d'ENTRÉE** : `trend_table.py::ACCUM_RETRACEMENT_HIGH = 0.61` et `fibonacci.py::FAVORABLE_MAX = 0.618` (dont le commentaire cite déjà ce Red Flag nommément). En faire aussi une SORTIE serait un troisième usage du même chiffre — le piège déjà documenté pour le 76% ("seuil d'ENTRÉE ici, cible de SORTIE ailleurs"), en pire : le corpus utilise 61% comme **minimum** d'entrée dans trois autres passages (`RULES_EXTRACTION.md` §1 "Range tendanciel ≥61,8%" ; #5 "entrée agressive possible dès 61%" ; #11 "zone d'intervention à Fibonacci 61,8%") — même chiffre, inégalité inverse. Et *"maintenue"* n'est chiffré nulle part (paramètre à inventer, motif de refus déjà retenu pour Fibonacci RANGE §1). Mesuré : déclencherait avant la sortie réelle sur 8,0% (1 clôture) / 5,5% (3 clôtures) des trades.
  - **RF1 (clôtures du contexte sous la MA20) = le seul non redondant, et le vrai apport de ce round : il a une 2e source plus précise, jamais reliée jusqu'ici** — `TRADING_LESSONS_CLUSTERS_PRIX.md:50` (#16), section *"« Règle de Trois » (invalidation statistique)"* : *"**Clôtures sous MA20** : clôtures multiples et marginales sous la moyenne mobile signalent qu'elle n'exerce plus son rôle d'attractivité"*. Bloqué par 4 obstacles : (1) **la conséquence n'est pas une sortie** — les deux puces voisines de la même section de #16 énoncent la leur ("prudence maximale", **déjà implémentée** en `RULE3_STREAK`/`RULE3_SIZE_MULT` ; *"arrêt des nouveaux engagements"*), donc abstention/réduction, pas fermeture d'une position ouverte ; (2) *"multiples"* et *"marginales"* = 2 paramètres à inventer, et "multiples" ne mord presque pas (12,7% des trades à 1 clôture vs 10,3% à 3 clôtures consécutives) ; (3) **les 2 sources placent la MA20 sur des UT différentes** — #10 dit *"clôtures du **contexte**"* (UT supérieure), le tableau de #16 qui introduit ce MA20 a pour *"Unité de Temps de Trading | Horaire (H1)"* (UT d'exécution, ce que `cluster_technique.py` a déjà tranché pour son propre usage) ; (4) **piège terminologique identique à celui déjà documenté pour "Cluster technique"** — le projet a DÉJÀ trois "moyennes 20 périodes" distinctes : `cluster_technique.py::compute_ma20_rebound` (MA20 de PRIX, UT d'exécution, ENTRÉE, clôture AU-DESSUS), `trend_table.py` H9 (MA20 de **VOLUME**, breakout) et celle-ci (MA20 de prix, sur le CONTEXTE, clôture EN DESSOUS, invalidation). Impact mesuré s'il était quand même codé comme sortie : avant la sortie réelle sur 8,3% (1 clôture) / 6,9% (3 clôtures) des trades, avance médiane **6 bougies H4** seulement (il anticipe surtout un flip déjà en route) ; mais sur les 104 trades qui atteignent réellement le stop, il sortirait avant le stop dans 65% / 54% des cas, avance médiane 103 à 214 bougies — donc pas un no-op, un vrai changement de comportement piloté par des paramètres inventés.
  - **Nouveau candidat catégorie C trouvé en chemin** : *puce 3 de la "Règle de Trois" de #16* — *"toute désynchronisation entre patterns ou cycles (Trend Sign Wave) doit entraîner un arrêt des nouveaux engagements"*. Sur les 3 puces de cette section, seule la puce 1 est implémentée ; la puce 3 est une règle d'abstention littérale jamais reliée à `proxy_v2.py`, où un désaccord entre `momentum_favorable`/`cycle_favorable`/`structure_favorable` laisse encore passer `score >= 2` — donc n'arrête rien aujourd'hui.
  - Aucun changement de comportement, aucun CSV régénéré (rien n'a changé numériquement), suite complète **157/157** avant et après. Seule modification de code : un bloc de commentaires en tête de `code/position_engine.py` (zéro comportement), même discipline que la note H7 ajoutée dans `trend_table.py` au 6e round.
- ~~**Sizing 1-1,5% + cible R:R 1:5** pour les trades d'accumulation/renversement (même source) — le projet utilise le sizing générique par profil (1/2/3/5%), jamais ce couple sizing/R:R spécifique à ce type de trade précis.~~ — **INVESTIGUÉ (17e application, cf. `PLAN.md`), PAS implémenté, item FERMÉ. Trois prémisses de la formulation ci-dessus étaient fausses, corrigées ci-dessous.**
  - **Citations exactes, vérifiées mot pour mot** (`TRADING_LESSONS_ZONE_ACCUMULATION.md`, #10 — **seule** source du corpus à porter ces deux nombres ; grep exhaustif "1:5", "1,5 %", "R:R", "sizing", "taille de position" sur les 17 sources + `RULES_EXTRACTION.md`) : `:36` *"**Taux de réussite moyen accepté : 50%** (nombreux trades clôturés à Break-Even) en échange d'un **ratio R:R élevé, cible moyenne 1:5**"* ; `:37` *"Taille de position en conséquence : **1 à 1,5% du capital** (modérée, car l'espérance de gain est déjà élevée grâce au R:R)"* ; `:50` *"Tenir une position à Break-Even ... pour capturer les tendances majeures qui offrent des ratios **supérieurs à 1:5**."* — **point de statut décisif** : `:36`/`:37` sont des **puces de synthèse** (ni guillemets ni bloc `>`), `:50` est la **seule citation verbatim**, et c'est celle qui dit *"supérieurs à"*.
  - **Prémisse fausse n°1 — la source.** L'item soupçonnait `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` (#5) : **#5 ne contient ni "1:5" ni "1-1,5%"** (vérifié) — son sizing est *"Stop Loss = taille du canal"* + *"/2 si canal très large"*, **déjà implémenté** (`WIDE_CHANNEL_STOP_FRAC`/`WIDE_CHANNEL_SIZE_FRAC`).
  - **Prémisse fausse n°2 — le mot "spécifique".** Aucun chiffre de sizing du corpus n'est scopé par TYPE DE TRADE ; ils le sont par EXPÉRIENCE (`RULES_EXTRACTION.md:64` ≤1% les 6 premiers mois, `:65` gradient expérience × profil psychologique, #15:40-41 0,5%→2%) ou par UNITÉ D'AGRÉGATION (#15:28 max 2% **par position**, #16:40-41 jamais >2% **par zone de prix** et 1%+1%). #10:37 énonce lui-même sa raison, et ce n'est pas le type de trade : *"car l'espérance de gain est déjà élevée grâce au R:R"*.
  - **Prémisse fausse n°3 — le conflit avec le "Ratio 1:1".** Il **n'existe pas**, et le corpus le tranche lui-même : le 1:1 de #12:8-9 est un **report d'amplitude** (`val_px = entry + local_range`), et #15:24 précise *"Le ratio 1:1 n'est pas un objectif de profit mais un point de validation mathématique du trade."* Un point de validation à 1:1 et un ratio de sortie à 1:5 sont compatibles — **ce n'est donc pas le motif du refus**.
  - **Motif réel, moitié sizing — (d) puis (c).** (d) **Déjà couvert sous un autre nom, MESURÉ et non affirmé** : le profil FAIBLE vaut `risk_pct = 0.01`, exactement la borne basse de [1% ; 1,5%] et exactement le ≤1% du manuel — forcer `risk_pct = 0.010` sur les 4 profils de v7 est un **no-op bit-à-bit sur exactement les 4 couples FAIBLE** (écart 0,0 pt, 0 trade). (c) Un taux fixe sacrifierait deux mécanismes du **manuel** : `RULES_EXTRACTION.md:54-59` §4 **donne déjà** le sizing de l'étape Accumulation profil par profil (*"Attente / +25% / +50% / +100%"*), implémenté tel quel en `trend_table.py::PROFILES_TREND.accum_frac` (0.00/0.25/0.50/1.00, correspondance exacte des 4 lignes) — le figer rendrait les 4 profils identiques à cette étape ; et `:65-66` indexe l'agressivité sur l'expérience, la psychologie et le palier de capital (choix utilisateur + `capital_tiers.py`).
  - **Motif réel, moitié R:R — (b) puis (c).** (b) **Le "5" n'est pas transposable** : il se mesure sur le stop de #10:38 (*"largeur moyenne du canal de tendance récent, **ou** sous le point bas de la 4ème borne"* — deux définitions, non tranchées), pas sur l'"Extreme Channel" UT+1 du moteur (#16:28 + #12:7, mieux établi). Mesuré sur les bougies d'entrée réelles (BTC/ETH/BNB/SOL H4, gate MTF v7), médianes : Validation **1,21-1,26 R** (stop H4) vs **0,53-0,58 R** (stop D1) ; Confirmation 1,93-2,10 vs 0,90-0,93 ; **Limite 2,89-3,15 R vs 1,35-1,40 R** — facteur **2,3** selon le stop. (c) `:50`, seule citation verbatim, exige des ratios ***supérieurs* à 1:5** : un take-profit dur à 5 R les rendrait impossibles par construction ; la lecture fidèle (retirer le plafond) supprimerait l'étape *"**Limite** (target atteinte) → TP100%"* de `RULES_EXTRACTION.md:41` et sa table `:43-48`. Enfin #15:30-31 donne un **autre** nombre pour le même type d'énoncé (*"ratio moyen visé 1:4"*) : retenir 5 plutôt que 4 serait arbitraire.
  - **Le processus de #10 est déjà implémenté, lui** : `:39-42` ("Connective Tissue" — Validation → stop réduit ; Confirmation → Break-Even ; *"option gratuite"*) est exactement l'échelle `val_px`/`conf_px`/`conf_to_be` de `process_tranche`. L'item n'ajoutait que les deux **annotations chiffrées** du processus.
  - **Mesures faites quand même** (monkeypatch jeté, aucun moteur modifié ; v7, 4 actifs × 4 profils) : "R:R 1:5" (`lim_px = entry + 5·(entry−stop)`) → **+3,71 pt de moyenne, 12/16 couples AMÉLIORÉS**, 4/16 dégradés, drawdown +0,74 pt (moins profond), −7,2 trades, dispersion ETH/TRES_AGRESSIF −70,9 pt à BTC/TRES_AGRESSIF +50,9 pt ; `risk_pct` à 1,0% → −51,94 pt, 12/16 dégradés, 4/16 identiques (les FAIBLE) ; à 1,5% → −35,61 pt, 12/16 dégradés. ⚠️ **La variante R:R 1:5 est donc mesurée LÉGÈREMENT FAVORABLE** : elle n'est pas écartée pour dégradation (motif interdit) **ni retenue parce qu'elle améliore** — la règle interdit **symétriquement** au Proxy de décider de la fidélité au corpus. Seuls (b) et (c) fondent la décision.
  - **Apport au backlog, sans le refermer** : #10:36 a été testé comme source possible du coefficient `1.5` de `lim_px` (signalé SANS SOURCE au 12e round, littéral dans 7 fichiers). **Réponse : non** — unités différentes (`1.5` multiplie un `context_range`, "1:5" un RISQUE) et conversion instable (**2,38-2,60** sur stop H4 vs **5,38-5,57** sur stop D1). L'item de backlog reste **OUVERT**.
  - Aucun changement de comportement, **aucun CSV régénéré** (rien ne change numériquement) — vérifié : `phase2_v7_mtf_results.csv` recalculé est **bit-à-bit identique** au CSV commité (64 × 9). **Aucun test ajouté** (le bloc est purement documentaire). **Comptage rendu ambigu par des sessions concurrentes écrivant dans le même checkout** — mesuré proprement plutôt que rapporté de mémoire : sur un checkout **HEAD propre** (`git archive HEAD`), la suite est à **164/164** ; en y appliquant **uniquement les deux éditions de ce round** (+150 lignes de commentaire dans `position_engine.py`, +11 dans `trend_table.py`), elle reste à **164/164** — **contribution nette : 0 test ajouté, 0 test cassé, comportement identique**. Le checkout partagé a par ailleurs traversé un état transitoire à **7 échecs** (`test_backtest_phase2_faithful.py` ×6, `test_unified_protocol.py` ×1), tous sur `backtest_phase2_faithful.py` — fichier **jamais touché par ce round**, alors en cours d'édition par une autre session ; non imputables ici, délibérément non "corrigés" pour ne pas écraser le travail en vol d'autrui, et **résolus depuis par cette session** : le checkout partagé termine à **172/172 verts** (ce round + trois sessions concurrentes). Modifications de code de ce round : bloc de commentaire PUR en tête de `code/position_engine.py` + note documentaire adossée à `PROFILES_TREND` dans `code/trend_table.py` (zéro ligne exécutable dans les deux cas).
- ~~**"Hiérarchie UT Supérieure = Le Potentiel"** (vérifier la marge de progression disponible avant d'engager un objectif, `TRADING_LESSONS_PULLBACK_MATURITE.md`) — non implémenté.~~ — **COUVERT par la contrainte "espace libre" MTF implémentée au 10e round** (cf. item correspondant ci-dessus) : relecture faite ce round, c'est le MÊME enseignement que la contrainte de la source #12, formulé plus brièvement (`TRADING_LESSONS_PULLBACK_MATURITE.md` l.14, *"vérifier qu'il reste assez de 'jus' (marge de progression) pour un nouvel objectif"*) — il était listé ici comme un item distinct par erreur de classification, pas parce qu'il décrivait un mécanisme différent. Sa présence en 2 sources indépendantes a au contraire servi d'argument POUR l'implémenter.
- **Nuance "Règle de Non-Existence" (#11) vs Conflit MTF implémenté** : le mécanisme codé (`regime_d1`, un proxy d'état continu par percentiles glissants) capture l'esprit de la source mais pas le déclencheur événementiel précis ("apparition d'une 3ème borne sur le Contexte") ni sa condition de levée exacte ("jusqu'à sortie de la structure de range"). Pas un défaut caché (le choix du niveau D1 reste bien étayé et déjà documenté), mais une équivalence approximative à ne pas confondre avec une implémentation littérale exacte.

**Nouvelle source, 29e application (`docs/GUIDE_STRATEGIE_PRO_INDICATORS.md`, guide officiel
"Guide de Stratégie" PRO-INDICATORS.com, extraction texte de 38 captures d'écran officielles fournies
par l'utilisateur — cf. `PLAN.md`)** : 3 écarts identifiés et VÉRIFIÉS CONTRE LE CODE RÉEL
(`emile/core/range_gates.py`, `emile/core/regime_classifier.py`), pas seulement supposés :

- **Routage RANGE "3ème borne" vs "Neuneu" (`Range/Range.png`)** — le guide décrit DEUX
  structures RANGE distinctes, choisies par une question explicite (*"range précédé d'une
  tendance ? (moyenne hors des contextes = tendance)"* → 3ème borne ; sinon, ou si range ≥4
  bornes, ou Forex UT hebdo → Neuneu), chacune avec sa PROPRE grille de money management.
  **PARTIELLEMENT IMPLÉMENTÉ (32e round)** — la grille "3ème borne" elle-même s'est révélée être
  DÉJÀ le mécanisme §3/§3bis existant (mêmes seuils 76%/61%, même renvoi au PDF pour les fractions
  de clôture) : le vrai travail neuf était la DÉCISION DE ROUTAGE. Construite et mesurée :
  `regime_classifier.py::compute_range_precedes_by_trend` (réutilise le régime H4 déjà classé,
  aucune invention) + `compute_range_border_count` (compte de bornes REMIS À ZÉRO par épisode de
  range — PAS une réutilisation de `n_borders`, dont la médiane glissante perpétuelle vaut 9 sur
  BTC H4 réel, >4 sur 99,7% des bougies : le réutiliser aurait rendu ce routage trivialement vrai
  partout, trouvaille faite AVANT d'écrire le code final) + `compute_use_neuneu`. Exposé dans
  `feat["use_neuneu"]`, mesuré non trivial (~82% des bougies RANGE routeraient vers Neuneu sur les
  4 actifs — cohérent avec le guide lui-même : "la stratégie à privilégier au moindre doute").
  **PAS ENCORE CONSOMMÉ par aucun moteur** : la grille "3BR" reste appliquée sans condition,
  comportement inchangé, vérifié bit-à-bit. Le mécanisme NEUNEU lui-même (grille de risque
  distincte) reste catégorie C, différé.
  **Diagnostic revu et corrigé (36e round)** — le blocage "stop trailing" du 32e round était
  SURESTIMÉ : la citation exacte (*"SL déplacé au sommet récent"* à la Validation) décrit un
  ajustement PONCTUEL, pas un stop réévalué en continu — structurellement identique au `conf_to_be`
  déjà existant (`process_tranche`), juste déclenché à un autre point et vers un autre niveau (même
  primitive que `swing_high` de `trend_table.py`). **Mais un blocage PLUS FONDAMENTAL, trouvé en
  revérifiant les captures elles-mêmes (pas seulement leur transcription texte), remplace celui-là** :
  Neuneu est en réalité **BIDIRECTIONNEL** — "Repli Neuneu" (`Repli-neuneu.png`, pattern creux→
  rebond→retour fibo 14,6-23,6%) est un LONG classique, compatible avec l'architecture existante ;
  "Borne Neuneu" (`Borne-neuneu.png`, *"Stoploss = au-dessus du plus haut du range"* — cohérent
  seulement pour un SHORT, un stop au-dessus de l'entrée n'ayant aucun sens pour un LONG) est un
  **SHORT**, structure qui n'existe NULLE PART dans `position_engine.py` (la seule jambe short du
  fichier, `H-Reverse-Range`, est explicitement BORNÉE — un seul stop/une seule cible, jamais une
  table à étapes) — construire "Borne Neuneu" fidèlement exigerait une vraie table RANGE short
  symétrique, chantier d'architecture à part entière. **Et "Repli Neuneu" (la moitié long,
  a priori compatible) bute à son tour sur un blocage catégorie A** : aucun chiffre, nulle part
  dans le corpus (grep confirmé sur les 17 sources + `RULES_EXTRACTION.md` : "Neuneu" n'existe que
  dans ce guide), ne définit la "dumb zone" elle-même (le point intermédiaire du pattern) — un
  seuil inventé de toutes pièces, ce que le principe du projet interdit. **Conclusion : toujours
  PAS implémenté, backlog ouvert, décision de conception (Borne Neuneu) + parties non chiffrables
  (Repli Neuneu) à traiter dans un round séparé** — détail complet : `PLAN.md`, section
  "36e application".
- ~~**Invalidation 3BR par SQUEEZE (`Range/3eme-borne/3ème-borne.png`), citation exacte** : *"On ne
  doit plus la trader si jamais le range produit un SQUEEZE... Si le range se forme juste après
  un SQUEEZE sur l'unité de temps supérieure, il faudra alors éviter de trader cette 3BR..."*~~ —
  **IMPLÉMENTÉ (31e round)** pour sa moitié manquante (la 1ère moitié — squeeze sur la PROPRE UT du
  range — était déjà couverte par coïncidence via le régime `EXCES` H4, cf. `range_gate` ligne 48).
  Nouvelle fonction `regime_classifier.py::compute_squeeze` (miroir exact de `compute_wide_channel`)
  distingue "D1 squeezé" de "D1 trop large" — les deux étaient pliés dans le même régime `EXCES` par
  `add_regime`, ce qui empêchait de bloquer spécifiquement sur le squeeze. `range_gates.py::
  range_gate` bloque désormais l'entrée si `feat["squeeze_d1"][i]` est vrai. **H-Squeeze-UT1-1**
  (hypothèse d'opérationnalisation, seule chose que le corpus ne chiffre pas — "juste après" n'a
  pas de fenêtre explicite) : lecture CONTEMPORAINE du squeeze D1 (même jointure causale que
  `regime_d1`, aucun délai supplémentaire inventé — la fenêtre glissante de 250 bougies du
  percentile fait déjà persister l'état squeeze sur plusieurs bougies consécutives). **Résultat
  honnête, mesuré (BTC/ETH/BNB/SOL × 4 profils)** : −17,2 trades en moyenne (gate PAS inerte,
  contrairement à `MIN_BORDERS`), retour moyen −2,78 pt (12/16 combinaisons dégradées, 4/16
  améliorées), drawdown quasi inchangé (+0,06 pt). Implémenté quand même, conformément au principe
  inviolable du projet. La 3ème clause du guide ("retour au niveau de la 1BR, double top/bottom")
  RESTE NON implémentée (aucun suivi du niveau de la 1ère borne dans ce moteur — backlog ouvert,
  pas tentée car ça exigerait une nouvelle primitive de suivi de niveau, pas juste un nouveau gate
  booléen comme celui-ci).
- **Variante d'entrée "Repli sur 3BR squeezée" en contexte TENDANCE/Suivi-de-tendance**
  (`Tendance/Tendance-primaire/Suivi-de-tendance/Repli-sur-3br-Squizee.png`) — même famille que la
  variante déjà implémentée côté RANGE (18e/22e rounds, `compute_squeezed_third_border`), mais ICI
  scopée à la table TENDANCE (pas RANGE) avec un niveau d'entrée alternatif explicite ("sinon au
  niveau du fibo 50%"). **VÉRIFIÉ depuis (30e application, audit indépendant) : ce n'est qu'un
  TIERS du trou réel** — voir item ci-dessous, plus large.

**30e application — audit indépendant demandé par l'utilisateur ("vérifier que nous avons bien
extrait toute la propriété [intellectuelle] et croisé avec le code, voir ce qui est erroné")** :
2 agents adversariaux mobilisés, l'un a rouvert les 38 captures une par une contre
`docs/GUIDE_STRATEGIE_PRO_INDICATORS.md`, l'autre a revérifié le croisement code ligne par ligne.
Corrections déjà appliquées au document d'extraction — retenu ici, ce qui change le diagnostic
du round précédent :

- **Le mécanisme "Suivi de tendance" (§4.3, PAS seulement sa variante squeeze) est ABSENT de
  `trend_table.py` en entier.** L'item ci-dessus (Repli sur 3BR squeezée) ne couvrait qu'une des 3
  branches (Repli à la moyenne / Cassure de 3BR / Repli sur 3BR squeezée), chacune avec sa propre
  grille STOPLOSS/VALIDATION/CONFIRMATION/OBJECTIF et ses 5 conditions d'activation. Le code actuel
  modélise le Breakout comme un simple "Renfort +X%" figé par profil puis saute directement à
  Divergence, sans aucune des 3 branches de ré-entrée par ordre en attente.
  **PARTIELLEMENT IMPLÉMENTÉ (33e round)** : les 5 conditions d'ACTIVATION construites et mesurées
  — `trend_table.py::compute_suivi_conditions` (moyenne haussière + pas de squeeze sur le canal H4,
  H-Suivi-1 : conditions 3/4 du guide traitées comme la même contrainte, le mot "alerte" désignant
  déjà le signal SQUEEZE ailleurs dans le corpus) + `SUIVI_MAX=2`. Mesuré non trivial (~93-94% des
  bougies TENDANCE satisfont ces conditions). Fonction NON appelée par `run_trend_table` — zéro
  changement de comportement.
  **Branche "Cassure de 3BR" IMPLÉMENTÉE (34e round)** — swing bas confirmé arme un ordre virtuel
  stop-achat au plus haut de campagne, remplissage sans bouger le stop (H-Suivi-Cassure3BR-1 :
  réutilise le stop de campagne existant plutôt que de plomber une donnée D1 pour une seule
  branche) ; taille = "risque max 2%" littéral (H-Suivi-Cassure3BR-2, `SUIVI_RISK_PCT`, pas une
  fraction inventée). **Résultat honnête, mesuré : EFFET NUL sur BTC/ETH/BNB/SOL × 4 profils** —
  cause identifiée, pas juste constatée : `stage_time_%["POST_BREAKOUT"]` vaut 0,0% sur 15/16
  combinaisons (campagnes traversant cette étape quasi instantanément avant Divergence) — aucune
  fenêtre réelle pour qu'un pattern de swing bas + cassure ait le temps de se former sur ces
  données. Implémenté quand même (littéral, testé), `use_suivi_de_tendance=False` par défaut.
  **Les 2 autres branches (Repli à la moyenne, Repli sur 3BR squeezée) restent catégorie C,
  différées** — même contrainte de fenêtre temporelle identifiée ci-dessus s'appliquerait
  probablement à elles aussi, quel que soit leur déclencheur propre.
  **Investigation plus poussée (35e round)** — la 16e combinaison (seule à ne PAS être à 0,0% :
  BTC/FAIBLE, 48,9% de temps POST_BREAKOUT) cachait en réalité un **vrai bug** (indépendant de
  "Suivi de tendance"), pas une fenêtre d'opportunité réelle : une campagne à `accum_frac=0.00`
  (profil FAIBLE) peut voir son stop devenir obsolète pendant l'Accumulation (rien ne le surveille
  tant qu'aucun capital n'est engagé) puis, au Breakout, se voir refuser tout remplissage par
  `add_leg` (prix déjà sous ce stop périmé) tout en basculant quand même vers POST_BREAKOUT —
  une campagne "zombie" (`remaining=0`) plus jamais clôturable, bloquant 48,2% de l'historique BTC
  (~7400 bougies H4, la quasi-totalité de la 2e moitié des données) jusqu'à la fin du dataset, 0
  trade. **Corrigé** (`trend_table.py::step_campaign`, transition conditionnée à `remaining>0`
  après l'appel — pas `actual_add>0`, qui casserait le cas légitime d'un plafond H3 déjà saturé par
  l'Accumulation d'un profil AGRESSIF/TRES_AGRESSIF), 2 nouveaux tests de non-régression, mesuré
  avant/après : impact confiné à BTC/FAIBLE/`space_gate=off` (0→2 trades, 0,0%→-1,8%), les 15
  autres combinaisons inchangées, `unified_protocol.py`/`risk_aggregation_triple_system.py` (mêmes
  fonctions réelles réutilisées) bit-à-bit identiques. **Effet sur `use_suivi_de_tendance` lui-même
  inchangé après correctif : toujours 0/16 différence** — le diagnostic "fenêtre quasi nulle" du
  34e round reste la conclusion opérationnelle valable pour ce mécanisme précis, une fois ce bug
  par ailleurs corrigé. Détail complet : `PLAN.md`, section "35e application".
- **Nouvel item trouvé** : les sous-patterns "Trend Follow"/"Exit" en régime Excès (§2.1 du guide,
  grilles de risque complètes, niveau EXPERT) ne sont jamais implémentés — `range_gates.py:48`
  bloque INCONDITIONNELLEMENT toute entrée en régime EXCES. Cohérent avec le choix déjà documenté
  ("Bulle/Excès → NE PAS TRADER"), mais mériterait d'être reconfirmé explicitement plutôt que de
  rester un silence face à ce nouveau contenu. Catégorie C — PAS implémenté.
- **Une conclusion du 29e round était FAUSSE, corrigée** : l'affirmation "confirme les seuils déjà
  codés dans `fibonacci.py`/`regime_classifier.py`" (à propos des seuils de retracement 3BR
  76%/61%) ne résiste pas à la relecture — `regime_classifier.py::add_regime` ne calcule AUCUN
  retracement Fibonacci ; `fibonacci.py::FAVORABLE_MIN/MAX` sert un mécanisme TENDANCE distinct
  (plafond d'invalidation du Pull-Back, pas plancher d'entrée RANGE) qui ne partage que la valeur
  numérique 0,618 par coïncidence ; et `backtest_phase2_faithful.py:299-313` documente LUI-MÊME,
  depuis un round antérieur, qu'aucun gate RANGE par retracement Fibonacci n'est implémenté — cette
  information était déjà dans le projet et aurait dû être croisée avant d'écrire "confirme".
  Détail complet, correction appliquée dans `docs/GUIDE_STRATEGIE_PRO_INDICATORS.md` section 5.
- **Erreurs de transcription/catégorisation corrigées dans le document d'extraction lui-même**
  (pas des écarts corpus↔code, des erreurs de ma propre extraction) : le contenu réel de
  `Bulle-spéculative.png` n'avait jamais été transcrit (un écran voisin, `Screenshot 2026-09-11
  23.20.54.png`, avait été présenté à sa place) ; "3ème conditions" aurait dû être "4 conditions"
  (`3br.png`) ; une citation "stop suiveur" était attribuée au mauvais champ (Objectif, pas
  Confirmation) ; un bloc entier ("Exemples des erreurs classiques", `3br.png`) et les 3 critères
  propres à l'écran `3ème-borne.png` n'étaient pas transcrits ; l'écran d'accueil et le placeholder
  "IMAGE A VENIR" de `Divergence.png` n'étaient pas signalés. Toutes corrigées, mêmes fichiers
  images revérifiés directement (pas la seule parole des agents) avant correction.

Aucun changement de comportement de code apporté par ce round ou le précédent (investigation +
correction documentaire uniquement) — les écarts ci-dessus attendent une décision de conception
dédiée, pas une implémentation improvisée.

- **NOUVEAU (38e round) — "Tendance Multi-timeframe" (trader chaque UT en parallèle avec 2% de
  risque chacun) : DÉTECTION implémentée et mesurée, CONSOMMATION bloquée par un angle mort GÉNÉRAL
  de l'architecture, pas spécifique à ce mécanisme.** Constat direct de l'utilisateur ("Philippe
  utilise sa stratégie pour trader un actif sur les différentes timeframes, nous devons agréger
  toute cette stratégie en une seule") vérifié juste : TOUS les moteurs existants (`unified_
  protocol.py` compris, malgré son nom) exécutent sur H4 SEUL, D1/Hebdomadaire n'étant jamais que
  des contextes/gates — aucune UT supérieure n'est elle-même tradée. Détection construite et
  testée (`trend_table.py::attach_regime_is_tendance`/`compute_multi_timeframe_trend`,
  `MTF_CASCADE_RISK_PCT=0.02`) : incidence réelle mesurée, ni nulle ni omniprésente (BTC 2,96% des
  bougies H4/27 épisodes, ETH 0,29%, BNB 2,34%, SOL 0,80%). **Trouvaille la plus importante** : en
  tentant de mesurer l'exposition résultante (illustration, profil MODERE), `run_trend_table` sur
  D1/Hebdomadaire ouvre 0 trade sur les 4 actifs — diagnostiqué (pas conclu au hasard) :
  `LOCAL_DURATION="5D"`/`CONTEXT_DURATION="15D"` (`backtest_phase2_v7.py`) sont des durées
  CALENDAIRES ABSOLUES, jamais recalibrées par UT depuis leur création — 15 jours = une fraction
  d'une seule bougie Hebdomadaire, `n_borders` ne peut structurellement jamais atteindre
  `MIN_BORDERS=3` à cette échelle (maximum observé : 1). **Ce n'est pas un défaut de "Tendance"
  mais de `prepare()` lui-même**, utilisé par TOUS les moteurs de ce projet — resté invisible
  jusqu'ici car ce projet n'avait jamais exécuté sur une UT autre que H4. **Catégorie C** :
  recalibrer `LOCAL_DURATION`/`CONTEXT_DURATION` par UT est un chantier À PART, plus large que ce
  round, sans valeur donnée par le corpus (inventer un facteur d'échelle serait la même erreur
  que d'inventer un seuil). Détail complet, chiffres exacts : `PLAN.md` section "38e application".
  **LEVÉ (39e round)** : `LOCAL_DURATION`/`CONTEXT_DURATION` acceptent désormais une fenêtre en
  NOMBRE DE BOUGIES (`local_duration=LOCAL_DURATION_H4_BARS=30`/`context_duration=CONTEXT_
  DURATION_H4_BARS=90`, mêmes valeurs que 5D/15D exprimées en bougies H4 — aucune invention),
  strictement additif, comportement inchangé par défaut. Mesuré après correction : `n_borders_
  median` ~9 sur les 3 UT (comparable), `D1_n_trades` n'est PLUS 0 (BTC 3, ETH 1, BNB 1, SOL 3) —
  le mécanisme "Tendance" transfère bien à D1. `Weekly_n_trades` reste à 0 mais pour une raison
  honnête différente (historique trop court en nombre de bougies, 303-367 selon l'actif — pas un
  artefact de calibration). Trouvaille venue d'un second angle : la clarification directe de
  l'utilisateur sur le contexte UT+2 utilisé pour le TP ("Confirmation") a révélé que `conf_px`
  (`position_engine.py`) souffre du MÊME défaut (`context_range` calculé sur H4, jamais sur une
  vraie UT+2) — jamais testé avec la fenêtre en bougies (catégorie C, non fait ce round, cf.
  `PLAN.md` "39e application" pour le détail). Le money-management "2% par UT en parallèle" de la
  Tendance Multi-timeframe reste, lui, catégorie C (sizing non chiffré par le corpus).
  **CÂBLÉ (42e round)**, sur décision directe de l'utilisateur d'accepter la lecture extrapolée
  nécessaire (H-MTF-Cascade-4/5 : gate d'ouverture restreint aux fenêtres de Tendance Multi-
  timeframe + jambe de Breakout dimensionnée par le risque à 2%, jambe d'Accumulation inchangée) —
  `run_trend_table(use_mtf_cascade=True, mtf_cascade_gate=...)`, strictement additif, défaut
  `False`. Mesuré sur BTC/ETH/BNB/SOL × H4/D1/Hebdomadaire (profil MODERE) : effet réel, 8/12
  lignes actif×UT changent — BTC/H4 et BNB/H4 voient moins de trades ET un meilleur retour (gate
  sélectif), ETH/SOL (H4+D1) et BTC/BNB (D1) tombent à 0 trade (le gate, très restrictif — 0,29-
  3,27% des bougies — élimine toute activité pour ces couples). Aucune conclusion de performance
  n'a influencé l'implémentation. Détail complet, tableau chiffré complet : `PLAN.md`
  "42e application".
  **MESURÉ (40e round)** : `conf_px` retesté avec un `context_range` sourcé sur une VRAIE UT+2
  (Hebdomadaire, fenêtre en bougies) — résultat honnête, symétrique et opposé à celui du 16e round.
  Le 16e round avait trouvé les lectures STRUCTURELLES (niveau absolu) collapsant quasi
  immédiatement (98-100% en 1 bougie, TROP TÔT). La lecture AMPLITUDE avec une vraie UT+2 donne
  l'inverse : sur les 200 tranches réelles ayant atteint la Validation (`backtest_phase2_
  faithful.py`), **0/200 atteignent cette Confirmation** (contre 164/200, 82%, pour la version H4
  actuellement en production) — le niveau résultant se situe à une distance médiane de **62,3%**
  au-dessus de l'entrée (le range Hebdomadaire sur 90 bougies vaut ~106% du prix pour BTC, cohérent
  avec son historique réel de cycles haussier/baissier). **Conclusion** : aucune des 3 lectures
  mesurées à ce jour (structurelle H4/D1, amplitude H4, amplitude UT+2 réelle) ne satisfait à la
  fois la citation et la praticabilité — le mécanisme en production (amplitude H4) reste le plus
  praticable PAR ÉLIMINATION, pas par fidélité littérale. Aucun changement de code de production.
  Détail complet : `PLAN.md` "40e application".

### Ce qui reste correctement classé, confirmé par les 3 agents (rien à changer)
Tout le reste des 17 sources + le manuel — UT+2 (mécanisme général), stop UT+1 réel, breakeven différé à la Confirmation, abstention Wall Street, canal manuel, Andrews, diversification 1%+1%, +Reverse scopé TRES_AGRESSIF, EXCES-H4, pyramidalisation-régime, TSI(14,7,9), garde-fous Phase 4 (hors périmètre code, correctement noté comme tel) — vérifié directement dans le code par les 3 agents, pas simplement relu dans ce document.

**Priorisation recommandée (formule du projet, probabilité × impact × coût de découverte tardive)** : catégorie B en premier (littéral, faible coût, aucune ambiguïté) ; catégorie C à traiter une règle à la fois avec la même discipline "design puis décision puis implémentation" que Fibonacci RANGE/Conflit MTF, en commençant par l'amplitude Validation/Confirmation cross-timeframe (portée la plus large) ; catégorie A à ne PAS tenter — implémenter une approximation inventée d'un algorithme propriétaire non divulgué serait le même risque de méthode que ce projet a toujours refusé de prendre. Détail complet du plan : `PLAN.md`.

## Mise à jour de PLAN.md
`PLAN.md` datait d'avant le refactoring, le classificateur de régime, la validation croisée MTF et la découverte des funding rates — il a été remis à jour (même commit que ce document) pour pointer vers ce document et refléter l'état réel. Remis à jour de nouveau ce cycle de travail pour le traitement du P0 (cycle causal), puis à nouveau pour le traitement du P0-bis (structure causale, occurrence #4/item 8).
