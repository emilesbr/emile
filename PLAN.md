# Plan directeur — Système de trading formalisé à partir des outils PRO Indicators

**Cadre du projet** : usage strictement personnel des indicateurs de Philippe Roux (PRO Indicators), sur la base d'un abonnement souscrit et d'un accord vérifié avec l'éditeur sur les CGU. Objectif : formaliser une stratégie de trading multi-timeframe testable, avec une gestion du risque explicite, avant toute mise en œuvre avec du capital réel.

**Statut du cadrage**
- Accord obtenu avec Philippe Roux + CGU vérifiées (usage personnel) : ✅ fait
- Pas d'automatisation de l'exécution des trades en phase initiale : ✅ acté
- Objectif "revenu journalier" reformulé : espérance de gain positive sur une fenêtre plus longue, risque borné et mesuré, validée statistiquement avant tout engagement de capital réel.

---

## Configuration recommandée (réponse à "quelle config utiliser en Phase 3 ?")

**`CONFIGURATION_RECOMMANDEE.md` est le document de synthèse qui répond,
pour la première fois explicitement, à la question "quelle configuration
utiliser en paper trading Phase 3 ?"** — jusqu'ici, chaque élément du
corpus avait été testé isolément contre la baseline v7 (12 moteurs
`backtest_phase2_*.py` distincts, cf. `code/run_all.py`), sans qu'aucun ne
combine tout ce qui est validé en excluant ce qui dégrade. Ce document
tranche les 10 décisions ON/OFF (cycle+structure causaux ON non négociable,
gate Hebdomadaire "UT+2 strict" ON, Fibonacci/Andrews/canal manuel/
diversification/stop cross-timeframe/+Reverse OFF par défaut, capital par
palier en paramètre), cite pour chacune le chiffre déjà mesuré qui la
justifie (rien n'est re-testé), et fournit LE chiffre de référence unique
(BTC/ETH/BNB/SOL × 4 profils, `code/phase2_recommended_results.csv`), un
walk-forward année par année (`code/walkforward_recommended_results.csv`,
pas d'année catastrophique) et une validation hors-échantillon XRP
(`code/oos_xrp_recommended_results.csv`, résultat honnête : échantillon de
3 trades, non concluant, gate MTF non testable faute de données H4/H1 pour
XRP). Moteur : `code/backtest_phase2_recommended.py` (+
`code/walkforward_recommended.py`, `code/oos_xrp_recommended.py`,
`code/test_backtest_phase2_recommended.py`, 7/7 tests), ajouté comme 13e
moteur de `code/run_all.py` (alias `recommended`). Ce document pointe vers
`COUVERTURE_ENSEIGNEMENTS.md`/`PLAN.md` plutôt que de dupliquer leur
contenu — il ne remplace ni l'un ni l'autre, il choisit.

**Limite non résolue par ce document au moment de sa rédaction** (trouvée en répondant à la question directe de l'utilisateur "avons-nous unifié tous les moteurs de décision dans un même protocole de trading ?", réponse honnête à l'époque : non) : la table "trade de tendance" à 5 étapes (`code/trend_table.py`) N'ÉTAIT PAS intégrée à la config recommandée — deux moteurs séparés, jamais aiguillés entre eux, alors que le principe le plus fondamental du manuel est "TOUJOURS TRADER DANS UN CONTEXTE" (table différente selon le régime). **Traité ce cycle** : voir section "Protocole unifié" ci-dessous — `code/unified_protocol.py` répond maintenant "oui" à cette question (un routeur unique existe), avec un résultat mesuré honnête (pas nécessairement meilleur, cf. ci-dessous) plutôt qu'une simple case cochée.

---

## Protocole unifié — routeur de régime range ↔ tendance (traité ce cycle, puis CORRIGÉ)

**Constat qui motive ce chantier** : le moteur range (`recommended.py`, lignée v4→v7) trade aussi en régime TENDANCE (pyramidalisation autorisée), le moteur tendance (`trend_table.py`) ne trade QUE quand son propre déclencheur d'Accumulation est actif (régime TENDANCE + range mature + rejet canal + retracement 38-61%, plus étroit que "régime==TENDANCE" seul). Les deux peuvent aujourd'hui vouloir agir sur le même actif en même temps — jamais arbitré, parce qu'ils tournent dans des scripts complètement séparés. Objectif du chantier, formulé directement par l'utilisateur : pouvoir lire le jeu de données d'un actif (OHLCV) et en tirer les positions à prendre pour réaliser des profits.

**Décisions d'architecture initiales** (mode ingénieur senior — ne pas laisser un agent délégué inventer l'architecture en cours de route) — **la décision #2 ci-dessous a depuis été retirée, cf. "CORRECTION" plus bas** :

1. ~~Priorité de régime : quand le déclencheur d'Accumulation du moteur tendance est vrai, le moteur TENDANCE a la priorité, le moteur RANGE ne tente aucune nouvelle tranche tant qu'une campagne tendance est active.~~
2. ~~**Exclusivité mutuelle par actif** : à tout instant, au plus UN système (RANGE ou TENDANCE) a une position ouverte sur un actif donné — jamais les deux simultanément (évite le double comptage d'exposition, respecte "un seul contexte à la fois"). État à tracker : `active_system ∈ {None, "range", "trend"}`.~~
3. **Aucune réimplémentation de logique métier** : réutiliser tel quel `position_engine.py::process_tranche`/`make_open_tranche_fn` (côté range, déjà des fonctions PAR BOUGIE, pas des boucles complètes) et `trend_table.py::step_campaign`/`try_open_campaign`/`step_reverse` (côté tendance, idem). Le seul code nouveau est la boucle d'orchestration bar-par-bar. **Toujours valide, inchangé par la correction.**
4. **Sortie "décision live"**, pas seulement un backtest agrégé — répond directement à la demande formulée ("lire chaque jeu de données qui définit un actif afin d'identifier les positions à prendre afin de réaliser des profits") : une fonction séparée qui prend l'historique le plus récent d'un actif et retourne l'état/la décision actuelle en sortie structurée. **Toujours valide, révisée pour rapporter RANGE et TENDANCE indépendamment (cf. correction).**
5. **Mesure honnête, sans présupposer que l'unification améliore le résultat**. **Toujours valide, inchangé par la correction.**

**CORRECTION (question directe de l'utilisateur, ce cycle)** : *"nous ne devons pas nous limiter à une seule stratégie par actif — je ne crois pas que les enseignements indiquent que nous ne pouvons pas trader plusieurs ranges ou plusieurs tendances ou des range et des tendances simultanément."* Vérification directe des sources plutôt qu'une supposition dans un sens ou l'autre (les deux précédentes décisions #1/#2 avaient été posées par moi, pas extraites d'une citation du corpus — erreur de méthode à corriger) :
- `RULES_EXTRACTION.md` section 1 ("Classification du contexte", la règle "TOUJOURS TRADER DANS UN CONTEXTE" qui avait motivé l'exclusivité) décrit comment CLASSER le régime pour choisir la bonne TABLE de décision — une règle de lecture du marché, pas une restriction du nombre de positions simultanées. Rien dans cette section ni ailleurs dans le manuel officiel n'interdit plusieurs positions concurrentes.
- `TRADING_LESSONS_CLUSTERS_PRIX.md` (source #16) documente EXPLICITEMENT le contraire : *"Diversification statistique du risque... 1% sur la pattern breakout/pullback + 1% sur la pattern de moyenne mobile (cluster)... jouer les deux augmente les chances d'être 'dans le train'"* — deux patterns indépendants, ouverts SIMULTANÉMENT, chacun avec son propre risque ; seule limite posée : un plafond de risque agrégé ("jamais >2% par zone de prix"), pas un système unique à la fois.
- `TRADING_LESSONS_PYRAMIDALISATION.md` (source #15) désigne les "positions multiples" comme le mécanisme même de la pyramidalisation, et cite un cas réel (S&P 500) de DEUX patterns "3ème borne" ouverts en même temps.
- Précédent déjà présent dans ce projet AVANT ce chantier : `MAX_TRANCHES=3` (pyramidalisation range) autorise déjà plusieurs tranches concurrentes sur le même actif — l'exclusivité RANGE/TENDANCE était une restriction plus stricte que ce que le projet appliquait déjà à l'intérieur d'un seul système.

**Conclusion** : l'exclusivité mutuelle était une simplification introduite unilatéralement lors de la conception initiale du routeur, jamais une exigence du corpus — contredite par les sources #15/#16. **Retirée.** `code/unified_protocol.py` fait désormais tourner RANGE et TENDANCE de façon totalement indépendante (chacun ouvre/gère ses positions sans jamais bloquer l'autre) ; les deux peuvent être actifs simultanément sur le même actif. Détail complet de la correction : "CORRECTION" en tête de `code/unified_protocol.py`. Limite ouverte, documentée plutôt que comblée par un chiffre inventé : aucun plafond de risque AGRÉGÉ n'est appliqué entre les deux systèmes quand ils sont ouverts en même temps (le plafond 2%/1%+1% du corpus vise une paire de patterns différente — diversification statistique — pas la paire RANGE-table/TENDANCE-table de ce routeur ; cf. choix U5 dans `unified_protocol.py`).

**Ce que ce chantier NE fait PAS** (hors périmètre explicite, pour ne pas dériver) : pas de diversification/Cluster Technique dans le routeur (reste un sleeve séparé, effet marginal déjà mesuré comme quasi nul) ; pas d'automatisation d'exécution (toujours Phase 1, signal + décision humaine) ; pas de nouvelle donnée OOS (limite déjà documentée comme structurelle, cf. `CONFIGURATION_RECOMMANDEE.md` section 4bis) ; pas de plafond de risque agrégé inventé entre RANGE et TENDANCE (cf. correction ci-dessus).

**Implémentation et résultat mesuré (REGÉNÉRÉ après la correction, PUIS après la CONSOLIDATION)** : `code/unified_protocol.py` (`run_unified`/`decide_now`), `code/test_unified_protocol.py` (6/6 tests — révisés pour vérifier la CONCURRENCE des deux systèmes ; non-régression exacte vs `_run_core` de `backtest_phase2_faithful.py` sur une séquence range pure toujours vérifiée ; cohérence structurelle de `decide_now` toujours vérifiée), 14e moteur de `code/run_all.py` (alias `unified`) :

**CONSOLIDATION (même cycle, réponse à "que ferait un ingénieur senior à présent ?")** : le côté RANGE de ce routeur utilisait encore `recommended.py` (les 3 règles littérales — stop UT+1, Wall Street, +Reverse — désactivées par erreur, cf. section "Config FIDÈLE" ci-dessus) alors que le côté TENDANCE, lui, était déjà fidèle. Incohérence corrigée : le côté RANGE applique désormais les MÊMES règles que `backtest_phase2_faithful.py`, sans condition — `unified_protocol.py` devient ainsi LE protocole complet unique à utiliser opérationnellement (RANGE fidèle + TENDANCE, concurrents et indépendants), plutôt que deux livrables séparés mesurant chacun une combinaison partielle. `run_unified`/`decide_now` prennent désormais un paramètre `d1` supplémentaire (stop UT+1). Détail complet : "CONSOLIDATION" en tête de `code/unified_protocol.py`.

- BTC/ETH/BNB/SOL × 4 profils (`code/backtest_phase2_unified_results.csv`, REGÉNÉRÉ après consolidation PUIS après la correction EXCES-H4 ci-dessous, comparé désormais à `faithful.py` et non plus `recommended.py`) : **147 campagnes tendance** se déclenchent (inchangé — le côté TENDANCE n'a pas changé). Vérifié explicitement (pas supposé) : ces campagnes se referment TOUJOURS en étape ACCUMULATION (spot-check instrumenté BTC/MODERE, 7/7).
- **Résultat honnête, TOUJOURS PAS une amélioration en moyenne, mais l'écart se resserre nettement une fois comparé à la bonne référence** : 10 des 16 combinaisons actif×profil ont un retour total inférieur à `faithful.py` seul (moyenne **-9,3 points** de retour, contre -19,2 points quand la comparaison se faisait à tort contre `recommended.py`), max drawdown en moyenne **-1,9 pt** (régénéré après la correction pyramidalisation-régime ci-dessous, cf. tableau "2e pattern récurrent" occurrence #4 — était -1,5 pt avant cette correction).
- **BNB/TRES_AGRESSIF, identifié comme le point le plus fragile AVANT la correction EXCES-H4 ci-dessous, est très largement résolu APRÈS cette correction — pas une amélioration marginale** : chiffres régénérés post-fix EXCES-H4 (`backtest_phase2_unified_results.csv`) : retour total **+24,5%** (contre -42,6% avant fix), max drawdown **-27,1%** (contre -72,5% avant fix) ; RE-régénéré après la correction pyramidalisation-régime (occurrence #4 ci-dessus) : retour **+27,9%**, max drawdown **-27,0%** — mouvement modeste dans le même sens, cohérent avec un fix qui restreint le nombre de renforts plutôt qu'un changement de direction ; PUIS après la correction Conflit MTF : retour **+22,0%**, max drawdown **-25,2%** (légère amélioration) ; **RE-régénéré une nouvelle fois après l'ajout de "Stop Loss = taille du canal" (catégorie B, item ii, cf. section dédiée ci-dessous)** : retour **+12,2%**, max drawdown **-30,5%** — mouvement DÉFAVORABLE cette fois (stop plus serré ⇒ plus de sorties prématurées sur ce couple spécifiquement volatil), mesuré et implémenté quand même conformément au principe inviolable du projet. Toujours le profil le plus faible des 16 combinaisons testées, toujours pas catastrophique (loin du -72,5% d'origine) — pas de raison de continuer à l'exclure d'un usage réel sur cette seule base, mais à surveiller plus attentivement qu'avant cette dernière correction. Conclusion actionnable révisée : voir walk-forward ci-dessous.
- `decide_now` produit désormais deux décisions indépendantes (`range`, `trend`), testées sur données réelles ET synthétiques — cf. `CONFIGURATION_RECOMMANDEE.md` pour un exemple concret de sortie.
- **Limites documentées, pas cachées** : (a) capital par palier (`capital_eur`) appliqué seulement au risk_pct du moteur RANGE, pas au moteur TENDANCE (`capital_tiers.py` n'a jamais été mesuré pour la table de tendance, cf. docstring `unified_protocol.py` U3) — extrapolation non mesurée, volontairement pas faite ici ; (b) aucun plafond de risque agrégé RANGE+TENDANCE (cf. correction ci-dessus, choix U5) — jusqu'à 4% de risque nominal simultané en profil MODERE (2%+2%), jamais plafonné par ce routeur.

**Walk-forward de `faithful.py` (même réponse à "que ferait un ingénieur senior à présent ?", backlog item 5)** : `code/walkforward_faithful.py`, sur le modèle exact de `walkforward_recommended.py`, jamais fait jusqu'ici sur le protocole désormais recommandé. Résultat rassurant, comparé directement à l'ancien walk-forward de `recommended.py` (`code/walkforward_recommended_results.csv`) : pire drawdown annuel **-8,2%** (contre -24,8% pour `recommended.py`), pire retour annuel **-3,7%** (régénéré après les corrections pyramidalisation-régime PUIS Conflit MTF, était -3,3% puis -2,8% avant, contre -14,5% pour `recommended.py`), moins d'années négatives sur BNB (1/7 contre 3/7) — cohérent avec le profil "retour réduit mais drawdown nettement réduit" déjà documenté pour `faithful.py` en agrégé (`CONFIGURATION_RECOMMANDEE.md` section 5ter). Aucune année catastrophique sur les 4 actifs testés.

**Walk-forward de `unified_protocol.py` (même réponse, cycle suivant)** : `_run_core_unified` supporte désormais `start`/`end` (non-régression vérifiée : résultat agrégé bit-à-bit identique au CSV déjà commité avant ce changement), `code/walkforward_unified.py` (nouveau, même modèle). **Trouvaille directe et concrète (AVANT la correction EXCES-H4 ci-dessous)** : le -72,5% de drawdown agrégé de BNB/TRES_AGRESSIF (backtest 6 ans) n'était PAS étalé — concentré sur **UNE SEULE année, 2021** : retour -45,9%, drawdown -63,5% sur cette seule année (6/7 années négatives au total). **Conclusion révisée APRÈS la correction EXCES-H4** (`code/walkforward_unified_results.csv`, régénéré) : l'année 2021 de ce même couple actif/profil passe à **retour +6,2%, drawdown -8,4%** — l'année catastrophique a disparu, pas seulement atténuée. La pire année du couple est désormais **2024** (retour -4,1%, drawdown -22,8%), un ordre de grandeur en dessous de l'ancien pire cas. **RE-régénéré après la correction pyramidalisation-régime** (occurrence #4 du tableau "2e pattern récurrent" ci-dessus) : 2021 passe à **retour +6,1%, drawdown -8,8%**, 2024 (toujours la pire année) à **retour -6,2%, drawdown -24,4%** — mouvement modeste dans le même sens que l'ensemble du fix (moins de renforts autorisés = positions légèrement plus petites en moyenne), aucune réapparition d'année catastrophique. PUIS après la correction Conflit MTF : 2021 à **+5,2%/-9,5%**, 2024 à **-4,0%/-22,6%** (légère amélioration). **RE-régénéré une nouvelle fois après "Stop Loss = taille du canal"** (cf. section dédiée ci-dessous) : 2021 **inchangé** (+5,2%/-9,5% — la règle ne s'y déclenche pas assez pour peser), 2024 (toujours la pire année) à **retour -11,7%, drawdown -27,2%** — dégradation notable sur cette seule année, cohérente avec le fait que 2024 concentre déjà le plus de bougies à canal très large de tout l'historique BNB testé. BNB/MODERE reste le profil le plus faible des 4 actifs testés mais sans année catastrophique. BTC/ETH/SOL en MODERE : aucune année catastrophique, inchangé. **Conclusion actionnable révisée : l'exclusion de BNB/TRES_AGRESSIF n'est toujours pas justifiée par ce chiffre** (2024 reste à -27,2%, loin de l'ancien -72,5%/2021) — mais le profil mérite désormais une vigilance accrue plutôt qu'un simple "à surveiller en priorité" générique, cf. section "Stop Loss = taille du canal" ci-dessous pour le détail complet et honnête de cette dernière correction.

**OOS XRP de `faithful.py`, fait (mobilisation multi-agents, cycle suivant, cf. règle de méthode ci-dessus)** : `code/oos_xrp_faithful.py`. Procédure décidée AVANT tout résultat (même discipline que `oos_xrp_recommended.py`) : XRP D1 (365 barres) devient l'exécution, Hebdomadaire (53 barres) le stop UT+1 (gardé ACTIF sans condition — règle littérale, la désactiver aurait reproduit le raisonnement interdit), Mensuel (13 barres) le gate UT+2 (désactivé, `use_mtf_gate=False` — nouveau paramètre STRICTEMENT ADDITIF ajouté à `backtest_phase2_faithful.py`, défaut `True` inchangé pour tous les appelants existants, 19/19 tests reconfirmés verts après l'ajout). **Résultat honnête, non concluant à l'origine** : seulement 3 trades sur 365 barres, quel que soit le profil. **RE-régénéré après la correction Conflit MTF** (cf. section dédiée plus bas, trouvé par vérification adversariale de ce cycle) : le rôle "D1" de ce moteur (stop UT+1, littéral, ici tenu par l'Hebdomadaire XRP) fait désormais aussi l'objet du check Conflit MTF, sans condition (comme le stop lui-même) — résultat honnêtement différent, **0 trade sur les 4 profils** sur cette fenêtre XRP. Pas un bug (causalité et logique vérifiées indépendamment) : une règle littérale appliquée sans exception, même quand elle assèche encore plus un échantillon déjà trop mince pour conclure quoi que ce soit — la limite structurelle (échantillon XRP trop court) reste la même, elle ne s'aggrave pas d'un point de vue méthodologique.

**Recherche systématique d'autres combinaisons dangereuses, faite (mobilisation multi-agents)** : `code/cross_stress_test_faithful_gates.py` (faithful.py + gate Fibonacci/Andrews, seul ou combinés) et `code/cross_stress_test_unified_capital_tiers.py` (unified.py × 3 paliers de capital), walk-forward annuel BTC/ETH/BNB/SOL × 4 profils. **Aucune NOUVELLE combinaison actif×profil catastrophique trouvée** (seuils testés : max_dd<-30% ou retour<-25%) — BNB/TRES_AGRESSIF/2021 reste le seul cas, déjà identifié. Deux résultats notables :
- **Le capital par palier NE résout PAS BNB/TRES_AGRESSIF** — pire encore, le palier 3 (>100k€, plafond 2% censé être le plus prudent) donne un résultat LÉGÈREMENT PIRE (dd -63,9% vs -63,5%, retour -57,2% vs -45,9%) qu'un risque nominal 5% malgré un risque effectif 2,5× plus faible — effet de chemin de l'équity sous sizing multiplicatif, vérifié comme un cas isolé (1/112 combinaisons année×actif×profil×palier testées), pas un défaut systémique du sizing par palier.
- **Le gate Fibonacci neutralise incidemment BNB/TRES_AGRESSIF/2021** (dd -60,2%→-5,3%, retour -39,3%→+3,0%, en bloquant ~70% des trades) — mais ça reste une hypothèse d'implémentation à nous (pas une règle littérale pour la table RANGE, cf. `CONFIGURATION_RECOMMANDEE.md`), donc ça ne change AUCUN défaut : un effet favorable incident sur CE cas précis ne rend pas le gate Fibonacci littéral, exactement le principe inverse ("la performance ne décide jamais") appliqué symétriquement.

**Note de lecture (cycle suivant)** : les deux résultats ci-dessus (capital par palier, gate Fibonacci) ont été mesurés AVANT la correction EXCES-H4 ci-dessous, sur le cas BNB/TRES_AGRESSIF/2021 alors catastrophique. Depuis la correction, ce cas précis n'est plus catastrophique (cf. walk-forward `unified_protocol.py` révisé ci-dessus) — les deux constats restent vrais tels quels (le palier 3 restait effectivement légèrement pire que le risque nominal sur ce cas isolé, le gate Fibonacci neutralisait bien incidemment le risque), mais ils décrivent désormais un problème qui n'existe plus sous cette forme précise ; conservés ici pour la traçabilité de la méthode (recherche systématique faite, résultats honnêtes rapportés), pas comme un correctif encore nécessaire.

---

## Correction EXCES-H4 — le gate ne vérifiait plus le régime du timeframe réellement tradé (mobilisation multi-agents, audit proactif)

**Root cause** (détail complet : 3e ligne du tableau "2e pattern récurrent" ci-dessus, et docstrings de tête de `backtest_phase2_faithful.py`/`unified_protocol.py`/`backtest_phase2_recommended.py`) : `RULES_EXTRACTION.md` §1 ("Bulle/Excès -> NE PAS TRADER") est une règle littérale et inconditionnelle sur le régime du marché **réellement tradé** (H4 natif), pas seulement sur son contexte supérieur. Depuis l'introduction de la validation croisée D1 dans `backtest_phase2_v7.py`, le gate vérifiait SEULEMENT le régime D1/Hebdomadaire — le check H4 natif existait toujours en donnée (`h4["regime"]`, calculé par `prepare()`) mais n'était plus jamais lu par le gate. Régression silencieuse (v6→v7), pas un choix documenté — preuve directe : le commentaire de tête de `backtest_phase2_fib.py` affirme encore "ni le H4 ni le D1 ne doivent être en régime EXCES" alors que son code ne vérifiait que le D1.

**Corrigé, avec impact chiffré honnête (avant/après, régénéré)**, dans les 3 moteurs opérationnellement recommandés :
- `backtest_phase2_faithful.py` : gate ajoute `regime_h4 != "EXCES"` (nouvelle clé `regime_h4` dans `_prepare_features`).
- `unified_protocol.py` (côté RANGE) : gate ajoute `feat["regime"] != "EXCES"` — réutilise une donnée déjà calculée pour le côté TENDANCE, aucun nouveau calcul.
- `backtest_phase2_recommended.py` : même correction, par cohérence (moteur toujours cité/comparé activement).
- **Impact le plus significatif, BNB/TRES_AGRESSIF** : cf. sections "Protocole unifié"/walk-forward ci-dessus — retour agrégé -42,6%→+24,5%, max drawdown -72,5%→-27,1% ; année 2021 seule -45,9%/-63,5%→+6,2%/-8,4%. **Résultat POSITIF de la correction d'un bug, pas recherché comme tel** — cohérent avec le principe du projet (la correction suit la fidélité au corpus, jamais l'inverse) : la correction a été faite parce que le gate ne respectait plus la règle littérale, l'amélioration du chiffre est une conséquence, pas le motif.
- **Non corrigé, documenté comme tel** (moteurs superseded/comparaison, pas réouverts) : `v7.py`, `ut2.py`, `capital_tiers.py`, `fib.py`, `diversification.py` (Pattern A) — cf. `COUVERTURE_ENSEIGNEMENTS.md`.
- Tests : `test_unified_protocol.py` (6/6, ajusté pour la nouvelle clé `regime_h4`/`regime`), `test_backtest_phase2_recommended.py` (7/7, `_synthetic_feat`/`_synthetic_feat` ajusté), 19/19 fichiers de test au total reconfirmés verts après la correction.

## Canal manuel D1 (mobilisation multi-agents, Agent A) — les deux lectures du corpus restent valides, aucun défaut changé

**Question** : le stop UT+1 littéral (`faithful.py`) utilise le canal EMA±ATR H4 remonté en D1 comme référence de stop — alors que le canal manuel (Supports→Apex→Tangente, `manual_trend_channel.py`) est une règle littérale DISTINCTE, jusqu'ici seulement mesurée sur H4 natif, jamais reconstruite sur D1. `code/backtest_phase2_faithful_manual_channel.py` (nouveau, réutilise `_prepare_features`/`_run_core` de `faithful.py` sans modification — hérite donc automatiquement de la correction EXCES-H4 ci-dessus) construit cette alternative. Faisabilité confirmée d'abord : couverture du canal manuel sur D1 entre 97,77% et 99,53% des barres selon actif/année (BTC/ETH/BNB/SOL, walk-forward 2020-2026) — la reconstruction D1 fonctionne sans adaptation.

**Résultat honnête, comparaison directe (`code/backtest_phase2_faithful_manual_channel_walkforward_results.csv`, 112 lignes actif×année, RE-régénéré après la correction pyramidalisation-régime — cf. occurrence #4 du tableau "2e pattern récurrent" ci-dessus, ce fichier hérite automatiquement du fix via `_run_core` de `faithful.py`)** : canal manuel D1 a un retour total moyen **9,91%** (était 10,46% avant cette correction) contre **4,02%** (était 5,31%) pour EMA±ATR D1 (le stop actuellement actif dans `faithful.py`), mais aussi un max drawdown moyen plus profond, **-6,21%** (était -6,58%) contre **-3,58%** (était -4,14%). Ni l'un ni l'autre n'est strictement meilleur (retour supérieur, risque supérieur) — conclusion inchangée par le fix, seule l'ampleur bouge légèrement. **Décision suivie : ne PAS remplacer le stop actuel** — le corpus ne tranche pas lequel des deux stops littéraux prime en cas de désaccord, et la performance ne doit jamais servir à choisir entre deux lectures également fidèles du corpus (même principe que "la performance ne décide jamais", appliqué ici entre deux implémentations valides plutôt qu'entre "implémenter ou non"). Les deux restent documentées comme des lectures valides, aucun code de production changé.

## Risque agrégé RANGE+TENDANCE+diversification (mobilisation multi-agents, Agent B) — backlog item 8, désormais quantifié

Répond à l'item resté ouvert au cycle précédent ("chiffrer le risque agrégé... jugé trop risqué à livrer sans tests approfondis"). `code/risk_aggregation_triple_system.py` (nouveau) fait tourner simultanément, sur le même actif/historique, les 3 systèmes qui peuvent aujourd'hui être actifs en même temps sans plafond commun : `unified_protocol.py` (RANGE+TENDANCE) et `diversification.py` (Pattern A + Pattern B), et calcule le risque nominal agrégé bougie par bougie. **Patché après coup, deux fois** pour que sa réplique interne du gate RANGE (nécessaire, dupliquée car la fermeture de `diversification.py` n'exporte pas cette fonction) applique la correction EXCES-H4 puis la correction pyramidalisation-régime (occurrence #4 ci-dessus), plutôt que de publier des chiffres basés sur un gate déjà connu comme buggé.

**Résultat honnête, RE-régénéré après la correction pyramidalisation-régime** (`risk_aggregation_full_history.csv` + `risk_aggregation_walkforward.csv`, BTC/ETH/BNB/SOL × 4 profils) : risque agrégé maximal observé **17,00%** (BTC/TRES_AGRESSIF, 2020-11-28, sur l'historique complet, inchangé par ce fix — le pic est atteint pendant une phase déjà en régime TENDANCE), très au-dessus du plafond global 5% documenté dans `RULES_EXTRACTION.md` §5. Sur la décomposition annuelle (112 combinaisons année×actif×profil), **67/112 dépassent le plafond 5%** (était 68/112 avant ce fix). **Root cause identifiée, pas un mystère** : le pyramidage RANGE seul (`MAX_TRANCHES=3` × 5% par tranche = 15% pour TRES_AGRESSIF) dépasse déjà le plafond avant même de combiner avec les autres systèmes — ce n'est pas la combinaison qui casse le plafond, c'est un système pris isolément qui peut déjà le dépasser au profil le plus agressif. **Volontairement PAS traité comme un défaut à corriger silencieusement** : le corpus ne spécifie pas de plafond agrégé pour CETTE combinaison précise de systèmes (RANGE+TENDANCE+diversification tournant simultanément est elle-même une architecture du projet, pas une prescription du corpus, cf. choix U5 de `unified_protocol.py`) — inventer un plafond serait le même risque de méthode que le 2e pattern récurrent ci-dessus (poser une règle sans citation). Documenté comme limite ouverte, cohérente avec la note U5 déjà présente.

---

## Document maître de couverture des enseignements

**`COUVERTURE_ENSEIGNEMENTS.md` est désormais le document de référence unique pour savoir ce qui, du corpus (manuel PDF + 17 sources Trading Lessons), est implémenté ou non.** Avant de considérer une phase "faite", vérifier cette table plutôt que ce résumé narratif. Rappel du principe qui gouverne cette table depuis sa formulation explicite par l'utilisateur : **la performance du proxy ne sert jamais à décider si un élément du corpus doit être implémenté ou non** — le proxy n'est pas le vrai signal, donc une contre-performance en backtest n'est jamais un motif de rejet d'un élément documenté de la propriété intellectuelle de Philippe. Un élément documenté et non encore implémenté reste une lacune à combler, jamais une décision de conception.

**Principe étendu (rappel direct de l'utilisateur, en tant que directeur, ce cycle) : "nous ne nous fions pas aux résultats du Proxy pour décider d'utiliser ou non la propriété intellectuelle de Philippe, nous l'utilisons dans tous les cas."** Ce principe ne s'arrêtait jusqu'ici qu'à la décision d'IMPLÉMENTER (coder) un élément du corpus — il s'étend maintenant explicitement à la décision de l'UTILISER OPÉRATIONNELLEMENT (l'activer dans le protocole réellement exécuté), pas seulement de l'avoir disponible en option désactivée par défaut. En creusant sérieusement plutôt que de s'arrêter à une réponse de surface ("que ferait un ingénieur senior ?", cf. méthode ci-dessous), constat honnête : `CONFIGURATION_RECOMMANDEE.md` violait ce principe étendu à PLUSIEURS endroits, pas seulement pour la table de tendance — 4 des 5 "filtres" que j'avais moi-même classés comme de simples hypothèses d'implémentation (donc légitimement réglables par backtest) sont en réalité des **règles littérales du corpus**, désactivées par défaut sur la seule base d'une contre-performance mesurée sur le proxy — exactement le raisonnement interdit :
- **Stop cross-timeframe réel UT+1** (`use_mtf_stop`) : `TRADING_LESSONS_BREAKOUT_RATIO11.md` (#12) — *"Stop-loss = clôture la plus basse du canal de tendance de l'UT+1"*, règle littérale, pas une hypothèse.
- **Abstention Wall Street** (`use_wall_street_abstention`) : `TRADING_LESSONS_ALTERNATIVE_MANUELLE.md`/`ANALYSE_SANS_INDICATEURS.md` (#3/#4) — *"aucun outil ne fonctionne, arrêter tout"*, abstention TOTALE prescrite, pas une prudence optionnelle (seule la définition NUMÉRIQUE du pattern est une hypothèse, pas le comportement une fois détecté).
- **+Reverse, profil TRES_AGRESSIF** (`reverse_at_limit`) : `RULES_EXTRACTION.md` §3 — la table Money Management range dit littéralement "TP100%+Reverse" à l'étape Limite, EXPLICITEMENT sur la seule ligne "Très agressif".
- **Table de tendance** (`trend_table.py`) et **diversification/Cluster Technique** (`diversification.py`) : déjà traités ce cycle (cf. sections dédiées ci-dessous/ci-dessus).

Seuls 2 éléments restent légitimement des hypothèses d'implémentation À NOUS (le corpus ne dit pas comment les appliquer, pas seulement "ça marche moins bien") : le gate Andrews Pitchfork sur l'entrée (le corpus documente le rôle/chiffre de l'outil, jamais comment l'utiliser comme filtre), et le gate Fibonacci sur l'entrée RANGE (la règle Fibonacci littérale existe mais pour valider un pullback de TENDANCE, déjà implémentée sans condition dans `trend_table.py` — l'appliquer EN PLUS sur la table RANGE est une extrapolation à nous). Ceux-là restent correctement réglables par performance mesurée. **Précision ajoutée par un cycle ultérieur (mobilisation multi-agents)** : cette classification pour Fibonacci RANGE s'est révélée INCOMPLÈTE, pas fausse — le manuel donne une lecture littérale distincte (seuils par régime, condition composite à 3 volets) jamais comparée à celle-ci ; désormais backlog P0/P1 documenté séparément (item 10), pas une simple hypothèse close. Par ailleurs, "Conflit Multi-Timeframe" (règle distincte, désignée "erreur numéro un" par le corpus) a depuis été trouvée manquante puis implémentée — cf. section dédiée plus bas.

**Traité ce cycle** : `code/backtest_phase2_faithful.py` (nouveau moteur RANGE, `run_faithful`) active les 3 premiers éléments SANS CONDITION (stop D1 UT+1, abstention Wall Street, +Reverse scopé TRES_AGRESSIF), `code/test_backtest_phase2_faithful.py` (5/5 tests à l'origine, 9/9 après les corrections ultérieures), 15e moteur de `code/run_all.py` (alias `faithful`). **Mesure ORIGINALE (obsolète, conservée pour traçabilité de la méthode) vs `recommended.py` seul** : 15/16 combinaisons avaient un retour total inférieur (moyenne -77,3 points), 15/16 un drawdown moins profond (moyenne +7,5 points), profit factor +0,32 en moyenne, et BNB/TRES_AGRESSIF sortait fortement du lot (-122,9 pts de retour, -27,9 pts de drawdown), confirmé par ablation comme une interaction stop D1 × +Reverse. **Ces chiffres ne sont plus d'actualité** : après les 3 corrections de fidélité IP trouvées par audit proactif (EXCES-H4, pyramidalisation-régime, Conflit MTF — cf. sections dédiées plus bas), `code/backtest_phase2_faithful_results.csv` régénéré donne moyenne **-37,2 points** de retour, **+11,5 points** de drawdown (16/16 améliorées), PF +0,09 — et **BNB/TRES_AGRESSIF n'est plus un cas hors norme** (-8,7 pts de retour, +13,5 pts de drawdown, dans la moyenne du tableau) : la composition des trades qui s'ouvrent a suffisamment changé pour dissoudre l'interaction observée à l'origine. Détail chiffré complet et honnête : `CONFIGURATION_RECOMMANDEE.md` section 5ter.

**Un seul tracker actif** (inspiré de la discipline documentaire du projet `ideeri-v2`, `docs/README.md` : *"pas de deuxième tracker"*) : `PLAN.md` (ce document) est la feuille de route/backlog vivant — mis à jour au fil de l'eau, jamais un deuxième endroit ne doit exister pour "ce qui reste à faire". `STATUS.md` est le classement documentaire (quel fichier lire pour quel sujet), pas une deuxième liste d'items ouverts. `COUVERTURE_ENSEIGNEMENTS.md` est la table de croisement détaillée corpus↔code (l'équivalent d'un "tableau maître" chez ideeri-v2) — elle alimente ce plan, elle ne le duplique pas. En cas de divergence entre ces documents, **ce fichier fait foi sur ce qui reste à faire**.

---

## Méthode de travail (inspirée de la discipline `ideeri-v2`)

Adopté après une observation directe de l'utilisateur : à chaque fois qu'il demande "un directeur ingénieur senior serait-il satisfait ?", une découverte réelle survient (bug de signe trouvé par contrôle aléatoire ; réserve batch/non-causale déjà documentée une fois mais jamais remontée jusqu'ici). Le projet `ideeri-v2` (même utilisateur) documente **exactement le même phénomène** dans son `CLAUDE.md` §16.2 point 7 — deux bugs de production trouvés uniquement parce que l'utilisateur a redemandé "que répondrait un ingénieur senior ?" une 2e fois. Plutôt que de laisser ce pattern se répéter en silence, on l'écrit une fois pour toutes et on en tire une méthode, comme `ideeri-v2` l'a fait.

### Prioriser par risque, pas par ordre de découverte

```
risque = probabilité que ça cache un problème × impact si silencieux × coût de la découverte tardive
```

C'est la formule utilisée dans `ideeri-v2/docs/archive/audit-senior-code-complet.md` §0. Appliquée ici : la composante "cycle" de `proxy_v2.py` obtient le score le plus élevé sur les 3 axes (probabilité : déjà 2 anomalies distinctes trouvées sur cette seule fonction ; impact : elle contribue à l'entrée de tous les moteurs v5/v6/v7 ; coût de découverte tardive : le pire moment pour la découvrir serait en paper trading ou pire, en capital réel) — d'où son classement P0 dans le backlog ci-dessous, avant tout nouvel élément du corpus non encore implémenté.

### Pattern récurrent — risque concentré, pas dispersé (4 occurrences maintenant, pas seulement dans une fonction)

Comme `ideeri-v2` le documente pour son propre pattern récurrent ("contexte de validité jamais revérifié", 7 occurrences trouvées) : quand la même **catégorie** de bug réapparaît sur plusieurs fonctions distinctes, ce n'est plus des hasards indépendants, c'est un signal que le risque se concentre sur une catégorie précise — ici : **une fonction batch appelée une fois sur toute la série historique, dont la sortie à l'instant t dépend silencieusement de barres futures, sans qu'aucune revue de code ne l'ait détecté avant un test empirique dédié.**

| # | Occurrence | Root cause commune | Statut |
|---|---|---|---|
| 1 | `sin(phase)` anti-corrélé au rendement du lendemain (BTC/ETH/SOL), trouvé par contrôle aléatoire | Convention de signe d'une sortie de transformée de Hilbert jamais vérifiée empiriquement avant usage | Corrigé (`-sin(phase)`), revalidé hors-échantillon (XRP, `OOS_VALIDATION_CYCLE_SIGN.md`) |
| 2 | `hilbert()` appelé sur la série entière d'un coup (non causal) — jamais quantifié sur données réelles | Fonction batch/FFT utilisée telle quelle sans vérifier l'hypothèse de causalité requise pour un usage en backtest | **Traité ce cycle — MITIGÉ, pas résolu au sens plein** : `compute_cycle_phase_causal` (fenêtre glissante) en production, v5/v6/v7 + OOS XRP rejoués. Edge **global** H4 confirmé en direction (48/48 configs v7) mais ampleur surestimée ~4× ; edge du **cycle isolé** non confirmé sur les 5 actifs testés (corrélation causale non significative partout, y compris XRP OOS). Détail : `COUVERTURE_ENSEIGNEMENTS.md` ⚠️→◐ |
| 3 | `cycle_ascending` (dérivée du sinewave) anti-corrélée au rendement futur sur sinusoïde synthétique pure — contredit la validation OOS réelle (positive) | Interprétation d'une grandeur dérivée non vérifiée sur cas synthétique contrôlé avant usage en production | **Refermée ce cycle — expliquée, pas un bug** : mesurée directement sur BTC/ETH/BNB/SOL réels (H4+D1, `code/cycle_ascending_investigation.py`, `cycle_ascending_real_data.csv`) — corrélation `cycle_ascending`/rendement futur proche de zéro partout (\|r\|≤0,045), signe incohérent d'un actif à l'autre, aucune significative après lecture honnête (2/8 tests à p<0,05 sur 8, consistant avec le taux de faux positifs attendu par hasard). L'artefact synthétique (r=-0,41, p≈1e-74) persiste à l'identique avec le calcul CAUSAL actuel — le P0 (causalité du cycle) n'a rien changé à CETTE divergence spécifique, qui concernait dès l'origine un artefact déterministe de la sinusoïde pure, pas une propriété du calcul batch. Robustesse (`code/cycle_ascending_robustness.py`, `cycle_ascending_robustness.csv`) : l'artefact ne s'efface qu'à un ratio bruit/amplitude ≈3-4x ou un mélange de fréquences fortement bruité — bien au-delà de ce qu'un bruit gaussien standard capture, cohérent avec des prix crypto réels dominés par une variation non cyclique (tendance, sauts, non-stationnarité). Hypothèse posée à l'origine **confirmée** : la sinusoïde pure est un artefact mathématique de dérivée-sur-signal-lisse, pas un terrain de test représentatif pour cette condition dérivée. Aucun fix nécessaire, `code/proxy_v2.py` non modifié. Détail complet : `code/test_proxy_v2.py` (docstring mise à jour) |
| 4 | `proxy_v2.py::compute_ascending_lows` (structure) appelait `scipy.signal.argrelextrema(..., order=SWING_ORDER)` en un seul appel batch sur toute la série — la classification "swing low" à l'instant *t* dépendait des `SWING_ORDER=3` barres qui suivent *t*, réellement futures. Vérifié empiriquement sur BTC H4 réel (200 barres échantillonnées) : 23/200 (11,5%) changeaient de classification entre batch et calcul sans barre future. Même `argrelextrema` batch aussi utilisé pour `n_borders` (maturité, v4-v7 + capital_tiers) et pour les swings de `code/fibonacci.py` | Même catégorie que l'occurrence #2 (fonction batch non causale), sur une fonction différente — jamais suspectée avant que la vérification du travail d'un agent tiers (Fibonacci) n'y mène incidemment | **Traité ce cycle — MITIGÉ, ampleur small CONFIRMÉE** (pas juste supposée) : `compute_swing_low_confirmed`/`compute_swing_high_confirmed` (causales, vérifiées bit-à-bit identiques à un recalcul tronqué sur 14 233 points BTC H4 réels) en production dans `proxy_v2.py`/v4/v5/v6/v7/capital_tiers/fibonacci.py. Rejeu v5/v6/v7 (H4) : **0/16, 0/32, 0/48 bascules de signe** — direction intacte à 100%, ampleur réduite de ~12-14% (pas ÷4 comme le P0 du cycle), win rate quasi inchangé (légère amélioration, +0,3 à +0,6 pt). Détail : `COUVERTURE_ENSEIGNEMENTS.md` ⚠️→◐ P0-bis. Reste ouvert (documenté, pas caché) : v4.py et capital_tiers.py corrigés dans le code mais pas rejoués/requantifiés (hors scope "v5/v6/v7 minimum") |

**Conséquence pratique** : toute future modification de `proxy_v2.py::compute_cycle_phase`/`add_proxy_v2_score`/`compute_ascending_lows`, ou de toute fonction basée sur `argrelextrema`/`hilbert` appelée en batch sur l'historique complet, mérite le "mode ingénieur senior" ci-dessous par défaut, pas seulement quand on y pense — **c'est maintenant une catégorie de risque identifiée, pas un cas isolé** : chercher systématiquement d'autres appels batch à une fonction dont la sortie à l'instant t peut dépendre de barres futures est désormais un réflexe à appliquer, pas une découverte à espérer.

### 2e pattern récurrent — décisions de conception jamais extraites d'une citation du corpus (4 occurrences, les 3e ET 4e trouvées PROACTIVEMENT par audit, pas par contestation)

Catégorie de risque DIFFÉRENTE de celle ci-dessus (pas un bug de calcul, une **erreur de méthode dans la prise de décision d'architecture/de configuration**) : une règle de conception a été posée sans citation précise du corpus, s'est avérée non conforme une fois vérifiée.

| # | Occurrence | Root cause commune | Statut |
|---|---|---|---|
| 1 | "Exclusivité mutuelle par actif" (`unified_protocol.py`) : au plus un système RANGE/TENDANCE actif à la fois, présentée comme fidèle à "TOUJOURS TRADER DANS UN CONTEXTE" | Règle posée par déduction/prudence de conception, jamais vérifiée contre une citation précise avant d'être implémentée et testée comme si elle était acquise | **Corrigé** — contredite par les sources #15/#16 (positions multiples documentées explicitement), retirée. Cf. "CORRECTION" en tête de `unified_protocol.py`. Découverte : contestation directe de l'utilisateur |
| 2 | 4 des 10 décisions ON/OFF de `CONFIGURATION_RECOMMANDEE.md` (stop UT+1, Wall Street, +Reverse, diversification) désactivées par défaut sur la seule base d'une contre-performance mesurée sur le proxy | Distinction "hypothèse d'implémentation vs règle littérale" appliquée par catégorie de surface (tous les "patterns/filtres" traités comme des réglages) plutôt que vérifiée source par source pour chacun | **Corrigé** — `code/backtest_phase2_faithful.py` les active sans condition. Cf. `PLAN.md` section ci-dessus, `CONFIGURATION_RECOMMANDEE.md` section 5ter. Découverte : contestation directe de l'utilisateur |
| 3 | Régime EXCES vérifié SEULEMENT sur le contexte supérieur (D1/Hebdo) depuis `backtest_phase2_v7.py`, JAMAIS sur le H4 natif (timeframe réellement tradé) — propagé silencieusement à `ut2`/`capital_tiers`/`fib`/`recommended`/`faithful`/`unified`(RANGE)/`diversification`(Pattern A) | Refactor de v6→v7 (introduction de la validation croisée D1) qui a REMPLACÉ le check H4 existant par un check D1, sans jamais documenter ce remplacement comme une perte de comportement — le commentaire de tête de `backtest_phase2_fib.py` affirme encore "ni le H4 ni le D1 ne doivent être en régime EXCES" alors que son code ne vérifie que le D1, preuve que c'est un oubli, pas un choix | **Corrigé pour le protocole opérationnel** (`recommended.py`, `faithful.py`, `unified_protocol.py`) — `regime_h4`/`feat["regime"]` ajouté au gate. **Non corrigé, documenté comme tel** dans les moteurs superseded/comparaison (`v7`, `ut2`, `capital_tiers`, `fib`, `diversification` Pattern A) — pas la peine de rouvrir des chantiers clos, cf. `COUVERTURE_ENSEIGNEMENTS.md`. Découverte : **audit systématique proactif** (mobilisation multi-agents), PAS une contestation utilisateur -- première fois que ce pattern est cassé avant d'être signalé de l'extérieur |
| 4 | Pyramidalisation (renfort, tranches 2/3) plus restreinte au régime TENDANCE/RANGE_TENDANCIEL depuis `backtest_phase2_v7.py` -- même schéma que l'occurrence #3, propagé silencieusement à `ut2`/`capital_tiers`/`fib`/`recommended`/`faithful`/`unified`(RANGE) | Refactor v6→v7 : `backtest_phase2_v6.py` calculait déjà `pyramiding_allowed = regime in ("TENDANCE","RANGE_TENDANCIEL")` appliqué SEULEMENT au renfort (`RULES_EXTRACTION.md` §3, "Renfort" absent de la table RANGE, présent partout dans la table TENDANCE §4) -- v7 a réuni le gate de l'entrée fraîche et du renfort en un seul, perdant cette distinction. Preuve littérale dans le code lui-même : le commentaire de `v7.py::gate_extra` dit "Même gate pour l'entrée fraîche et le renfort (pas de distinction ici, contrairement à v6/fib)" -- un abandon documenté au moment du refactor, jamais remonté jusqu'à `COUVERTURE_ENSEIGNEMENTS.md` (qui affirmait à tort "Pyramidalisation réservée au régime Tendance \| v6, v7") | **Corrigé pour le protocole opérationnel** (`recommended.py`, `faithful.py`, `unified_protocol.py`) -- `gate_extra` distingue désormais entrée fraîche (inchangée) et renfort (`pyramiding_allowed` réutilisant `regime_h4`/`feat["regime"]`, déjà calculé). **Non corrigé, documenté comme tel** dans les moteurs superseded (`v7`, `ut2`, `capital_tiers`, `fib`). Vérifié empiriquement AVANT correction : 25,7% des renforts réellement ouverts (BTC/ETH/BNB/SOL, moteurs faithful+recommended) l'étaient en régime RANGE_NEUTRE. Découverte : **audit systématique proactif** (2e round de mobilisation multi-agents), même méthode que l'occurrence #3 |

**Conséquence pratique** : avant de poser toute règle de conception qui RESTREINT ou DÉSACTIVE un comportement ("un seul système à la fois", "ce filtre reste optionnel", "cette combinaison est hors périmètre"), vérifier explicitement si le corpus l'exige littéralement ou si c'est une inférence à moi — et le dire dans le commentaire/docstring au moment où la règle est posée, pas seulement après coup si l'utilisateur la conteste. Les 2 premières occurrences auraient pu être évitées par cette seule discipline ; les 3e et 4e montrent que la mobilisation multi-agents (règle de méthode ci-dessous) porte ses fruits de façon répétée, pas seulement une fois — deux régressions du même refactor (v6→v7) trouvées par audit avant d'être découvertes de l'extérieur, contrairement aux deux premières. **Signal à ne pas ignorer** : les occurrences #3 et #4 partagent la MÊME root cause précise (le refactor v7 a fusionné/simplifié des gates qui distinguaient plusieurs cas dans v6, sans documenter la perte de comportement) — un audit dédié de tout ce que v6 faisait de plus fin que v7 (pas seulement EXCES et pyramidalisation) reste une piste ouverte, pas encore menée exhaustivement.

### Règle de méthode ajoutée (rappel direct de l'utilisateur, ce cycle) : "que ferait un ingénieur senior ?" mobilise une ÉQUIPE, pas une réponse individuelle

Jusqu'ici, chaque "que ferait un ingénieur senior ?" a été traité par une analyse portée SEULE (par moi, séquentiellement) — ce qui explique en partie le 2e pattern récurrent ci-dessus : un seul jugement, jamais confronté à un second regard, a laissé passer deux erreurs de conception avant que l'utilisateur ne les conteste. L'utilisateur a explicitement demandé que ce déclencheur mobilise désormais **plusieurs agents en parallèle** (comme une vraie équipe d'ingénierie — revue indépendante, recherche dédiée, vérification croisée), pas une réponse individuelle. Appliqué concrètement à partir de ce cycle : quand "que ferait un ingénieur senior (directeur) ?" est demandé, identifier les items de backlog/risques pertinents et les répartir entre plusieurs agents délégués tournant EN PARALLÈLE plutôt que de les traiter un par un moi-même — chaque retour d'agent reste vérifié indépendamment avant d'être accepté (discipline déjà en place pour tout travail délégué, cf. ci-dessus), la mobilisation multi-agents ne remplace pas la vérification, elle s'y ajoute.

**Première application (même cycle)** : 3 agents lancés en parallèle, chacun dans un worktree isolé, sur les 3 items les plus prioritaires selon la formule du projet — (1) vérification indépendante et adversariale de la consolidation `unified_protocol.py`/`faithful.py` (19/19 tests reconfirmés, 4 CSV reproduits bit-à-bit, chiffre BNB/TRES_AGRESSIF 2021 reproduit indépendamment, RIEN de cassé trouvé — une seule réserve mineure notée : mark-to-market des campagnes tendance qui exclut `pnl_accum`, comportement HÉRITÉ de `trend_table.py` d'origine, jamais exercé empiriquement, sans impact sur les chiffres publiés), (2) recherche systématique d'autres combinaisons dangereuses (résultats ci-dessous), (3) OOS XRP de `faithful.py` (résultat ci-dessous). Les 3 retours ont ENSUITE été vérifiés indépendamment par moi (pas acceptés tels quels) avant intégration — rerun des tests/scripts dans chaque worktree, diff des CSV, reproduction des chiffres-clés — cohérent avec "la mobilisation multi-agents ne remplace pas la vérification, elle s'y ajoute" énoncé ci-dessus.

**2e application (cycle suivant, question directe "un ingénieur senior serait-il satisfait ? par rapport à la propriété intellectuelle de Philippe")** : 3 agents relancés en parallèle, cette fois spécifiquement sur la fidélité IP — (A) recherche systématique d'autres régressions de la même catégorie qu'EXCES-H4, (B) vérification adversariale de la correction EXCES-H4 elle-même + des 2 fichiers agents intégrés au cycle précédent (canal manuel D1, risque agrégé), (C) re-audit indépendant, DE ZÉRO, de la classification "règle littérale vs hypothèse" sur tout le corpus contre `faithful.py`/`unified_protocol.py`. Les 3 retours vérifiés indépendamment avant toute action :

- **Agent B (vérification adversariale) : verdict propre.** Tous les chiffres du cycle précédent reproduits à l'identique (rejeu frais, pas lecture de CSV), test synthétique à vérité terrain connue confirmant la correction EXCES-H4 sur les 3 moteurs (3/3 OK, y compris le cas "EXCES D1 seul" qui prouve que la correction n'a pas cassé le gate préexistant), duplication du gate dans `risk_aggregation_triple_system.py` vérifiée identique à `_run_core_unified` par rejeu réel (0 divergence sur 16 combinaisons). Deux notes cosmétiques sans impact (clé `regime_h4` morte dans `unified_protocol.py`, seuils littéraux non dérivés des constantes dans `risk_aggregation_triple_system.py`). 19/19 tests confirmés.
- **Agent A (recherche de régressions) : trouvaille confirmée, corrigée.** Cf. occurrence #4 du tableau ci-dessus (pyramidalisation-régime) — vérifié indépendamment par moi (relecture directe du code des 3 moteurs, script de reproduction de l'agent rejoué à l'identique : 4544 renforts observés, 1168 (25,7%) en régime RANGE_NEUTRE), puis CORRIGÉ dans `faithful.py`/`recommended.py`/`unified_protocol.py` (RANGE) + `risk_aggregation_triple_system.py` (gate dupliqué patché par cohérence), tests dédiés ajoutés (scénario synthétique à vérité terrain connue, contrôle positif ET négatif, dans les 3 fichiers de test), 22/22 tests désormais verts, CSV régénérés avec impact honnête chiffré ci-dessous.
- **Agent C (re-audit classification littérale vs hypothèse) : 2 trouvailles significatives, NON corrigées ce cycle, documentées comme limites ouvertes prioritaires.**
  1. **Gate Fibonacci RANGE — classification incomplète.** `RULES_EXTRACTION.md` §1 (le manuel lui-même, pas seulement les 17 sources vidéo) donne des seuils Fibonacci LITTÉRAUX et conditionnés par régime pour l'ENTRÉE : Range neutre ≥76,4%, Range tendanciel ≥61,8%, Tendance ≥23% — vérifié directement (`RULES_EXTRACTION.md` lignes 9-13). `code/fibonacci.py` note que "76,4%" n'apparaît dans aucune des 5 sources vidéo consultées, mais ne compare jamais ce chiffre au manuel lui-même, qui le donne pourtant explicitement comme condition d'entrée RANGE. La classification actuelle ("hypothèse, réglable par performance") reste correcte pour la synthèse vidéo [23%,61,8%] déjà implémentée dans `trend_table.py`, mais elle occulte cette lecture MANUEL-littérale distincte, jamais implémentée ni testée pour la table RANGE. `TRADING_LESSONS_INDEX.md` ligne 11 avait déjà noté une "tension à réconcilier" avec `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` (signal couleur→61% Fibo, signal gris→76% Fibo) — jamais réconciliée depuis.
  2. **"Conflit Multi-Timeframe" (source #5, désignée "erreur numéro un" PAR LE CORPUS LUI-MÊME) — jamais implémentée, jamais même classée ON/OFF.** `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` (vérifié directement, lignes 5-11) : *"Conflit Multi-Timeframe (MTF) : L'erreur numéro un. Ne jamais trader une borne de range si un range d'unité de temps supérieure est déjà actif."* — confirmée par sa propre checklist pré-trade. `TRADING_LESSONS_INDEX.md` ligne 11 avait déjà identifié cette règle comme DISTINCTE de "UT+2" au moment du traitement initial des sources, sans jamais la reprendre ensuite dans `COUVERTURE_ENSEIGNEMENTS.md`/`CONFIGURATION_RECOMMANDEE.md`. Vérifié dans le code : ni `faithful.py` ni `unified_protocol.py` ne vérifient si le régime du contexte supérieur (D1/Hebdo) est LUI-MÊME en range avant d'ouvrir une tranche RANGE sur H4 — le gate actuel (score Hebdomadaire ≥2) est une condition de momentum/alignement, pas une exclusion "le TF supérieur est en range", les deux ne sont pas équivalents.

**Pourquoi ces 2 trouvailles ne sont PAS corrigées dans la foulée, contrairement à l'occurrence #4** : contrairement à EXCES-H4/pyramidalisation-régime (une donnée déjà calculée, simplement jamais relue — fix mécanique, sans ambiguïté de conception), ces deux éléments exigent une décision de conception non triviale avant tout code : pour Fibonacci, comment le retracement est-il mesuré exactement dans ce projet pour être comparable au seuil du manuel (quel swing de référence) ? Pour Conflit MTF, que signifie précisément "un range actif" avec les catégories déjà existantes de `regime_classifier.py` (RANGE_NEUTRE et RANGE_TENDANCIEL sont-ils tous deux visés, ou seulement l'un des deux ?), et sur quel(s) niveau(x) de contexte (D1 seul, Hebdomadaire aussi) ? Implémenter dans la précipitation, sans citation exacte de CE choix précis, reproduirait exactement l'erreur de méthode du "2e pattern récurrent" ci-dessus (poser une règle de conception sans la vérifier contre le corpus). **Traité comme backlog prioritaire** (item 10, ci-dessous), PAS comme un chantier clos ni comme une invention silencieuse.

**3e application (cycle suivant, même question "que ferait un ingénieur senior ?")** : plutôt que de coder directement les 2 items du backlog P0/P1, 3 agents lancés en parallèle pour produire des PROPOSITIONS DE DESIGN citées (pas d'implémentation) — (D1) recherche approfondie + proposition pour Conflit MTF, (D2) recherche approfondie + proposition pour Fibonacci manuel-littéral, (D3) second avis totalement indépendant sur les deux sujets (garde-fou explicite contre "un seul jugement", cf. citation ci-dessus). Les 3 retours convergent sur un point décisif : **le niveau de contexte visé par Conflit MTF change le résultat d'un ordre de grandeur** — calé sur Hebdomadaire, ~89% des tranches RANGE actuellement ouvertes seraient bloquées (quasi-suppression du système RANGE, mesuré empiriquement par l'agent D3 en rejouant `faithful.py` avec `record_trace=True`) ; calé sur D1, seulement ~12-19% (BTC 13,0%/ETH 7,5%/BNB 19,1%/SOL 3,7%). Aucune source ne nomme le niveau explicitement, mais `TRADING_LESSONS_TROISIEME_BORNE.md` (source #11) précise que le mécanisme opère entre l'UT "Contexte" (immédiatement supérieure) et l'UT "Tendance" (exécution) — cohérent avec le vocabulaire "UT+1" déjà utilisé dans ce projet pour le stop D1. **Décision prise (D1, pas Hebdo) et implémentée** : cf. section dédiée ci-dessous. Sur Fibonacci, les 3 agents convergent aussi, dans l'autre sens : la règle §1 du manuel est en réalité une condition COMPOSITE à 3 volets (retracement + "débordement du contexte" + "signal & triangle de confirmation"), dont 2 des 3 n'ont AUCUNE définition utilisable ailleurs dans le corpus ("triangle" n'a qu'une seule mention, jamais reliée à cette règle ; "débordement" n'est défini que pour un usage différent — cible de sortie, pas condition d'entrée) — les implémenter exigerait d'inventer ces 2 définitions, ce que ce projet refuse explicitement de faire. **Décision prise : NE PAS implémenter Fibonacci ce cycle**, laissé backlog, avec la raison précise désormais documentée (pas seulement "seuil à ajuster" comme le disait la version précédente de ce document, mais "2 des 3 conditions n'existent nulle part dans le corpus sous forme codable").

### Correction Conflit Multi-Timeframe — implémentée (mobilisation multi-agents, design puis décision puis implémentation)

`TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` (source #5) désigne, dans son propre texte, une règle comme *"l'erreur numéro un"* : *"Ne jamais trader une borne de range si un range d'unité de temps supérieure est déjà actif. La structure supérieure prime systématiquement."* — confirmée par sa checklist pré-trade. Jamais implémentée avant ce cycle.

**Décision de conception (2 points que le corpus ne tranche pas explicitement, résolus par la lecture la mieux étayée plutôt que laissés en attente indéfiniment)** :
- **Niveau de contexte : D1, pas Hebdomadaire.** cf. "3e application" ci-dessus pour le détail complet (mesure empirique décisive : Hebdo bloquerait ~89% des tranches actuelles, D1 seulement ~12-19%). Le choix D1 est en plus étayé textuellement par analogie avec `TRADING_LESSONS_TROISIEME_BORNE.md` (source #11, "Contexte" = UT immédiatement supérieure).
- **Entrée fraîche ET renfort, appliqué uniformément.** Ni source #5 ("aucune borne de range") ni source #11 ("toute borne... devient inexistante") ne distinguent explicitement les deux cas — contrairement au cas pyramidalisation-régime (occurrence #4 ci-dessus), où l'absence littérale de la cellule "Renfort" en §3 permettait de trancher précisément. Ici rien d'équivalent, et la table RANGE n'a de toute façon pas de mécanique "renfort" distincte d'une "entrée" dans l'architecture de ce projet (`open_tranche_fn` unique). Appliqué via `gate()` (déjà partagé par les deux cas), pas par une distinction inventée dans `gate_extra`.

**Implémenté dans `backtest_phase2_faithful.py` et `unified_protocol.py` (RANGE)** — donnée réutilisée, pas recalculée : `ctx["D1"]["regime"]` (`attach_multi_context`, déjà causal, déjà joint pour le stop `ctx_support_d1`) — même schéma "donnée déjà calculée, jamais lue pour cette règle précise" que les occurrences #3/#4. `risk_aggregation_triple_system.py` patché par cohérence (3e patch de son gate dupliqué). **Non implémenté dans `backtest_phase2_recommended.py`** — contrairement à EXCES-H4/pyramidalisation-régime (aucune dépendance nouvelle), cette correction exige le contexte D1 que `recommended.py` ne charge jamais par construction (sa fonction documentée est justement de mesurer l'effet de l'ABSENCE des règles littérales de `faithful.py`) ; lui ajouter D1 élargirait son périmètre plutôt que de le laisser comme référence stable. Tests dédiés ajoutés dans `test_backtest_phase2_faithful.py` (2 nouveaux) et `test_unified_protocol.py` (2 nouveaux), scénario synthétique à vérité terrain connue, 19/19 fichiers de test verts.

**Impact chiffré honnête, régénéré (mouvement modeste, cohérent avec l'ampleur des corrections précédentes)** :
- `faithful.py`, BNB/TRES_AGRESSIF : 373→310 trades, retour +76,5%→+68,3%, drawdown -17,6%→-15,6% (amélioré).
- `unified_protocol.py`, BNB/TRES_AGRESSIF (agrégé) : retour +27,9%→+22,0%, drawdown -27,0%→-25,2% (amélioré). Aucune combinaison ne devient catastrophique.
- Walk-forward `faithful.py` : pire drawdown annuel -8,1%→-8,2%, pire retour annuel -3,3%→-3,7% (ETH 2021 dans les deux cas) — toujours aucune année catastrophique sur les 4 actifs.
- Walk-forward `unified_protocol.py`, BNB/TRES_AGRESSIF : 2021 passe de +6,1%/-8,8% à +5,2%/-9,5% ; 2024 (toujours la pire année) s'améliore légèrement, de -6,2%/-24,4% à -4,0%/-22,6%.
- Canal manuel D1 vs EMA±ATR D1 (`backtest_phase2_faithful_manual_channel.py`, hérite automatiquement du fix) : retour moyen 9,91%→8,78% (manuel) / 4,02%→3,42% (EMA±ATR), drawdown moyen -6,21%→-6,05% / -3,58%→-3,24% — conclusion qualitative inchangée.
- Risque agrégé (`risk_aggregation_triple_system.py`) : maximum 17,00% inchangé (le pic survient pendant une phase déjà TENDANCE, non affectée par ce fix), 67/112→66/112 combinaisons dépassant le plafond 5%.

### 4e application (cycle suivant, même question "que ferait un ingénieur senior directeur de ce projet à présent ?")

3 agents lancés en parallèle : (1) vérification adversariale de l'implémentation Conflit MTF (qui n'avait, contrairement à EXCES-H4, pas eu de second regard indépendant), (2) diff systématique v6↔v7 exhaustif (piste explicitement laissée ouverte au cycle précédent : "un audit dédié de tout ce que v6 faisait de plus fin que v7... pas encore menée exhaustivement"), (3) bilan directeur "regard neuf" — pas une recherche de bug de plus, la question "ce projet est-il prêt pour la Phase 3 ?" et les angles morts HORS fidélité-corpus.

- **(2) Diff v6↔v7 : fermé, rien de nouveau.** Diff textuel complet des deux fichiers, `prepare()` byte-identique, et les deux seuls axes de distinction que v6 faisait (EXCES natif, pyramidalisation réservée au régime) sont exactement les occurrences #3/#4 déjà trouvées et corrigées — confirmé aussi par le contrat documenté de `make_open_tranche_fn` (factory partagée par les 9 moteurs). Résultat négatif crédible, méthode exhaustive plutôt qu'une relecture partielle.
- **(3) Bilan directeur : plusieurs trouvailles concrètes, toutes vérifiées et traitées.**
  - **La section "Critères de sortie Phase 2 → Phase 3" elle-même était restée figée** depuis sa rédaction, alors que 5 des plus gros chantiers du projet sont arrivés après — réécrite (cf. section dédiée ci-dessous).
  - **`risk_aggregation_triple_system.py` (LE chiffre le plus cité comme preuve du risque non plafonné) n'avait AUCUN test**, malgré 3 resynchronisations manuelles déjà nécessaires (EXCES-H4, pyramidalisation-régime, Conflit MTF) — angle mort à haute probabilité de dérive future, haute impact, haut coût de découverte tardive (silencieux). **Corrigé** : `code/test_risk_aggregation_triple_system.py` (4/4 tests) — garde-fou anti-dérive direct : le gate dupliqué doit produire EXACTEMENT le même nombre de clôtures que le VRAI `unified_protocol.run_unified`/`diversification.run_diversified` sur données réelles, pas seulement sur un scénario synthétique.
  - **`decide_now()` (l'interface opérationnelle réelle de la Phase 3) ne signalait AUCUNE information sur le risque agrégé**, alors que le projet le chiffre lui-même à 17% contre un plafond 5% documenté — un humain qui suit `decide_now()` à la lettre n'avait aucun signal avant de calculer l'exposition à la main. **Corrigé** : nouveau champ `aggregate_risk_warning` (risque RANGE réellement engagé, formule R1 déjà éprouvée, warning INFORMATIF — jamais un plafond appliqué, cohérent avec le principe "aucune règle sans citation" du corpus), testé (2 tests à vérité terrain connue dans `test_unified_protocol.py`).
  - **2 incohérences documentaires trouvées et corrigées** : `COUVERTURE_ENSEIGNEMENTS.md` citait encore "68/112" (stale, 2 corrections en retard) pour le risque agrégé — corrigé à 66/112 ; `CONFIGURATION_RECOMMANDEE.md` affirmait "15e moteur" pour `unified_protocol.py` (contredisant `STATUS.md`/`PLAN.md`) — corrigé à 14e (vérifié directement dans `run_all.py::ENGINES`, `faithful.py` est bien le 15e). Le Fibonacci manuel-littéral, correctement documenté en prose ailleurs, était absent de la table "❌ Jamais implémenté" de `COUVERTURE_ENSEIGNEMENTS.md` — ajouté.
  - **Garde-fou mécanique anti-automatisation-prématurée** (déjà noté "angle mort assumé" dans ce document) : confirmé non encore matérialisé (aucun code de ce projet ne contacte une API d'exchange), mais le bilan recommande de le construire AVANT que `decide_now()` ne devienne assez pratique pour qu'un humain soit tenté de l'automatiser lui-même, pas seulement "avant la Phase 4/5" — coût de le construire maintenant jugé trivial. **Pas encore fait ce cycle** (hors scope immédiat), conservé comme recommandation explicite plutôt que ré-enterré.
  - **Chantiers "clos" revérifiés** (canal manuel D1, gate Andrews Pitchfork, diversification/Cluster Technique) : conclusions toutes confirmées tenir encore, rien à rouvrir.
- **(1) Vérification adversariale Conflit MTF** : lancée en parallèle des deux autres, interrompue une première fois par un rate-limit d'infrastructure (même incident déjà documenté dans ce projet, cf. section "Incident vague 2" plus bas), relancée intégralement sur instruction directe de l'utilisateur ("reprends le travail relance les agents nécessaires"). **Tous les chiffres déjà rapportés confirmés par rejeu indépendant (19/19 tests, causalité vérifiée sans lookahead), sauf un, trouvé et corrigé** : `code/oos_xrp_faithful.py` (résultat cité "3 trades") ne reproduisait plus (0 trade avec le code actuel) — cause isolée et vérifiée : le rôle "D1" de ce moteur (stop UT+1, ici tenu par l'Hebdomadaire XRP dans cette procédure décimée) fait désormais aussi l'objet du check Conflit MTF, sans condition (comme le stop lui-même, `use_mtf_gate=False` ne le neutralise pas). **Pas un bug** — une règle littérale appliquée sans exception, même sur un échantillon déjà trop mince pour conclure — corrigé dans la documentation (CSV régénéré, `CONFIGURATION_RECOMMANDEE.md`/`STATUS.md` mis à jour), aucun changement de code nécessaire. **Second point relevé, réel et non documenté jusqu'ici** : `gate()` alimente aussi `gated_long_signal`, qui pilote la sortie "flip de signal" de `process_tranche` — Conflit MTF peut donc aussi FERMER une tranche déjà ouverte mais pas encore Validée si `regime_d1` bascule en range en cours de vie, pas seulement bloquer une ouverture (mécanisme préexistant pour EXCES-H4/UT+2, hérité silencieusement) — documenté désormais explicitement dans les deux fichiers concernés. Décision de conception (niveau D1, application uniforme) confirmée défendable après relecture indépendante des sources.
- **Investigation annexe, pas un bug** : en construisant le test de garde-fou anti-dérive pour `risk_aggregation_triple_system.py`, un écart de comptage (102 vs 104 trades) est apparu entre `faithful.py` seul et `unified_protocol.py` sur la même donnée réelle — investigué à fond (isolé sur l'ancien commit CONSOLIDATION pour confirmer que ce n'était pas une régression de ce cycle, reproduit avec une réimplémentation indépendante de la boucle) avant de conclure : **fausse alerte**, `run_unified()["n_trades"]` compte les clôtures RANGE ET TENDANCE combinées (102 range + 2 trend = 104), alors que `faithful.py` ne compte que le RANGE seul — une différence de définition de métrique, pas un bug de gate. Documenté ici pour la traçabilité de la vérification, aucun code changé.

### 5e application (cycle suivant) — "reste-t-il de l'IP de Philippe non implémentée ?"

3 agents lancés en parallèle, chacun couvrant un tiers du corpus (manuel + sources 1-6, sources 7-12, sources 13-17 + un balayage exhaustif de `TRADING_LESSONS_INDEX.md` pour toute remarque "à réconcilier"/"distinct" jamais reprise — exactement la méthode qui avait révélé Conflit MTF), consigne explicite de ne PAS se fier aux classifications déjà écrites dans `COUVERTURE_ENSEIGNEMENTS.md`/`PLAN.md` et de vérifier chaque règle directement dans le code. **Réponse honnête : oui, ~18 éléments supplémentaires trouvés**, classés A (probablement pas reproductible, dépend de l'algorithme PRO non divulgué)/B (littéral, faible coût)/C (littéral mais exige une décision de conception) — détail complet, citations et preuves : `COUVERTURE_ENSEIGNEMENTS.md` section dédiée, backlog item 11 ci-dessus. Aucune implémentation faite ce round (volume trop important pour une décision unilatérale sans priorisation) — documentation complète produite à la place, cohérent avec le principe "jamais un trou silencieux". Le reste des 17 sources + le manuel a été vérifié directement dans le code par les 3 agents et confirmé correctement classé — pas de nouvelle divergence au-delà de la liste ci-dessus.

### 6e application (cycle suivant, "continue comme l'aurait fait un ingénieur senior directeur du projet") — catégorie B restante + vérification adversariale du round précédent

3 agents lancés en parallèle : (1) implémentation "Stop Loss = taille du canal" (catégorie B point ii), (2) sensibilité `MIN_BORDERS` 3 vs 4 (catégorie B point iii), (3) vérification adversariale à froid du commit qui venait d'être poussé (Règle des 50%, 6e application précédente).

**(3) Vérification adversariale — un vrai bug trouvé et corrigé, pas seulement des trous documentaires.** `classify_regle_50` ne plafonnait pas le retracement par le haut, supprimant de fait le Red Flag #10 que `fibonacci.py` documente lui-même — la règle "complète" était en réalité PLUS LARGE que `fib_favorable` en nombre de bougies (2642 vs 2388 sur BTC H4), contredisant l'affirmation "beaucoup plus restrictive" du commit d'origine. Corrigé (plafond `FAVORABLE_MAX` ajouté, `fib_regle_50` est désormais un sous-ensemble STRICT vérifié de `fib_favorable`), plus 2 erreurs de chiffres dans les docs (80-167→**80-137** pour `fib_favorable` ; "15/16 dégradés" corrigé en "15/16 rendement absolu négatif, 16/16 dégradé vs baseline") et 2 imprécisions de citation (la référence à `backtest_phase2_v7.py::prepare` était fausse — seul `trend_table.py::add_trend_context`/`accum_retracement_frac` correspond bit-à-bit). La tension avec l'hypothèse H7 de `trend_table.py`, présentée initialement comme "phrase distincte", est en réalité une justification fragile — désormais tracée dans `trend_table.py` lui-même à côté de H7 (H7 non modifiée). Détail complet : `code/fibonacci.py` MISE À JOUR de tête, `code/trend_table.py` note H7, `test_fibonacci.py` (8/8). **Confirmé sans bug** : exactitude mathématique de `compute_context_position` (recalcul manuel indépendant), non-régression bit-à-bit des 4 variantes pré-existantes (rejeu réel avant/après, pas lecture de code), reproductibilité du CSV, edge cases (canal plat, colonne `date` manquante → erreur claire).

**(2) Sensibilité `MIN_BORDERS` — trouvaille plus significative que la question posée.** Grille 2/3/4/5 rejouée sur `run_v7`/`trend_table` × BTC/ETH/BNB/SOL × 4 profils (moteurs de production réels, overridés temporairement via un context manager, pas dupliqués) : **0,0 pt d'écart sur les 32 couples testés, quel que soit le seuil de la grille**. Diagnostic dédié (mesure de la population réellement soumise au gate, PAS présentée comme une preuve de robustesse sans elle) : `n_borders` (swings bas confirmés sur une fenêtre glissante de 15 jours, médiane 9, minimum 5 chez les bougies candidates) est structurellement toujours ≥5 — le premier seuil qui "mordrait" est 6, pas 3 ou 4. **Le gate de maturité est INERTE, ce n'est pas une preuve que MIN_BORDERS=3 est le bon choix** : la grandeur calculée par le code (comptage sur fenêtre glissante fixe) et celle décrite par le corpus (points de contact d'une structure de range identifiée) ne sont pas au même ordre de grandeur. Relecture complète des sources (pas seulement la citation de départ) : le corpus est réellement partagé entre "min 4 bornes" (`TRADING_LESSONS_ZONE_ACCUMULATION.md`, `PULLBACK_MATURITE.md`) et "min 3 bornes" (`BREAKOUT_RATIO11.md`, `STRUCTURES_ALTERATIONS.md`), `RULES_EXTRACTION.md` §1 parlant lui d'un MAXIMUM (pas un minimum) — trouvaille qui réconcilie les deux camps : `TRADING_LESSONS_MTF_SUIVI_TENDANCE.md` décrit la 3e et la 4e borne comme deux ÉTAPES successives d'une même séquence, pas deux valeurs concurrentes du même seuil. **Décision de conception non tranchée ici** (catégorie C, même discipline que Fibonacci RANGE/Conflit MTF) : recalibrer `n_borders` sur une structure de range identifiée plutôt qu'une fenêtre glissante fixe (seul point qui rendrait "3 vs 4" à nouveau mesurable), ou lire le corpus comme décrivant 2 gates distincts plutôt qu'1 seuil unique. Aucun moteur de production modifié (script de mesure séparé `code/min_borders_sensitivity.py`, même discipline que `cluster_technique_threshold_robustness.py`), 9 nouveaux tests (`test_min_borders_sensitivity.py`, 9/9, dont un qui verrouille la trouvaille "gate inerte" et échouera volontairement si `n_borders` change un jour). **Trouvaille annexe** : `phase2_v7_mtf_results.csv` (et `MTF_CROSS_VALIDATION_H4_D1.md` qui le cite) étaient périmés depuis les corrections EXCES-H4/Conflit MTF (BTC 504→492 trades) — régénérés et refresh honnête fait (retour causal confirmé, ampleur légèrement modifiée, conclusions qualitatives inchangées).

**(1) "Stop Loss = taille du canal"** : interrompu par un rate-limit (incident déjà connu, "Incident vague 2") en plein cœur de l'implémentation — relancé (même agent, contexte préservé) après diagnostic d'un `KeyError` bloquant trouvé en vérifiant son worktree avant relance. Terminé, intégré et vérifié indépendamment (recalcul manuel du mécanisme des deux "/2", non-régression bit-à-bit des 9 moteurs existants avec la règle désactivée, delta moyen -2,6 pts recalculé indépendamment depuis les CSV avant/après) — détail complet, citation exacte, 4 hypothèses H-Canal-Large-1..4 et résultat chiffré honnête : backlog item 11 catégorie B ci-dessus.

### 7e application (cycle suivant, "continue l'implémentation de l'IP, un ingénieur senior directeur") — catégorie C prioritaire investiguée, PAS implémentée

Catégorie B soldée (3/3), passage à la catégorie C — l'item signalé comme le plus prioritaire par le round précédent : **"amplitude Validation/Confirmation jamais réellement cross-timeframe"**. Discipline "design avant code" appliquée strictement : 2 agents de conception INDÉPENDANTS (même méthode que "recherche + second avis indépendant" pour Conflit MTF), ne codant rien, chacun relisant le corpus de zéro sans se fier à la classification déjà écrite. **Verdict du directeur après arbitrage des deux rapports, vérifié personnellement par citation exacte avant de trancher (`grep` direct dans `RULES_EXTRACTION.md`/`TRADING_LESSONS_BREAKOUT_RATIO11.md`/`TRADING_LESSONS_MTF_SUIVI_TENDANCE.md`, pas une confiance aveugle dans les rapports) : NE PAS IMPLÉMENTER l'item tel que formulé — décision prise et documentée, pas un abandon silencieux.**

**Ce que les deux agents ont trouvé, convergent sur ce point précis** : la citation de départ (« UT+1 = Validation, UT+2 = Confirmation, tous deux explicitement prescrits ») est **partiellement fausse**. `TRADING_LESSONS_BREAKOUT_RATIO11.md` lignes 7-9, vérifié mot pour mot : *"Stop-loss = ... canal de tendance de **l'UT+1**"* (ligne 7) ; *"Phase de Validation ... amplitude du range d'accumulation local **(UT)**"* (ligne 8, PAS UT+1) ; *"Phase de Confirmation ... utiliser **l'UT+2**"* (ligne 9). Confirmé par la source elle-même, ligne 19 : *"UT+1 sert au positionnement du stop, UT+2 sert à la confirmation/déblocage du breakeven."* **UT+1 est le rôle du STOP, déjà implémenté sans condition** (`ctx_support_d1`, `backtest_phase2_faithful.py`) — pas de la Validation. `local_range` (Validation) est donc déjà au bon niveau (UT locale = H4) ; seule `context_range` (Confirmation) a un rôle UT+2 littéral. Le périmètre réel de l'item était donc moitié moins large que documenté — corrigé ci-dessous et dans `COUVERTURE_ENSEIGNEMENTS.md`.

**Pourquoi la moitié restante (Confirmation = amplitude UT+2 réelle) n'est PAS implémentée, 3 raisons convergentes, chacune vérifiée personnellement** :
1. **Le corpus lui-même ne budgète que 2 UT pour la table RANGE** (celle que `position_engine.py` implémente) : `TRADING_LESSONS_MTF_SUIVI_TENDANCE.md:13`, vérifié — *"Unités de temps | 2 UT (ex: Daily/H4) [Range] | 3 UT impératives [Tendance]"*. Introduire une vraie dépendance UT+2 (Hebdomadaire) dans la machine à états RANGE contredit frontalement cette ligne — la 3e UT est réservée à la table TENDANCE (`trend_table.py`), qui n'utilise d'ailleurs pas `local_range`/`context_range` (vérifié par grep, elle a ses propres `ctx_high`/`ctx_low`/`local_high`).
2. **Le corpus est lui-même partagé sur ce qu'est "Confirmation"** : `RULES_EXTRACTION.md` §3 — LE manuel officiel, la source la plus autoritaire du projet — dit *"Confirmation (médiane canal contexte, **clôturée**)"* (vérifié, ligne 41), un NIVEAU STRUCTUREL ABSOLU confirmé par clôture, pas une distance projetée depuis l'entrée. 4 sources sur 6 (le manuel + `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md`/`PULLBACK_MATURITE.md`/`CLUSTERS_PRIX.md`) décrivent cette même lecture structurelle ; seules 2 sources (`BREAKOUT_RATIO11.md`/`PYRAMIDALISATION.md`) décrivent l'amplitude/Ratio 1:1 actuellement codée. `PULLBACK_MATURITE.md:11` réconcilie même les deux vocabulaires lui-même : *"Même intitulé exact... reformulé en termes de prix plutôt qu'en unités de temps"* (vérifié) — un indice fort que "UT+2" désigne QUEL canal on projette (celui de "contexte"), pas une prescription de rééchantillonner l'OHLC.
3. **Toute lecture littérale avec de vraies bougies Hebdomadaires met en péril deux éléments du corpus déjà mieux établis que celui-ci** : (a) le passage au breakeven à la Confirmation est *"la correction la mieux étayée de tout le corpus"* (`TRADING_LESSONS_INDEX.md`, confiance très élevée, 2 sources concordantes) — une cible Confirmation repoussée à +46-88% du prix (mesuré par un agent, fenêtre Hebdo × 15 bougies) la rend quasi inatteignable, neutralisant de fait cette correction ; (b) `oos_xrp_faithful.py` (la seule validation hors-échantillon du moteur phare) n'a que 13 barres Mensuelles disponibles — une fenêtre Hebdo de profondeur comparable à l'actuelle produirait 100% de NaN sur cette validation, la détruisant. Sacrifier deux éléments déjà mieux établis pour en implémenter un troisième plus ambigu n'est pas un calcul de fidélité au corpus, c'est le perdre en moyenne.

**Argument complémentaire (pas décisif seul, mais qui aurait dû faire hésiter sans les 3 raisons ci-dessus)** : au niveau D1 (UT+1), "rendre cross-timeframe" est mesuré comme un quasi no-op — `max(high)-min(low)` sur une fenêtre calendaire est invariant par agrégation de bougies (ratio médian 1,00 sur les 4 actifs, mesuré indépendamment par les 2 agents). Contrairement au stop `EMA±ATR` (path-dépendant, chantier D1 substantiel), cette grandeur particulière ne gagnait presque rien à devenir "vraiment" cross-timeframe au niveau où le corpus la place le plus clairement.

**Décision** : `COUVERTURE_ENSEIGNEMENTS.md`/`PLAN.md` corrigés (citation UT+1 = stop, pas Validation). Item PAS implémenté — **requalifié** en un nouvel item catégorie C, plus précis et mieux étayé par le manuel officiel : *"Confirmation comme niveau structurel absolu (médiane du canal de contexte, clôturée) plutôt qu'une distance `entry + context_range` figée à l'entrée"* — hors périmètre de ce cycle (changement de mécanisme plus profond que ce round, `position_engine.py` ne recalcule aujourd'hui aucun seuil après l'ouverture ; un niveau absolu lu en direct exigerait une mécanique proche de `ctx_resistance`/`ctx_high` de `trend_table.py`, pas un simple changement d'appelant). Portée réelle si un jour traité (grep vérifié par les 2 agents, cohérent) : 13 appelants directs de `make_open_tranche_fn` + 5 copies inline non refactorées + ~9 wrappers, ~27 fichiers au total, ~30-35 CSV à régénérer — comparable en surface au chantier stop D1, mais sans le début de citation solide dont ce dernier bénéficiait. Aucun code modifié ce cycle, aucun commit de code — uniquement la correction documentaire et la requalification de l'item, cf. `COUVERTURE_ENSEIGNEMENTS.md` pour le détail complet des deux rapports de conception.

### 8e application (cycle suivant) — catégorie C, "définition stricte de la 3ème borne par clôtures (pas mèches)" : investiguée, PAS implémentée, item requalifié en deux items nouveaux

Item suivant de la catégorie C, signalé par l'audit du 5e round comme ayant un "blast radius large (utilisé par `n_borders` partout)". Même discipline qu'au 7e round : relecture intégrale de la source, grep des 17 sources + du manuel sur `borne`/`clôtur`/`mèche`/`wick`, lecture complète du mécanisme existant, mesure empirique AVANT toute décision, et refus de se fier à la citation fournie en tête de tâche. **Verdict : NE PAS IMPLÉMENTER — l'énoncé de l'item était une erreur de catégorie. Aucun code modifié, 157/157 tests inchangés.**

**Citation exacte relue (`TRADING_LESSONS_TROISIEME_BORNE.md` lignes 20-23)** — *"Définition stricte d'une 3ème borne valide (3 critères séquentiels, aucune approximation acceptée)"* : (1) *"Mouvement tendanciel : flux directionnel puissant et sans ambiguïté"* ; (2) *"Cassure du contexte : le prix doit rompre la structure de prix, validée par des **clôtures de bougies au-delà du contexte** (pas un simple dépassement intra-bougie)"* ; (3) *"Le Pullback : retour tester la zone rompue, zone d'intervention à **Fibonacci 61,8%**, uniquement si le prix maintient ses clôtures sous/sur le contexte — un simple effleurement sans tenue des clôtures = invalidation technique"*. Aucun seuil ni chiffre supplémentaire que le résumé aurait manqué : le seul chiffre est 61,8%, déjà implémenté (`fibonacci.py::FAVORABLE_MAX`, verrouillé par `test_fibonacci.py` qui cite la source #11 nommément).

**L'erreur de catégorie** : les deux clauses "clôtures" de la source sont des **prédicats de validation appliqués à un niveau déjà tracé** — le prix casse-t-il le contexte ? le pullback tient-il ses clôtures ? — et **jamais une règle de localisation d'un point de swing**. La source ne dit pas un mot de la façon dont le prix d'une borne est déterminé. L'item transposait une règle de validation en règle de détection ; rien dans le corpus n'autorise cette transposition. Les 3 critères sont d'ailleurs **déjà couverts, chacun à sa place** : critère 2 = `trend_table.py::breakout_raw` (`c[i-1] > local_high_v[i-1]` : niveau tracé sur le max glissant des HAUTS, cassure testée sur la CLÔTURE) et `excess_raw` (`c[i] > ctx_high_v[i-1]`) ; critère 3 = niveau 61,8% de `fibonacci.py` + déclenchements Validation/Confirmation/Limite sur clôture (`PHASE2_CORRECTION_CLOSES.md`) ; l'hypothèse H5 de `trend_table.py` code même la combinaison explicite des deux conventions (*"la mèche basse touche/dépasse `ctx_support` ET la clôture referme au-dessus"*).

**3 sources contredisent frontalement une détection de bornes par clôtures** (trouvées par relecture, pas dans la citation de départ) : `TRADING_LESSONS_CLUSTERS_PRIX.md:30` (#16) — *"remonté sous le dernier creux structurel (**bas de clôture ou mèche**)"*, le corpus refuse explicitement de trancher ; `TRADING_LESSONS_ZONE_ACCUMULATION.md:38` (#10) — *"sous le **point bas** de la 4ème borne"*, une borne a un extrême sous lequel on place le stop ; `TRADING_LESSONS_STRUCTURES_ALTERATIONS.md:28` (#14) — *"zone de tolérance, pas une ligne mathématique — les mèches peuvent pénétrer l'ancien territoire, mais les clôtures de bougies doivent rester à l'extérieur pour valider la structure"*, l'énoncé le plus net de la convention réelle et c'est l'INVERSE de l'item (le niveau vient des extrêmes, les clôtures sont le test ; cf. aussi `BREAKOUT_RATIO11.md:27`, *"3 bornes **testées**"*). Nuance honnête en sens inverse, pour ne pas surinterpréter : `BREAKOUT_RATIO11.md:7` définit bien un niveau par des clôtures (*"Stop-loss = clôture la plus basse ... du canal de tendance de l'UT+1"*) — seul cas du corpus, et il concerne le stop UT+1, pas une borne. **Contradiction mécanique induite** : une borne est aussi un PRIX (`compute_ascending_lows` compare `low_v[i - SWING_ORDER]`, `fibonacci.py` s'en sert au dénominateur, le corpus place le stop "sous le point bas") — détecter par clôtures impose un stop À L'INTÉRIEUR de la mèche, contre la convention déjà écrite du projet (`position_engine.py:87-90`, *"stop touché sur la MÈCHE (ordre réel intrabar) ; cible atteinte sur la CLÔTURE"*).

**Mesure empirique (exigée, pas supposée — BTC/ETH/BNB/SOL, H4 et D1, tout l'historique)** : fréquence de détection quasi inchangée (swing lows H4 1476→1461 BTC, 1444→1448 ETH, 1413→1409 BNB, 1259→1286 SOL ; -1,0% à +2,1%) **mais seulement ~40% des barres détectées sont les mêmes** (582/~1470 BTC H4) — le changement déplace les bornes sans en changer le nombre. **Le blast radius annoncé par l'item est inversé** : (a) sur `n_borders` (le consommateur que l'item désignait), aucun effet utile — médiane 9,0→9,0, minimum chez les candidates 5→5/4/6/5, premier seuil mordant 6→6/5/7/6, **le gate `MIN_BORDERS=3` reste INERTE dans les 8 configurations** (cohérent avec la trouvaille du 6e round) ; (b) sur `compute_ascending_lows` → `structure_favorable` → `score` de `proxy_v2` (que l'item ne désignait pas), effet silencieux réel : `structure_favorable` change sur **10,7-14,9%** des barres, le signal d'entrée `score >= 2` basculerait sur **5,6-8,1%** des barres, sur les 8 couples actif × TF. Portée réelle si un jour traité (grep exhaustif, pas une estimation) : **10 fichiers avec un vrai site d'appel** de `compute_swing_low_confirmed`/`compute_swing_high_confirmed`, **5 producteurs** de `n_borders`, **30 fichiers `code/*.py`** le mentionnant (dont 6 de tests), ~30-35 CSV sur 63 à régénérer.

**Décision** : item **retiré** de la catégorie C — ce n'est pas un manque, c'est déjà couvert ailleurs et correctement. Sacrifier 3 sources concordantes et une convention déjà codée pour satisfaire une lecture qu'aucune source n'énonce serait exactement le risque de méthode que ce projet refuse (même arbitrage qu'au 7e round). **Deux items nouveaux, mieux étayés, trouvés par la relecture et ajoutés à sa place** :
1. **"Règle d'or : calculs sur clôtures, jamais sur les mèches" (#15) vs `local_range`/`context_range` en mèches** — `TRADING_LESSONS_PYRAMIDALISATION.md:20` en fait un **titre de section** : *"## Patterns de pyramidalisation (règle d'or : calculs sur clôtures, jamais sur les mèches)"*, précisé ligne 22 (*"validation au ratio 1:1 (**report de l'amplitude du range en clôture**)"*) et ligne 39 (*"ratios de validation en clôture"*). C'est l'énoncé le plus littéral et le plus général du corpus sur clôtures vs mèches, et il porte sur les **amplitudes projetées** — or `backtest_phase2_v7.py::prepare` calcule `local_range`/`context_range` en `max(high) - min(low)`, projetés tels quels en `val_px`/`conf_px`/`lim_px` (`position_engine.py:409-411`). `PHASE2_CORRECTION_CLOSES.md` avait corrigé les *déclenchements* et laissé les *amplitudes* en mèches : incohérence interne jamais relevée. **Mesuré** : une amplitude en clôtures vaut 0,786-0,808 de celle en mèches sur `local_range` H4 (-20%), 0,862-0,876 sur `context_range` H4 (-13%), 0,552-0,578 / 0,720-0,752 en D1 (-45% / -27%) — pas un no-op, contrairement à l'item écarté. Décision de conception requise (le corpus ne précise pas si "amplitude du range" désigne la fenêtre glissante ou une structure identifiée) ; portée = les 13 appelants de `make_open_tranche_fn` + copies inline, ~30-35 CSV. **Recommandé comme prochain item de la catégorie C** : seul item "clôtures vs mèches" du corpus à la fois littéral, non ambigu sur le principe, et non couvert par le code.
2. **`fibonacci.py::compute_retracement` est hybride, non documenté comme tel** — `(swing_high - close) / (swing_high - swing_low)` : numérateur en clôture (conforme), dénominateur en mèches. Même tension, en plus petit ; à trancher avec l'item 1, pas séparément.

**Trouvaille annexe — une citation partiellement fausse, même classe d'erreur qu'au 7e round.** `PHASE2_CORRECTION_CLOSES.md` attribuait la règle des clôtures à *"3 sources indépendantes (#9, #11, #14)"* avec la glose *"(considérées comme du bruit)"*. Vérifié par grep : **#9 (`TRADING_LESSONS_MTF_SUIVI_TENDANCE.md`) ne dit rien sur clôtures vs mèches** (0 occurrence), et la glose *"considérées comme du bruit"* n'existe nulle part dans le corpus appliquée aux mèches (la seule occurrence proche, `TROISIEME_BORNE.md:18`, qualifie les bornes de l'UT de tendance, pas les mèches). Sources réelles rétablies dans le document : #11, #14, #10, #13 et surtout **#15, jamais citée alors que c'est l'énoncé le plus littéral**. La correction appliquée à l'époque reste bien fondée — seule son attribution était fausse.

### 9e application (cycle suivant, catégorie C) — "Red Flags d'invalidation précoce" investigués, PAS implémentés

Item suivant de la catégorie C : **"Red Flags d'invalidation précoce"**, cité par `COUVERTURE_ENSEIGNEMENTS.md` comme *"pas de mécanisme distinct du stop de protection standard"*. Même discipline que la 7e application (relecture de zéro, aucune confiance dans la classification déjà écrite, mesure sur les VRAIS moteurs avant de trancher). **Verdict : NE PAS IMPLÉMENTER — décision prise et documentée, pas un abandon silencieux.** Trois raisons distinctes, une par Red Flag, plus une correction de la prémisse de l'item lui-même.

**Citation exacte, vérifiée mot pour mot (`TRADING_LESSONS_ZONE_ACCUMULATION.md` lignes 44-47) — il y en a exactement 3, pas plus** :

    ## Signaux d'invalidation précoce ("Red Flags")
    - Clôtures du contexte sous la MA20
    - Retracement profond : clôture maintenue au-delà de 61% de la structure
    - Retour au contexte opposé : atteinte de la borne extérieure opposée du range initial

**Correction de la prémisse de l'item (comme en 7e application, la formulation de départ était partiellement fausse)** : *"pas de mécanisme distinct du stop de protection standard"* est INEXACT. `position_engine.py::process_tranche` a **deux** voies de sortie distinctes, pas une — (2) l'Invalidation/stop sur la mèche, et (3) la **sortie "flip de signal" AVANT toute étape** (`not long_signal_prev and not val_done and not conf_done`), qui est littéralement un mécanisme d'invalidation PRÉCOCE : elle ne s'applique qu'avant la Validation, et elle est déjà le canal par lequel les règles d'abstention du corpus (EXCES-H4, gate UT+2 Hebdomadaire, Conflit MTF D1) FERMENT une tranche déjà ouverte — comportement déjà documenté explicitement dans `backtest_phase2_faithful.py` (« `gate()` alimente aussi `gated_long_signal`, qui pilote la sortie "flip de signal" … cette tranche est FERMÉE au pas suivant »). **Mesuré sur les 4 388 trades de `faithful.py` (BTC/ETH/BNB/SOL × 4 profils, historique complet)** : la répartition réelle des sorties est **FLIP 4 116 (93,8%) / LIMITE 168 (3,8%) / STOP 104 (2,4%)** — l'invalidation précoce n'est pas absente du projet, elle est déjà la voie de sortie DOMINANTE. Si les Red Flags devaient un jour être implémentés, l'endroit architecturalement juste est ce canal-là (`gate()`), pas un nouveau mécanisme.

**Red Flag 3 ("Retour au contexte opposé") — REDONDANT, et prouvé NON PRÉCOCE, mesuré.** C'est mot pour mot la même règle que la ligne 28 du MÊME fichier (*"si le prix retourne tester le contexte opposé (borne extrême du range initial), l'accumulation est invalidée"*) et que son critère de maturité ligne 26 (*"sans jamais atteindre le contexte opposé"*) — une seule règle énoncée trois fois dans une seule source, pas trois règles. Or le stop RANGE est déjà placé EXACTEMENT à ce niveau : `stop_price = min(ctx_support_v[j], entry*0.999)` avec `ctx_support_d1` = borne basse du canal de contexte D1 (UT+1), déclenché sur la MÈCHE — soit exactement la définition de « atteinte de la borne extérieure opposée ». Vérifié empiriquement, pas déduit : sur 4 388 trades, la branche `entry*0.999` du `min()` ne mord **jamais** (0/4 388), donc le stop EST le niveau du Red Flag 3 ; ce niveau est atteint par 24 trades seulement (0,5%), et dans **24/24 cas la bougie de déclenchement du Red Flag EST la bougie de sortie réelle** (avance = 0). **0 / 4 388 trades sortiraient plus tôt.** Ajouter le Red Flag 3 serait un doublon bit-à-bit du stop. (Détail annexe utile : 80 des 104 sorties au stop sont des stops remontés au BREAK-EVEN à la Confirmation, pas des cassures du canal de contexte.) La seule variante qui sortirait vraiment plus tôt est un `ctx_support` relu EN DIRECT à chaque bougie (1,0% des trades, avance médiane 96 bougies) — mais c'est un stop suiveur sur la bande de contexte, ce qui contredit frontalement la règle la mieux étayée de tout le corpus (interdiction de resserrer/passer au break-even avant la Confirmation, sources #12/#13/#15/#16 nommées, `TRADING_LESSONS_CLUSTERS_PRIX.md` s'annonçant lui-même comme la « 5ᵉ/6ᵉ confirmation », `TRADING_LESSONS_INDEX.md` : *"la correction la mieux étayée de tout le corpus"*). Même schéma de raisonnement que la 7e application : ne pas sacrifier un élément mieux établi pour en implémenter un plus ambigu.

**Red Flag 2 ("clôture maintenue au-delà de 61%") — DÉJÀ IMPLÉMENTÉ, deux fois, comme plafond d'ENTRÉE.** `trend_table.py::ACCUM_RETRACEMENT_HIGH = 0.61` (gate de l'étape Accumulation, le 61% littéral de `RULES_EXTRACTION.md` §1) et `fibonacci.py::FAVORABLE_MAX = 0.618`, dont le commentaire cite déjà ce Red Flag nommément (*"Au-delà de 61,8% (> FAVORABLE_MAX) = défavorable / invalidé (Red Flag explicite de #10)"*). C'est d'ailleurs la suppression involontaire de ce plafond qui avait été trouvée et corrigée au 6e round. En faire EN PLUS une condition de SORTIE serait un troisième usage du même chiffre — exactement le piège déjà identifié et documenté pour le 76% (*"le même chiffre 76% désigne un seuil d'ENTRÉE ici et une cible de SORTIE ailleurs — deux règles distinctes à ne jamais confondre"*), et ici c'est **pire** : le corpus utilise 61% comme un **minimum** d'entrée dans trois autres passages (`RULES_EXTRACTION.md` §1 *"Range tendanciel … Retracement ≥61,8%"* ; `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` §« entrée agressive possible dès **61%** Fibonacci » ; `TRADING_LESSONS_TROISIEME_BORNE.md` *"zone d'intervention à Fibonacci 61,8%"*) — même chiffre, inégalité INVERSE, référentiels différents. Ajouter une quatrième lecture du même nombre est un risque de confusion, pas un gain de fidélité. Blocage supplémentaire décisif : le mot *"maintenue"* n'est chiffré NULLE PART dans le corpus (aucun nombre de bougies) — l'implémenter exigerait d'inventer ce paramètre, exactement le motif pour lequel le gate Fibonacci RANGE §1 est refusé depuis 3 rounds. Mesuré quand même, pour ne pas décider à l'aveugle : déclenche avant la sortie réelle sur 8,0% des trades (1 clôture) / 5,5% (3 clôtures consécutives).

**Red Flag 1 ("Clôtures du contexte sous la MA20") — le SEUL non redondant, et le seul vrai apport de ce round : il a une 2e source, plus précise, que l'audit n'avait pas reliée.** `TRADING_LESSONS_CLUSTERS_PRIX.md:50` (source #16), section *"« Règle de Trois » (invalidation statistique)"* : *"**Clôtures sous MA20** : clôtures multiples et marginales sous la moyenne mobile signalent qu'elle n'exerce plus son rôle d'attractivité"*. Non implémenté, confirmé. **Mais 4 obstacles, dont 3 sont des décisions que le corpus ne tranche pas** : (1) **la conséquence n'est pas une sortie** — dans cette section de #16, les deux puces voisines énoncent leur conséquence explicitement, et c'est « prudence maximale » (puce 1, épuisement après 3 zones — **déjà implémentée**, `RULE3_STREAK=3`/`RULE3_SIZE_MULT=0.5`, risque divisé par deux) et *"arrêt des **nouveaux engagements**"* (puce 3, divergences) : par analogie interne à sa propre source, « Clôtures sous MA20 » est une abstention/réduction, pas la fermeture d'une position ouverte. (2) **Deux paramètres à inventer** : « multiples » (combien ?) et « marginales » (à quelle distance sous la MA ?) n'ont aucun chiffre dans tout le corpus — et mesuré, « multiples » ne mord presque pas (12,7% des trades à 1 clôture, 10,3% à 3 clôtures consécutives), donc le paramètre inventé travaillerait beaucoup pour presque rien. (3) **Les deux sources placent la MA20 sur des unités de temps DIFFÉRENTES** : #10 dit *"clôtures **du contexte**"* (UT supérieure, D1 ici), alors que le tableau de #16 qui introduit ce MA20 a pour *"Unité de Temps de Trading | **Horaire (H1)**"* (UT d'exécution) — et `cluster_technique.py` a déjà tranché dans le sens « UT d'exécution » pour SON usage. Conflit réel, non réconcilié par le corpus. (4) **Piège terminologique, exactement celui déjà documenté pour « Cluster technique »** : le projet contient DÉJÀ trois « moyennes 20 périodes » distinctes — `cluster_technique.py::compute_ma20_rebound` (MA20 de PRIX, UT d'exécution, signal d'ENTRÉE, exige la clôture AU-DESSUS), `trend_table.py` H9 (MA20 de **VOLUME**, confirmation de breakout) et celle de ce Red Flag (MA20 de prix, sur le CONTEXTE, clôture EN DESSOUS, invalidation). Trois règles, un seul nom. **Impact mesuré si implémenté quand même comme sortie** (pour ne pas décider à l'aveugle) : déclencherait avant la sortie réelle sur 8,3% des trades (1 clôture) / 6,9% (3 clôtures), avance médiane seulement **6 bougies H4** — il anticiperait surtout un flip déjà en route ; mais sur les 104 trades qui atteignent réellement le stop, il sortirait avant le stop dans 65% (1 clôture) / 54% (3 clôtures) des cas, avance médiane 103 à 214 bougies. Ce n'est donc PAS un no-op : c'est un vrai changement de comportement, piloté intégralement par deux paramètres inventés et un choix d'UT que le corpus ne tranche pas.

**Décision et requalification** : item Red Flags **fermé comme "3 Red Flags = 1 déjà implémenté (RF2, plafond d'entrée, deux fois) + 1 doublon bit-à-bit du stop et prouvé non précoce (RF3) + 1 réel mais bloqué par 2 paramètres inventés et un conflit d'UT entre ses 2 sources (RF1)"**, pas comme "mécanisme manquant". La prémisse *"pas de mécanisme distinct du stop"* est corrigée dans `COUVERTURE_ENSEIGNEMENTS.md` (le flip-avant-Validation est ce mécanisme, et il est la voie de sortie dominante à 93,8%). **Nouveau candidat catégorie C trouvé en chemin, ajouté au backlog** : *puce 3 de la « Règle de Trois » de #16 — "toute désynchronisation entre patterns ou cycles (Trend Sign Wave) doit entraîner un arrêt des nouveaux engagements"* — sur les 3 puces de cette section du manuel-vidéo, seule la puce 1 est implémentée ; la puce 3 est une règle d'abstention littérale, jamais reliée à `proxy_v2.py` (où un désaccord entre `momentum_favorable`/`cycle_favorable`/`structure_favorable` laisse encore passer `score >= 2`, donc n'arrête RIEN). Aucun changement de comportement ce cycle, aucun CSV régénéré (rien à régénérer : rien n'a changé numériquement) ; suite complète **157/157** avant et après. Seule modification de code : un bloc de commentaires en tête de `code/position_engine.py` (zéro comportement) pour qu'un futur lecteur de CE fichier voie que les Red Flags ont été examinés et pourquoi ils n'y sont pas — même discipline que la note H7 ajoutée dans `trend_table.py` au 6e round.

### 10e application (cycle suivant) — catégorie C : contrainte "espace libre" MTF avant breakout, IMPLÉMENTÉE après vérification que le motif de refus du round précédent s'inverse ici

Item de catégorie C suivant : **contrainte "espace libre" MTF avant un breakout**, cité sur LA MÊME SOURCE (`TRADING_LESSONS_BREAKOUT_RATIO11.md`) que l'item refusé au 7e round — donc traité avec la même méfiance envers la citation de départ (le round précédent avait trouvé la sienne partiellement fausse). Discipline identique : relecture intégrale de la source de zéro, vérification personnelle de chaque citation par `grep`/lecture directe, mesures de faisabilité AVANT toute ligne de code, décision explicite, puis implémentation.

**Verdict : citation de départ cette fois EXACTE, et le motif de refus du round précédent s'inverse en faveur de l'implémentation. IMPLÉMENTÉE, mesurée, résultat honnête mitigé à dégradé, gardée optionnelle.**

**1) Citation, vérifiée mot pour mot** — `TRADING_LESSONS_BREAKOUT_RATIO11.md` **ligne 17** :

> *"Contrainte Multi-Timeframe (MTF) : il est impératif de vérifier l'absence d'obstacles sur les unités de temps supérieures (UT+1 et UT+2). Le breakout doit disposer d'un 'rendement escompté' suffisant, c'est-à-dire d'un espace libre de toute structure majeure pour permettre l'épanouissement de la tendance."*

Contrairement à l'item du 7e round, la paraphrase de l'audit ("vérifier l'absence d'obstacles UT+1/UT+2 avant un breakout") est **fidèle** : le libellé porte bien sur les DEUX niveaux, et bien sur une place disponible — pas sur les rôles Stop (UT+1) / Confirmation (UT+2) déjà traités par ailleurs dans la même source (l.7/l.9/l.19). Aucune confusion à corriger cette fois.

**2e source INDÉPENDANTE du même mécanisme**, trouvée en relisant le corpus et non reprise d'un document existant — `TRADING_LESSONS_PULLBACK_MATURITE.md` **ligne 14** : *"UT Supérieure ('Le Potentiel') : vérifier qu'il reste assez de 'jus' (marge de progression) pour un nouvel objectif"*. Elle était listée comme un item de catégorie C SÉPARÉ ("Hiérarchie UT Supérieure = Le Potentiel") : c'est en réalité le même enseignement, désormais couvert par la même implémentation. Deux sources indépendantes = même niveau de confiance que la règle UT+2 elle-même.

**Distinction vérifiée avec les deux règles MTF déjà codées** (ce n'est pas un doublon déguisé) : "Conflit MTF" (`TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` l.7, *"Ne jamais trader une borne de range si un range d'unité de temps supérieure est déjà actif"*) = RÉGIME, table RANGE ; "Rigueur Multi-Timeframe" (même source #12, l.21, *"Si l'UT+1 est en fin de cycle, le breakout local est un piège"*) = PHASE DE CYCLE de l'UT+1. Ni l'une ni l'autre ne mesure une distance disponible.

**2) LE POINT CRUCIAL — la conclusion "2 UT pour RANGE / 3 UT pour TENDANCE" du 7e round s'applique ICI EN SENS INVERSE.** Le motif de refus n°1 du round précédent était que le corpus ne budgète que 2 UT à la table RANGE (celle de `position_engine.py`). Revérifié personnellement, la MÊME ligne dit l'inverse pour la table TENDANCE — celle de `trend_table.py`, le moteur modifié ici. `TRADING_LESSONS_MTF_SUIVI_TENDANCE.md` **ligne 13** : *"| Unités de temps | 2 UT (ex: Daily/H4) | **3 UT impératives** (ex: Mensuel/Hebdo/Daily) |"* ; **ligne 15** : *"| Hiérarchie | Dépend de l'UT supérieure | Nécessite validation sur **2 UT supérieures** |"* (colonne "Gestion d'une Tendance" dans les deux cas). La table de tendance a donc explicitement DROIT à un UT+1 et un UT+2 réels : **le motif qui bloquait le 7e round autorise au contraire ce chantier précis**. C'est même le premier endroit du projet où cette ligne du corpus est appliquée à la table qu'elle vise.

**3) Aucune définition inventée** (le motif de refus du gate Fibonacci RANGE ne s'applique pas ici) :
- *"structure majeure"* sur UT+1/UT+2 = la borne HAUTE du canal Extreme Channel de ce niveau (`ctx_resistance` = `ema_slow + 2×ATR`) — définition **déjà présente** dans `trend_table.py::add_trend_context`, miroir exact du `ctx_support` que le corpus fait déjà servir de structure de stop UT+1, et le corpus traite lui-même la borne opposée du canal comme la structure que le prix vient buter (`TRADING_LESSONS_ZONE_ACCUMULATION.md` l.40, *"Validation : prix atteint la borne opposée du canal de tendance"*). Calculée sur de VRAIES bougies D1/Hebdo, jointe sans lookahead par la même mécanique et le même `CLOSURE_DELAY` que `backtest_phase2_ut2.py` (importé, ce fichier partagé n'a PAS été modifié).
- *"rendement escompté"* = l'amplitude du range d'accumulation local, c'est-à-dire l'objectif "Ratio 1:1 Tendance" que la même source définit 9 lignes plus haut (l.8, *"projeter l'amplitude du range d'accumulation local (UT)"* — UT LOCALE, point déjà établi au 7e round). Grandeur **déjà calculée** par `prepare` (`local_range`), simplement jamais lue par ce moteur jusqu'ici.
- Multiplicateur `space_mult` = **1.0 (Ratio 1:1), la seule valeur citée par le corpus** ; paramétrable uniquement pour mesurer la sensibilité, jamais pour calibrer.
- Sémantique explicite : un niveau **déjà sous le prix n'est pas un obstacle** (marge non bornée) ; un niveau **inconnu (NaN) fait ÉCHOUER** le gate (*"il est impératif de VÉRIFIER"* ne peut pas être satisfait sans donnée — convention déjà en place ailleurs). Hypothèses complètes : **H13-H17** en tête de `trend_table.py`.

**4) Mesures de faisabilité faites AVANT de coder** (les deux vérifiées personnellement, la 2e étant précisément ce qui avait condamné l'item du round précédent) :
- **Ce n'est PAS un no-op entre unités de temps** : marge médiane jusqu'à l'obstacle **2,52% en H4 / 5,99% en D1 / 8,50% en Hebdo** (BTC), ratio médian **D1/H4 = 2,1-2,4×** et **Hebdo/H4 = 2,8-5,5×** selon l'actif — parce que `EMA±ATR` est path-dépendant, exactement la distinction que le 7e round avait lui-même relevée. En revanche `ctx_high` (max glissant sur fenêtre calendaire) EST un quasi no-op (6,22% / 6,36% / 7,47% sur BTC) : l'invariance par agrégation mesurée au round précédent est ainsi **confirmée indépendamment**, et ce candidat a été **explicitement écarté** au profit de `ctx_resistance` (H14).
- **Aucun problème de disponibilité de données** : **0,00% de NaN** sur D1 comme sur Hebdo après le warmup H4, sur toute la période testée (2117-2374 bougies D1, 303-340 bougies Hebdo selon l'actif). Rien à voir avec les 13 bougies Mensuelles qui condamnaient l'item du 7e round côté `oos_xrp_faithful.py` — moteur qui n'utilise de toute façon pas cette table (vérifié par grep des imports réels : les seuls consommateurs de `trend_table` sont `backtest_phase2_trend.py`, `unified_protocol.py`, `min_borders_sensitivity.py`, `risk_aggregation_triple_system.py`, `walkforward_unified.py`, `cross_stress_test_unified_capital_tiers.py` et leurs 3 fichiers de test — plusieurs n'en importent d'ailleurs que `load_volume`).

**5) Implémentation** — `code/trend_table.py` (+ ~310 lignes dont l'essentiel en hypothèses documentées), `code/backtest_phase2_trend.py` (variantes + diagnostic), `code/test_trend_table.py` (6→12 tests) :
- 3 fonctions PURES nouvelles, testées à vérité terrain calculée à la main (`fractions.Fraction`, même méthode que le reste du fichier) : `free_room_frac` (sémantique obstacle/franchi/inconnu), `breakout_space_ok` (le gate lui-même, ET sur les deux niveaux), `attach_obstacle_level` (jointure causale, testée sur série synthétique à vérité terrain connue : NaN avant toute clôture supérieure, puis 10/10/20/30 aux 4 instants suivants).
- `step_campaign` lit `ev.get("breakout_space_ok", True)` : **défaut = absence de contrainte**, donc `unified_protocol.py::_campaign_ev` (qui réplique ce dict `ev` sans la clé) reste bit-à-bit identique sans être modifié — vérifié par test dédié en plus des 13/13 de son propre fichier.
- `run_trend_table(..., use_breakout_space_gate=False, df_ut1=None, df_ut2=None, space_mult=1.0)` : défaut OFF (comportement historique préservé, même convention que `use_mtf_stop`/`use_fib_gate`), et **ValueError explicite** si le gate est activé sans les DEUX niveaux — la citation porte sur UT+1 ET UT+2, le gate ne se dégrade pas silencieusement en un seul niveau (test dédié).
- **Non-régression vérifiée à deux niveaux** : les 16 lignes historiques de `phase2_trend_table_results.csv` sont retrouvées **identiques au chiffre près** (écart max 2,2e-16, imputable à l'écriture CSV) ; suite complète **163/163** (157 avant, +6 tests).

**6) Résultat mesuré, rapporté tel quel** (`phase2_trend_table_results.csv`, nouvelle colonne `space_gate` : `off` / `on_x0.5` / `on_x1.0` / `on_x1.5`) — au Ratio 1:1 vs sans gate, sur BTC/ETH/BNB/SOL × 4 profils :

| Couple | Retour sans gate | Retour avec gate 1:1 | Écart |
|---|---|---|---|
| BTC / FAIBLE | 0,0% (0 trade) | +1,1% (1 trade) | **+1,1 pt** |
| ETH / FAIBLE | -0,1% | -2,0% | **-1,9 pt** |
| BNB / FAIBLE | +1,1% (3 trades) | +0,3% (2 trades) | **-0,8 pt** |
| SOL / FAIBLE | +1,1% (3 trades) | -0,0% (1 trade) | **-1,1 pt** |
| 12 autres couples (MODERE / AGRESSIF / TRES_AGRESSIF) | — | — | **0,0 pt (inchangés)** |

**1 amélioré, 3 dégradés, 12 inchangés — mitigé à dégradé.** Implémenté malgré ce résultat, conformément au principe inviolable du projet (la performance du proxy ne décide jamais d'implémenter ou non une règle de l'IP). Gardé OPTIONNEL et désactivé par défaut pour une raison de méthode, pas de performance : ce moteur est le banc de mesure ISOLÉ de la table de tendance (H11, mono-timeframe volontaire), et tout l'historique publié de ce CSV doit rester comparable. Sensibilité (0,5× / 1,5×) rejouée et publiée dans le même CSV : monotone et sans surprise (à 1,5×, SOL/FAIBLE tombe à 0 trade, BNB/FAIBLE à -0,2%).

**7) Le gate n'est PAS inerte — diagnostic dédié, indispensable ici** (`phase2_trend_table_space_gate_diagnostic.csv`, `trend_table.breakout_space_binding_stats`). Le chiffre de performance ci-dessus est structurellement condamné à ne bouger que sur 4 couples ; il ne dit donc rien du pouvoir discriminant de la règle, exactement la distinction que la sensibilité `MIN_BORDERS` avait dû faire au 6e round (0,0 pt mesuré mais gate INERTE, ce qui n'était PAS une preuve de robustesse). Mesuré séparément sur **toutes** les bougies :

| Actif | Bougies candidates au breakout | Passent à 0,5× | Passent à 1:1 | Passent à 1,5× |
|---|---|---|---|---|
| BTC | 306 | 232 (75,8%) | 203 (66,3%) | 171 (55,9%) |
| ETH | 295 | 220 (74,6%) | 185 (62,7%) | 164 (55,6%) |
| BNB | 304 | 240 (78,9%) | 202 (66,4%) | 170 (55,9%) |
| SOL | 264 | 195 (73,9%) | 150 (56,8%) | 129 (48,9%) |

**34-43% des breakouts sont refusés au Ratio 1:1** (21-26% à 0,5×, 44-51% à 1,5×), 0 blocage pour donnée manquante, et l'obstacle vient plus souvent de l'UT+1 (76-108 blocages) que de l'UT+2 (36-68) — la règle mord, et mord de façon graduelle et interprétable.

**8) Correction documentaire trouvée en passant — une généralisation abusive répétée dans 4 endroits.** *"100% des campagnes de tendance se referment en étape Accumulation, aucune n'atteint jamais Breakout"* (ce document ×2, `CONFIGURATION_RECOMMANDEE.md`, `code/backtest_phase2_recommended.py`, `code/unified_protocol.py::main`) n'est vrai que pour **3 profils sur 4**. En faveur de la mesure d'origine : la section dédiée de `COUVERTURE_ENSEIGNEMENTS.md` scopait correctement le constat aux 3 profils MODERE/AGRESSIF/TRES_AGRESSIF — ce sont les résumés qui ont perdu le périmètre en route. Compté par instrumentation du VRAI moteur (enveloppe autour de `try_open_campaign`/`step_campaign`, identité de campagne fiable — un premier comptage par `id()` s'est révélé faux, CPython recyclant les adresses, corrigé avant d'en tirer une conclusion) :

| Profil | Campagnes ouvertes (4 actifs) | Atteignent Breakout | Atteignent Divergence | Atteignent Pull-Back/Excès |
|---|---|---|---|---|
| FAIBLE | 30 | **8** | **5** | **1** |
| MODERE | 39 | 0 | 0 | 0 |
| AGRESSIF | 39 | 0 | 0 | 0 |
| TRES_AGRESSIF | 39 | 0 | 0 | 0 |

Les étapes 2 à 5 de la table de tendance **sont donc exercées empiriquement**, sur le seul profil FAIBLE (une campagne SOL va jusqu'à l'Excès final). C'est précisément pourquoi le gate ne peut faire bouger que les 4 couples FAIBLE — une raison structurelle, pas une inertie de la règle. Le motif mécanique de la différence entre profils est identifiable : FAIBLE a `accum_frac = 0.00` (rien n'est engagé en Accumulation), donc son stop de protection — qui exige `remaining > 0` — ne peut pas clôturer la campagne avant le Breakout, alors que les 3 autres profils engagent une jambe dès l'Accumulation et se font stopper là. Constat rapporté tel quel, non "corrigé" : changer ce comportement serait un changement de mécanisme hors périmètre de ce round.

**Aucun commit** (contrainte du round). Fichiers modifiés : `code/trend_table.py`, `code/backtest_phase2_trend.py`, `code/test_trend_table.py`, `code/phase2_trend_table_results.csv` (régénéré), `code/phase2_trend_table_space_gate_diagnostic.csv` (nouveau), plus les 4 documents de suivi.

### 11e application (cycle suivant, catégorie C) — "Mécanisme d'emboîtement T/T-1" investigué, PAS implémenté

Item suivant de la catégorie C, celui que l'audit du 5e round plaçait en tête de liste : **"mécanisme d'emboîtement T/T-1"**, cité comme *"`trend_table.py` reconnaît DÉJÀ lui-même (H11) être mono-timeframe, jamais cette validation croisée précise"*. Même discipline qu'aux 7e/8e/9e rounds : relecture intégrale de la source, grep des 17 sources + du manuel, lecture complète de `trend_table.py`/`backtest_phase2_ut2.py`, et mesure sur le VRAI moteur avant de trancher. **Verdict : NE PAS IMPLÉMENTER — décision prise et documentée. Aucun changement de comportement, aucun CSV régénéré, 157/157 tests inchangés.**

**Citation exacte, relue mot pour mot (`TRADING_LESSONS_ZONE_ACCUMULATION.md:13`, section "Mécanisme d'emboîtement des timeframes")** :

> *"Pour un professionnel, l'analyse d'un graphique unique est insuffisante. Le succès repose sur la compréhension de la mécanique d'emboîtement des unités de temps (UT). La règle d'or : le canal de tendance observé sur votre unité de temps de contexte (T) n'est rien d'autre que la structure interne de l'unité de temps inférieure (T-1)."*

**Première correction : la paraphrase de l'audit perd le mot porteur.** L'item écrivait *"le canal observé sur l'UT contexte T"* — la source dit *"le **canal de tendance** observé"*. Or « canal de tendance » n'est pas un synonyme de « contexte » dans ce corpus, c'est un **objet nommé situé à un niveau précis, distinct du contexte** : `TRADING_LESSONS_TROISIEME_BORNE.md:14-18` (#11) oppose explicitement deux colonnes, *"UT « Contexte »"* et *"UT « Canal de Tendance »"*, la seconde décrivant le *"mouvement interne"* et *"deven[ant] « bruit » dès qu'une borne supérieure est détectée"* ; `TRADING_LESSONS_BREAKOUT_RATIO11.md:7` (#12) situe le *"canal de tendance de **l'UT+1**"* un cran sous le *"range de contexte principal"* de l'UT+2 (l.9). Restitué, l'énoncé de #10 n'est donc pas *"recalcule le contexte depuis les données du niveau inférieur"* mais *"l'objet que tu affiches sous le nom de canal de tendance vit un cran sous ton contexte"* — une **identité de lecture graphique**, sans seuil, sans gate, sans si/alors. C'est le seul énoncé "emboîtement" du corpus : grep de `emboît`/`structure interne`/`T-1`/`millefeuille` sur les 17 sources + `RULES_EXTRACTION.md` ne renvoie aucune autre occurrence, et les deux occurrences de « mille-feuille » (#9:7, #11:43) énoncent l'inverse de l'item — *"vision en « mille-feuille » … synchroniser trois unités de temps"* et *"maîtrise du « millefeuille » avec **respect de la priorité du contexte sur la tendance**"*.

**Deuxième correction : H11 ne parle PAS du même sujet, et la confusion est dans l'item.** H11 (vérifiée dans `code/trend_table.py`) dit : *"ce moteur ne fait PAS de validation croisée multi-timeframe (**H4 validé par D1**) — décision de scope volontaire pour mesurer la table de tendance ISOLÉMENT"*. C'est la direction **vers le HAUT** (UT+1/UT+2), celle que le corpus prescrit ; l'item demande la structure de l'UT **INFÉRIEURE**. « Mono-timeframe » couvre trivialement les deux directions, mais l'exemple et la justification de H11 ne concernent que l'ascendante — l'item s'appuyait donc sur un aveu qui ne porte pas sur lui. (Même classe d'erreur qu'au 7e round : "UT+1 = Validation" alors que la source disait "UT+1 = le stop".)

**La table TENDANCE a bien droit à 3 UT — mais les deux autres sont AU-DESSUS, pas en dessous (citation vérifiée personnellement).** `TRADING_LESSONS_MTF_SUIVI_TENDANCE.md:13` : *"| Unités de temps | 2 UT (ex: Daily/H4) [Range] | **3 UT impératives** (ex: Mensuel/Hebdo/Daily) [Tendance] |"* — l'item est donc légitime en principe là où l'amplitude UT+2 côté RANGE ne l'était pas (7e round). Mais la ligne suivante de la MÊME table (l.15) tranche la direction : *"| Hiérarchie | Dépend de l'UT supérieure | Nécessite validation sur **2 UT supérieures** |"*, et l'exemple *"Mensuel/Hebdo/Daily"* place le Daily tradé **en bas** du trio. Le 3e niveau que le corpus accorde à `trend_table.py` est l'Hebdo/Mensuel au-dessus, pas le H1 en dessous. Implémenter T-1 avant T+1/T+2 serait implémenter le moins étayé des deux, dans l'ordre inverse — et "UT+2 pour la tendance" est déjà un item de backlog distinct.

**Tension de direction, 5 sources contre 1 (trouvée par relecture, absente de l'item).** Toutes les autres sources placent l'EXÉCUTION en bas de l'échelle et donnent la priorité au niveau supérieur : #15 `PYRAMIDALISATION.md:10` — *"Timeframe d'Exécution (Horaire) … **on descend de deux niveaux sous la référence**"* ; #16 `CLUSTERS_PRIX.md:10` — *"| **Unité de Temps de Trading** | **Horaire (H1) — Soit 2 niveaux en dessous** |"* ; #12:19 — *"UT+1 sert au positionnement du stop, UT+2 sert à la confirmation"* ; #14 `STRUCTURES_ALTERATIONS.md:15` — *"la Loi de l'Unité de Temps Supérieure"* ; #11:9 — *"à partir du moment où une Troisième Borne apparaît sur l'unité de temps « Contexte », toute borne sur l'unité de temps « Tendance » devient **inexistante et non-avenue** … La priorité est toujours donnée à la structure macro"*. Le seul passage qui place le trade AU-DESSUS de sa structure est la glose de #10 (l.16-18, *"Sur l'UT supérieure (contexte, T) : c'est ici qu'on définit le positionnement du trade"*, *"La stratégie normale utilise la structure de l'UT inférieure pour valider un positionnement robuste sur l'UT supérieure"*) — et ce sont des lignes de synthèse rédigées par le projet, pas la citation. **Tension réelle jamais consignée** : #10:18 fait de la structure inférieure un intrant de validation, #11:9/18 la déclare *"inexistante et non-avenue"* / *"« bruit »"* dès qu'une borne supérieure apparaît.

**La seule traduction opérationnelle de l'emboîtement dans tout le corpus est DÉJÀ implémentée — et c'est la projection vers le BAS, pas le calcul vers le haut.** #16 `CLUSTERS_PRIX.md:12` nomme l'objet : *"| Contextes | **Extreme Channel** (contexte de l'UT supérieure **affiché sur l'UT de trading**) |"*, et l.28 *"Stop Loss : placé sous l'**Extreme Channel** (le contexte de l'UT supérieure)"* ; #12:7 *"Stop-loss = clôture la plus basse … du canal de tendance de l'UT+1 — « l'analyse des marches d'escalier du canal de l'UT supérieure définit la zone de protection technique réelle »"* ; #10 lui-même, l.38, *"Stop-loss = largeur moyenne du canal de tendance récent, ou sous le point bas de la 4ème borne"*. C'est mot pour mot le **stop UT+1** déjà implémenté SANS CONDITION dans `backtest_phase2_faithful.py` (`ctx_support_d1`, structure détectée sur le H4 tradé, canal lu sur le D1) et déjà paramétrable dans `backtest_phase2_ut2.py::run_ut2` (`use_mtf_stop`). **L'item est donc partiellement déjà couvert** — même classe de trouvaille que le Red Flag 3 / la sortie flip au 9e round : ce qui manquait n'était pas le mécanisme, c'était de voir qu'il était déjà là sous un autre nom.

**Mesures (exigées avant de trancher, jamais supposées — BTC/ETH/BNB/SOL, historique complet, mêmes `prepare`/`add_trend_context` que la production ; le harnais reproduit bit-à-bit les compteurs du moteur réel : 7/9/16/8 déclenchements Accumulation = les `n_trades` 7/8/16/8 de `code/phase2_trend_table_results.csv`)** :
1. **Joindre un niveau EN DESSOUS est mécaniquement gratuit** : `attach_context_level` marche à l'identique vers le bas (`merge_asof` backward, `closure_delay=1h` au lieu de 1 jour) et reste strictement causal — la dernière bougie H1 close à l'instant d'une bougie H4 est exactement la 4e sous-bougie de la H4 précédente. Aucune brique à inventer de ce côté-là.
2. **…mais elle n'apporte AUCUNE information.** `ctx_high`/`ctx_low` calculés nativement en H1 puis joints au H4 sont identiques aux valeurs H4 natives sur **94,6-95,5%** des barres, et sur **100,00%** une fois neutralisé l'artefact de granularité du `shift(1)` (1 barre = 1h vs 4h) : un max/min sur fenêtre **calendaire** est exactement invariant par agrégation de bougies (généralisation exacte du "ratio médian 1,00" trouvé au 7e round pour `context_range`). La clôture H1 jointe est celle de la bougie H4 précédente sur **100%** des barres (une bougie H4 clôture avec sa dernière sous-bougie H1). Donc `accum_retracement_frac` — **la seule grandeur numérique du gate Accumulation** — est mathématiquement insensible au choix T vs T-1.
3. **Le gate ADDITIF « exiger une structure T-1 mature » est inerte bit-à-bit** : 7/7, 9/9, 16/16, 8/8 déclenchements Accumulation, **écart 0 sur 4/4 actifs**. Raison mesurée : `n_borders` est un comptage sur fenêtre calendaire de 15 jours, donc en H1 il vaut **médiane 37** (contre 9 en H4) et **32-34 au minimum sur les barres candidates**, contre un seuil `MIN_BORDERS=3` — troisième confirmation indépendante du "gate inerte" des 6e et 8e rounds.
4. **La seule composante réellement dépendante du niveau est la bande EMA±ATR** (path-dépendante) : écart médian de `ctx_support` de 2,1-3,8% vers le bas (H1) et de 7,4-12,7% vers le haut (D1) ; le rejet de canal donne des ensembles d'événements franchement différents (BTC 902 en H4 vs 825 en H1, **115 communs seulement**). Mais c'est un artefact de notre proxy EMA/ATR, pas une structure spécifiée par le corpus — et le corpus place ce canal **au-dessus**, pas en dessous (point précédent).
5. **Dans la direction prescrite (vers le haut), le chantier est incompatible avec H3 dans CE moteur** : un stop posé sur le canal D1 passe d'une distance médiane de **1,20-2,26% à 14,75-19,88%** (×8,6 à ×14,6), ce qui fait tomber le plafond de campagne de H3 (5% de risque) de **2,2-4,2 fois U à 0,25-0,34** — les **4 profils s'écrasent sur le même plafond** et la table §4 (Renfort +25/+50/+100/+150/+200%), seul objet que `trend_table.py` existe pour mesurer, devient inexprimable. `position_engine.py` n'a pas ce problème (chaque tranche dimensionnée indépendamment, `risk_pct / stop_pct`, sans plafond cumulé) — c'est pourquoi `ctx_support_d1` y fonctionne sans dégât. Et utiliser le canal D1 comme référence du **rejet** fait tomber les déclenchements Accumulation de 7/9/16/8 à **0/0/0/3** : la table ne traderait plus, donc ne serait plus mesurable.

**Contrainte de données : la prémisse "H1/M15 NO-GO" ne bloque PAS ce chantier — correction honnête.** Le NO-GO de `STATUS.md` porte sur H1/M15 comme **UT d'exécution** (*"détruits par les frais de transaction"*, `CASCADE3_H1_EXECUTION_TEST.md`) ; un gate de structure ne paie aucun frais, et le H1 est de toute façon la donnée BRUTE du projet (`load_h1`, disponible pour les 4 actifs). Ce n'est donc pas la faisabilité qui bloque, c'est la lecture du corpus et les mesures ci-dessus. (Seule vraie limite de données trouvée : M15 n'existe que pour BTC, donc un T-1 sous H1 serait mono-actif.)

**Décision et requalification** : item **fermé comme "identité de lecture graphique, dont la seule traduction opérationnelle du corpus (Extreme Channel / stop UT+1) est déjà implémentée dans `faithful.py`, et dont la lecture littérale T-1 est un no-op mesuré (100% d'invariance, gate additif inerte 4/4)"**, pas comme "validation croisée manquante". Prémisses corrigées dans `COUVERTURE_ENSEIGNEMENTS.md` (paraphrase amputée de « de tendance » ; H11 ne porte pas sur la direction descendante). **Deux items nouveaux, mieux étayés, trouvés par la relecture et ajoutés à la catégorie C à sa place** :
1. **Le `ctx_support` de `trend_table.py` n'est PAS l'« Extreme Channel » du corpus** — #16:12 le définit comme *"le contexte de l'**UT supérieure** affiché sur l'UT de trading"*, et `faithful.py` l'honore (`ctx_support_d1`), mais `trend_table.py` (comme v6 et `run_ut2` avec `use_mtf_stop=False`) appelle « Extreme Channel » la bande `ema_slow - 2*ATR` de l'UT tradée elle-même (H4). Écart mesuré : 7,4-12,7% médian. Ce n'est plus une hypothèse de scope (H11) mais un **niveau de stop non conforme au corpus dans le moteur de tendance**, avec un blocage concret identifié (H3, point 5 ci-dessus) — donc un vrai item de conception : soit porter le stop UT+1 dans `trend_table.py` ET redéfinir H1/H3 (l'unité U et le plafond de campagne) pour que la table §4 reste exprimable, soit renommer honnêtement la bande H4 pour ne plus lui donner le nom du corpus.
2. **Tension #10:18 vs #11:9/18 sur le statut de la structure inférieure**, jamais consignée : intrant de validation (*"utilise la structure de l'UT inférieure pour valider"*) contre *"inexistante et non-avenue"* / *"« bruit » dès qu'une borne supérieure est détectée"*. À trancher avant tout futur chantier descendant, sinon les deux lectures produisent des gates de signe opposé.

Seule modification de code : **une note de 54 lignes de docstring insérée dans `code/trend_table.py` entre H11 et H12 (insertion pure après l'ancienne ligne 169, zéro ligne exécutable modifiée)** — même discipline que la note H7 du 6e round et le bloc Red Flags de `position_engine.py` au 9e, pour qu'un futur lecteur de H11 ne rouvre pas le chantier à l'aveugle.

### Mode ingénieur senior — quand ralentir sans qu'on ait à le redemander

Adapté de `ideeri-v2/CLAUDE.md` §16. Déclencheurs, sur ce projet :
- Toute modification de la composante cycle de `proxy_v2.py` (risque concentré, tableau ci-dessus).
- Tout passage de phase (2→3, 3→4, 4→5) — en pratique irréversible une fois du capital réel engagé.
- Toute décision qui fige un chiffre de performance comme "validé" avant la fin de la Phase 3.

Pratiques concrètes :
1. **Vérifier empiriquement, pas seulement relire le code** — test synthétique à vérité terrain connue plutôt qu'une déduction depuis la formule.
2. **Chercher les implications ailleurs avant de clore** — un fix/une réserve sur `proxy_v2.py` affecte tous les moteurs qui l'utilisent (v5/v6/v7) et tous les documents qui en rapportent la performance (MTF, funding rate, OOS) ; vérifier la liste complète, pas seulement le fichier qu'on vient de modifier.
3. **Documenter en continu**, pas en résumé final une fois le chantier déclaré clos.
4. **Distinguer "mitigé" de "résolu"** — appliqué concrètement au traitement du P0 (occurrence #2 ci-dessus) : le calcul causal a été implémenté et mesuré, l'ampleur du biais **est maintenant quantifiée** (edge H4 global surestimé ~4× en batch), mais ça ne fait pas du sujet un "résolu" au sens plein — la mesure révèle un fait plus sérieux qu'une simple correction de magnitude : le composant cycle **isolé** ne montre aucune corrélation causale significative sur les 5 actifs réels testés. "Mitigé" s'applique à la performance globale (son sens tient) ; le composant cycle lui-même reste une question ouverte — le test d'ablation fait depuis (cf. `COUVERTURE_ENSEIGNEMENTS.md`) ne tranche pas non plus : 4 actifs en désaccord de direction (ETH s'améliore sans le cycle, SOL se dégrade), donc ni "à retirer" ni "confirmé neutre" — un résultat non concluant reste un résultat honnête, pas un échec de méthode.
5. **"Tests unitaires verts" ne prouve ni que l'edge survivra à un calcul causal, ni qu'il survivra en paper trading** — un chiffre n'est définitif qu'après Phase 3, jamais avant.

**Garde-fou mécanique — angle mort assumé, pas encore traité.** `ideeri-v2` (`CLAUDE.md` §16.4) constate qu'un texte seul ("pas d'automatisation de l'exécution en phase initiale", acté en tête de ce document) ne suffit pas à garantir qu'il soit respecté au bon moment, et a construit un hook mécanique bloquant pour ses actions à fort impact. Ce projet n'a **aucun** équivalent aujourd'hui — l'engagement "pas d'automatisation" repose uniquement sur l'accord verbal/textuel. **Confirmé toujours non matérialisé** (mobilisation multi-agents, bilan directeur "regard neuf") : recherche ciblée dans `code/` (clé API, fonction `place_order`/`create_order`, dépendance exchange) — rien trouvé, le risque reste hypothétique. **Construit ce cycle** (le coût jugé trivial par le bilan a été vérifié : c'est effectivement le cas) : `code/test_no_execution_automation.py` (2/2 tests) — échoue si un motif de passage d'ordre/credentials d'exchange (`place_order`, `ccxt`, `python-binance`, `API_KEY`...) apparaît dans `code/`, exécuté à chaque suite de tests comme les 21 autres fichiers, pas un script à part qu'on pourrait oublier de lancer. Volontairement grossier (motifs textuels, pas une analyse statique complète) — un filet de sécurité contre un franchissement évident, pas une preuve formelle. Confirmé propre aujourd'hui (aucun motif trouvé). Si un futur changement de phase (4/5) rend ce test obsolète, il doit être mis à jour EXPLICITEMENT après une décision documentée dans ce document, jamais l'inverse.

---

## État réel au dernier cycle de travail — résumé exécutif

Ce plan a beaucoup évolué depuis sa version initiale. **Documents de référence à jour** (ne pas se fier aux anciens documents `PHASE2_MONEYMANAGEMENT.md`/`PHASE2_FULL_MATRIX.md`/`PHASE2_V3_ATTEMPT_REGRESSION.md`, tous supersédés) :

| Sujet | Document de référence actuel |
|---|---|
| Règles extraites du manuel PDF officiel | `RULES_EXTRACTION.md` |
| Corpus Trading Lessons (17 sources vidéo) | `TRADING_LESSONS_INDEX.md` (index + statut de la question multi-timeframe, résolue) |
| Couverture complète du corpus (✅/❌ exhaustif) | `COUVERTURE_ENSEIGNEMENTS.md` |
| Backtest Phase 1 (multi-timeframe, proxy générique) | `PHASE1_CLOSEOUT.md` |
| Moteur de risque Phase 2 — version de base | `PHASE2_V4_IMPLEMENTATION_COMPLETE.md` |
| Signal Phase 2 — proxy reconstruit (TSI + cycle + structure) | `PROXY_V2_TSI_CYCLE_STRUCTURE.md` + `AUDIT_QUALITE_ET_CORRECTION_CYCLE.md` (bug de signe trouvé, corrigé, revalidé hors-échantillon) |
| Classification de régime (Range/Tendance/Excès) | `REGIME_CLASSIFIER_RANGE_VS_TENDANCE.md` |
| Validation croisée multi-timeframe réelle (H4/D1) | `MTF_CROSS_VALIDATION_H4_D1.md` |
| Coût funding rate (ordre de grandeur) | `FUNDING_RATE_ANALYSIS.md` |
| Validation hors-échantillon indépendante (XRP) du signe du cycle | `OOS_VALIDATION_CYCLE_SIGN.md` |
| Lacunes de qualité identifiées et statut | `AUDIT_QUALITE_ET_CORRECTION_CYCLE.md` |
| État du code (架構, tests) | `code/` — moteur de position factorisé et testé (`position_engine.py` + `test_position_engine.py`, 5/5 tests passent) |
| Point d'entrée unique / état du projet | `STATUS.md` |

**Chaîne des moteurs de backtest** (chacun ajoute une brique sur le précédent, tous dans `code/`) :
`backtest_phase2.py` (base, EMA confluence, table range) → `_v4.py` (+ Règle de Trois, amplitude en durée, maturité swing, pyramidalisation) → `_v5.py` (signal remplacé par proxy TSI+cycle+structure) → `_v6.py` (+ classification de régime, interdiction EXCES, pyramidalisation gated) → `_v7.py` (+ validation croisée réelle H4 exécution / D1 référence, sans lookahead).

**Résultat le plus solide à ce jour, ampleur révisée à la baisse après P0** : v7 (validation croisée H4/D1) — sur les 4 actifs testés et sans exception, la validation par D1 réduit le nombre de trades (~2,5-3×), améliore le win rate/profit factor et réduit le drawdown. La direction est confirmée, cohérente avec 7 sources indépendantes du corpus, mais l'**ampleur** (chiffrée initialement avec le calcul batch du cycle, +5 à +13 points de win rate) est en réalité bien plus modeste une fois le calcul rendu causal : +0,9 à +8,5 points selon l'actif, quasi neutre sur BNB. Chiffres à jour et nuance complète : `MTF_CROSS_VALIDATION_H4_D1.md`.

**Réserve non résolue depuis le début** : le signal reste un proxy (TSI+cycle+structure), jamais validé contre le vrai PRO Framework/Momentum (accès TradingView requis — hors de portée technique actuelle). C'est la limite fondamentale qui borne tout le reste. Le bug de signe du cycle a été revalidé hors-échantillon (XRP, `OOS_VALIDATION_CYCLE_SIGN.md`) pour réduire (pas éliminer) le risque de biais rétrospectif.

**P0 (calcul causal du cycle) traité ce cycle de travail** — voir occurrence #2 ci-dessus et `COUVERTURE_ENSEIGNEMENTS.md` ⚠️→◐ pour le détail complet. Résumé honnête : la performance **globale** rapportée sur H4 (v5/v6/v7) reste positive en direction (100 % des configurations testées gardent le même signe une fois le cycle rendu causal) mais son ampleur était surestimée d'environ 4× et son win rate d'environ 10 points — nouvelle référence chiffrée : `phase2_v5_causal_results.csv`, `phase2_v6_regime_causal_results.csv`, `phase2_v7_mtf_causal_results.csv`, `cycle_causal_vs_batch_comparison.csv`. Constat plus sérieux, à ne pas minimiser : le composant cycle **pris isolément** ne montre plus aucune corrélation significative avec le rendement futur une fois causal, sur aucun des 5 actifs testés (BTC/ETH/BNB/SOL + XRP OOS) — l'hypothèse la plus probable est que la performance globale tient surtout grâce au momentum et à la structure, le cycle devenant un filtre proche du bruit. **Test d'ablation fait depuis** (`code/ablation_test_cycle.py`) : ne confirme pas cette hypothèse simplement — résultat par actif non consensuel (ETH s'améliore nettement sans le cycle, SOL se dégrade nettement, BTC/BNB mixtes), cf. `COUVERTURE_ENSEIGNEMENTS.md` pour le détail. Ne pas retirer le cycle du score sur cette seule base.

**Ce qui n'est toujours pas fait, malgré tout ce travail** — voir `COUVERTURE_ENSEIGNEMENTS.md` pour la liste exhaustive. Table "trade de tendance" à 5 étapes : **traitée ce cycle** (`code/trend_table.py`, `code/backtest_phase2_trend.py`, cf. item 1 du backlog ci-dessous et `COUVERTURE_ENSEIGNEMENTS.md`) — implémentée fidèlement et testée unitairement (6/6), mais résultat mesuré mitigé : sur BTC/ETH/BNB/SOL H4, les campagnes fermées se referment en étape Accumulation (stop ou abandon régime-EXCES) **pour 3 profils sur 4** — les étapes 2-5 ne sont exercées que par le profil FAIBLE (**CORRECTION du 8e round** : 30 campagnes, dont 8 atteignent le Breakout, 5 la Divergence, 1 l'Excès final ; ce paragraphe disait "100% des campagnes… aucune n'atteint jamais Breakout", généralisation abusive de la mesure d'origine qui était, elle, correctement scopée aux 3 autres profils dans `COUVERTURE_ENSEIGNEMENTS.md`). Points les plus significatifs restants : règle "UT+2" exacte (2 niveaux au-dessus, pas le niveau immédiat) non implémentée. Patterns/outils du manuel (Wall Street, Andrews Pitchfork, canal manuel Supports→Apex→Tangente, Cluster Technique, mécanisme +Reverse) : tous désormais construits, cf. item 7 du backlog ci-dessous. Capital par palier : **traité ce cycle** (`code/capital_tiers.py`, cf. item 6 du backlog ci-dessous et `COUVERTURE_ENSEIGNEMENTS.md`). Fibonacci retracement comme critère d'entrée : **traité ce cycle** (`code/fibonacci.py`, `code/backtest_phase2_fib.py`, cf. item 4 du backlog ci-dessous) — filtre implémenté fidèlement et mesuré, dégrade uniformément la performance de ce proxy (32/32 configs BTC/ETH/BNB/SOL × 4 profils), gardé optionnel (`use_fib_gate=False` par défaut) sans que cela remette en cause l'implémentation, cf. `COUVERTURE_ENSEIGNEMENTS.md`. Le stop "Extreme Channel" bénéficie désormais du vrai `ctx_support` D1 (volet cross-timeframe traité, cf. item 4 de la liste des lacunes ouvertes ci-dessous) mais reste, comme avant, une bande EMA±ATR et non un vrai canal géométrique (volet distinct, toujours ouvert, cf. item 7).

---

## Phase 0 — Extraction des règles déterministes
**Statut : faite.** Manuel PDF lu intégralement + 17 sources Trading Lessons traitées. Voir `RULES_EXTRACTION.md` et `TRADING_LESSONS_INDEX.md`. Élément non résolu : le signal réel des indicateurs (PRO Framework/Momentum) n'a jamais été exporté/observé directement — tout ce qui suit reste un proxy.

## Phase 1 — Backtest multi-timeframe
**Statut : substantiellement faite.** Voir `PHASE1_CLOSEOUT.md`, `WALKFORWARD_ANALYSIS.md`. Conclusion : H4/D1 = GO, H1/M15 = NO-GO (avec le proxy générique d'origine). `backtest_cascade3.py` rejoué avec le moteur actuel par l'agent délégué A : conclusion H1 confirmée (toujours NO-GO), chiffres améliorés (`cascade3_h1execution_results.csv`).

## Phase 2 — Gestion du risque + reconstruction du signal + contexte de marché
**Statut : cycle de reconstruction achevé (v4→v7), avec réserves documentées explicitement dans `COUVERTURE_ENSEIGNEMENTS.md`.**
- Money management dérivé du corpus (breakeven différé, clôtures vs mèches, amplitude réelle calibrée en durée, Règle de Trois, maturité par bornes swing, pyramidalisation multi-tranches) : `PHASE2_V4_IMPLEMENTATION_COMPLETE.md`
- Signal reconstruit à partir du corpus (TSI+cycle+structure, remplaçant l'EMA générique) : `PROXY_V2_TSI_CYCLE_STRUCTURE.md`
- Audit qualité + contrôle aléatoire + bug de signe trouvé, corrigé, revalidé hors-échantillon : `AUDIT_QUALITE_ET_CORRECTION_CYCLE.md`, `OOS_VALIDATION_CYCLE_SIGN.md`
- Classification de régime de marché (Range/Tendance/Excès), interdiction de trader en Excès, pyramidalisation restreinte à la Tendance : `REGIME_CLASSIFIER_RANGE_VS_TENDANCE.md`
- Validation croisée multi-timeframe réelle (H4 exécution validé par D1 référence, sans lookahead) : `MTF_CROSS_VALIDATION_H4_D1.md`
- Coût funding rate quantifié à l'ordre de grandeur annuel PUIS calculé EXACT par timestamp (événement réel toutes les 8h, taille réelle du trade, sorties partielles Validation/Confirmation prises en compte) : `FUNDING_RATE_ANALYSIS.md` — constat affiné ce cycle : l'estimation grossière annualisée était une approximation correcte pour FAIBLE/MODERE (écart ≤ ±1,6 pt de retour cumulé) mais sous-estimait le retour net réel sur les profils agressifs (jusqu'à -19,9 pts SOL TRES_AGRESSIF) ; sur H4/FAIBLE (le profil jugé le plus à risque par l'estimation grossière), le coût exact ne renverse ni n'efface l'edge sur aucun des 4 actifs testés (BTC/ETH/BNB/SOL)
- Code : logique de position dédupliquée et testée (`code/position_engine.py`, 5 tests unitaires passent), plus de duplication entre `backtest_phase2.py`/`_v4.py`/`_v5.py`/`_v6.py`/`_v7.py`

**Lacunes ouvertes (liste complète et exhaustive : `COUVERTURE_ENSEIGNEMENTS.md`), les plus significatives :**
1. ~~Table "trade de tendance" à 5 étapes jamais implémentée~~ — **traité ce cycle** (`code/trend_table.py`, `code/backtest_phase2_trend.py`), cf. item 1 du backlog ci-dessous et `COUVERTURE_ENSEIGNEMENTS.md`
2. ~~Fibonacci retracement jamais utilisé comme critère d'entrée~~ — **traité ce cycle** (`code/fibonacci.py`, `code/backtest_phase2_fib.py`), cf. item 4 du backlog ci-dessous et `COUVERTURE_ENSEIGNEMENTS.md`
3. Règle "UT+2" exacte (2 niveaux au-dessus) non implémentée — v7 ne valide qu'avec le niveau immédiatement supérieur
4. ~~Stop "Extreme Channel" toujours calculé sur le même timeframe que celui tradé~~ — **traité ce cycle** : volet cross-timeframe résolu et mesuré (`MTF_CROSS_VALIDATION_H4_D1.md`), dégrade le ratio retour/drawdown dans la majorité des cas testés donc gardé optionnel (`use_mtf_stop`, défaut `False`). Le volet "vraie construction de canal" (pas EMA±ATR) reste ouvert, cf. point 7 ci-dessous
5. ~~Capital par palier (<10k€/10-100k€/>100k€) jamais pris en compte dans le sizing~~ — **traité ce cycle** (autre agent), cf. `COUVERTURE_ENSEIGNEMENTS.md`
6. ~~Funding rate : ordre de grandeur quantifié, pas encore modélisé par timestamp exact~~ — **traité ce cycle** : calcul exact événement-par-événement (`code/funding_rate_exact.py`), appliqué au moteur v7 causal (BTC/ETH/BNB/SOL, 4 profils), cf. `FUNDING_RATE_ANALYSIS.md` section "Calcul EXACT par timestamp" et `COUVERTURE_ENSEIGNEMENTS.md`
7. ~~Patterns/outils jamais construits~~ : règle "Wall Street" (élargissement = abstention), Fourchette d'Andrews, canal manuel (Supports→Apex→Tangente), Cluster Technique (second pattern), diversification 1%+1%, mécanisme "+Reverse" (Très Agressif) — **tous traités**, cf. item 7 du backlog ci-dessous et `COUVERTURE_ENSEIGNEMENTS.md` (résultats mesurés, majoritairement mitigés — aucun ne se démarque nettement)

## Phase 3 — Paper trading (inchangée)
Durée minimale 4-8 semaines, signaux temps réel sur démo. Critère GO/NO-GO : performance paper dans l'intervalle de confiance du backtest. **Pas encore engagée** — les lacunes de Phase 2 ci-dessus restent à trancher (combler avant Phase 3, ou accepter et documenter le périmètre non couvert) avant de basculer.

## Phase 4 — Pilote capital réel, exécution 100 % manuelle (inchangée)
Capital 10-15 % de la cible, wallet connecté sans transaction automatique, garde-fous structurels ajoutés suite au corpus (checklist pré-trade, latence de 10s, session max 60-90 min — cf. `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` et `TRADING_LESSONS_NEUROBIOLOGIE.md`).

## Phase 5 — Automatisation de l'exécution (non engagée)
Inchangée — discussion différée après Phase 4.

---

## Backlog priorisé (remplace la liste plate précédente)

Un directeur d'ingénierie priorise par impact sur la validité de ce qui est déjà rapporté, avant d'ajouter de nouvelles fonctionnalités. D'où l'ordre ci-dessous — pas l'ordre de découverte.

| # | Item | Impact si non traité | Effort | Priorité |
|---|---|---|---|---|
| 0 | ~~Calcul non causal de `compute_cycle_phase` (Hilbert sur série entière)~~ | **Traité ce cycle** — `compute_cycle_phase_causal` (fenêtre W=150, choisie empiriquement) en production, v5/v6/v7 + OOS XRP rejoués (`phase2_v{5,6,7}_*causal_results.csv`, `cycle_causal_vs_batch_comparison.csv`, `oos_xrp_cycle_validation_CAUSAL.csv`). Résultat MITIGÉ (pas résolu) : edge global H4 tient en direction (48/48) mais surestimé ~4× en ampleur ; edge du cycle isolé non significatif sur les 5 actifs testés — cf. `COUVERTURE_ENSEIGNEMENTS.md` ⚠️→◐ pour le détail et la nuance | Fait | **Traité** |
| 1 | ~~Table "trade de tendance" à 5 étapes jamais testée~~ | **Traité ce cycle** — moteur dédié (`code/trend_table.py`, justification de ne pas réutiliser `position_engine.py` documentée en tête de fichier), gate régime TENDANCE réutilisé tel quel (`regime_classifier.add_regime`), 12 hypothèses d'implémentation documentées (H1-H12, manuel sous-spécifié sur plusieurs points), 6/6 tests unitaires (`code/test_trend_table.py`, séquence complète + P&L exact + plafond de risque H3 + mécanisme +Reverse H10). Mesuré BTC/ETH/BNB/SOL H4 (`phase2_trend_table_results.csv`) : résultat honnête MITIGÉ — les campagnes fermées se referment en étape Accumulation (stop ou abandon régime-EXCES) **pour les profils MODERE/AGRESSIF/TRES_AGRESSIF** (39 campagnes chacun, 0 Breakout) ; **CORRECTION du 10e round** : le profil FAIBLE, lui, exerce bien les étapes 2-5 (30 campagnes, dont 8 atteignent le Breakout, 5 la Divergence, 1 l'Excès final) — la formulation "100% des campagnes, aucune n'atteint jamais Breakout" écrite ici était une généralisation abusive. `test_trend_table.py` est passé à 12/12 au même round (gate "espace libre" MTF, cf. section "10e application") — cf. `COUVERTURE_ENSEIGNEMENTS.md` pour le détail complet et les pistes non tranchées | Élevé (nouveau barème complet) | **Traité** |
| 2 | ~~Stop "Extreme Channel" toujours same-timeframe malgré le nom~~ | **Traité ce cycle** — `attach_higher_context` transmet désormais aussi le `ctx_support` D1 au H4 (même jointure sans lookahead que le score), `run_v7(..., use_mtf_stop=True)`. Mesuré avec le moteur causal (4 actifs × 4 profils, `MTF_CROSS_VALIDATION_H4_D1.md`) : dégrade le ratio retour/drawdown dans 13/16 combinaisons, réduit le drawdown absolu dans 16/16 (sizing à risque fixe + stop D1 réel ~2,7× plus loin en moyenne ⇒ position mécaniquement plus petite). Réglage par défaut des campagnes conservé à `use_mtf_stop=False` ; option disponible pour comparaison. Le volet "channel" (construction géométrique réelle, pas EMA±ATR) reste séparément ouvert (item 7 ci-dessous) | Fait | **Traité** |
| 3 | ~~Règle "UT+2" exacte (2 niveaux au-dessus, pas 1)~~ | **Traité ce cycle** — relecture précise des 6 sources : système à 3 niveaux à rôles distincts (le niveau immédiat = canal/stop, déjà en v7 ; la validation de tendance porte sur le niveau 2 crans au-dessus, EN SAUTANT l'intermédiaire), pas une règle cumulative. `code/backtest_phase2_ut2.py` (généralise `attach_higher_context` à N niveaux, D1+Hebdo), `code/test_ut2.py` (4/4, y compris la vérification empirique du délai de clôture réel d'une bougie Hebdomadaire — 1 jour, pas 7). Mesuré BTC/ETH/BNB/SOL × 4 profils : la règle littérale (Hebdo seul) confirme la direction déjà connue et fait même légèrement mieux que D1 seul sur le profit factor (2,14 vs 1,81) sans trancher clairement lequel est supérieur — cf. `COUVERTURE_ENSEIGNEMENTS.md` | Moyen | **Traité** |
| 4 | ~~Fibonacci retracement comme critère d'entrée~~ | **Traité ce cycle** — règle resynthétisée fidèlement depuis les 5 sources citées (#9/#10/#11/#13/#14) : PAS la "zone dorée 38-61,8%" supposée au départ (2 des 5 sources ne documentent en réalité aucun niveau d'entrée chiffré) ; zone favorable retenue [23%,61,8%], sous-zone optimale [23%,50%]. `code/fibonacci.py` (détection de swing réutilisée de `proxy_v2.py`, 5/5 tests), `code/backtest_phase2_fib.py` (filtre optionnel `use_fib_gate`, sur l'entrée fraîche seulement — raison structurelle documentée pour ne pas l'appliquer aux renforts). Mesuré BTC/ETH/BNB/SOL × 4 profils : dégrade uniformément la performance (32/32 configs négatives), gardé optionnel (`use_fib_gate=False` par défaut) sans remettre en cause l'implémentation — cf. `COUVERTURE_ENSEIGNEMENTS.md` | Moyen | **Traité** |
| 5 | ~~Funding rate exact par timestamp~~ | **Traité ce cycle** — `code/position_engine.py` (trace optionnelle `record_trace=True`, additive, 5/5 tests toujours au vert), `code/backtest_phase2_v7.py` (`run_v7(..., record_trace=True)`), `code/funding_rate_exact.py` (calcul événement-par-événement, taille réelle par trade, sorties partielles prises en compte), `code/test_funding_rate_exact.py` (4/4, cas synthétiques). Mesuré BTC/ETH/BNB/SOL × 4 profils H4 (moteur v7 causal) : l'estimation grossière annualisée précédente était une bonne approximation sur FAIBLE/MODERE (écart ≤ ±1,6 pt) mais sous-estimait le retour net réel sur les profils agressifs (jusqu'à -19,9 pts SOL TRES_AGRESSIF) ; sur H4/FAIBLE, le coût exact ne renverse ni n'efface l'edge sur aucun des 4 actifs — cf. `FUNDING_RATE_ANALYSIS.md` | Faible-moyen | **Traité** |
| 6 | ~~Capital par palier (<10k/10-100k/>100k€)~~ | **Traité ce cycle** — `code/capital_tiers.py` (règle extraite + hypothèse d'implémentation explicite, cf. `COUVERTURE_ENSEIGNEMENTS.md`), `code/backtest_phase2_capital_tiers.py` (BTC/ETH/BNB/SOL, 3 montants représentatifs des 3 paliers), `code/test_capital_tiers.py` (10 fonctions de test, frontières incluses, toutes vertes). Effet mesuré honnête : win rate/profit factor quasi inchangés (le sizing ne change pas quels trades gagnent), retour total et drawdown affectés proportionnellement au risk_pct effectif (de +7 à +27 pts de retour moyen sous 10k€ selon profil, -44 à -104 pts au-dessus de 100k€ pour AGRESSIF/TRES_AGRESSIF) | Faible | **Traité** |
| 7 | ~~Règle "Wall Street", Andrews Pitchfork, canal manuel~~, ~~Cluster Technique~~, ~~"+Reverse" (table range)~~ | **5/5 traités** (Wall Street/Andrews/canal manuel — voir note dédiée `COUVERTURE_ENSEIGNEMENTS.md`, résultats mesurés mitigés, aucun ne se démarque nettement ; "+Reverse" table range — **traité ce cycle**, `code/position_engine.py` : `process_reverse` + paramètre optionnel `reverse_at_limit=False`, AUCUN changement de comportement pour les appelants existants, 8/8 tests dont 5 historiques toujours au vert + 3 nouveaux, hypothèse H-Reverse-Range documentée car le manuel ne donne ni taille ni stop ni cible pour la jambe short ; appliqué à `code/backtest_phase2_v7_reverse.py` sur TRES_AGRESSIF BTC/ETH/BNB/SOL — résultat mixte, 3/4 actifs améliorés (+2,2 à +18,7 pts), 1/4 dégradé (BNB -6,1 pts), cf. note dédiée `COUVERTURE_ENSEIGNEMENTS.md` ; Cluster Technique/diversification 1%+1% — **traité ce cycle** par un agent en parallèle (après l'incident d'infrastructure de la vague 2 ci-dessous, qui avait interrompu la 1ère tentative avant tout code écrit) : `code/cluster_technique.py` (Pattern B, 6/6 tests), `code/diversification.py` (moteur "1%+1%", 5/5 tests), `code/backtest_phase2_diversification.py` (BTC/ETH/BNB/SOL). Résultat mesuré honnête MITIGÉ : une comparaison naïve suggérait une réduction de drawdown massive (+6,5 à +13,2 pts), mais une fois la pyramidalisation neutralisée des deux côtés (comparaison isolée, cf. `COUVERTURE_ENSEIGNEMENTS.md`), l'effet marginal réel du 2ème pattern est proche de zéro et mixte selon l'actif (+0,2/+0,3/-0,2/-0,1 pt BTC/ETH/BNB/SOL) — l'avantage promis par la source n'est pas confirmé empiriquement) | Faible (item soldé) | **Traité** |
| 8 | **P0-bis, TRAITÉ ce cycle** : `compute_ascending_lows` (structure, `proxy_v2.py`) et `n_borders` (v4-v7 + capital_tiers) utilisaient `argrelextrema` en un seul appel batch — classification à l'instant t dépendante de 3 barres futures réelles (23/200 barres BTC H4 changeaient de classification). Corrigé via `compute_swing_low_confirmed`/`compute_swing_high_confirmed` (causales, vérifiées bit-à-bit) ; `fibonacci.py` propagé et vérifié (tests ajustés) ; `trend_table.py` hérite automatiquement (aucune modif requise, vérifié). **Dette de requantification résorbée ce cycle** : `v4.py`/`capital_tiers.py` vérifiés (import direct de `compute_swing_low_confirmed` depuis `proxy_v2.py`, pas de copie locale) et rejoués BTC/ETH/BNB/SOL tous profils | Affectait la fiabilité du composant "structure"/maturité dans tous les moteurs depuis v4 — ampleur CONFIRMÉE small (contrairement au P0 du cycle) : 0/48+0/32+0/16 bascules de signe sur H4 (v5/v6/v7), retour réduit de ~12-14% (pas ÷4), win rate quasi stable. `capital_tiers.py` : même schéma confirmé (0/48 bascule de signe, retour ÷1,08 environ). `v4.py` H4 : **0 changement, bit-à-bit identique** (maturité par comptage glissant sur 90 bougies, peu sensible au décalage causal de 3 bougies, contrairement à `compute_ascending_lows`) | Fait — voir `COUVERTURE_ENSEIGNEMENTS.md` ⚠️→◐ P0-bis pour le détail chiffré complet | **Traité intégralement — plus de propagation restante** (v4.py reste un moteur superseded, ce rejeu est de la complétude documentaire, pas une réhabilitation) |

**Règle de traitement** : l'item 0 (cycle) est traité. L'item 8 (structure/swing) est la même catégorie de risque (fonction batch non causale) trouvée sur un composant différent — à traiter avec la même rigueur avant de considérer que "toutes les fonctions batch du projet sont propres". P1 peut être mené en parallèle (ne dépend pas de la causalité des fonctions batch). P2/P3 après.

## Critères de sortie Phase 2 → Phase 3 (gate explicite, RÉÉCRIT ce cycle — trouvé stale par un bilan directeur "regard neuf")

**Constat (mobilisation multi-agents, bilan directeur)** : cette section, écrite il y a plusieurs cycles, n'avait jamais été revisée depuis — alors que 5 des plus gros chantiers du projet (protocole unifié, config fidèle, les 3 corrections de fidélité IP, le risque agrégé) sont tous arrivés APRÈS sa rédaction. Aucun des 4 anciens critères ne référençait `unified_protocol.py`/`backtest_phase2_faithful.py` (les moteurs À UTILISER opérationnellement, désignés comme tels ailleurs dans ce document) ni le risque agrégé (17% vs plafond 5%, le fait le plus explicitement chiffré du projet). Rien de coché n'était FAUX, mais la liste elle-même avait cessé de refléter ce que le projet est devenu — exactement le type de dérive documentaire que ce projet a appris à surveiller ailleurs (cf. tableau "2e pattern récurrent"). Anciens critères 1-4 conservés ci-dessous pour l'historique (repli dans une note), remplacés par les 5 critères suivants :

1. **Fidélité IP du protocole opérationnel** (`backtest_phase2_faithful.py`/`unified_protocol.py`, pas les moteurs superseded v4-v7) : les règles littérales identifiées comme telles sont actives sans condition — **fait, vérifié par 3 rounds de mobilisation multi-agents successifs** : EXCES-H4, pyramidalisation-régime, Conflit MTF (tous corrigés et testés), gate Fibonacci littéral (backlog P0/P1, raison précise documentée, pas une invention). Un diff systématique v6↔v7 (mobilisation multi-agents) confirme qu'aucune 4e régression de la même catégorie n'existe. Cf. `CONFIGURATION_RECOMMANDEE.md`/`COUVERTURE_ENSEIGNEMENTS.md`.
2. **Tests de régression** couvrant les composants critiques : moteur de position (`test_position_engine.py`, 8/8), signal proxy causal (`test_proxy_v2.py`), classificateur de régime (`test_regime_classifier.py`, 10/10), les 3 moteurs opérationnels (`test_backtest_phase2_faithful.py` 11/11, `test_unified_protocol.py` 13/13, `test_backtest_phase2_recommended.py` 9/9) — **fait**. **Trouvé manquant ce cycle et corrigé** : `risk_aggregation_triple_system.py` (le calcul du risque agrégé le plus cité du projet) n'avait AUCUN test malgré 3 resynchronisations manuelles déjà nécessaires — `test_risk_aggregation_triple_system.py` (4/4) ajouté, garde-fou anti-dérive direct (compare le gate dupliqué au VRAI moteur sur données réelles, pas seulement sur scénario synthétique).
3. **Pipeline unique rejouable** — `code/run_all.py` (15 moteurs désormais, `--only`/`--confirm-full`, sortie jamais à la racine du dépôt) — **fait**, cf. note historique ci-dessous pour le détail.
4. **Interface opérationnelle Phase 3 (`decide_now()`) signale les risques déjà quantifiés par le projet** — **trouvé manquant ce cycle et corrigé** (bilan directeur) : `decide_now()` ne signalait jusqu'ici AUCUNE information sur le risque agrégé, alors que le projet le chiffre lui-même à 17% (vs plafond 5% documenté). Nouveau champ `aggregate_risk_warning` (risque RANGE réellement engagé, formule R1, avertissement informatif — jamais un plafond appliqué), testé (`test_unified_protocol.py`, 2 tests dédiés à vérité terrain connue).
5. **Reste ouvert, PAS un critère rempli** : aucun pipeline d'ingestion de données live, aucune trace/journal des décisions Phase 3 pour évaluation statistique ultérieure — la Phase 3 au sens opérationnel (pas seulement "le backtest est prêt") n'est pas encore outillée. Documenté ici plutôt que découvert au moment de vouloir réellement démarrer la Phase 3.

**Anciens critères 1-4 (avant cette réécriture, conservés pour l'historique, pas pour être suivis désormais)** : P0 (cycle causal) traité v5/v6/v7 rejoués, mitigé (edge global confirmé, composant isolé non significatif) ; P1 (table tendance testée, stop cross-timeframe mesuré et gardé optionnel) traité ; tests de régression sur position_engine/cycle/regime_classifier faits ; pipeline unique rejouable (`run_all.py`, alors 12 moteurs) fait. Tous restent vrais sur leur périmètre d'origine (v4-v7), simplement dépassés par ce que le projet est devenu depuis.

## Incident vague 2 — rate-limit de compte, pas un problème de code

Les 4 agents lancés en parallèle pour la vague 2 (UT+2, patterns géométriques, diversification/Cluster Technique, +Reverse table range) ont tous les 4 été interrompus par la même erreur d'infrastructure (`rate_limit`, HTTP 429, "individual spend limit"), sans lien avec la qualité du travail engagé. Conséquence inégale selon l'agent, vérifiée directement (pas seulement le rapport de chaque agent) :
- **UT+2 et patterns géométriques (Wall Street/canal manuel/Andrews)** : les modules eux-mêmes et (pour UT+2) le backtest complet avec résultats étaient déjà finis avant l'interruption — seuls les tests et (pour les patterns) le moteur de mesure manquaient. Complété directement (sans agent) : `code/test_wall_street_pattern.py` (4/4), `code/test_manual_trend_channel.py` (5/5), `code/test_andrews_pitchfork.py` (5/5), `code/test_ut2.py` (4/4), `code/backtest_phase2_patterns.py` (nouveau). Suite complète du projet revérifiée après coup : **53/53 tests passent** (10 fichiers).
- **Diversification/Cluster Technique et +Reverse table range** : ces 2 agents sont morts avant d'écrire le moindre fichier. **+Reverse table range relancé et traité** (session suivante, ce cycle) : `code/position_engine.py` (`process_reverse`, `reverse_at_limit`), `code/backtest_phase2_v7_reverse.py`, `code/test_position_engine.py` (8/8), cf. note dédiée `COUVERTURE_ENSEIGNEMENTS.md` et item 7 du backlog ci-dessus. **Diversification/Cluster Technique également relancé et traité** (session suivante, ce cycle, par un agent en parallèle) : `code/cluster_technique.py` (6/6 tests), `code/diversification.py` (5/5 tests), `code/backtest_phase2_diversification.py` — résultat MITIGÉ (cf. note dédiée `COUVERTURE_ENSEIGNEMENTS.md` et item 7 du backlog ci-dessus : effet marginal sur le drawdown proche de zéro une fois isolé de la pyramidalisation).

## Prochaine action immédiate
P0, P0-bis, P1 "stop cross-timeframe", le test d'ablation du cycle, et désormais **tous les items du backlog** (diversification/Cluster Technique — item 7 — et "+Reverse table range" tous deux traités ce cycle, voir incident ci-dessus) sont **traités**. `MTF_CROSS_VALIDATION_H4_D1.md` a aussi été corrigé (chiffres causaux, plus les anciens chiffres batch 4× surestimés). Voir "Plan d'autonomie 8h" ci-dessous pour la suite.

## Plan d'autonomie 8h (démarré après la clôture du backlog corpus)

Le backlog "un élément du corpus par ligne" est soldé. Ce qui reste n'est plus "implémenter tel enseignement" mais consolider, requantifier et synthétiser un projet devenu tentaculaire (41 fichiers Python, 11 scripts `backtest_phase2_*.py` distincts, 67 tests). Rétrospective honnête (mode ingénieur senior) ayant motivé ce plan :

**Ce qui ne satisferait pas un ingénieur senior en l'état** :
1. ~~Aucune synthèse : chaque feature a été testée isolément contre la baseline v7, personne n'a construit LA config recommandée combinant tout ce qui est validé (cycle causal, structure causale, UT+2 strict) en excluant ce qui dégrade (Fibonacci, gate Andrews) — pas de chiffre de référence unique à citer aujourd'hui.~~ — **traité** : `CONFIGURATION_RECOMMANDEE.md`, cf. section "Configuration recommandée" en tête de ce document.
2. **Toujours ouvert** : hypothèses jamais croisées SYSTÉMATIQUEMENT (H1-H12 trend_table, ×1.25 capital_tiers, jambe reverse "miroir", seuil confluence Cluster Technique) — chacune raisonnable isolément, jamais stress-testées ensemble. `code/backtest_phase2_faithful.py` combinait pour la première fois 3 règles jusque-là mesurées isolément (stop UT+1, Wall Street, +Reverse) et avait trouvé un effet d'interaction réel sur BNB/TRES_AGRESSIF (stop D1 × +Reverse, -122,9 pts de retour, -27,9 pts de drawdown, confirmé par ablation à l'époque) — **ce cas précis a depuis disparu** (résolu par les 3 corrections de fidélité IP ultérieures — EXCES-H4, pyramidalisation-régime, Conflit MTF — pas par un changement de cette combinaison spécifique, cf. `CONFIGURATION_RECOMMANDEE.md` section 5ter pour le détail), mais le CONSTAT MÉTHODOLOGIQUE reste valable : la première vraie combinaison testée avait révélé une interaction que personne n'aurait anticipée, confirmant que le risque de "hypothèses jamais croisées" n'est pas théorique. Aucun test d'interaction systématique (toutes paires/triplettes de règles) n'a été fait à ce jour — seulement des cas trouvés en vérifiant un résultat qui semblait anormal, pas par une recherche exhaustive.
3. ~~`v4.py`/`capital_tiers.py` tournent encore sur l'ancienne structure batch (pré-P0-bis) — dette de requantification connue, jamais résorbée~~ — **résorbée ce cycle** : les deux fichiers importent bien la version causale (`compute_swing_low_confirmed` depuis `proxy_v2.py`, vérifié empiriquement, aucune copie locale), rejoués BTC/ETH/BNB/SOL tous profils. `capital_tiers.py` confirme le même schéma que v5/v6/v7 (0/48 bascule de signe, retour ÷1,08 environ) ; `v4.py` H4 est même **bit-à-bit identique** à l'ancien chiffre batch (sa maturité par comptage sur 90 bougies est peu sensible au décalage causal de 3 bougies, contrairement à `compute_ascending_lows`) — détail chiffré complet dans `COUVERTURE_ENSEIGNEMENTS.md` ⚠️→◐ P0-bis. Rappel : `v4.py` reste superseded (v4→v5→v6→v7), ce rejeu est de la complétude documentaire, pas une réhabilitation.
4. ~~`regime_classifier.py` : zéro test, alors qu'il conditionne toutes les décisions d'entrée depuis v6.~~ — **traité ce cycle** : `code/test_regime_classifier.py` (10/10), voir "Critères de sortie Phase 2 → Phase 3" item 3 ci-dessus pour le détail.
5. **Traité intégralement (3 sessions)** : walk-forward et OOS XRP jamais rejoués sur le moteur pleinement corrigé (P0+P0-bis) — faits une fois, sur `recommended.py`, avant les corrections de ce cycle. **Walk-forward de `faithful.py` fait** : résultat rassurant, strictement meilleur que l'ancien walk-forward de `recommended.py` sur chaque métrique. **Walk-forward de `unified_protocol.py` fait**, PUIS révisé après la correction EXCES-H4 : le -72,5%/-45,9% de BNB/TRES_AGRESSIF/2021 (mesuré avant cette correction) devient +6,2%/-8,4% après — conclusion actionnable révisée : ce couple n'a plus besoin d'être exclu d'un usage réel, cf. section dédiée ci-dessus. **OOS XRP de `faithful.py` fait** (mobilisation multi-agents) : résultat honnête non concluant, 3 trades à l'origine puis 0 après la correction Conflit MTF (cf. section dédiée). Détail complet : section "Protocole unifié" et section "Correction EXCES-H4" ci-dessus.
6. ~~`cycle_ascending` (occurrence #3 du pattern récurrent) toujours non tranché~~ — **tranché ce cycle**, cf. tableau occurrence #3 ci-dessus : expliqué, pas un bug (artefact de sinusoïde pure, ne se reproduit pas sur données réelles).
7. ~~Duplication réelle : 9 moteurs `backtest_phase2_*.py` (v4/v5/v6/v7/ut2/patterns/capital_tiers/fib/recommended) portaient chacun leur propre copie quasi-identique (~40 lignes) de `open_tranche_fn`.~~ — **résorbée ce cycle** : factory partagée `position_engine.py::make_open_tranche_fn`, **9/9 moteurs migrés**. Chaque migration vérifiée par rejeu réel avant/après (pas seulement "les tests passent") : `git stash` sur le moteur, rejeu = vraie baseline, restauration, rejeu = résultat migré, diff exact des CSV — **0 écart sur les 5 métriques pour les 9 moteurs**, y compris les 2 cas les plus subtils (v6 : gate régime différent entrée fraîche/renfort ; fib : `fib_gate_pyramid` asymétrique). ~270 lignes de duplication supprimées. Une première comparaison sur v6 avait montré un "écart massif" — fausse alerte due à un CSV racine resté périmé depuis une correction antérieure (P0-bis), jamais régénéré sous ce nom : vérifié et écarté avant de conclure, pas pris pour une régression. Suite complète (103 tests, 17 fichiers) revalidée après chaque migration. Note opérationnelle : ce chantier a été interrompu 2 fois par un rate-limit de compte pendant qu'un agent délégué le menait (4/9 puis mi-chemin sur le 5e) — terminé directement (sans agent) plutôt que relancé, avec la même rigueur de vérification.
8. **Désormais chiffré (mobilisation multi-agents, Agent B)**, toujours non résolu par un plafond mécanique : le cumul RANGE+TENDANCE+diversification simultané, jamais chiffré au cycle précédent, atteint **17,00% de risque nominal agrégé** (BTC/TRES_AGRESSIF) — très au-dessus du plafond 5% documenté (`RULES_EXTRACTION.md` §5), avec 67/112 combinaisons année×actif×profil qui le dépassent en walk-forward (régénéré après la correction pyramidalisation-régime, était 68/112 avant). Root cause identifiée : le pyramidage RANGE seul (3×5%=15% en TRES_AGRESSIF) dépasse déjà le plafond avant toute combinaison. Documenté comme limite ouverte à chaque endroit concerné (choix U5 `unified_protocol.py`, section dédiée ci-dessus), volontairement PAS comblé par un plafond inventé (le corpus ne spécifie rien pour cette combinaison précise de systèmes) — aucun garde-fou mécanique n'existe pour empêcher un cumul de risque non voulu si ces sleeves sont un jour utilisés ensemble en réel.
9. **2e catégorie de risque récurrent identifiée** (pas un bug de calcul, une erreur de méthode dans la prise de décision) — désormais 4 occurrences (cf. tableau dédié dans "Méthode de travail" ci-dessus), les 2 premières découvertes UNIQUEMENT par contestation directe de l'utilisateur, les 2 dernières (EXCES-H4, pyramidalisation-régime) trouvées PROACTIVEMENT par audit systématique (mobilisation multi-agents) — consigne retenue pour la suite : vérifier la citation du corpus AU MOMENT de poser toute règle qui restreint un comportement, pas après coup.
10. **(a) Conflit Multi-Timeframe — implémenté (mobilisation multi-agents, design puis implémentation, cycle suivant)** : cf. section dédiée ci-dessus pour le détail complet (décision D1 vs Hebdo, uniformité entrée/renfort, impact chiffré). `faithful.py`/`unified_protocol.py` (RANGE) corrigés, `recommended.py` volontairement pas étendu (dépendance nouvelle hors de son périmètre documenté). **(b) Gate Fibonacci RANGE — toujours PAS implémenté, désormais avec une raison plus précise qu'"un seuil à ajuster"** : la règle §1 du manuel est une condition COMPOSITE à 3 volets (retracement + "débordement du contexte" + "signal & triangle de confirmation"), dont 2 des 3 n'ont aucune définition utilisable ailleurs dans le corpus (vérifié par re-lecture exhaustive, 3 agents convergents) — les implémenter exigerait d'inventer ces définitions, refusé explicitement. Risque de confusion supplémentaire identifié : le même chiffre "76%" désigne un seuil d'ENTRÉE ici (§1) et une cible de SORTIE ailleurs (`TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` §5) — deux règles distinctes à ne jamais confondre. Reste backlog, documenté, pas comblé silencieusement.
11. **Nouveau ce cycle (5e round de mobilisation multi-agents, 3 agents, corpus COMPLET relu de zéro)** — réponse directe à "reste-t-il de l'IP de Philippe non implémentée ?" : **oui, ~18 éléments supplémentaires trouvés**, classés en 3 catégories (détail complet, citations exactes et preuves code : `COUVERTURE_ENSEIGNEMENTS.md` section dédiée) :
    - **Catégorie A (3 éléments) — probablement PAS reproductible**, même limite déjà acceptée pour tout le proxy (`RULES_EXTRACTION.md` le dit dès sa 1ère ligne : formule/algorithme PRO non divulgués) : gradient de confiance 0-100, compensation Fibonacci par qualité de signal couleur, "cas d'urgence" sur bascule de couleur — toutes 3 dépendent de la classification en catégories de signal (TP/Overload/DIV/EXIT/SurAchat/BULL-BEAR/SQUEEZE, §2 du manuel) que seul l'algorithme propriétaire produit. **Ne pas tenter d'implémenter une approximation inventée ici** — même risque de méthode que ce projet refuse depuis le début.
    - **Catégorie B (3 éléments) — littéral, computable, candidats d'implémentation directe, faible coût** : ~~(i) "Règle des 50%" du Pull-Back — 2e condition (pénétration 50% inférieurs du contexte) citée dans `fibonacci.py` mais jamais codée, seule la 1ère condition (retracement %) l'est~~ — **(i) implémenté ce cycle, PUIS CORRIGÉ (vérification adversariale dédiée, même cycle)** : `fibonacci.py::compute_context_position`/`classify_regle_50` ajoutent la 2e condition manquante (canal de contexte `CONTEXT_DURATION="15D"`, MÊME définition que `trend_table.py::add_trend_context`/`accum_retracement_frac` — PAS `backtest_phase2_v7.py::prepare`, qui ne calcule ni `ctx_high` ni `ctx_low` : citation corrigée après vérification — recalculée localement pour ne pas créer de dépendance, H6 préservé), colonnes `fib_context_position`/`fib_regle_50` sur `add_fibonacci_columns`. **Bug réel trouvé et corrigé par la vérification adversariale** : la version initiale ne plafonnait pas le retracement (`r >= FAVORABLE_MIN` sans `r <= FAVORABLE_MAX`), supprimant de fait le Red Flag #10 que ce fichier documente lui-même — mesuré : la version buguée était en réalité PLUS LARGE que `fib_favorable` en nombre de bougies (2642 vs 2388 sur BTC H4), contredisant la prétention "beaucoup plus restrictive". Corrigé (plafond `FAVORABLE_MAX` ajouté), `fib_regle_50` est désormais un sous-ensemble STRICT vérifié de `fib_favorable` (920/14239 bougies BTC H4, vs 2388 pour favorable). Point de vigilance nuancé (pas tranché comme une distinction propre) : la tension avec l'hypothèse H7 de `trend_table.py` est réelle et non réconciliée — désormais tracée aussi dans `trend_table.py` à côté de H7 (H7 elle-même non modifiée), cf. MISE À JOUR en tête de `fibonacci.py`. Câblé dans `backtest_phase2_fib.py::run_v7_fib` comme nouvelle variante `use_fib_regle_50` (même pattern que `use_fib_optimal`), mesurée honnêtement dans `phase2_fib_results.csv` régénéré après correction : 2 à 13 trades selon l'actif (vs **80-137**, chiffre corrigé, pour `fib_favorable`), rendement absolu négatif sur 15/16 couples actif×profil (seul SOL/FAIBLE positif, +0,5 pt), mais **dégradé vs `baseline_v7` sur 16/16** (delta -21 à -201 pts — formulation corrigée, l'ancienne "15/16 dégradés" confondait le signe absolu avec la comparaison à la baseline). 3 nouveaux tests à vérité terrain (`test_fibonacci.py`, 8/8, dont un test dédié au cas `retracement>61,8%` qui aurait révélé le bug initial). Implémenté quand même intégralement malgré ce résultat, conformément au principe inviolable du projet. ; ~~(ii) "Stop Loss = taille du canal" — réduction symétrique stop+position sur canal très large, jamais implémentée~~ — **implémenté ce cycle** (mobilisation multi-agents, agent relancé après incident rate-limit, résultat vérifié indépendamment — recalcul manuel du mécanisme, non-régression bit-à-bit des 9 moteurs déjà en place quand la règle est désactivée, delta moyen recalculé indépendamment) : citation exacte `TRADING_LESSONS_MAITRISE_GRADIENT_RISQUE.md` §5 ("Taille du Canal / 2 = Taille du Stop Loss ET Taille de Position / 2 simultanément, préserve une exposition capital constante"), érigée en obligation par la checklist pré-trade de la même source. 4 hypothèses documentées (H-Canal-Large-1..4, bloc dédié en tête de `position_engine.py`) : (1) mécanique des deux "/2" — le stop est divisé par deux ET la taille dérivée du risque à CE stop réduit est divisée par deux, les deux se compensant exactement dans un moteur dimensionné par le risque (exposition inchangée, capital risqué divisé par deux — vérifié par recalcul indépendant : `size = (risk/(s/2))/2 = risk/s`) ; (2) canal = le même "Extreme Channel" déjà en place (`ctx_width_pct`, jusqu'ici calculé mais jamais conservé), mesuré au niveau qui porte le stop (H4 natif ou D1 selon le moteur) ; (3) seuil "très large" = percentile glissant causal p80 de la même série (aucun chiffre dans le corpus — hypothèse assumée, justifiée AVANT toute mesure par 3 contraintes : distinct d'EXCES p95, au-dessus de la moyenne récente citée par une autre source, cohérent avec la bande de fréquence ~20% libre du manuel) ; (4) périmètre RANGE seulement (`position_engine.py`), PAS la table TENDANCE (`trend_table.py`, hors périmètre, 2e décision de conception non tranchée par le corpus). Activé sans condition dans `backtest_phase2_faithful.py`/`unified_protocol.py` (côté RANGE) — mesuré comme banc de sensibilité isolé (`use_wide_channel_halving`) dans `backtest_phase2_v7.py`. Résultat honnête : banc isolé, dégrade 16/16 couples au seuil retenu (-13,1 pts de retour moyen) ; `faithful.py` (inconditionnel) : -2,6 pts en moyenne, signe MIXTE (BTC/BNB dégradés jusqu'à -13,5 pts, SOL amélioré jusqu'à +8,1 pts, ETH exactement inchangé — vérifié : ses seules sorties au stop ne tombent sur aucune bougie à canal très large). Seuil p80 **non révisé** malgré p90 mesurant mieux (-10,1 pts au lieu de -13,1) — choisir un paramètre pour la performance du proxy est le raisonnement que ce projet s'interdit. Effet secondaire sur le risque agrégé : dépassements du plafond §5 en walk-forward 66/112→**64/112** (amélioration, cohérent avec un capital risqué réduit sur les bougies concernées). 10 nouveaux tests à vérité terrain (`test_position_engine.py` 8→13, `test_regime_classifier.py` 10→15, calculs à la main). BNB/TRES_AGRESSIF (le couple le plus fragile déjà suivi) impacté négativement — cf. paragraphes dédiés ci-dessus pour le détail chiffré complet. ; ~~(iii) tension "3 vs 4 bornes" jamais formellement tranchée~~ — **mesurée ce cycle (mobilisation multi-agents)** : voir section dédiée ci-dessous — trouvaille plus importante que prévu (le gate `n_borders` s'avère INERTE sur toute la grille 2-5, pas juste 3 vs 4).
    - **Catégorie C (10 éléments) — réels, littéraux, exigent une décision de conception avant tout code** (même discipline que Fibonacci RANGE/Conflit MTF, pas une invention silencieuse) : ~~mécanisme d'emboîtement T/T-1 (déjà reconnu comme absent par `trend_table.py` lui-même, H11)~~ — **INVESTIGUÉ au 11e round, PAS implémenté et FERMÉ : deux prémisses de l'item étaient fausses** (la paraphrase perdait le mot porteur — la source dit « le **canal de tendance** observé », et « canal de tendance » est un objet situé un cran SOUS le contexte dans #11:14-18/#12:7, ce qui fait de la citation une identité de lecture graphique, pas une procédure de calcul ; et H11 décline la validation par l'UT SUPÉRIEURE — « H4 validé par D1 » — donc ne porte pas sur la direction descendante que l'item réclamait). La seule traduction opérationnelle de l'emboîtement dans tout le corpus est la projection vers le BAS — *"Extreme Channel (contexte de l'UT supérieure affiché sur l'UT de trading)"* (#16:12), stop sous ce canal (#16:28, #12:7, #10:38) — **déjà implémentée sans condition** (`ctx_support_d1`, `faithful.py`), donc item partiellement déjà couvert (même classe que Red Flag 3/flip au 9e round) ; 5 sources contre 1 placent l'exécution en BAS de l'échelle (#15:10, #16:10, #12:19, #14:15, #11:9) ; et la lecture littérale T-1 est un **no-op mesuré** (`ctx_high`/`ctx_low` H1 joints au H4 identiques sur **100,00%** des barres après neutralisation de l'artefact `shift(1)` — un max/min sur fenêtre calendaire est invariant par agrégation ; gate additif « structure T-1 mature » **inerte bit-à-bit, écart 0 sur 4/4 actifs**, `n_borders` H1 valant 32-34 au minimum contre un seuil de 3), tandis que la direction prescrite (stop D1) est incompatible avec H3 dans ce moteur (stop ×8,6-14,6, plafond de campagne 2,2-4,2 → 0,25-0,34, les 4 profils s'écrasent) — remplacé par deux items nouveaux mieux étayés, cf. section "11e application" : **nouveau, trouvé au 11e round** : le `ctx_support` de `trend_table.py` n'est PAS l'« Extreme Channel » du corpus (bande H4 native au lieu du contexte UT+1, écart médian 7,4-12,7%) ; **nouveau, trouvé au 11e round** : tension #10:18 (*"utilise la structure de l'UT inférieure pour valider"*) vs #11:9/18 (*"inexistante et non-avenue"* / *"« bruit »"*) jamais consignée ; ~~contrainte "espace libre" MTF avant breakout~~ — **IMPLÉMENTÉE, MESURÉE et documentée au 10e round (cf. section "10e application" ci-dessous)** : citation cette fois vérifiée EXACTE, 2 sources indépendantes, et la conclusion "2 UT pour RANGE / 3 UT pour TENDANCE" du 7e round s'applique ici EN SENS INVERSE (la table TENDANCE a explicitement droit à 3 UT) ; couvre du même coup l'item "Hiérarchie UT Supérieure = Le Potentiel" ; ~~**amplitude Validation/Confirmation jamais réellement cross-timeframe malgré des rôles UT+1/UT+2 explicitement prescrits par le corpus**~~ — **INVESTIGUÉE ce cycle (7e round de mobilisation, 2 agents de conception indépendants), PAS implémentée, décision prise et documentée** : cf. section "7e application" ci-dessous pour le détail complet — la citation de départ était partiellement fausse (UT+1 = le stop, déjà implémenté, pas la Validation), et la lecture littérale restante (Confirmation = amplitude UT+2 réelle) contredit une citation du corpus qui n'attribue que 2 UT à la table RANGE ; requalifiée en un nouvel item plus précis (Confirmation = niveau structurel absolu, pas une distance projetée) ; ~~définition stricte de la 3ème borne par clôtures (pas mèches)~~ — **INVESTIGUÉE au 8e round, PAS implémentée et RETIRÉE de la catégorie C : erreur de catégorie** (les clauses "clôtures" de la source #11 sont des prédicats de validation d'un niveau, pas une règle de localisation d'un swing ; les 3 critères sont déjà couverts par `trend_table.py::breakout_raw`/`excess_raw`, H5, `fibonacci.py` et `PHASE2_CORRECTION_CLOSES.md`), remplacée par deux items nouveaux mieux étayés, cf. section "8e application" : **nouveau** : "règle d'or : calculs sur clôtures, jamais sur les mèches" (#15, `PYRAMIDALISATION.md:20`) vs `local_range`/`context_range` calculés en `max(high) - min(low)` — le prochain item C recommandé ; **nouveau** : `fibonacci.py::compute_retracement` hybride (numérateur clôture, dénominateur mèches) ; UT+2 scopé aux trades "renversement" seulement ; variante d'entrée "3ème borne squeezée" ; altérations de structure Vague 1/5 (Overlap, "Rivière") ; confusion terminologique "Cluster technique" de confirmation de breakout vs "Cluster Technique" MA20 déjà codé ; ~~Red Flags d'invalidation précoce~~ — **INVESTIGUÉS au 9e round, PAS implémentés, décision prise et documentée** : cf. section "9e application" ci-dessous — la prémisse de l'item ("pas de mécanisme distinct du stop de protection standard") était FAUSSE (la sortie "flip de signal" avant Validation de `process_tranche` EST ce mécanisme, et elle est la voie de sortie dominante à **93,8%** des 4 388 trades de `faithful.py`) ; sur les 3 Red Flags, RF2 (>61%) est déjà implémenté deux fois comme plafond d'ENTRÉE, RF3 (contexte opposé) est un doublon bit-à-bit du stop et **prouvé non précoce**, RF1 (clôtures sous MA20) est le seul réel mais exige d'inventer 2 paramètres et de trancher un conflit d'UT entre ses 2 sources ; sizing 1-1,5%/R:R 1:5 spécifique aux trades d'accumulation ; **nouveau, trouvé au 7e round** : "Confirmation comme niveau structurel absolu (médiane du canal de contexte, clôturée)" — cf. section "7e application" ; **nouveau, trouvé au 9e round** : puce 3 de la "Règle de Trois" de #16 ("toute désynchronisation entre patterns ou cycles doit entraîner un arrêt des nouveaux engagements") — sur les 3 puces de cette section, seule la puce 1 (risque /2 après 3 zones) est implémentée, cf. "9e application".
    - **Priorité recommandée** : catégorie B d'abord (faible coût, aucune ambiguïté) ; catégorie C une règle à la fois, en commençant par l'amplitude cross-timeframe Validation/Confirmation (portée la plus large) ; catégorie A jamais tentée.

**Vagues prévues, priorisées par la formule du projet (probabilité × impact × coût de découverte tardive)** :

- **Vague 3 (parallèle, fichiers disjoints)** :
  - Tests unitaires pour `regime_classifier.py` (zéro couverture malgré un rôle central depuis v6)
  - ~~Rejeu de `v4.py`/`backtest_phase2_capital_tiers.py` avec la structure causale P0-bis (dette de requantification déjà identifiée, code déjà corrigé, juste jamais rejoué)~~ — **traité ce cycle** : rejoués, import causal vérifié, résultat conforme (v4 H4 bit-à-bit identique, capital_tiers ÷1,08 environ, 0 bascule de signe partout) — cf. `COUVERTURE_ENSEIGNEMENTS.md` ⚠️→◐ P0-bis
  - ~~Investigation dédiée de `cycle_ascending` (occurrence #3, non tranchée depuis le début) sur données réelles (pas seulement synthétique) — trancher si possible, sinon documenter pourquoi ça reste ouvert~~ — **traitée ce cycle** : tranchée (expliquée, pas un bug), cf. tableau occurrence #3 en tête de document
  - Pipeline unique rejouable (script qui relance tous les moteurs actuels d'un coup, remplace le geste manuel ad hoc)
- ~~**Vague 4 (séquentielle, après vague 3)** : synthèse — construire LA config recommandée (cycle+structure causaux, UT+2 strict, sans Fibonacci ni gate Andrews qui dégradent), produire un chiffre de référence unique, rejouer walk-forward + OOS XRP dessus.~~ — **traitée** : cf. section "Configuration recommandée" en tête de ce document et `CONFIGURATION_RECOMMANDEE.md`. Walk-forward : pas d'année catastrophique sur BTC/ETH/BNB/SOL (2020-2026) ; OOS XRP : résultat honnête non concluant (3 trades, gate MTF non testable faute de données H4/H1 pour XRP, cf. document dédié section 4).
- **Vague 5 — traitée** : pistes ouvertes non tranchées — autre hypothèse de gating Andrews (celle testée dégrade nettement, pas forcément la seule lecture possible), robustesse du seuil de confluence Cluster Technique.
  - **Andrews — lecture alternative (H-Andrews-Contextuel)** : `code/andrews_gate_alternative.py` (`code/test_andrews_gate_alternative.py`, 8/8, dont non-régression contre `backtest_phase2_patterns.py`). Lecture retenue, justifiée par le TEXTE du corpus ("prend le relais QUAND la tendance est BRISÉE" — un relais est conditionnel, pas permanent) : le gate `close > pitchfork_p1` n'est appliqué QUE lorsque le régime (`regime_classifier.py`, réutilisé tel quel) de la bougie précédente est `RANGE_TENDANCIEL` (seule catégorie déjà disponible décrivant une tendance qui a cessé de progresser mais garde un biais hérité). Résultat, BTC/ETH/BNB/SOL × 4 profils, moyennes : gate permanent (hypothèse originale) +29,0% de retour moyen vs +501,4% sans gate ; gate contextuel +315,6% — **réduit très nettement la dégradation par rapport au gate permanent, sans l'éliminer** (reste ~37% sous "aucun gate"). Ni l'un ni l'autre adopté par défaut. Détail complet : `COUVERTURE_ENSEIGNEMENTS.md`.
  - **Cluster Technique — robustesse du seuil de confluence** : `code/cluster_technique_threshold_robustness.py` (`code/test_cluster_technique_threshold_robustness.py`, 5/5, dont non-régression contre `diversification.py` au seuil de référence). Grille 0,10/0,15/0,20(réf.)/0,25/0,30 testée sur BTC/ETH/BNB/SOL H4 (20 configs) : l'effet marginal isolé du Cluster Technique sur le drawdown reste partout **≤ ±0,4 point** (amplitude totale 0,7 point) — **conclusion "effet quasi nul et mixte" jugée ROBUSTE au choix du seuil dans cette plage**, pas un artefact du seuil retenu. Un seul actif (BNB) change de signe selon le seuil, mais dans une amplitude elle-même proche de zéro (0,3 point). Détail complet : `COUVERTURE_ENSEIGNEMENTS.md`.
- ~~OOS élargie, gate MTF activé, sur un actif horaire jamais utilisé (limite la plus significative restante de `CONFIGURATION_RECOMMANDEE.md`)~~ — **recherche menée ce cycle, résultat honnête : rien d'exploitable trouvé.** Recherche GitHub étendue (recherche par nom de fichier et par contenu sur ADA/DOGE/LTC/LINK/DOT/ETC/TRX/MATIC/XLM/ATOM/XMR, dépôt `priyanshux/cryptopy` cloné et vérifié, dépôt `ireneannx/deeplearning_crypto` vérifié comme mal étiqueté, dépôts `kochlisGit/VIT2`/`iamaryaak/RL-Crypto-Bot` vérifiés comme journaliers uniquement, dataset Kaggle "G-Research Crypto Forecasting" confirmé non committé sur GitHub — détail complet des 7 pistes essayées et rejetées : `CONFIGURATION_RECOMMANDEE.md` section 4bis). Aucune donnée horaire (ou plus fine), sur un actif jamais utilisé dans ce projet, avec assez d'historique pour faire converger un gate Hebdomadaire au-delà des 53 bougies déjà jugées trop minces sur XRP, n'a été trouvée. Conformément au principe "ne pas forcer un résultat avec une donnée insuffisante", aucun script d'OOS n'a été écrit sur les données insuffisantes trouvées (LTC horaire 4 mois seulement, `priyanshux/cryptopy`) — un "non trouvé, documenté" plutôt qu'un chiffre non concluant de plus. La limite reste donc ouverte, mais sa cause (donnée indisponible, pas la méthode) et la procédure déjà prête (`run_recommended(..., use_mtf_gate=True)`) sont documentées pour le jour où une telle donnée devient disponible.

Chaque retour d'agent vérifié indépendamment dans un clone isolé avant tout commit, comme fait pour les 13 chantiers précédents — aucun changement de discipline. Check-in de clôture programmé à l'échéance des 8h (`send_later`) pour un rapport final, que le travail soit fini avant ou non.
